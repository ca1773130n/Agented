# v0.6 Hardening Audit — Findings Ledger

**Date:** 2026-06-20 · **Branch:** `chore/v0.6-cleanup-hardening`

A multi-agent audit (6 dimensions, every finding adversarially verified) raised
**47 confirmed findings: 3 high, 15 medium, 29 low.** This pass fixed all highs,
the meaningful mediums, and the lint/dep lows; the rest are tracked here.

## Fixed this pass (committed)

- **HIGH** `context_policy=reset` operator-safety cluster (stop no-op, SSE goes
  dark, memory fragmentation, subscribe race, lost note) — reset now keeps a
  stable operator id; only `live_id` follows the child.
- **HIGH** sandbox snapshot copied escaping symlinks → neutralized.
- **MED** sandbox/judge: process-group SIGKILL on timeout; judge `inherit` path
  now env-scrubbed.
- **MED** `_get_unique_session_id` shadowing (super-agent sessions checked the
  wrong table); stale-check counter made stagnation unreachable; `useSketchChat`
  EventSource leak.
- **MED** ruff never gated → `just lint` added, repo brought clean + formatted.
- **MED** `npm audit fix` (picomatch ReDoS, postcss XSS) → 0 prod vulns.
- **MED** CLAUDE.md unified-loop layer documented; LoopTracePanel gate
  modify/abort coverage.
- **LOW (lint, incidental)** F821 missing imports, duplicate `Caller`,
  29× F401, unused `team_id` — all cleared by the ruff pass.

## Deferred — tracked for a future pass

### Medium
- **[test]** `grd_routes.py` — GRD genome/plan-selection/promote/tournament
  handlers have no HTTP-level tests (thin glue over unit-tested runners; add a
  `create_test_client` suite). *Missing regression net, not a live bug.*

### Low — security / hardening
- **[sec]** `grd_genome_patterns_runner` — user/agent strings passed as
  positional CLI args without a `--` terminator (argument-injection surface into
  `gd`/`tesserae`). Add `--` once confirmed the CLIs accept it.
- **[sec]** `project_discovery_service` — scans/imports arbitrary server paths
  with no allowlist + runs `git` in each (admin-gated, operator-driven). Apply
  the `leaf_crud_g._ensure_path_allowed` pattern.
- **[sec]** `setup_service` — `claude plugins install` runs with the full
  process env into every account config (remote-code surface; trusted
  marketplace). Scope the env.
- **[sec]** `goal_loop_runner` — agent hypothesis/reason text is re-injected
  verbatim into later prompts (self prompt-injection loop). Consider fencing.
- **[sec]** frontend `dompurify`/`postcss`/`brace-expansion` *moderate* dev/
  transitive advisories (the high/prod ones are fixed).

### Low — dead code / simplification
- **[simplify]** `ralph_monitor_service.py` shim is fully dead (zero importers) →
  delete.
- **[simplify]** `grdPlanning.planTournament` is a dead vertical slice (API +
  route + runner, no UI caller) — keep only if a tournament UI is planned.
- **[simplify]** `list_plan_selections` / `list_genome_suggestions` dead exports.
- **[simplify]** `ask_tesserae`/`context_tesserae` duplicate the run/log/return
  body; `grd_genome_patterns_runner` reinvents `GrdCliService`'s binary
  detection + subprocess runner; `sandbox_eval._dir_size` duplicates `_IGNORE`.
- **[simplify]** `grd_genome_patterns` min_occurrences/effect_size/fdr_q are
  speculative request plumbing through 3 layers (no UI sets them).
- **[maint]** orphaned `RalphMonitorResponse` model; `ralph-loop` still in the
  setup bundle though the skill is slated for retirement.

### Low — tests / maintenance
- **[test]** termination-ladder ORDERING (token_cap vs convergence vs
  stagnation) only unit-tested per-predicate, not as ordered loop branches.
- **[test]** `grd_genome_patterns` `--apply`+mirror and timeout branches
  partially uncovered; `memory-system.ts` API module has no test file.
- **[bug]** `tesserae_integration` sessions-import + `_dir_size` TOCTOU notes
  (low-likelihood races).
- **[maint]** 3 remaining tracked runtime `.log` files (evolve.log etc.) should
  be `git rm --cached` (now gitignored); >500 kB un-split frontend chunks; stale
  `tesserae project <cmd>` strings in gitignored non-Claude harness mirror dirs
  (`.zed/` etc. — untracked, cosmetic).
