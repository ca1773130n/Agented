---
phase: 21-one-click-team-harness-setup
plan: 03
type: execute
wave: 2
depends_on: ["21-02"]
files_modified:
  - backend/app/services/team_harness_setup_service.py
  - backend/tests/test_team_harness_setup_service.py
autonomous: true
verification_level: proxy
hypothesis: "Step a reconciles GRD init by skipping when .planning/ already exists (else invoking the auto_init_project path), and step b checks for existing team instances before calling create_team_instances then post-updates each SA instance's driver column to 'grd' — together giving idempotent topology with driver=grd that does not duplicate on re-run."
predicted_outcome: "test_team_harness_setup_service.py -k 'driver' passes (get_instance_driver(instance_id)=='grd' for every SA instance step b created), and -k 'idempotent' shows step b creates no second set of instances on re-run."
must_haves:
  truths:
    - "Step a (grd_init) skips when the project's .planning/ already exists (reconcile), else runs the GRD-init path; never re-inits a populated project (S4 unblocked downstream)"
    - "Step b queries existing instances for (project_id, team_id) and does NOT call create_team_instances again when instances already exist (P1 — SA creation is the one non-constraint-deduped step)"
    - "Every SA instance created by step b has get_instance_driver(instance_id) == 'grd' (S4) — driver threaded via post-create update(driver='grd'), the local change per RESEARCH open-question 2"
    - "Re-run of steps a+b produces zero duplicate project_sa_instances / project_team_instances rows (P1)"
  artifacts:
    - path: "backend/app/services/team_harness_setup_service.py"
      provides: "_step_grd_init and _step_team_topology bodies replacing the placeholders"
      contains: "driver"
  key_links:
    - from: "backend/app/services/team_harness_setup_service.py"
      to: "backend/app/services/instance_service.py"
      via: "create_team_instances after existing-instance check"
      pattern: "create_team_instances"
    - from: "backend/app/services/team_harness_setup_service.py"
      to: "backend/app/db/project_sa_instances.py"
      via: "post-create update(driver='grd') / get_instance_driver"
      pattern: "driver"
---

<objective>
Implement the first two service steps: (a) GRD-init reconcile and (b) team topology + SA instances with `driver=grd`. Step b carries the phase's two sharpest pitfalls — SA-instance creation is NOT constraint-deduped (must existence-check first) and `create_team_instances` does NOT forward a `driver` kwarg (must post-update).

Purpose: Stand up the team's super-agent instances tagged for GRD-driven routing (Phase-19), idempotently.
Output: `_step_grd_init` and `_step_team_topology` bodies + driver/idempotency tests added to `test_team_harness_setup_service.py`.
Research basis: RESEARCH.md Seam 2 + Pitfalls 1,2 + Open Question 2. `create_team_instances(project_id, team_id)` (instance_service.py:52) takes no driver kwarg; the driver column lives on project_sa_instances (add_instance driver at :24, update driver at :104, get_instance_driver at :61). Decision (locked here): post-create `update(driver="grd")` per instance to keep the change local to this phase.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-RESEARCH.md
@.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-EVAL.md
@.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-02-SUMMARY.md
@backend/app/services/instance_service.py
@backend/app/db/project_sa_instances.py
@backend/app/services/grd_planning_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Step a — GRD-init reconcile (skip if .planning/ exists)</name>
  <files>backend/app/services/team_harness_setup_service.py</files>
  <action>
    Replace the `_step_grd_init` placeholder:
    - Resolve the project workspace from `get_project(project_id)["local_path"]`.
    - Reconcile: if `<local_path>/.planning/` already exists, return `StepResult("grd_init", "skipped", "planning already present")`.
    - Else invoke the GRD-init path. Prior art is `GrdPlanningService.auto_init_project` (grd_planning_service.py:165), which runs map-codebase + new-project via PTY. For the unit-testable surface, call into the existing service entry point rather than re-implementing PTY logic; if auto_init_project is fire-and-forget/async, record `StepResult("grd_init", "ok", "init triggered")` and let the deferred dogfood (D1) validate real completion. Keep it monkeypatchable in tests.
    - Fingerprint: hash the presence/mtime of `.planning/` so a re-run after init reliably skips.
    AVOID: never delete or re-init an existing .planning/ (no destructive deletes — SC4).
  </action>
  <verify>
    `cd /Users/neo/Developer/Projects/Agented/backend && uv run pytest tests/test_team_harness_setup_service.py -k "grd_init or idempotent" -v` green (Level 2: Proxy — P1)
  </verify>
  <done>_step_grd_init skips when .planning/ exists, else triggers init; re-run skips; no destructive re-init.</done>
</task>

<task type="auto">
  <name>Task 2: Step b — team topology + SA instances with driver=grd (existence-check + post-update) and tests</name>
  <files>backend/app/services/team_harness_setup_service.py, backend/tests/test_team_harness_setup_service.py</files>
  <action>
    Replace `_step_team_topology`:
    - Resolve the project's team (`owner_team_id` from get_project, or the project's team per the existing convention).
    - EXISTENCE CHECK FIRST (pitfall 1): query existing instances for (project_id, team_id). If a team instance + its SA instances already exist, do NOT call create_team_instances again — return `StepResult("team_topology", "skipped", ...)` (but still ensure driver is 'grd' via reconciliation below).
    - Else call `InstanceService.create_team_instances(project_id, team_id)` (instance_service.py:52).
    - DRIVER (pitfall 2 + Open Question 2 decision): for EACH created/existing SA instance, post-update its driver to "grd" via the project_sa_instances update path (project_sa_instances.py:104, `update(..., driver="grd")`). Verify with `get_instance_driver(instance_id)` (:61). This reconciles driver even on the skip path so re-runs converge to driver=grd.
    - Return `StepResult("team_topology", "ok"|"skipped", detail with instance count)`.
    Tests (add to test_team_harness_setup_service.py):
    - `test_team_topology_sets_driver_grd` (driver keyword): after step b, every SA instance has `get_instance_driver(instance_id) == "grd"`.
    - `test_team_topology_idempotent_no_duplicate` (idempotent keyword): run step b twice; assert instance count unchanged (existence-check prevents duplication).
    Use a seeded team + super-agents fixture (mirror tests/test_instance_service.py setup).
  </action>
  <verify>
    `cd /Users/neo/Developer/Projects/Agented/backend && uv run pytest tests/test_team_harness_setup_service.py -k "driver or idempotent" -v` green (Level 1 S4 + Level 2 P1)
  </verify>
  <done>SA instances created once (no duplicates on re-run) and all carry driver='grd' via get_instance_driver.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): S4 — `uv run pytest tests/test_team_harness_setup_service.py -k "driver" -v` asserts get_instance_driver=='grd'.
Level 2 (Proxy): P1 — idempotency for steps a+b (no duplicate instance rows on re-run).
</verification>

<success_criteria>
- Step a skips when .planning/ exists, else triggers GRD init; never destructive.
- Step b existence-checks before creating; no duplicate instances on re-run; every SA instance driver=='grd'.
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-03-SUMMARY.md`
</output>
