---
phase: 21-one-click-team-harness-setup
plan: 08
type: execute
wave: 3
depends_on: ["21-07"]
files_modified: []
autonomous: false
verification_level: deferred
hypothesis: "With all 21-01..07 plans merged, a single live ProjectDashboard click bootstraps a real team harness end-to-end — all 6 steps reach ok/skipped, the .claude projection materializes, all 4 backends compile it, a second run is idempotent, and the house gates (just build / pytest watchdog / frontend) pass."
predicted_outcome: "Live trigger returns 201/202; SSE completes with {step:'__done__', status:'ready'}; harness_setup_status=='ready'; .claude/ exists with all 4 renderers compiling; session auto-import second run adds zero forge_origin rows; just build exit 0; pytest watchdog (or disclosed targeted set) and frontend show no new failures."
must_haves:
  truths:
    - "DEFER-21-01: one live end-to-end setup run on a real git-tracked project completes — all 6 steps ok/skipped, .claude/ on disk, all 4 backends (claude/codex/gemini/opencode) compile the projection, status 'ready' (SC5)"
    - "DEFER-21-02: session-completion auto-import second run is idempotent — zero new forge_origin rows for the same content hash (closes DEFER-17-02)"
    - "DEFER-21-03: just build (vue-tsc + vite) exits 0 with the new Vue surface + 4-locale i18n (SC6)"
    - "DEFER-21-04: backend pytest under ~12-min watchdog (targeted disclosed set on hang) + frontend no-new-failures (SC6)"
  artifacts: []
  key_links: []
---

<objective>
Close the deferred validations (EVAL Level 3 / DEFER-21-01..04): the live 4-backend dogfood, session auto-import idempotency, and the three house gates. These require a real project, real subprocess/PTY execution, and live AI backend credentials — none available in the unit-test environment — so they run only after 21-01..07 are merged.

Purpose: Prove SC5 (end-to-end, all 4 backends compile) and SC6 (house gates) on real infrastructure.
Output: A dogfood run record + gate results captured in 21-08-SUMMARY.md.
Research basis: EVAL.md Level 3 (D1-D4); CLAUDE.md verification procedure (pytest serial-hang watchdog + targeted-set disclosure; frontend 7 known failures exempt; just kill port-scoped).
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-RESEARCH.md
@.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-EVAL.md
@.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-07-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full one-click team harness setup (21-01..07): status column + service + 6 steps + route/SSE + ProjectDashboard surface. This checkpoint runs the live dogfood that automated tests cannot.</what-built>
  <how-to-verify>
    DEFER-21-01 (live dogfood): On a real git-tracked project with local_path + .planning/ + STACK.md, click "Setup Team Harness" in ProjectDashboard.
    1. Trigger returns 201/202; the step panel streams progress.
    2. SSE completes with `{"step":"__done__","status":"ready"}`; GET status shows harness_setup_status == "ready", all 6 steps ok/skipped.
    3. Inspect the project workspace: `.claude/` exists with non-empty content + manifest at `.claude/agented-forge/manifest.json`.
    4. Confirm the step-f compile-smoke log shows all 4 backends (claude/codex/gemini/opencode) compiled with no error.
    5. Re-click "Setup Team Harness": confirm reconcile (steps skipped, no duplicate instances/bindings, status returns to ready) — no destructive deletes.
    DEFER-21-02 (auto-import idempotency): trigger session-complete import twice on the dogfood project; confirm the second run adds zero new forge_origin rows for the same content hash.
    Fallback on a failing renderer/step: use per-step retry (re-trigger) to isolate the failing step without re-running completed ones.
  </how-to-verify>
  <resume-signal>Type "approved" with the observed status + .claude/ contents + 4-backend compile result, or describe the failing step/backend.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: House gates — just build + pytest watchdog + frontend</name>
  <files></files>
  <action>
    DEFER-21-03: `cd /Users/neo/Developer/Projects/Agented && just build` — expect exit 0 (vue-tsc + vite). Fix any TS error in the new Vue component / api types / missing i18n key before proceeding.
    DEFER-21-04 (per CLAUDE.md procedure):
    - Backend: `cd /Users/neo/Developer/Projects/Agented/backend && timeout 720 uv run pytest`. On the known ~40-48% serial hang, kill and run the disclosed targeted set: `uv run pytest tests/test_team_harness_setup_service.py tests/test_harness_setup_status_migration.py tests/routes/test_harness_setup_routes.py tests/test_forge_materialization.py tests/test_forge_bindings_routes.py tests/test_instance_service.py tests/test_tesserae_integration.py tests/test_harness_autonomy.py -v`. Disclose the substitution in the SUMMARY/PR; never present the targeted run as the full suite.
    - Frontend: `cd /Users/neo/Developer/Projects/Agented/frontend && npm run test:run` — gate is no NEW failures beyond the 7 known (RateLimitGauge, MarkdownContent, WorkingMemoryView, useTourMachine).
    Record all three gate outcomes in 21-08-SUMMARY.md.
  </action>
  <verify>
    just build exit 0; backend targeted/full suite no new failures; frontend no new failures (Level 3: Deferred — D3, D4)
  </verify>
  <done>All three house gates pass (or the pytest hang substitution is disclosed); results recorded in SUMMARY.</done>
</task>

</tasks>

<verification>
Level 3 (Deferred):
- D1: live 4-backend dogfood completes, status ready, .claude/ on disk (checkpoint).
- D2: session auto-import second run idempotent (checkpoint).
- D3: just build exit 0.
- D4: backend pytest watchdog (or disclosed targeted set) + frontend no-new-failures.
</verification>

<success_criteria>
- Live setup run reaches ready with all 6 steps ok/skipped, .claude/ materialized, all 4 backends compiling; re-run reconciles non-destructively.
- Session auto-import second run adds zero forge_origin rows.
- just build exit 0; backend + frontend gates show no new failures.
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-08-SUMMARY.md` recording the dogfood observations and all three gate results.
</output>
