---
phase: 21-one-click-team-harness-setup
plan: 05
type: execute
wave: 2
depends_on: ["21-02"]
files_modified:
  - backend/app/services/team_harness_setup_service.py
  - backend/tests/test_team_harness_setup_service.py
autonomous: true
verification_level: proxy
hypothesis: "A single project_autonomy_config row of AutonomyPolicy(enabled=True, allowed_kinds=['discovered_procedure'], block_deletes=True, max_ops_per_round=1) satisfies BOTH dual consumers — _auto_apply_policy returns True (takeaway auto-apply ON, scoped) while autonomous_apply_eligible stays conservative because allowed_kinds excludes rule/hook and block_deletes is on — written idempotently via upsert_policy."
predicted_outcome: "test_team_harness_setup_service.py -k 'autonomy_policy' passes: get_policy returns the row with block_deletes True and allowed_kinds ['discovered_procedure'], and _auto_apply_policy(project_id) is True; re-run leaves a single row."
must_haves:
  truths:
    - "Step e writes a project_autonomy_config row via upsert_policy (ON CONFLICT(project_id) DO UPDATE — idempotent, single row on re-run) (P1)"
    - "_auto_apply_policy(project_id) returns True for kind 'discovered_procedure' (takeaway auto-apply ON, scoped to skill-from-repetition) (SC2/P7)"
    - "Evolution autonomy stays conservative: block_deletes=True, allowed_kinds limited to ['discovered_procedure'] (so rule/hook patches are gate-blocked), max_ops_per_round=1, conservative rate_limit (SC2/P7)"
    - "Re-run does NOT delete or duplicate the autonomy config row (no destructive deletes — SC4)"
  artifacts:
    - path: "backend/app/services/team_harness_setup_service.py"
      provides: "_step_default_policies body building the dual-consumer AutonomyPolicy"
      contains: "discovered_procedure"
  key_links:
    - from: "backend/app/services/team_harness_setup_service.py"
      to: "backend/app/db/project_autonomy_config.py"
      via: "upsert_policy(project_id, AutonomyPolicy(...))"
      pattern: "upsert_policy"
    - from: "backend/app/services/repeated_request_gate.py"
      to: "backend/app/db/project_autonomy_config.py"
      via: "_auto_apply_policy reads enabled + discovered_procedure in kinds"
      pattern: "_auto_apply_policy"
---

<objective>
Implement step (e): write the dual-consumer default autonomy policy. The single `project_autonomy_config` row is read by BOTH `repeated_request_gate._auto_apply_policy` (must return True, scoped to `discovered_procedure`) and `harness_autonomy.autonomous_apply_eligible` (must stay conservative). Resolve the tension with `enabled=True` + a tightly-scoped `allowed_kinds`.

Purpose: Turn on takeaway auto-apply (skill-from-repetition) while keeping evolution autonomy conservative — both from one policy row.
Output: `_step_default_policies` body + the autonomy_policy test asserting both gate readers.
Research basis: RESEARCH.md Seam 5 + Open Question 1. Verified field names (app/models/autonomy_policy.py): enabled, confidence_threshold, max_ops_per_round, allowed_kinds, block_deletes, cooldown_seconds, rate_limit_per_day. `upsert_policy` (project_autonomy_config.py:25, ON CONFLICT idempotent); `_auto_apply_policy` (repeated_request_gate.py:125) requires enabled=True AND "discovered_procedure" in kinds (:156). Recommended row: `AutonomyPolicy(enabled=True, allowed_kinds=["discovered_procedure"], block_deletes=True, max_ops_per_round=1)`.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-RESEARCH.md
@.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-EVAL.md
@.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-02-SUMMARY.md
@backend/app/models/autonomy_policy.py
@backend/app/db/project_autonomy_config.py
@backend/app/services/repeated_request_gate.py
@backend/app/services/harness_autonomy.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Step e — dual-consumer AutonomyPolicy via upsert_policy</name>
  <files>backend/app/services/team_harness_setup_service.py</files>
  <action>
    Replace `_step_default_policies`:
    - Build `policy = AutonomyPolicy(enabled=True, allowed_kinds=["discovered_procedure"], block_deletes=True, max_ops_per_round=1)`. Leave confidence_threshold at the conservative default (0.85) and set a conservative rate_limit_per_day (keep default 10 or lower — document choice). These values arm the takeaway-auto-apply `enabled` gate while keeping evolution autonomy cautious (allowed_kinds excludes rule/hook → those evolution patches are gate-blocked; block_deletes stays on; max_ops_per_round=1 caps blast radius).
    - `upsert_policy(project_id, policy)` (project_autonomy_config.py:25) — ON CONFLICT(project_id) DO UPDATE makes re-run a single-row update.
    - Reconcile: if `get_policy(project_id)` already equals the target, return StepResult skipped; else ok.
    - Return StepResult with a one-line policy summary in detail.
    AVOID: never DELETE the autonomy config row on re-run (SC4).
  </action>
  <verify>
    `cd /Users/neo/Developer/Projects/Agented/backend && uv run pytest tests/test_team_harness_setup_service.py -k "autonomy_policy" -v` green (Level 2: Proxy — P7)
  </verify>
  <done>Step e upserts the dual-consumer policy row; re-run keeps a single row.</done>
</task>

<task type="auto">
  <name>Task 2: autonomy_policy test — both gate readers + idempotency</name>
  <files>backend/tests/test_team_harness_setup_service.py</files>
  <action>
    Add `test_default_policies_dual_consumer` (autonomy_policy keyword) with isolated_db:
    - Run step e (or full setup with other steps stubbed ok).
    - Assert `get_policy(project_id)` returns a row with `enabled is True`, `block_deletes is True`, `allowed_kinds == ["discovered_procedure"]`, `max_ops_per_round == 1`.
    - Assert `_auto_apply_policy(project_id) is True` (import from repeated_request_gate; it reads the same isolated DB). This is the takeaway-auto-apply-ON, scoped-to-discovered_procedure assertion.
    - Conservative-evolution assertion: assert allowed_kinds does NOT contain "rule"/"hook" and block_deletes True — i.e. the policy that satisfies auto-apply also constrains evolution. (Optionally exercise harness_autonomy.autonomous_apply_eligible with a fabricated rule-kind round and assert NOT eligible on the allowed_kinds gate; if that helper needs many fixtures, document the structural assertion instead per EVAL P7 blind-spot note.)
    - Idempotency: run step e twice; assert exactly one project_autonomy_config row.
  </action>
  <verify>
    `cd /Users/neo/Developer/Projects/Agented/backend && uv run pytest tests/test_team_harness_setup_service.py -k "autonomy_policy" -v` green (Level 2: Proxy — P7)
  </verify>
  <done>get_policy row shape correct; _auto_apply_policy True; allowed_kinds scoped + block_deletes on; single row on re-run.</done>
</task>

</tasks>

<verification>
Level 2 (Proxy): P7 — conservative autonomy policy row + scoped auto-apply ON, both gate readers checked; single row on re-run.
</verification>

<success_criteria>
- project_autonomy_config row = AutonomyPolicy(enabled=True, allowed_kinds=["discovered_procedure"], block_deletes=True, max_ops_per_round=1).
- _auto_apply_policy(project_id) is True; evolution stays conservative; idempotent (one row).
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-05-SUMMARY.md`
</output>
