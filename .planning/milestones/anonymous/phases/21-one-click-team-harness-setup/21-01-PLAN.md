---
phase: 21-one-click-team-harness-setup
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/app/db/migrations/v07_features.py
  - backend/app/db/projects.py
  - backend/app/services/grd_planning_service.py
  - backend/tests/test_harness_setup_status_migration.py
autonomous: true
verification_level: sanity
hypothesis: "A PRAGMA-guarded migration 159 mirroring grd_init_status (v05:38) plus a harness_setup_steps table (PK project_id,step_key) gives an idempotent persistence floor that get/set_harness_setup_status read/write and that double-apply leaves unchanged."
predicted_outcome: "test_harness_setup_status_migration.py is all green: PRAGMA shows projects.harness_setup_status (default 'none'), harness_setup_steps exists with PK (project_id,step_key), double-apply raises nothing, and get_harness_setup_status returns 'none' for a fresh project."
must_haves:
  truths:
    - "projects.harness_setup_status column exists, TEXT DEFAULT 'none', added by migration 159 with a PRAGMA-guard so re-apply is a no-op (S1)"
    - "harness_setup_steps table exists with PK (project_id, step_key) and columns status/detail/fingerprint/updated_at; ON CONFLICT upsert reconciles (S1)"
    - "get_harness_setup_status(project_id) returns 'none' for a fresh project, coercing NULL to 'none' exactly like get_init_status (S2)"
    - "set_harness_setup_status(project_id, status) and a harness_setup_steps upsert helper persist and round-trip"
  artifacts:
    - path: "backend/app/db/migrations/v07_features.py"
      provides: "_migrate_159_harness_setup added to V07_MIGRATIONS list"
      contains: "harness_setup_status"
    - path: "backend/app/db/projects.py"
      provides: "update_project gains harness_setup_status kwarg; set_harness_setup_status + get_harness_setup_status helpers + harness_setup_steps upsert/read helpers"
      contains: "harness_setup_status"
    - path: "backend/tests/test_harness_setup_status_migration.py"
      provides: "S1/S2 sanity tests"
      exports: ["test_get_harness_setup_status_defaults_none"]
  key_links:
    - from: "backend/app/db/projects.py"
      to: "projects.harness_setup_status column"
      via: "UPDATE projects SET harness_setup_status=?"
      pattern: "harness_setup_status"
    - from: "backend/app/services/grd_planning_service.py"
      to: "backend/app/db/projects.py"
      via: "get_harness_setup_status coalesce-to-none mirrors get_init_status"
      pattern: "get_harness_setup_status"
---

<objective>
Build the persistence floor for the one-click team harness setup: a `projects.harness_setup_status` column (none/running/ready/failed) and a `harness_setup_steps` table that gives per-step idempotent upsert + retry granularity, plus the status read/write helpers everything downstream depends on.

Purpose: Every later plan (service skeleton, step groups, route, dashboard) reads or writes this status and the step rows. This is the single hard dependency for the whole phase.
Output: Migration 159, `update_project` kwarg + `get/set_harness_setup_status` + `harness_setup_steps` upsert/read helpers, and the S1/S2 sanity test file.
Research basis: RESEARCH.md Seam 0 — copy the PRAGMA-guard from `v05_features.py:38-44` (NOT v07:1181); next free migration is 159; register in the `V07_MIGRATIONS` list near `v07_features.py:1189`.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-RESEARCH.md
@.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-EVAL.md
@backend/app/db/migrations/v07_features.py
@backend/app/db/projects.py
@backend/app/services/grd_planning_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 159 — harness_setup_status column + harness_setup_steps table</name>
  <files>backend/app/db/migrations/v07_features.py</files>
  <action>
    Add `_migrate_159_harness_setup(conn)` mirroring the PRAGMA-guard pattern of
    `_migrate_v54_project_grd_init_status` at v05_features.py:38-44:
    - `cols = {r[1] for r in conn.execute("PRAGMA table_info(projects)")}`
    - `if "harness_setup_status" not in cols:` →
      `conn.execute("ALTER TABLE projects ADD COLUMN harness_setup_status TEXT DEFAULT 'none'")`
    - Create the steps table guarded by existence:
      `CREATE TABLE IF NOT EXISTS harness_setup_steps (project_id TEXT NOT NULL, step_key TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'pending', detail TEXT, fingerprint TEXT, updated_at TEXT, PRIMARY KEY (project_id, step_key))`
    Register `_migrate_159_harness_setup` in the `V07_MIGRATIONS` list (the same list that ends with `_migrate_158_driver_columns` near v07_features.py:1189). Use migration id 159.
    AVOID: do NOT copy from v07:1181 (`_migrate_157_forge_origin`) — that is the wrong baseline cited in stale EVAL text. The guard MUST make double-apply a pure no-op.
  </action>
  <verify>
    `cd /Users/neo/Developer/Projects/Agented/backend && uv run python -c "from app.db.migrations.v07_features import V07_MIGRATIONS; print(any('159' in getattr(m,'__name__','') for m in V07_MIGRATIONS))"` prints True (Level 1: Sanity)
  </verify>
  <done>Migration 159 registered; applying it on a fresh DB adds the column (default 'none') and the steps table; re-applying raises nothing.</done>
</task>

<task type="tdd">
  <name>Task 2: Status helpers + step upsert + S1/S2 tests (RED→GREEN)</name>
  <files>backend/tests/test_harness_setup_status_migration.py, backend/app/db/projects.py, backend/app/services/grd_planning_service.py</files>
  <action>
    RED — write `backend/tests/test_harness_setup_status_migration.py` using the `isolated_db` fixture:
    - `test_migration_adds_column_and_table`: PRAGMA shows `harness_setup_status` in projects; `harness_setup_steps` exists; PK is `(project_id, step_key)` (query `PRAGMA table_info`/`PRAGMA index_list`). Apply migration twice → no error.
    - `test_get_harness_setup_status_defaults_none`: create a project, assert `get_harness_setup_status(project_id) == "none"` BEFORE any set call.
    - `test_set_and_get_roundtrip`: `set_harness_setup_status(pid, "running")` then `get_harness_setup_status(pid) == "running"`.
    - `test_upsert_step_idempotent`: call the steps-upsert helper twice for the same `(pid, step_key)`; assert exactly one row and the latest status/detail wins.
    GREEN — implement in projects.py:
    - Add `harness_setup_status: str = None` to `update_project` (projects.py:60) with an `if harness_setup_status is not None:` block mirroring the grd_init_status block (~projects.py:109-111).
    - Add `set_harness_setup_status(project_id, status)` → direct `UPDATE projects SET harness_setup_status=?`.
    - Add `upsert_harness_setup_step(project_id, step_key, status, detail=None, fingerprint=None)` → `INSERT ... ON CONFLICT(project_id, step_key) DO UPDATE SET status=excluded.status, detail=excluded.detail, fingerprint=excluded.fingerprint, updated_at=...` (mirror upsert_policy at project_autonomy_config.py:25). Set updated_at to an ISO timestamp.
    - Add `get_harness_setup_steps(project_id) -> list[dict]` reading all rows for the project.
    - Add `get_harness_setup_status(cls, project_id) -> str` to GrdPlanningService mirroring `get_init_status` at grd_planning_service.py:149 (`return project.get("harness_setup_status") or "none"`), OR a module-level helper in projects.py if that is more consistent with where get_init_status lives. Match whichever module hosts get_init_status.
  </action>
  <verify>
    `cd /Users/neo/Developer/Projects/Agented/backend && uv run pytest tests/test_harness_setup_status_migration.py -v` all green (Level 1: Sanity — maps S1, S2)
  </verify>
  <done>All tests in test_harness_setup_status_migration.py pass; get_harness_setup_status returns 'none' for a fresh project; step upsert is idempotent (one row).</done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- S1: `uv run pytest tests/test_harness_setup_status_migration.py -v` — column + table + PK present, double-apply no-op.
- S2: `uv run pytest tests/test_harness_setup_status_migration.py::test_get_harness_setup_status_defaults_none -v` — returns 'none'.
- S5 prerequisite: `uv run ruff check app/db/projects.py app/db/migrations/v07_features.py` clean.
</verification>

<success_criteria>
- Migration 159 registered in V07_MIGRATIONS; fresh-apply creates column (default 'none') + harness_setup_steps table (PK project_id,step_key); double-apply is a no-op.
- get_harness_setup_status coerces NULL→'none'; set_harness_setup_status round-trips; step upsert is idempotent.
- Ruff clean on touched modules.
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-01-SUMMARY.md`
</output>
