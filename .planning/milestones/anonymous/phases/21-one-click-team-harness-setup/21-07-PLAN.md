---
phase: 21-one-click-team-harness-setup
plan: 07
type: execute
wave: 3
depends_on: ["21-02", "21-03", "21-04", "21-05", "21-06"]
files_modified:
  - backend/app_litestar/routes/grd_routes.py
  - backend/tests/routes/test_harness_setup_routes.py
  - frontend/src/views/ProjectDashboard.vue
  - frontend/src/services/api/index.ts
  - frontend/src/locales/en.json
  - frontend/src/locales/ko.json
  - frontend/src/locales/ja.json
  - frontend/src/locales/zh.json
  - frontend/src/views/__tests__/ProjectDashboard.harness-setup.test.ts
autonomous: true
verification_level: proxy
hypothesis: "POST /admin/projects/{id}/harness-setup flips status to running and spawns setup() on a thread (grd-chat pattern), GET .../status returns the step list, and GET .../stream yields text/event-stream step events (agents_and_tracing.py:228 Stream pattern); ProjectDashboard renders a Setup-Team-Harness button + status chip + step panel driven by EventSource with 4-locale i18n."
predicted_outcome: "tests/routes/test_harness_setup_routes.py green (POST→202 + running, status→step list, stream→text/event-stream with a data: step event) and frontend npm run test:run has no NEW failures beyond the 7 known, with the new ProjectDashboard.harness-setup test passing."
must_haves:
  truths:
    - "POST /admin/projects/{id}/harness-setup returns 202, flips harness_setup_status to 'running', and spawns TeamHarnessSetupService.setup on a background thread (mirrors grd chat thread-spawn at grd_routes.py:709) (P5)"
    - "GET /admin/projects/{id}/harness-setup/status returns {harness_setup_status, steps:[...]} guarded by _ensure_project (P5)"
    - "GET /admin/projects/{id}/harness-setup/stream returns media_type text/event-stream and yields event:/data: framed JSON step events (Stream pattern agents_and_tracing.py:228) (P5)"
    - "ProjectDashboard shows a 'Setup Team Harness' button when status 'none', a status chip per state (running/ready/failed), and a step panel fed by EventSource — mirroring grdInitStatus wiring (P8)"
    - "harnessSetup.* i18n namespace added to all four locales (en/ko/ja/zh), key-identical (P8 / D3)"
  artifacts:
    - path: "backend/app_litestar/routes/grd_routes.py"
      provides: "POST harness-setup + GET status + GET stream endpoints"
      contains: "harness-setup"
    - path: "frontend/src/views/ProjectDashboard.vue"
      provides: "harnessSetupStatus ref + button + chip + step panel + EventSource"
      contains: "harnessSetup"
    - path: "backend/tests/routes/test_harness_setup_routes.py"
      provides: "P5 route/SSE tests"
    - path: "frontend/src/views/__tests__/ProjectDashboard.harness-setup.test.ts"
      provides: "P8 dashboard tests"
  key_links:
    - from: "backend/app_litestar/routes/grd_routes.py"
      to: "backend/app/services/team_harness_setup_service.py"
      via: "POST handler spawns TeamHarnessSetupService.setup on a thread"
      pattern: "TeamHarnessSetupService"
    - from: "frontend/src/views/ProjectDashboard.vue"
      to: "backend/app_litestar/routes/grd_routes.py"
      via: "fetch status + EventSource(stream) + POST trigger via api client"
      pattern: "harness-setup"
---

<objective>
Wire the operator surface: backend route trio (POST trigger / GET status / GET SSE stream) and the ProjectDashboard button + status chip + step panel, with 4-locale i18n. This makes the whole phase reachable from one click.

Purpose: Deliver REQ-19 / SC1 — a single ProjectDashboard button bootstraps the team harness with step-level SSE progress, mirroring the existing grd_init pattern.
Output: 3 endpoints in grd_routes.py, route/SSE test, ProjectDashboard surface, harnessSetup api client method, en/ko/ja/zh i18n, ProjectDashboard test (first one).
Research basis: RESEARCH.md Seam 0. SSE Stream pattern: agents_and_tracing.py:11 (`from litestar.response import Stream`), :182 (`@get(.../stream, media_type="text/event-stream", sync_to_thread=False)`), :201-228 (`event:`/`data:` framing + `return Stream(...)`). Thread-spawn + status endpoint prior art: grd_routes.py:709/718/732 (planning invoke + status returning `{"grd_init_status": ...}`); guard with `_ensure_project`. Dashboard wiring: ProjectDashboard.vue:91 (`grdInitStatus = ref('none')`), :298-304 (fetch+watch), :519-537 (chip + buttons). Test prior art: tests/routes/test_forge_bindings_routes.py.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-RESEARCH.md
@.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-EVAL.md
@.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-02-SUMMARY.md
@backend/app_litestar/routes/grd_routes.py
@backend/app_litestar/routes/agents_and_tracing.py
@backend/tests/routes/test_forge_bindings_routes.py
@frontend/src/views/ProjectDashboard.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend route trio + P5 route/SSE test</name>
  <files>backend/app_litestar/routes/grd_routes.py, backend/tests/routes/test_harness_setup_routes.py</files>
  <action>
    In grd_routes.py (mirroring the planning invoke/status endpoints at :718/:732 and the SSE Stream at agents_and_tracing.py:228):
    - `@post("/{project_id:str}/harness-setup", status_code=202)`: `_ensure_project`; `set_harness_setup_status(project_id, "running")`; spawn `threading.Thread(target=TeamHarnessSetupService.setup, args=(project_id,), daemon=True).start()` (same thread-spawn shape as grd chat at grd_routes.py:709); return `{"harness_setup_status": "running"}`.
    - `@get("/{project_id:str}/harness-setup/status")`: `_ensure_project`; return `{"harness_setup_status": get_harness_setup_status(project_id), "steps": get_harness_setup_steps(project_id)}`.
    - `@get("/{project_id:str}/harness-setup/stream", media_type="text/event-stream", sync_to_thread=False)`: `_ensure_project`; `async def event_generator():` polls `get_harness_setup_steps` + status, yields `f"event: step\ndata: {json.dumps({'step': k, 'status': s, ...})}\n\n"` per changed step, and a terminal `event: done` / `data: {"step":"__done__","status": final}` when status is ready/failed; `return Stream(event_generator(), media_type="text/event-stream")`.
    Confirm these mount under the existing `/admin/projects` router prefix the planning endpoints use.
    Test (tests/routes/test_harness_setup_routes.py) mirroring test_forge_bindings_routes.py with TestClient + isolated_db:
    - Monkeypatch `TeamHarnessSetupService.setup` to a synchronous stub that writes 2 step rows and sets status 'ready'.
    - Assert POST → 202 and status flips to running (or ready after the stubbed sync setup — assert the documented behavior).
    - Assert GET status returns the step list.
    - Assert GET stream → `content-type: text/event-stream` with at least one `data:` line containing a `{"step": ...}` shape.
    Per CLAUDE.md: TestClient logger doesn't propagate to caplog — if asserting warnings, spy on `grd_routes.logger.warning` via monkeypatch.
  </action>
  <verify>
    `cd /Users/neo/Developer/Projects/Agented/backend && uv run pytest tests/routes/test_harness_setup_routes.py -v` green (Level 2: Proxy — P5)
  </verify>
  <done>POST 202 + running; GET status returns steps; GET stream is text/event-stream with data: step events.</done>
</task>

<task type="auto">
  <name>Task 2: ProjectDashboard surface + api client + 4-locale i18n</name>
  <files>frontend/src/views/ProjectDashboard.vue, frontend/src/services/api/index.ts, frontend/src/locales/en.json, frontend/src/locales/ko.json, frontend/src/locales/ja.json, frontend/src/locales/zh.json</files>
  <action>
    api client: add a `harnessSetupApi` (or extend the relevant per-domain module re-exported through services/api/index.ts) with `trigger(projectId)` (POST), `getStatus(projectId)` (GET), and a stream URL helper for EventSource. Mirror the existing grd planning api calls.
    ProjectDashboard.vue (mirror grdInitStatus at :91/:298/:304/:519-537):
    - `const harnessSetupStatus = ref<string>('none')`; fetch it alongside grdInitStatus (~:298-300); `watch` to react.
    - Add a "Setup Team Harness" button (near setup-btn :537) shown when status is 'none' or 'failed' (retry); on click call trigger() then open an `EventSource` on the stream URL.
    - Status chip badges for running/ready/failed (mirror :519-521).
    - A step panel listing step rows (key + status + detail) updated from EventSource `message` events; close the EventSource on the `__done__`/done event.
    - All visible strings via `useI18n` under a new `harnessSetup.*` namespace.
    i18n: add the `harnessSetup.*` namespace to en/ko/ja/zh, key-identical (button label, chip states none/running/ready/failed, step labels). Per CLAUDE.md i18n parity rule — all four locales must have identical keys.
  </action>
  <verify>
    `cd /Users/neo/Developer/Projects/Agented/frontend && npm run test:run` — new ProjectDashboard.harness-setup test passes; no NEW failures beyond the 7 known (Level 2: Proxy — P8). Also confirm i18n parity by key count across the 4 locale files.
  </verify>
  <done>Dashboard shows button + chip + step panel wired to EventSource; harnessSetup.* present and key-identical in all 4 locales.</done>
</task>

<task type="auto">
  <name>Task 3: ProjectDashboard.harness-setup.test.ts (P8)</name>
  <files>frontend/src/views/__tests__/ProjectDashboard.harness-setup.test.ts</files>
  <action>
    Create the first ProjectDashboard test (Vitest + @vue/test-utils + happy-dom), mocking `EventSource` and the harnessSetup api:
    - Button renders when harnessSetupStatus is 'none'.
    - Status chip shows the correct label/state for each of running/ready/failed.
    - Step panel renders step rows pushed via a mocked EventSource `message` dispatch.
    Mock the api module so no network call fires. Keep within the no-NEW-failures gate.
  </action>
  <verify>
    `cd /Users/neo/Developer/Projects/Agented/frontend && npm run test:run` — new test green, no new failures (Level 2: Proxy — P8)
  </verify>
  <done>ProjectDashboard.harness-setup.test.ts passes: button, chip states, step panel via mocked EventSource.</done>
</task>

</tasks>

<verification>
Level 2 (Proxy):
- P5: route/SSE — POST 202+running, GET status step list, GET stream text/event-stream.
- P8: frontend — button + chip + step panel render; no NEW failures beyond 7 known; 4-locale i18n parity.
Level 3 (Deferred): D3 (just build) depends on this plan's frontend changes landing — validated in 21-08.
</verification>

<success_criteria>
- POST/GET-status/GET-stream endpoints behave per P5; stream is text/event-stream with step events.
- ProjectDashboard renders Setup-Team-Harness button + status chip + EventSource-fed step panel.
- harnessSetup.* i18n key-identical across en/ko/ja/zh.
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-07-SUMMARY.md`
</output>
