---
phase: 21-one-click-team-harness-setup
plan: 02
type: execute
wave: 1
depends_on: ["21-01"]
files_modified:
  - backend/app/services/team_harness_setup_service.py
  - backend/tests/test_team_harness_setup_service.py
autonomous: true
verification_level: sanity
hypothesis: "A thin sequential orchestrator with HARNESS_SETUP_STEP_KEYS (6), per-step try/except → StepResult, a none→running→ready/failed state machine, step-row persistence, and retry-skips-completed logic gives a reusable skeleton that the four step-group plans plug functions into."
predicted_outcome: "TeamHarnessSetupService and HARNESS_SETUP_STEP_KEYS import cleanly with len(HARNESS_SETUP_STEP_KEYS)==6; a stubbed step set runs none→running→ready, records 6 step rows, and on a re-run skips rows already status='ok'."
must_haves:
  truths:
    - "from app.services.team_harness_setup_service import TeamHarnessSetupService, HARNESS_SETUP_STEP_KEYS succeeds with no NameError and len==6 (S3)"
    - "setup(project_id) sets status 'running' at start, 'ready' if all steps ok/skipped, 'failed' if any step raises — and never raises out of setup() itself"
    - "each step is wrapped in try/except producing a StepResult(status ok|skipped|failed, detail); a failure records the step row and stops/flags overall failed without rolling back prior steps"
    - "re-run skips steps whose harness_setup_steps row is already status='ok' (or whose fingerprint is unchanged); failed/pending steps are re-attempted (P6 retry foundation)"
  artifacts:
    - path: "backend/app/services/team_harness_setup_service.py"
      provides: "TeamHarnessSetupService orchestrator + HARNESS_SETUP_STEP_KEYS + StepResult + step dispatch table"
      exports: ["TeamHarnessSetupService", "HARNESS_SETUP_STEP_KEYS"]
      min_lines: 80
    - path: "backend/tests/test_team_harness_setup_service.py"
      provides: "skeleton tests: import smoke, state machine, retry-skips-ok; extended by 21-03..06"
  key_links:
    - from: "backend/app/services/team_harness_setup_service.py"
      to: "backend/app/db/projects.py"
      via: "set_harness_setup_status + upsert_harness_setup_step + get_harness_setup_steps"
      pattern: "harness_setup_step"
---

<objective>
Build the `TeamHarnessSetupService` skeleton: the six-step dispatch framework, the per-step StepResult contract, the overall status state machine, step-row persistence, and the retry-skips-completed logic. The four wave-2 step-group plans (21-03..06) implement the actual step bodies against this contract; the wave-3 route plan (21-07) calls `setup()`.

Purpose: Decouple the orchestration/idempotency/failure-handling machinery (built once, here) from the six step bodies (built in parallel after). This is the second half of the wave-1 foundation.
Output: The service module with stubbed/placeholder step functions wired into the dispatch table, plus the cross-cutting test scaffold.
Research basis: RESEARCH.md "Primary recommendation" — thin sequential orchestrator; each step (a) checks fingerprint/existence to skip-vs-run, (b) calls an existing helper, (c) writes a harness_setup_steps row; wrap each in try/except so failure records `failed` and leaves the log intact.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-RESEARCH.md
@.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-EVAL.md
@.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-01-SUMMARY.md
@backend/app/db/projects.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Service skeleton — step keys, StepResult, state machine, dispatch table</name>
  <files>backend/app/services/team_harness_setup_service.py</files>
  <action>
    Create `team_harness_setup_service.py`:
    - `HARNESS_SETUP_STEP_KEYS = ["grd_init", "team_topology", "bundle_binding", "tesserae_enable", "default_policies", "materialize_compile"]` (exactly 6, ordered a→f).
    - `@dataclass StepResult: step_key: str; status: str  # "ok" | "skipped" | "failed"; detail: str = ""`.
    - `class TeamHarnessSetupService:` with a classmethod `setup(cls, project_id: str) -> str` (returns final status):
      1. `set_harness_setup_status(project_id, "running")`.
      2. Load existing step rows via `get_harness_setup_steps(project_id)` into a dict by step_key.
      3. For each key in HARNESS_SETUP_STEP_KEYS, call the dispatch function `cls._STEP_FUNCS[key](project_id, existing_row)`. SKIP (record StepResult ok/skipped without re-doing work) when the existing row is already `status == "ok"` AND its fingerprint is unchanged — but always re-attempt rows that are `failed`/`pending`/absent.
      4. Wrap each step call in try/except: on exception build `StepResult(key, "failed", str(exc))`, `upsert_harness_setup_step(project_id, key, "failed", detail=...)`, set an `overall_failed` flag, and STOP iterating further steps (record nothing for later steps so they remain retryable).
      5. On success, `upsert_harness_setup_step(project_id, key, result.status, detail=result.detail, fingerprint=...)`.
      6. After the loop: `set_harness_setup_status(project_id, "failed" if overall_failed else "ready")` and return it.
    - Define `_STEP_FUNCS: dict[str, Callable]` mapping each key to a placeholder `_step_<name>(project_id, existing_row) -> StepResult` that, for now, returns `StepResult(key, "ok")`. 21-03..06 replace these bodies. Keep the placeholders importable and side-effect-free so import smoke (S3) passes immediately.
    - Add a module logger; never let setup() itself raise (catch-all guard around the loop).
    AVOID: do NOT roll back prior steps on a later failure (no destructive deletes — SC4). Do NOT mark later steps as anything; absence keeps them retryable.
  </action>
  <verify>
    `cd /Users/neo/Developer/Projects/Agented/backend && uv run python -c "from app.services.team_harness_setup_service import TeamHarnessSetupService, HARNESS_SETUP_STEP_KEYS; assert len(HARNESS_SETUP_STEP_KEYS)==6; print('ok')"` prints ok (Level 1: Sanity — S3)
  </verify>
  <done>Service + HARNESS_SETUP_STEP_KEYS import cleanly (len 6); setup() runs the placeholder steps none→running→ready and writes 6 step rows.</done>
</task>

<task type="auto">
  <name>Task 2: Skeleton tests — import smoke, state machine, retry-skips-ok</name>
  <files>backend/tests/test_team_harness_setup_service.py</files>
  <action>
    Create `test_team_harness_setup_service.py` with `isolated_db`. These tests are EXTENDED by 21-03..06 (driver, idempotent, no_destructive, bundle_selection, renderer_compile, failed_step, autonomy_policy keyworded tests); author the skeleton subset here:
    - `test_import_smoke_six_step_keys`: assert len(HARNESS_SETUP_STEP_KEYS)==6 and keys match the ordered list.
    - `test_state_machine_fresh_run_ready`: monkeypatch `_STEP_FUNCS` to all return StepResult ok; run setup(); assert final status "ready" and `get_harness_setup_steps` has 6 rows all status 'ok'.
    - `test_failed_step_sets_failed_and_stops` (failed_step keyword): monkeypatch the 2nd step to raise; run setup(); assert status "failed", the failed step row has status='failed' with non-empty detail, and steps AFTER the failed one have NO rows (still retryable).
    - `test_rerun_skips_ok_steps` (idempotent keyword): run a successful setup once; monkeypatch a counter into one step; re-run; assert the already-ok step's body did NOT execute again (skip path) while a failed/absent step would.
    Use `-k` keywords ("failed_step", "idempotent") consistent with the EVAL P1/P6 commands so later plans add to the same file.
  </action>
  <verify>
    `cd /Users/neo/Developer/Projects/Agented/backend && uv run pytest tests/test_team_harness_setup_service.py -v` green (Level 1: Sanity for S3; foundation for P1/P6)
  </verify>
  <done>Skeleton tests pass: import smoke, fresh-run→ready with 6 ok rows, failed step→failed status + log + later steps unrecorded, re-run skips ok steps.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- S3: import smoke prints ok, 6 step keys.
- S5 prerequisite: `uv run ruff check app/services/team_harness_setup_service.py` clean.
Level 2 (Proxy, foundation): P6 (failed-step + retryable) and P1 (idempotent skip) scaffolded here, completed as step bodies land in 21-03..06.
</verification>

<success_criteria>
- TeamHarnessSetupService + HARNESS_SETUP_STEP_KEYS (6) import cleanly.
- setup() implements none→running→ready/failed with per-step try/except → StepResult and step-row persistence.
- Re-run skips ok steps; failed step leaves a step log and stops without touching later steps (retryable).
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-02-SUMMARY.md`
</output>
