---
phase: 21-one-click-team-harness-setup
plan: 06
type: execute
wave: 2
depends_on: ["21-02"]
files_modified:
  - backend/app/services/team_harness_setup_service.py
  - backend/tests/test_team_harness_setup_service.py
autonomous: true
verification_level: proxy
hypothesis: "Step f materializes bound primitives into a workspace via materialize_primitives (relying on its built-in _NEVER_DELETE guard for no-destructive-delete), then runs a per-backend compile smoke by calling renderer_for(b).render(...) for claude/codex/gemini/opencode and asserting each runs cleanly over the projection."
predicted_outcome: "test_team_harness_setup_service.py -k 'renderer_compile' passes: materialize writes a .claude projection and all four renderer_for(backend) renders complete without raising; re-materialize preserves _NEVER_DELETE files."
must_haves:
  truths:
    - "Step f calls materialize_primitives(project, kinds, workspace_path) (forge_materialization_service.py:183) — note signature takes the project dict, not project_id (P4)"
    - "For each backend in claude/codex/gemini/opencode, renderer_for(backend) (context_renderers/__init__.py:27) is fetched and its .render(...) runs cleanly over the materialized bundle without raising (compile smoke — SC2)"
    - "The materialize relies on the built-in _NEVER_DELETE guard (manifest, .claude/settings.json, .claude/mcp.json) for the no-destructive-delete invariant; no manual deletes (SC4/P2)"
    - "Re-running step f is deterministic — re-materialize over an existing workspace does not error and preserves _NEVER_DELETE files (P1)"
  artifacts:
    - path: "backend/app/services/team_harness_setup_service.py"
      provides: "_step_materialize_compile body — materialize + 4-renderer compile smoke"
      contains: "renderer_for"
  key_links:
    - from: "backend/app/services/team_harness_setup_service.py"
      to: "backend/app/services/forge_materialization_service.py"
      via: "materialize_primitives(project, kinds, workspace_path)"
      pattern: "materialize_primitives"
    - from: "backend/app/services/team_harness_setup_service.py"
      to: "backend/app/services/context_renderers/__init__.py"
      via: "renderer_for(backend).render(...) for 4 backends"
      pattern: "renderer_for"
---

<objective>
Implement step (f): materialize the bound forge primitives into the project workspace, then run a per-backend compile smoke check across all four harnesses (claude/codex/gemini/opencode). "Compile smoke" = each renderer runs cleanly over the materialized projection (not a subprocess compile).

Purpose: Produce the `.claude/` projection and prove every backend renderer accepts it — the final step that makes the team harness usable across all four backends.
Output: `_step_materialize_compile` body + the renderer_compile test mirroring `test_forge_materialization.py`.
Research basis: RESEARCH.md Seam 6 + Pitfalls 6,7. `materialize_primitives(project: dict, kinds: list[str], workspace_path: Path)` (forge_materialization_service.py:183) — pass the project DICT (from get_project), not project_id; `_NEVER_DELETE` (:27) is the built-in destructive-safety guard. Renderer access is the PUBLIC `renderer_for(backend)` (context_renderers/__init__.py:27) over the PRIVATE `_REGISTRY` — NEVER reference a `RENDERERS` symbol (it does not exist). `Renderer.render(...)` mutates (cmd, env) given a ContextBundle.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-RESEARCH.md
@.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-EVAL.md
@.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-02-SUMMARY.md
@backend/app/services/forge_materialization_service.py
@backend/app/services/context_renderers/__init__.py
@backend/tests/test_forge_materialization.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Step f — materialize primitives + 4-backend compile smoke</name>
  <files>backend/app/services/team_harness_setup_service.py</files>
  <action>
    Replace `_step_materialize_compile`:
    - `project = get_project(project_id)` (DICT — required by the signature). Resolve `workspace_path = Path(project["local_path"])`.
    - Determine `kinds` from the materialization service's supported kinds (read forge_materialization_service for the kind list / default).
    - `result = materialize_primitives(project, kinds, workspace_path)` — writes into `workspace_path/.claude`; deterministic; the `_NEVER_DELETE` set (:27) protects manifest/settings/mcp.json. Do NOT add any manual delete logic.
    - Compile smoke: for `backend in ("claude", "codex", "gemini", "opencode")`: `renderer = renderer_for(backend)`; assert renderer is not None; build a minimal ContextBundle from the materialized projection (mirror how test_forge_materialization.py constructs inputs) and call `renderer.render(...)`; treat a clean return (no exception, non-empty mutation) as pass, a raised exception as that backend's compile failure.
    - Aggregate: if all 4 render cleanly → StepResult ok (detail lists backends); if any raises → StepResult failed naming the backend (the service marks overall failed, leaving the step retryable).
    AVOID: never reference a `RENDERERS` symbol; use renderer_for. Never manually delete projection files (rely on _NEVER_DELETE).
  </action>
  <verify>
    `cd /Users/neo/Developer/Projects/Agented/backend && uv run pytest tests/test_team_harness_setup_service.py -k "renderer_compile" -v` green (Level 2: Proxy — P4)
  </verify>
  <done>Step f materializes the projection and all 4 renderers run cleanly; failure of any backend yields a failed StepResult naming it.</done>
</task>

<task type="auto">
  <name>Task 2: renderer_compile test — golden-file pattern, 4 backends, _NEVER_DELETE preserved</name>
  <files>backend/tests/test_team_harness_setup_service.py</files>
  <action>
    Add `test_step_f_renderer_compile_all_backends` (renderer_compile keyword), mirroring backend/tests/test_forge_materialization.py:
    - Use `tmp_path` as workspace + a seeded project with at least the forge-creator bundle bound (reuse the 21-04 bundle fixture).
    - Run step f; assert `<tmp_path>/.claude` exists with non-empty content and the manifest at `.claude/agented-forge/manifest.json`.
    - For each of the 4 backends, assert `renderer_for(backend)` is not None and its render output is non-empty / does not raise.
    - Re-run step f; assert it does not raise and the _NEVER_DELETE files (manifest, .claude/settings.json, .claude/mcp.json) still exist (idempotency + no-destructive — supports P1/P2).
  </action>
  <verify>
    `cd /Users/neo/Developer/Projects/Agented/backend && uv run pytest tests/test_team_harness_setup_service.py -k "renderer_compile" -v` green (Level 2: Proxy — P4)
  </verify>
  <done>.claude projection materialized; 4 renderers compile non-empty/no-raise; re-run preserves _NEVER_DELETE files.</done>
</task>

</tasks>

<verification>
Level 2 (Proxy): P4 — 4 renderers compile the materialized projection (golden-file pattern); re-materialize preserves _NEVER_DELETE (supports P1/P2).
</verification>

<success_criteria>
- materialize_primitives writes the .claude projection (project dict signature); _NEVER_DELETE guards deletes.
- All 4 backends (claude/codex/gemini/opencode) render cleanly via renderer_for; failure names the backend.
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-06-SUMMARY.md`
</output>
