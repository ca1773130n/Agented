---
phase: 21-one-click-team-harness-setup
plan: 04
type: execute
wave: 2
depends_on: ["21-02"]
files_modified:
  - backend/app/services/team_harness_setup_service.py
  - backend/tests/test_team_harness_setup_service.py
autonomous: true
verification_level: proxy
hypothesis: "Step c parses .planning/codebase/STACK.md's ## Languages section to a static language→bundle map, binds the matched bundle (or the forge-creator floor when none matches / STACK.md absent) via bind_bundle_to_project, and step d enables tesserae via the already-idempotent set_tesserae_root — both reconcile cleanly on re-run."
predicted_outcome: "test_team_harness_setup_service.py -k 'bundle_selection' passes all 3 fixtures (Python→matched-or-creator, TypeScript→matched-or-creator, missing STACK.md→forge-creator only) asserting bind_bundle_to_project bundle_id, and tesserae step is idempotent."
must_haves:
  truths:
    - "Step c parses ## Languages from .planning/codebase/STACK.md; the forge-creator bundle is ALWAYS bound as the floor (the only unconditionally-assertable selection — P3) (SC3)"
    - "A static language→bundle-name map drives optional language-specific binds via get_forge_bundle_by_name; unmatched language or absent STACK.md falls back to forge-creator alone"
    - "bind_bundle_to_project (delegating to upsert_binding) is the only bind path used; DELETE/unbind paths (project_forge_bindings.py:187,204) are NEVER called (no destructive deletes — SC4)"
    - "Step d calls set_tesserae_root (idempotent per tesserae_integration.py:103); unset_tesserae_root_bindings is NEVER called on re-run (P2)"
  artifacts:
    - path: "backend/app/services/team_harness_setup_service.py"
      provides: "_step_bundle_binding + _step_tesserae_enable bodies + a _select_bundles_for_stack(stack_md) helper"
      contains: "forge-creator"
  key_links:
    - from: "backend/app/services/team_harness_setup_service.py"
      to: "backend/app/db/forge_bundles.py"
      via: "get_forge_bundle_by_name + bind_bundle_to_project"
      pattern: "bind_bundle_to_project"
    - from: "backend/app/services/team_harness_setup_service.py"
      to: "backend/app/services/tesserae_integration.py"
      via: "set_tesserae_root"
      pattern: "set_tesserae_root"
---

<objective>
Implement steps (c) project-tailored bundle binding and (d) tesserae enablement. Bundle selection is the REQ-21 tailoring surface: parse STACK.md `## Languages`, map to bundle names, always bind the `forge-creator` floor, and add any language-specific bundle that is actually seeded. Tesserae enablement leans on the already-idempotent `set_tesserae_root`.

Purpose: Bind the right forge primitives for the project's stack and turn on per-project Tesserae, both reconcile-safe.
Output: `_step_bundle_binding`, `_step_tesserae_enable`, and a pure `_select_bundles_for_stack` helper (strong TDD target) + bundle-selection/tesserae tests.
Research basis: RESEARCH.md Seam 3 + Seam 4 + Open Question 3. `forge-creator` (forge_creator_seed.py BUNDLE_NAME) is the guaranteed floor; `get_forge_bundle_by_name` (forge_bundles.py:74) + `bind_bundle_to_project` (:152→upsert_binding); `set_tesserae_root` (tesserae_integration.py:103) is explicitly idempotent; STACK.md at `.planning/codebase/STACK.md` with `## Languages` (confirmed: "Python 3.10+", "TypeScript 5.4").
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-RESEARCH.md
@.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-EVAL.md
@.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-02-SUMMARY.md
@backend/app/db/forge_bundles.py
@backend/app/services/forge_creator_seed.py
@backend/app/services/tesserae_integration.py
@.planning/codebase/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Step c — STACK.md-tailored bundle binding with forge-creator floor</name>
  <files>backend/app/services/team_harness_setup_service.py, backend/tests/test_team_harness_setup_service.py</files>
  <action>
    Add a pure helper `_select_bundles_for_stack(stack_md_text: str | None) -> list[str]` (TDD-friendly: input string → ordered list of bundle names):
    - Always include `"forge-creator"` first (the floor).
    - Parse the `## Languages` section: for each recognized language, append a language-keyed bundle name from a small static map, e.g. `{"python": "forge-python", "typescript": "forge-typescript"}` (match case-insensitively against the Languages lines).
    - Dedup while preserving order. With `None`/missing STACK.md → `["forge-creator"]` only.
    Replace `_step_bundle_binding`:
    - Read `.planning/codebase/STACK.md` from the project's local_path (None if absent).
    - `names = _select_bundles_for_stack(text)`.
    - For each name: `bundle = get_forge_bundle_by_name(name)`; if found, `bind_bundle_to_project(project_id, bundle["id"])`. SKIP silently when a language-specific bundle is not seeded (forge-creator is guaranteed to exist). bind is idempotent via upsert_binding.
    - Return StepResult with the bound names in detail.
    Tests (bundle_selection keyword) — direct unit tests on `_select_bundles_for_stack` (3 fixtures: Python STACK.md, TypeScript STACK.md, None) AND a step-level test asserting `bind_bundle_to_project` is called with the forge-creator bundle id (spy/monkeypatch). Assert re-run produces no duplicate binding rows.
    AVOID: never call project_forge_bindings DELETE/unbind paths (SC4).
  </action>
  <verify>
    `cd /Users/neo/Developer/Projects/Agented/backend && uv run pytest tests/test_team_harness_setup_service.py -k "bundle_selection" -v` green (Level 2: Proxy — P3)
  </verify>
  <done>forge-creator always bound; language-specific bundles bound when seeded; missing STACK.md → forge-creator only; no duplicate bindings on re-run.</done>
</task>

<task type="auto">
  <name>Task 2: Step d — tesserae enable (idempotent)</name>
  <files>backend/app/services/team_harness_setup_service.py, backend/tests/test_team_harness_setup_service.py</files>
  <action>
    Replace `_step_tesserae_enable`:
    - Resolve the tesserae root (the project's local_path, or a conventional subdir if the codebase uses one — check get_tesserae_root usage).
    - Call `set_tesserae_root(project_id, root)` (tesserae_integration.py:103) — explicitly idempotent; also best-effort binds the per-project Tesserae MCP server.
    - Reconcile: if `get_tesserae_root(project_id)` already returns the same root, return StepResult skipped; else ok.
    - Return StepResult.
    Test (tesserae keyword): run step d twice; assert set path persists and `unset_tesserae_root_bindings` is NEVER called (monkeypatch it to raise — ties into P2). Assert get_tesserae_root returns the set root.
    AVOID: never call unset_tesserae_root_bindings (tesserae_integration.py:131) on re-run (SC4 / P2).
  </action>
  <verify>
    `cd /Users/neo/Developer/Projects/Agented/backend && uv run pytest tests/test_team_harness_setup_service.py -k "tesserae" -v` green (Level 2: Proxy — supports P2)
  </verify>
  <done>set_tesserae_root called idempotently; re-run does not unset; get_tesserae_root returns the configured root.</done>
</task>

</tasks>

<verification>
Level 2 (Proxy):
- P3: bundle_selection — 3 fixtures + forge-creator floor bind asserted.
- P2 (partial): tesserae step never calls the destructive unset path.
</verification>

<success_criteria>
- forge-creator always bound; STACK.md Languages drive optional language-specific binds; missing STACK.md → floor only; no duplicate binding rows.
- set_tesserae_root idempotent; unset never called on re-run.
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/21-one-click-team-harness-setup/21-04-SUMMARY.md`
</output>
