---
phase: 20-grd-frontend-wiring
plan: 04
type: execute
wave: 2
depends_on: ["20-02"]
files_modified:
  - frontend/src/views/ProjectHarnessPage.vue
  - frontend/src/router/routes/projects.ts
  - frontend/src/components/grd/harness/AutonomyEditor.vue
  - frontend/src/components/grd/harness/RoundList.vue
  - frontend/src/components/grd/harness/RoundDetail.vue
  - frontend/src/components/grd/harness/SharedForgeBrowser.vue
  - frontend/src/components/grd/harness/HarnessPanelHost.vue
  - frontend/src/components/grd/harness/panels/HealthPanel.vue
  - frontend/src/components/grd/harness/panels/ThinkPanel.vue
  - frontend/src/components/grd/harness/panels/DeadEndsPanel.vue
  - frontend/src/components/grd/harness/panels/GenomePanel.vue
  - frontend/src/components/grd/harness/panels/VerifyPanel.vue
  - frontend/src/components/grd/harness/panels/ReflectionsPanel.vue
  - frontend/src/components/grd/harness/panels/EvolvePanel.vue
  - frontend/src/components/grd/harness/__tests__/harness-panels.test.ts
  - frontend/src/components/grd/harness/__tests__/autonomy-rounds-forge.test.ts
  - frontend/src/locales/en.json
  - frontend/src/locales/ko.json
  - frontend/src/locales/ja.json
  - frontend/src/locales/zh.json
autonomous: true
verification_level: proxy

hypothesis: "Wiring the already-shipped life-harness backends to frontend panels — an AutonomyEditor (GET/PUT autonomy), RoundList/RoundDetail with a confirm-guarded revert, a SharedForgeBrowser (list/adopt), and seven tabbed panels covering the 16 GRD routes — gives the life-harness completion a complete UI with no backend change."
predicted_outcome: "All harness components mount in Vitest with no [Vue warn]; the /projects/:projectId/harness route resolves; the revert action is confirm-guarded; surface.harness.* exists key-identical in all four locales; P5 + S6 green."

eval_metrics:
  primary: "harness-panels + autonomy-rounds-forge tests pass (P5)"
  secondary: ["/harness route present", "surface.harness.* in all 4 locales (S6)"]
  baseline: "GrdSettings.vue form pattern; existing TabbedViewHost"
  target: "0 new failures; all 16 routes covered by a panel; revert confirm-guarded"

must_haves:
  truths:
    - "AutonomyEditor GETs /admin/projects/{id}/autonomy, renders AutonomyPolicy fields as a form, PUTs on save"
    - "RoundList lists rounds; RoundDetail shows impact and exposes a revert action that is confirm-guarded (no API call without explicit confirmation — revert is destructive/git-reversible)"
    - "SharedForgeBrowser lists /admin/shared-forge and adopts a binding via POST /admin/projects/{id}/adopt-shared/{bindingId}"
    - "seven panels (Health, Think, Dead-Ends, Genome, Verify, Reflections+Verdicts, Evolve) collectively call all 16 GRD routes from grdHarnessApi"
    - "HarnessPanelHost mounts the panels in a tabbed host (reuse the repo's TabbedViewHost if present); the /projects/:projectId/harness route resolves"
    - "every visible string uses t('surface.harness.*'); the namespace is key-identical across en/ko/ja/zh"
  artifacts:
    - path: "frontend/src/components/grd/harness/AutonomyEditor.vue"
      provides: "autonomy policy read/edit/write form"
    - path: "frontend/src/components/grd/harness/RoundDetail.vue"
      provides: "round detail + confirm-guarded revert"
    - path: "frontend/src/components/grd/harness/SharedForgeBrowser.vue"
      provides: "shared-forge browse + adopt"
    - path: "frontend/src/components/grd/harness/HarnessPanelHost.vue"
      provides: "tabbed host for the 7 GRD-route panels"
    - path: "frontend/src/views/ProjectHarnessPage.vue"
      provides: "harness route view"
  key_links:
    - from: "frontend/src/components/grd/harness/AutonomyEditor.vue"
      to: "frontend/src/services/api/grdHarness.ts"
      via: "getAutonomy / setAutonomy"
      pattern: "Autonomy"
    - from: "frontend/src/components/grd/harness/RoundDetail.vue"
      to: "grdHarnessApi.revertRound"
      via: "confirm-guarded revert"
      pattern: "revert"
    - from: "frontend/src/components/grd/harness/HarnessPanelHost.vue"
      to: "grdHarnessApi (16 routes)"
      via: "panels call GRD routes"
      pattern: "grdHarnessApi"
---

<objective>
Wire the already-shipped life-harness backends (REQ-16) to a frontend: autonomy
policy editor, round list/detail with a confirm-guarded revert, shared-forge
browse/adopt, and seven tabbed panels covering the 16 GRD routes. No backend change —
these are fixed contracts. All strings localized in four catalogs.

Purpose: SC-4. Closes the life-harness completion's missing UI layer (every one of
these backends currently has zero frontend callers).

Output: harness page + route + autonomy/rounds/forge components + panel host + 7 panels
+ tests + surface.harness.* i18n keys.

Research basis: N/A. Forms mirror GrdSettings.vue; panel host reuses TabbedViewHost
(20-RESEARCH.md §4).
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/milestones/anonymous/phases/20-grd-frontend-wiring/20-RESEARCH.md
@frontend/src/components/settings/GrdSettings.vue
@frontend/src/services/api/grdHarness.ts
@frontend/src/router/routes/projects.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Autonomy editor + rounds (list/detail/revert) + shared-forge</name>
  <files>frontend/src/components/grd/harness/AutonomyEditor.vue, frontend/src/components/grd/harness/RoundList.vue, frontend/src/components/grd/harness/RoundDetail.vue, frontend/src/components/grd/harness/SharedForgeBrowser.vue, frontend/src/components/grd/harness/__tests__/autonomy-rounds-forge.test.ts</files>
  <action>
    AutonomyEditor.vue: mirror GrdSettings.vue form patterns. On mount call grdHarnessApi.getAutonomy(projectId),
    render the AutonomyPolicy fields as form controls, call setAutonomy(projectId, policy) on save.
    RoundList.vue: grdHarnessApi.listProjectRounds / listAllRounds -> rows. RoundDetail.vue: getRoundDetail +
    getRoundImpact; expose approve/abort/revert. CRITICAL: the revert action MUST be confirm-guarded — show
    a confirmation step and only call grdHarnessApi.revertRound after explicit confirm (revert is destructive,
    git-reversible). SharedForgeBrowser.vue: listSharedForge -> rows; adopt button -> adoptShared(projectId, bindingId).
    Use t('surface.harness.*') for all strings.
    Tests: mount each; assert AutonomyEditor PUTs the edited policy; assert RoundDetail does NOT call revertRound
    until confirmation is given, then DOES; assert SharedForgeBrowser adopt calls adoptShared with the binding id.
  </action>
  <verify>
    cd frontend && npx vitest --run src/components/grd/harness/__tests__/autonomy-rounds-forge.test.ts (Level 2: Proxy — part of P5)
  </verify>
  <done>Autonomy read/edit/write works; revert confirm-guard verified by test; shared-forge adopt wired; suite green.</done>
</task>

<task type="auto">
  <name>Task 2: 7 GRD-route panels + tabbed host + route</name>
  <files>frontend/src/components/grd/harness/HarnessPanelHost.vue, frontend/src/components/grd/harness/panels/HealthPanel.vue, frontend/src/components/grd/harness/panels/ThinkPanel.vue, frontend/src/components/grd/harness/panels/DeadEndsPanel.vue, frontend/src/components/grd/harness/panels/GenomePanel.vue, frontend/src/components/grd/harness/panels/VerifyPanel.vue, frontend/src/components/grd/harness/panels/ReflectionsPanel.vue, frontend/src/components/grd/harness/panels/EvolvePanel.vue, frontend/src/views/ProjectHarnessPage.vue, frontend/src/router/routes/projects.ts, frontend/src/components/grd/harness/__tests__/harness-panels.test.ts</files>
  <action>
    Build seven panels mapping the 16 GRD routes (20-RESEARCH.md §2 Group A):
    - HealthPanel: getHealth. ThinkPanel: think (button -> briefing). DeadEndsPanel: listDeadEnds +
      addDeadEnd + promoteDeadEnds. GenomePanel: getGenome + snapshotGenome + listGenomeSnapshots +
      latestGenomeSnapshot. VerifyPanel: verifyMechanical(phase). ReflectionsPanel: listPhaseReflections +
      verdictCounts. EvolvePanel: startEvolve + listEvolveRuns + getEvolveRun + stopEvolveRun.
    HarnessPanelHost.vue: tabbed host mounting all seven. REUSE the repo's existing TabbedViewHost if present
    (MEMORY.md sidebar-prune work) — confirm its path/API before building a new one; only hand-roll tabs if
    it is absent. ProjectHarnessPage.vue: EntityLayout + PageHeader + AutonomyEditor + HarnessPanelHost +
    RoundList + SharedForgeBrowser. Add the `/projects/:projectId/harness` route (mirror the planning entry).
    Do NOT auto-add a top-level sidebar slot — mount under the project surface (MEMORY.md: sidebar IA is a
    product judgment, not auto-classified). All strings via t('surface.harness.*').
    Tests: mount the host + each panel with mocked grdHarnessApi; assert each panel fires its expected
    grdHarnessApi call and renders without [Vue warn]; assert collectively all 16 routes are exercised.
  </action>
  <verify>
    cd frontend && npx vitest --run src/components/grd/harness/__tests__/harness-panels.test.ts (Level 2: Proxy — P5)
  </verify>
  <done>7 panels cover all 16 GRD routes; host mounts; /harness route resolves; no Vue warn; suite green.</done>
</task>

<task type="auto">
  <name>Task 3: surface.harness.* i18n keys in all 4 catalogs</name>
  <files>frontend/src/locales/en.json, frontend/src/locales/ko.json, frontend/src/locales/ja.json, frontend/src/locales/zh.json</files>
  <action>
    Add a `surface.harness.*` namespace (autonomy field labels, round list/detail labels + revert-confirm
    copy, shared-forge labels, and the seven panel titles/empty-states/action labels) to ALL FOUR catalogs,
    key-identical. Keys must match exactly the t('surface.harness.*') calls from Tasks 1-2. Ship parity here
    so the 20-06 key-diff finds zero diff for this surface.
  </action>
  <verify>
    cd frontend && node -e "const fs=require('fs');['en','ko','ja','zh'].forEach(l=>{const d=JSON.parse(fs.readFileSync('src/locales/'+l+'.json','utf8'));console.log(l, JSON.stringify(d).includes('\"harness\"'))})" (Level 1: Sanity — S6)
  </verify>
  <done>surface.harness.* present and key-identical in en/ko/ja/zh; all four parse as valid JSON.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): /harness route resolves; surface.harness.* present in all 4 locales (S6); `just build` clean (S1).
Level 2 (Proxy): harness-panels + autonomy-rounds-forge tests green (P5); revert confirm-guard asserted; no new failures.
Level 3 (Deferred): operator walkthrough drives autonomy/revert/forge + triggers GRD routes (DEFER-20-02); locale visual review (DEFER-20-03).
</verification>

<success_criteria>
SC-4: life-harness completion surfaces exist — autonomy editor, confirm-guarded round revert,
shared-forge browse/adopt, and panels covering all 16 GRD routes — over the 20-02 grdHarnessApi,
localized in four catalogs, with no backend change.
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/20-grd-frontend-wiring/20-04-SUMMARY.md`
noting the /harness route path + panel inventory (so 20-05 can deep-link harness commands).
</output>
