---
phase: 20-grd-frontend-wiring
plan: 03
type: execute
wave: 2
depends_on: ["20-01", "20-02"]
files_modified:
  - frontend/src/views/ProjectResearchPage.vue
  - frontend/src/router/routes/projects.ts
  - frontend/src/composables/useResearchSession.ts
  - frontend/src/components/grd/research/QuestionIntake.vue
  - frontend/src/components/grd/research/ThreadList.vue
  - frontend/src/components/grd/research/HypothesisLedger.vue
  - frontend/src/components/grd/research/ReportViewer.vue
  - frontend/src/components/grd/research/PortfolioRuns.vue
  - frontend/src/views/__tests__/ProjectResearchPage.test.ts
  - frontend/src/components/grd/research/__tests__/research-components.test.ts
  - frontend/src/composables/__tests__/useResearchSession.test.ts
  - frontend/src/locales/en.json
  - frontend/src/locales/ko.json
  - frontend/src/locales/ja.json
  - frontend/src/locales/zh.json
autonomous: true
verification_level: proxy

hypothesis: "A ProjectResearchPage mirroring ProjectPlanningPage — composing QuestionIntake, ThreadList, HypothesisLedger, ReportViewer, PortfolioRuns over useResearchSession (SSE) and researchApi — makes the autoresearch loop fully operable from the frontend with all strings localized in all four catalogs."
predicted_outcome: "ProjectResearchPage and its five children mount in happy-dom with no [Vue warn]; the /projects/:projectId/research route resolves; useResearchSession subscribes to the SSE stream; the surface.research.* namespace exists key-identical in en/ko/ja/zh; P4 + S6 green."

eval_metrics:
  primary: "ProjectResearchPage + research-components + useResearchSession tests pass (P4)"
  secondary: ["route entry present", "surface.research.* keys in all 4 locales (S6)"]
  baseline: "ProjectPlanningPage mounts; usePlanningSession streams"
  target: "0 new failures; mount with no Vue warn"

must_haves:
  truths:
    - "ProjectResearchPage.vue mounts in Vitest/happy-dom with stub props and no [Vue warn]"
    - "the /projects/:projectId/research route exists (lazy import, props:true, requiresEntity:projectId) and resolves"
    - "QuestionIntake submits a question via researchApi.startResearch; ThreadList renders status/iteration rows from researchApi.listThreads"
    - "HypothesisLedger renders the HYPOTHESES.md ledger; ReportViewer renders FINDING.md via a GREEN markdown renderer (NOT the baseline-broken MarkdownContent); PortfolioRuns aggregates threads"
    - "useResearchSession mirrors usePlanningSession: createAuthenticatedEventSource + output/question/complete/error listeners + cleanup"
    - "every visible string uses t('surface.research.*'); the namespace is key-identical across en/ko/ja/zh"
  artifacts:
    - path: "frontend/src/views/ProjectResearchPage.vue"
      provides: "Research page composing the five research components"
    - path: "frontend/src/composables/useResearchSession.ts"
      provides: "SSE composable for research sessions"
    - path: "frontend/src/components/grd/research/QuestionIntake.vue"
      provides: "question intake form"
    - path: "frontend/src/components/grd/research/ThreadList.vue"
      provides: "thread list with status/iteration"
    - path: "frontend/src/components/grd/research/HypothesisLedger.vue"
      provides: "hypothesis ledger view"
    - path: "frontend/src/components/grd/research/ReportViewer.vue"
      provides: "FINDING.md report viewer"
    - path: "frontend/src/components/grd/research/PortfolioRuns.vue"
      provides: "portfolio/thread aggregate"
  key_links:
    - from: "frontend/src/views/ProjectResearchPage.vue"
      to: "frontend/src/services/api/research.ts"
      via: "researchApi import"
      pattern: "researchApi"
    - from: "frontend/src/composables/useResearchSession.ts"
      to: "createAuthenticatedEventSource"
      via: "SSE subscription"
      pattern: "createAuthenticatedEventSource"
    - from: "frontend/src/router/routes/projects.ts"
      to: "ProjectResearchPage.vue"
      via: "lazy route component"
      pattern: "research"
---

<objective>
Build the Research page (REQ-15): question intake, thread list, hypothesis ledger,
report viewer, and portfolio runs, streaming live session output over SSE — mirroring
the proven ProjectPlanningPage composition. All strings localized in all four catalogs.

Purpose: SC-3. Turns the 20-01 backend + 20-02 researchApi into an operable surface.

Output: page + route + composable + five components + tests + surface.research.* i18n keys.

Research basis: N/A. Composition mirrored from ProjectPlanningPage / usePlanningSession
(20-RESEARCH.md §3).
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/milestones/anonymous/phases/20-grd-frontend-wiring/20-RESEARCH.md
@frontend/src/views/ProjectPlanningPage.vue
@frontend/src/composables/usePlanningSession.ts
@frontend/src/router/routes/projects.ts
@frontend/src/services/api/research.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: useResearchSession composable + route</name>
  <files>frontend/src/composables/useResearchSession.ts, frontend/src/router/routes/projects.ts, frontend/src/composables/__tests__/useResearchSession.test.ts</files>
  <action>
    Mirror usePlanningSession.ts (the createAuthenticatedEventSource + .addEventListener
    'output'|'question'|'complete'|'error' + cleanup pattern). Expose reactive state (status,
    output buffer, threadId, error) and start(question, opts) / resume(threadId) actions that call
    researchApi then subscribe via researchApi.streamResearch.
    Add the route in router/routes/projects.ts mirroring the /projects/:projectId/planning entry:
    `path: '/projects/:projectId/research'`, `component: () => import('../../views/ProjectResearchPage.vue')`,
    `props: true`, `meta: { requiresEntity: 'projectId' }`.
    Test useResearchSession mirroring useProjectSession.test.ts: mock researchApi + EventSource, assert
    start subscribes and output events append to the buffer, cleanup removes listeners.
  </action>
  <verify>
    cd frontend && npx vitest --run src/composables/__tests__/useResearchSession.test.ts (Level 2: Proxy — part of P4)
  </verify>
  <done>useResearchSession streams + cleans up; route entry resolves; composable test green.</done>
</task>

<task type="auto">
  <name>Task 2: ProjectResearchPage + 5 components</name>
  <files>frontend/src/views/ProjectResearchPage.vue, frontend/src/components/grd/research/QuestionIntake.vue, frontend/src/components/grd/research/ThreadList.vue, frontend/src/components/grd/research/HypothesisLedger.vue, frontend/src/components/grd/research/ReportViewer.vue, frontend/src/components/grd/research/PortfolioRuns.vue, frontend/src/views/__tests__/ProjectResearchPage.test.ts, frontend/src/components/grd/research/__tests__/research-components.test.ts</files>
  <action>
    ProjectResearchPage.vue: mirror ProjectPlanningPage (EntityLayout + PageHeader + intake + session
    panel). Wire QuestionIntake.submit -> useResearchSession.start; show ThreadList (researchApi.listThreads),
    selected-thread HypothesisLedger + ReportViewer (researchApi.getThread), and PortfolioRuns aggregate.
    ReportViewer.vue: render FINDING.md markdown. CRITICAL: MarkdownContent is in the 7-failure baseline —
    do NOT use it. Find the GREEN markdown renderer used elsewhere (e.g. chat) and use that; if none is
    confirmed safe, render as preformatted text rather than risk a new failure. (20-RESEARCH.md §9.4.)
    All five components use `const { t } = useI18n()` and t('surface.research.*') for every label —
    no hardcoded strings.
    Tests: mount the page + each component with stub props/mocked researchApi; assert no [Vue warn]
    and that key rendered content appears (intake field, a thread row, a ledger entry, report text).
  </action>
  <verify>
    cd frontend && npx vitest --run src/views/__tests__/ProjectResearchPage.test.ts src/components/grd/research/ (Level 2: Proxy — P4)
  </verify>
  <done>Page + 5 components mount with no Vue warn; tests assert rendered content; 0 new failures.</done>
</task>

<task type="auto">
  <name>Task 3: surface.research.* i18n keys in all 4 catalogs</name>
  <files>frontend/src/locales/en.json, frontend/src/locales/ko.json, frontend/src/locales/ja.json, frontend/src/locales/zh.json</files>
  <action>
    Add a `surface.research.*` namespace (intake prompt/submit, thread-list headers, status labels,
    ledger column labels, report viewer title/empty-state, portfolio labels) to ALL FOUR catalogs,
    key-identical. Translate values per locale; never translate code/paths. Keys must match exactly the
    t('surface.research.*') calls added in Tasks 1-2. This is REQ-18 for the research surface — the final
    parity sweep (20-06) will run the key-diff, but ship parity here so 20-06 finds zero diff.
  </action>
  <verify>
    cd frontend && node -e "const fs=require('fs');['en','ko','ja','zh'].forEach(l=>{const d=JSON.parse(fs.readFileSync('src/locales/'+l+'.json','utf8'));console.log(l, JSON.stringify(d).includes('\"research\"'))})" (Level 1: Sanity — S6)
  </verify>
  <done>surface.research.* present and key-identical in en/ko/ja/zh; all four parse as valid JSON.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): route resolves; surface.research.* present in all 4 locales (S6); `just build` clean (S1).
Level 2 (Proxy): ProjectResearchPage + research-components + useResearchSession tests green (P4); no new failures.
Level 3 (Deferred): live SSE renders hypothesis rows incrementally in a real browser (DEFER-20-01); locale visual review (DEFER-20-03).
</verification>

<success_criteria>
SC-3: Research page mounts and functions — intake submits, threads list, ledger + report render,
portfolio aggregates — over the 20-02 researchApi, with all strings localized in four catalogs.
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/20-grd-frontend-wiring/20-03-SUMMARY.md`
noting the route path and the markdown renderer chosen (so 20-05 deep-links correctly).
</output>
