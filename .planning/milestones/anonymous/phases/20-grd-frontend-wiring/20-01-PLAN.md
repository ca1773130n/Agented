---
phase: 20-grd-frontend-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/execution_type_handler.py
  - backend/app/services/grd_cli_service.py
  - backend/app_litestar/routes/grd_routes.py
  - backend/tests/test_grd_research_handler.py
  - backend/tests/test_grd_research_routes.py
autonomous: true
verification_level: proxy

hypothesis: "A grd_research execution-type handler that mirrors GrdChatSessionHandler (spawning /grd:research <json.dumps(question)>) plus on-disk thread-browser routes will make autoresearch reachable from the backend without touching the proven grd_chat path."
predicted_outcome: "HANDLER_REGISTRY['grd_research'] resolves to GrdResearchSessionHandler; test_grd_research_handler.py asserts create_session called with execution_type='grd_research', stream_json=True, use_pty=False, prompt containing json.dumps(question); test_grd_research_routes.py asserts POST /research/start returns {session_id}, GET /research/threads returns [] for a missing dir and parses THREAD.md frontmatter when present — all green."

eval_metrics:
  primary: "test_grd_research_handler.py + test_grd_research_routes.py all pass (exit 0)"
  secondary: ["ruff format --check clean", "handler import smoke OK"]
  baseline: "grd_chat handler tests green (test_grd_chat_handler.py)"
  target: "0 failures, 0 errors across both new test files"

must_haves:
  truths:
    - "HANDLER_REGISTRY.get('grd_research') returns a GrdResearchSessionHandler instance (S3 import smoke passes)"
    - "GrdResearchSessionHandler.start calls ProjectSessionManager.create_session with execution_type='grd_research', stream_json=True, use_pty=False"
    - "The spawned research prompt is /grd:research with json.dumps(question) framing (NOT naive f-string interpolation — keeps the 19-04 security fix)"
    - "GET /research/threads returns [] when .planning/research/threads/ is missing, and parses THREAD.md frontmatter (id/question/status/iteration) when present"
    - "GET /research/threads/{thread_id} returns a None-safe bundle of THREAD.md + HYPOTHESES.md + FINDING.md"
    - "POST /research/start returns {session_id}; POST /research/{thread_id}/resume spawns a gd research resume session"
  artifacts:
    - path: "backend/app/services/execution_type_handler.py"
      provides: "GrdResearchSessionHandler + HANDLER_REGISTRY['grd_research'] entry"
      contains: "grd_research"
    - path: "backend/app/services/grd_cli_service.py"
      provides: "research_status / list_threads / read_thread helpers"
      contains: "research"
    - path: "backend/app_litestar/routes/grd_routes.py"
      provides: "5 /api/projects/{id}/research/* routes"
      contains: "research"
    - path: "backend/tests/test_grd_research_handler.py"
      provides: "handler contract tests"
    - path: "backend/tests/test_grd_research_routes.py"
      provides: "route shape tests"
  key_links:
    - from: "backend/app/services/execution_type_handler.py"
      to: "ProjectSessionManager.create_session"
      via: "start() spawns grd_research session"
      pattern: "create_session\\(.*execution_type=.?grd_research"
    - from: "backend/app_litestar/routes/grd_routes.py"
      to: "GrdResearchSessionHandler"
      via: "POST /research/start creates a grd_research session"
      pattern: "research/start"
    - from: "backend/app_litestar/routes/grd_routes.py"
      to: ".planning/research/threads/"
      via: "thread browser reads THREAD.md frontmatter on disk"
      pattern: "research/threads"
---

<objective>
Add the only net-new backend slice of phase 20: a `grd_research` execution-type
handler plus a thin CLI-wrapper surface and five `/api/projects/{id}/research/*`
routes, so the autoresearch loop (`gd research`) is fully reachable from the
backend with SSE streaming.

Purpose: REQ-14. Everything else in phase 20 is frontend wiring of already-shipped
backends; this plan is the one piece that needs new server code. It unblocks
Wave B (Research page, REQ-15).

Output: GrdResearchSessionHandler (registered), research helpers on GrdCliService,
five research routes, and two backend test files.

Research basis: N/A (feature-wiring phase, no papers). Patterns mirrored verbatim
from the proven grd_chat path (phase 19).
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/anonymous/phases/20-grd-frontend-wiring/20-RESEARCH.md
@.planning/milestones/anonymous/phases/20-grd-frontend-wiring/20-EVAL.md
@backend/app/services/execution_type_handler.py
@backend/app/services/grd_cli_service.py
@backend/app_litestar/routes/grd_routes.py
@backend/tests/test_grd_chat_handler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: GrdResearchSessionHandler + registry entry + CLI helpers</name>
  <files>backend/app/services/execution_type_handler.py, backend/app/services/grd_cli_service.py</files>
  <action>
    Mirror GrdChatSessionHandler (execution_type_handler.py:683) verbatim, creating
    GrdResearchSessionHandler:
    - `start`: build cmd resolving cwd via ProjectWorkspaceService.resolve_working_directory(project_id)
      (raises ValueError when no clone — keep that behavior). Spawn
      `["claude","-p","--output-format","stream-json","--verbose", f"/grd:research {json.dumps(question)}"]`.
      CRITICAL: use json.dumps(question) for prompt framing — this is the 19-04 prompt-injection
      hardening. Do NOT use naive f'"{question}"' interpolation.
    - Call ProjectSessionManager.create_session(..., execution_type="grd_research",
      stream_json=True, use_pty=False, forge_bundle=..., super_agent_id=...) exactly as grd_chat does.
    - Translate optional opts: max_iterations -> append `--max-iterations N` to the prompt tail,
      no_gates -> append `--no-gates`. Only append when provided.
    - `monitor`/`stop`/`get_output`: delegate to ProjectSessionManager identically to grd_chat.
    - Register in HANDLER_REGISTRY (:824/:830): add `"grd_research": GrdResearchSessionHandler()`.

    In grd_cli_service.py, add a research family mirroring the existing typed wrappers
    (get_health :299, think :315). These are for read-only/status surfaces, NOT the long loop:
    - `research_status(cls, project_path, thread_id=None)` -> run_gd_json(cwd, "research", "status",
      *([thread_id] if thread_id else [])). Guard with available()/gd_path(); return {"error": ...} when unavailable.
    - `list_threads(cls, project_path)` -> read `.planning/research/threads/*/THREAD.md` frontmatter
      on disk (parse id/question/status/iteration/max_iterations). Return [] when the dir is missing
      (it does NOT exist until the first run — handle gracefully). On-disk read, not a CLI round-trip.
    - `read_thread(cls, project_path, thread_id)` -> read THREAD.md + HYPOTHESES.md + FINDING.md from
      `.planning/research/threads/<id>/`; each field None when the file is absent.
    Do NOT invent a `gd research report` or `gd research portfolio` CLI call — they do not exist;
    report = FINDING.md on disk, portfolio = the thread list.
  </action>
  <verify>
    cd backend && uv run python -c "from app.services.execution_type_handler import HANDLER_REGISTRY; h=HANDLER_REGISTRY.get('grd_research'); assert h is not None; print('OK:', type(h).__name__)" (Level 1: Sanity — S3)
    cd backend && uv run ruff format --check app/services/execution_type_handler.py app/services/grd_cli_service.py (Level 1: Sanity — S2)
  </verify>
  <done>HANDLER_REGISTRY['grd_research'] resolves to GrdResearchSessionHandler; research_status/list_threads/read_thread exist and list_threads returns [] for a missing dir; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Five research routes on grd_router</name>
  <files>backend/app_litestar/routes/grd_routes.py</files>
  <action>
    Add five routes under the existing `grd_router = Router(path="/api/projects")` (:1658),
    mirroring `POST /{project_id}/planning/invoke` (:718) and the generic session routes
    (`GET /{project_id}/sessions` :919, `GET /{project_id}/sessions/{session_id}/output` :928):
    - `POST /{project_id}/research/start` (body: question, optional max_iterations/no_gates) ->
      create a grd_research session via the handler, return {"session_id": ...}.
    - `POST /{project_id}/research/{thread_id}/resume` -> spawn a grd research resume session,
      return {"session_id": ...}.
    - `GET /{project_id}/research/threads` -> GrdCliService.list_threads(project_path) (portfolio/browser).
    - `GET /{project_id}/research/threads/{thread_id}` -> GrdCliService.read_thread(...) bundle.
    - `GET /{project_id}/research/status` -> GrdCliService.research_status(...) passthrough.
    Reuse the generic session-output SSE (the existing `/sessions/{session_id}/output` route) for
    streaming — do NOT build a research-specific bridge. cwd/project resolution mirrors the chat route.
  </action>
  <verify>
    cd backend && uv run ruff format --check app_litestar/routes/grd_routes.py (Level 1: Sanity)
    cd backend && uv run python -c "import app_litestar.routes.grd_routes as m; print('import OK')" (Level 1: Sanity)
  </verify>
  <done>Five research routes registered under grd_router; module imports cleanly; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 3: Handler + route tests</name>
  <files>backend/tests/test_grd_research_handler.py, backend/tests/test_grd_research_routes.py</files>
  <action>
    test_grd_research_handler.py — mirror test_grd_chat_handler.py (spy_psm fixture). Assert
    GrdResearchSessionHandler.start calls create_session with execution_type='grd_research',
    stream_json=True, use_pty=False; assert the spawned prompt contains json.dumps(question)
    (e.g. a question with a `"` is escaped, not interpolated raw); assert cwd resolution and stop delegation.
    test_grd_research_routes.py — use isolated_db + Litestar TestClient. Spy on
    `module.logger.warning` via monkeypatch (TestClient logger doesn't propagate to caplog per CLAUDE.md).
    Mock GrdCliService/disk reads. Assert: POST /research/start returns {session_id};
    GET /research/threads returns [] for a missing/empty `.planning/research/threads/` dir AND
    parses THREAD.md frontmatter when a fixture thread dir is present; GET /research/threads/{id}
    returns a None-safe bundle (THREAD/HYPOTHESES/FINDING each None when absent).
  </action>
  <verify>
    cd backend && uv run pytest tests/test_grd_research_handler.py tests/test_grd_research_routes.py -v (Level 2: Proxy — P1+P2)
  </verify>
  <done>Both test files pass with 0 failures/0 errors; cover handler contract + all five routes incl. empty-dir and frontmatter-present cases.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): handler import smoke (S3) prints `OK: GrdResearchSessionHandler`; ruff format --check clean on the three backend files (S2); grd_routes imports without error.
Level 2 (Proxy): test_grd_research_handler.py + test_grd_research_routes.py all green (P1+P2); no regression in test_grd_chat_handler.py.
Level 3 (Deferred): live SSE stream from `gd research` through PSM (DEFER-20-01) — requires gd binary + running server; out of scope here.
</verification>

<success_criteria>
SC-1 backend half: autoresearch is reachable from the backend — handler registered, five routes present, CLI helpers handle the missing-thread-dir case, and both test files pass. The grd_chat path is untouched.
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/20-grd-frontend-wiring/20-01-SUMMARY.md`
documenting the handler/route signatures (these are the contracts 20-02 consumes).
</output>
