---
phase: 20-grd-frontend-wiring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/services/api/research.ts
  - frontend/src/services/api/grdHarness.ts
  - frontend/src/services/api/index.ts
  - frontend/src/services/api/research.test.ts
  - frontend/src/services/api/grdHarness.test.ts
autonomous: true
verification_level: proxy

hypothesis: "Two new api modules (research.ts wrapping the five /api/projects/{id}/research/* routes, grdHarness.ts wrapping the 16 /api/projects GRD routes plus the /admin harness-evolution routes) re-exported through the barrel will give every phase-20 UI surface a typed, tested client without touching existing api modules."
predicted_outcome: "research.test.ts and grdHarness.test.ts pass: each method asserts apiFetch is called with the correct path, HTTP method, and body; /admin routes carry the admin base; barrel exports researchApi and grdHarnessApi resolve (S4 import smoke OK)."

eval_metrics:
  primary: "research.test.ts + grdHarness.test.ts all pass (P3)"
  secondary: ["barrel import smoke resolves new exports (S4)", "vue-tsc clean (S1)"]
  baseline: "existing api-module tests (triggers.test.ts, bot-health.test.ts) green"
  target: "0 new failures"

must_haves:
  truths:
    - "researchApi exposes startResearch, resumeThread, listThreads, getThread, streamResearch (SSE) and each hits the matching /api/projects/{id}/research/* path"
    - "grdHarnessApi covers all 16 GRD routes (health/think/dead-ends/genome/verify-mechanical/reflections/verdict-counts/evolve) AND the /admin harness routes (autonomy get/set, rounds list/detail/impact/apply/abort/revert, shared-forge list, adopt)"
    - "the /admin-prefixed methods route to the admin base and carry admin auth (verified against client.ts), distinct from the X-API-Key /api base"
    - "both modules are re-exported through the api index.ts barrel"
    - "every method has a unit test asserting path + method + body via a mocked apiFetch"
  artifacts:
    - path: "frontend/src/services/api/research.ts"
      provides: "researchApi client (5 methods incl. SSE streamResearch)"
      contains: "research/start"
    - path: "frontend/src/services/api/grdHarness.ts"
      provides: "grdHarnessApi client (16 GRD routes + /admin harness routes)"
      contains: "autonomy"
    - path: "frontend/src/services/api/index.ts"
      provides: "barrel re-exports of researchApi + grdHarnessApi"
    - path: "frontend/src/services/api/research.test.ts"
      provides: "per-method path/method/body tests"
    - path: "frontend/src/services/api/grdHarness.test.ts"
      provides: "per-method path/method/body tests"
  key_links:
    - from: "frontend/src/services/api/research.ts"
      to: "frontend/src/services/api/client.ts"
      via: "apiFetch + createAuthenticatedEventSource"
      pattern: "apiFetch|createAuthenticatedEventSource"
    - from: "frontend/src/services/api/grdHarness.ts"
      to: "client.ts admin base"
      via: "admin-auth header for /admin routes"
      pattern: "/admin"
    - from: "frontend/src/services/api/index.ts"
      to: "research.ts, grdHarness.ts"
      via: "barrel re-export"
      pattern: "export .*research|export .*grdHarness"
---

<objective>
Build the two frontend api-client modules every phase-20 UI surface needs:
`research.ts` (REQ-15 plumbing — the five autoresearch routes from 20-01) and
`grdHarness.ts` (REQ-16 plumbing — the 16 GRD routes + the /admin harness-evolution
routes). Pure, tested plumbing; no UI.

Purpose: decouple UI plans (20-03, 20-04) from raw fetch wiring and give them a typed
contract. Mirrors the per-domain api-package convention (triggers.ts, budgets.ts).

Output: two api modules, barrel re-exports, two test files.

Research basis: N/A. Route table verified in 20-RESEARCH.md §1e and §2.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/milestones/anonymous/phases/20-grd-frontend-wiring/20-RESEARCH.md
@frontend/src/services/api/client.ts
@frontend/src/services/api/grd.ts
@frontend/src/services/api/index.ts
@frontend/src/services/api/__tests__/bot-health.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: research.ts api module + tests</name>
  <files>frontend/src/services/api/research.ts, frontend/src/services/api/research.test.ts</files>
  <action>
    Mirror grd.ts (grdApi). Export `researchApi` with:
    - `startResearch(projectId, question, opts?)` -> POST /api/projects/{projectId}/research/start
      (body { question, max_iterations?, no_gates? }), returns { session_id }.
    - `resumeThread(projectId, threadId)` -> POST /api/projects/{projectId}/research/{threadId}/resume.
    - `listThreads(projectId)` -> GET /api/projects/{projectId}/research/threads.
    - `getThread(projectId, threadId)` -> GET /api/projects/{projectId}/research/threads/{threadId}.
    - `streamResearch(projectId, sessionId)` -> createAuthenticatedEventSource on the generic
      session-output URL (mirror grdApi.streamSession in grd.ts — research reuses the generic session SSE).
    Use apiFetch from client.ts for the JSON calls. Add minimal TS types for the thread/bundle shapes
    (THREAD frontmatter: id/question/status/iteration/max_iterations; bundle adds hypotheses/finding).
    Tests: mirror bot-health.test.ts — `vi.mock('../client')`, one test per method asserting apiFetch is
    called with the exact path, method, and body; for streamResearch assert createAuthenticatedEventSource
    is called with the session URL.
  </action>
  <verify>
    cd frontend && npx vitest --run src/services/api/research.test.ts (Level 2: Proxy — part of P3)
  </verify>
  <done>researchApi exports all 5 methods; every test asserts path/method/body; suite green.</done>
</task>

<task type="auto">
  <name>Task 2: grdHarness.ts api module + tests</name>
  <files>frontend/src/services/api/grdHarness.ts, frontend/src/services/api/grdHarness.test.ts</files>
  <action>
    Export `grdHarnessApi`. Cover the two route groups from 20-RESEARCH.md §2.
    Group A (16 GRD routes, /api/projects base, X-API-Key): getHealth, think, addDeadEnd,
    promoteDeadEnds(phase), listDeadEnds, getGenome, snapshotGenome, listGenomeSnapshots,
    latestGenomeSnapshot, verifyMechanical(phase), listPhaseReflections(phaseId), verdictCounts,
    startEvolve, listEvolveRuns, getEvolveRun(runId), stopEvolveRun(runId).
    Group B (/admin base, admin auth): getAutonomy(projectId), setAutonomy(projectId, policy),
    listProjectRounds(projectId), listAllRounds, getRoundDetail(roundId), getRoundImpact(roundId),
    approveRound(roundId), abortRound(roundId), revertRound(roundId),
    listSharedForge, adoptShared(projectId, bindingId).
    CRITICAL: Group B hits /admin/* and must carry admin auth. Verify how client.ts distinguishes
    the admin base / admin-auth header (per 20-RESEARCH.md §2 gotcha and §9.3) and route Group B
    through it — do NOT send /admin calls with only the X-API-Key path.
    Tests: per-method path/method/body assertions with mocked apiFetch; explicitly assert Group B
    methods use the /admin base.
  </action>
  <verify>
    cd frontend && npx vitest --run src/services/api/grdHarness.test.ts (Level 2: Proxy — part of P3)
  </verify>
  <done>grdHarnessApi covers all 16 GRD routes + all /admin harness routes; Group B uses admin base; suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Barrel re-exports + import smoke</name>
  <files>frontend/src/services/api/index.ts</files>
  <action>
    Re-export researchApi and grdHarnessApi from the api index.ts barrel (alongside grdApi at the
    existing export site). Keep export naming consistent with the rest of the barrel so
    `import { researchApi, grdHarnessApi } from '../services/api'` resolves.
  </action>
  <verify>
    cd frontend && node --input-type=module -e "import('./src/services/api/index.ts').then(m=>{console.log('research:',typeof m.researchApi,'harness:',typeof m.grdHarnessApi)})" 2>/dev/null || echo "ts import needs S1 type-check"  (Level 1: Sanity — S4; S1 subsumes if ts-node unavailable)
    cd /Users/neo/Developer/Projects/Agented && just build (Level 1: Sanity — S1, vue-tsc clean)
  </verify>
  <done>Barrel re-exports resolve; vue-tsc build passes with no new TS errors.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): barrel import smoke (S4) shows researchApi/grdHarnessApi as objects; `just build` clean (S1).
Level 2 (Proxy): research.test.ts + grdHarness.test.ts green (P3); no new frontend failures vs the 7-failure baseline.
Level 3 (Deferred): real network calls against a live Litestar server — covered by DEFER-20-02 walkthrough.
</verification>

<success_criteria>
SC-2: both api modules wired, barrel-exported, fully unit-tested (path/method/body), with /admin routes
correctly admin-authed. UI plans 20-03/20-04 can import them as a stable contract.
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/20-grd-frontend-wiring/20-02-SUMMARY.md`
listing the exported method signatures (the contract 20-03 and 20-04 consume).
</output>
