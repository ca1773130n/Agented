---
phase: 20-grd-frontend-wiring
plan: 05
type: execute
wave: 2
depends_on: ["20-02"]
files_modified:
  - frontend/src/components/grd/planningCommands.ts
  - frontend/src/components/grd/PlanningCommandBar.vue
  - frontend/src/views/ProjectPlanningPage.vue
  - frontend/src/components/grd/__tests__/planningCommands.test.ts
  - frontend/src/components/grd/__tests__/PlanningCommandBar.test.ts
  - frontend/src/locales/en.json
  - frontend/src/locales/ko.json
  - frontend/src/locales/ja.json
  - frontend/src/locales/zh.json
autonomous: true
verification_level: proxy

hypothesis: "Extracting PlanningCommandBar's inline commandGroups into a typed, testable planningCommands.ts manifest grouped Plan/Execute/Verify/Research/Harness/Misc — and routing invoke by group (research->research-start, harness->panel deep-link, else->planning session) — exposes the full supported /grd: command set from one declarative source."
predicted_outcome: "planningCommands.test.ts asserts all 6 groups present, total command count >= current + new GRD commands, every command has non-empty name+labelKey; PlanningCommandBar.test.ts mounts and shows >=1 item per group; deprecated commands (evolve) are marked; P6 green."

eval_metrics:
  primary: "planningCommands.test.ts + PlanningCommandBar.test.ts pass (P6)"
  secondary: ["group count >= 6", "manifest is a plain TS module (unit-testable)"]
  baseline: "existing declarative commandGroups in PlanningCommandBar.vue"
  target: "0 new failures; 6 groups; full /grd: set represented"

must_haves:
  truths:
    - "GRD_COMMAND_MANIFEST is exported from planningCommands.ts as GrdCommandGroup[], typed {name,labelKey,descKey}"
    - "the manifest has six groups by key: Plan, Execute, Verify, Research, Harness, Misc"
    - "the manifest covers the supported /grd: set (plan-phase, plan-milestone-gaps, autoplan, discuss-phase, execute-phase, autopilot, quick, verify-phase, verify-work, assess-baseline, research, survey, deep-dive, compare-methods, feasibility, harness, settings, sync, map-codebase, progress, help) with deprecated commands (evolve) marked"
    - "PlanningCommandBar.vue consumes GRD_COMMAND_MANIFEST (no inline group array remains) and renders >=1 item per group"
    - "invoke routing: research-group commands route to the research-start path, harness-group commands deep-link to the /harness panels, all others spawn a planning/grd_chat session"
    - "every command label/desc uses planningCommandBar.cmd.* / .groups.* keys, key-identical across en/ko/ja/zh"
  artifacts:
    - path: "frontend/src/components/grd/planningCommands.ts"
      provides: "GRD_COMMAND_MANIFEST + GrdCommand/GrdCommandGroup types"
      contains: "GRD_COMMAND_MANIFEST"
    - path: "frontend/src/components/grd/PlanningCommandBar.vue"
      provides: "command bar consuming the manifest + group-aware invoke"
    - path: "frontend/src/components/grd/__tests__/planningCommands.test.ts"
      provides: "manifest structural assertions"
  key_links:
    - from: "frontend/src/components/grd/PlanningCommandBar.vue"
      to: "frontend/src/components/grd/planningCommands.ts"
      via: "imports GRD_COMMAND_MANIFEST"
      pattern: "GRD_COMMAND_MANIFEST"
    - from: "frontend/src/views/ProjectPlanningPage.vue"
      to: "research-start / harness route"
      via: "group-aware invoke routing"
      pattern: "research|harness"
---

<objective>
Complete REQ-17: extract PlanningCommandBar's commandGroups into a standalone, typed,
testable `planningCommands.ts` manifest; regroup to Plan/Execute/Verify/Research/Harness/Misc
covering the full supported /grd: set; and route invoke per group (research -> research-start,
harness -> panel deep-link, else -> planning session). All command strings localized.

Purpose: SC-5. The bar is already declarative — this finishes it into a single-source manifest
and wires the new surfaces from 20-03/20-04 into the invoke path.

Output: manifest module + updated bar + group-aware invoke + tests + expanded
planningCommandBar.* i18n keys.

Research basis: N/A. The bar already uses a declarative commandGroups const (20-RESEARCH.md §5).
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/milestones/anonymous/phases/20-grd-frontend-wiring/20-RESEARCH.md
@frontend/src/components/grd/PlanningCommandBar.vue
@frontend/src/views/ProjectPlanningPage.vue
</context>

<tasks>

<task type="tdd">
  <name>Task 1: planningCommands.ts manifest (TDD)</name>
  <files>frontend/src/components/grd/planningCommands.ts, frontend/src/components/grd/__tests__/planningCommands.test.ts</files>
  <action>
    RED: write planningCommands.test.ts first asserting the manifest contract:
    (a) GRD_COMMAND_MANIFEST exports six groups keyed Plan/Execute/Verify/Research/Harness/Misc;
    (b) total command count >= the current bar's count + the new GRD commands;
    (c) every command has non-empty `name`, `labelKey`, `descKey`;
    (d) deprecated commands (evolve) carry a deprecated marker;
    (e) names are unique across the manifest.
    GREEN: implement planningCommands.ts:
      export interface GrdCommand { name: string; labelKey: string; descKey: string; deprecated?: boolean; group?: string }
      export interface GrdCommandGroup { labelKey: string; commands: GrdCommand[] }
      export const GRD_COMMAND_MANIFEST: GrdCommandGroup[]
    Populate the six groups from the supported /grd: set listed in must_haves; map each command to
    planningCommandBar.cmd.* / .groups.* i18n keys. This is a pure TS module — TDD applies cleanly.
  </action>
  <verify>
    cd frontend && npx vitest --run src/components/grd/__tests__/planningCommands.test.ts (Level 2: Proxy — part of P6)
  </verify>
  <done>RED test written and failing first, then GREEN: manifest satisfies all six structural assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Bar consumes manifest + group-aware invoke</name>
  <files>frontend/src/components/grd/PlanningCommandBar.vue, frontend/src/views/ProjectPlanningPage.vue, frontend/src/components/grd/__tests__/PlanningCommandBar.test.ts</files>
  <action>
    Replace PlanningCommandBar.vue's inline commandGroups with an import of GRD_COMMAND_MANIFEST; render
    grouped items (>=1 per group). Keep the `invoke` emit. In ProjectPlanningPage.vue's handleInvokeCommand,
    branch on the command's group: research-group -> route to the research-start path (20-03 surface);
    harness-group -> deep-link to the /harness route/panel (20-04 surface); all others -> the existing
    grdApi.invokePlanningCommand / grd_chat planning session path. Mark deprecated commands visually
    (e.g. a badge) and keep them non-default.
    Test PlanningCommandBar.test.ts: mount the bar, assert all six groups render with >=1 item each, and
    that selecting a command emits `invoke` with the command name/group.
  </action>
  <verify>
    cd frontend && npx vitest --run src/components/grd/__tests__/PlanningCommandBar.test.ts (Level 2: Proxy — P6)
    cd /Users/neo/Developer/Projects/Agented && just build (Level 1: Sanity — S1)
  </verify>
  <done>Bar renders all 6 groups from the manifest; invoke routes per group; build clean; suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Expand planningCommandBar.* i18n keys in all 4 catalogs</name>
  <files>frontend/src/locales/en.json, frontend/src/locales/ko.json, frontend/src/locales/ja.json, frontend/src/locales/zh.json</files>
  <action>
    Add the new `planningCommandBar.groups.*` (Plan/Execute/Verify/Research/Harness/Misc) and
    `planningCommandBar.cmd.*` (label+desc for every command in the manifest) keys to ALL FOUR catalogs,
    key-identical, preserving existing keys. Keys must match exactly the labelKey/descKey values in
    planningCommands.ts. Ship parity here so the 20-06 key-diff finds zero diff for this surface.
  </action>
  <verify>
    cd frontend && node -e "const fs=require('fs');['en','ko','ja','zh'].forEach(l=>{const d=JSON.parse(fs.readFileSync('src/locales/'+l+'.json','utf8'));console.log(l, JSON.stringify(d).includes('planningCommandBar'))})" (Level 1: Sanity — S6)
  </verify>
  <done>planningCommandBar.groups.* + .cmd.* present and key-identical in en/ko/ja/zh; valid JSON.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): `just build` clean (S1); planningCommandBar.* keys in all 4 locales (S6).
Level 2 (Proxy): planningCommands.test.ts + PlanningCommandBar.test.ts green (P6); 6 groups; no new failures.
Level 3 (Deferred): real CLI invocation per group end-to-end (covered by DEFER-20-02 walkthrough).
</verification>

<success_criteria>
SC-5: PlanningCommandBar exposes the full supported /grd: command set, grouped into six groups from a
single declarative manifest, with group-aware invoke routing and all strings localized in four catalogs.
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/20-grd-frontend-wiring/20-05-SUMMARY.md`
listing the six groups + the command roster.
</output>
