---
phase: 20-grd-frontend-wiring
plan: 06
type: execute
wave: 3
depends_on: ["20-03", "20-04", "20-05"]
files_modified:
  - frontend/src/locales/en.json
  - frontend/src/locales/ko.json
  - frontend/src/locales/ja.json
  - frontend/src/locales/zh.json
  - frontend/scripts/i18n-parity.mjs
autonomous: true
verification_level: proxy

hypothesis: "A deterministic key-diff over the four locale catalogs plus the full house-gate run (just build + frontend no-new-failures + targeted backend) will confirm every phase-20 surface is fully localized and the change introduces zero regressions — closing REQ-18 and the phase quality bar."
predicted_outcome: "The i18n-parity script reports `Total diff count: 0` (exit 0) across en/ko/ja/zh; just build exits 0; frontend test failures <= 7 (all in the known baseline areas); targeted backend suites for the touched files pass."

eval_metrics:
  primary: "i18n key-diff count = 0 (P7)"
  secondary: ["just build exit 0 (S1)", "frontend failures <= 7 (proxy gate)", "targeted backend suites pass"]
  baseline: "0 key diff (established invariant); 7 known frontend failures"
  target: "0 diff; 0 new failures; build clean"

must_haves:
  truths:
    - "the key sets of en.json/ko.json/ja.json/zh.json are identical — zero keys present in one locale but absent in another (P7 reports diff count 0)"
    - "surface.research.*, surface.harness.*, and the expanded planningCommandBar.* namespaces are all present and key-identical in all four catalogs"
    - "a committed parity script (frontend/scripts/i18n-parity.mjs) reproduces the key-diff and exits non-zero on any diff"
    - "just build exits 0 with no new TS errors"
    - "the frontend suite shows no NEW failures beyond the 7 known baseline (RateLimitGauge, MarkdownContent, WorkingMemoryView, useTourMachine)"
    - "the backend suites touched by 20-01 (grd_research handler + routes, grd_chat regression) pass under the targeted-run procedure"
  artifacts:
    - path: "frontend/scripts/i18n-parity.mjs"
      provides: "deterministic locale key-diff (exit non-zero on diff)"
      contains: "diff"
  key_links:
    - from: "frontend/scripts/i18n-parity.mjs"
      to: "frontend/src/locales/*.json"
      via: "flatten + set-diff of all four catalogs"
      pattern: "locales"
---

<objective>
Close REQ-18 and the phase quality bar: reconcile any i18n parity gaps left across
the three UI plans, commit a reusable key-diff script, and run the full house gate
(build + frontend no-new-failures + targeted backend) to certify zero regressions.

Purpose: SC-6 plus the phase-wide gate. 20-03/04/05 each ship their own catalog keys,
but this plan owns the cross-surface parity proof and the green-gate certification.

Output: any missing-key fixes + a committed i18n-parity.mjs script + a clean gate run.

Research basis: N/A. Parity approach from 20-RESEARCH.md §6; gates from 20-EVAL.md.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/milestones/anonymous/phases/20-grd-frontend-wiring/20-RESEARCH.md
@.planning/milestones/anonymous/phases/20-grd-frontend-wiring/20-EVAL.md
@frontend/src/locales/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n parity script + reconcile any gaps</name>
  <files>frontend/scripts/i18n-parity.mjs, frontend/src/locales/en.json, frontend/src/locales/ko.json, frontend/src/locales/ja.json, frontend/src/locales/zh.json</files>
  <action>
    Write frontend/scripts/i18n-parity.mjs: flatten each of the four locale JSONs to dotted key paths,
    build a key Set per locale, diff every locale against en (report MISSING/EXTRA per locale), print
    `Total diff count: N`, and `process.exit(N > 0 ? 1 : 0)`. Use the exact algorithm from 20-EVAL.md P7.
    Run it. For ANY reported diff (a key one plan added that another surface's locale missed, or a
    translation gap), add the missing key to the deficient catalog(s) with a proper per-locale translation
    so the namespaces surface.research.*, surface.harness.*, planningCommandBar.* are all key-identical.
    Re-run until `Total diff count: 0`.
  </action>
  <verify>
    cd frontend && node scripts/i18n-parity.mjs (Level 2: Proxy — P7; expect "Total diff count: 0", exit 0)
  </verify>
  <done>Script committed; key-diff is 0 across all four locales; all phase-20 namespaces key-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Full house-gate run + certification</name>
  <files>frontend/src/locales/en.json</files>
  <action>
    Run the three house gates from CLAUDE.md / 20-EVAL.md and certify:
    1. `cd /Users/neo/Developer/Projects/Agented && just build` — must exit 0, no new `error TS` lines.
    2. `cd frontend && npm run test:run` — failures must be <= 7 and all within the known baseline areas
       (RateLimitGauge, MarkdownContent, WorkingMemoryView, useTourMachine). Any failure outside those
       names is a NEW failure and blocks — fix it.
    3. Backend: attempt `cd backend && uv run pytest` under a ~12-minute watchdog; on the known ~40-48%
       hang, kill it and run a targeted set: tests/test_grd_research_handler.py, tests/test_grd_research_routes.py,
       tests/test_grd_chat_handler.py, plus execution/streaming/harness regression suites. Disclose the
       substitution in the SUMMARY. All targeted suites must pass.
    (No locale edit is expected in this task; en.json is listed only so the gate-certification owner can
    record final parity state in a comment if needed — do not change keys here.)
  </action>
  <verify>
    cd /Users/neo/Developer/Projects/Agented && just build (Level 1: Sanity — S1)
    cd frontend && npm run test:run 2>&1 | tail -20 (Level 2: Proxy gate — failures <= 7)
    cd backend && uv run pytest tests/test_grd_research_handler.py tests/test_grd_research_routes.py tests/test_grd_chat_handler.py -v (Level 2: Proxy — targeted backend)
  </verify>
  <done>just build exits 0; frontend failures <= 7 all in baseline; targeted backend suites pass; substitution disclosed in SUMMARY.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): just build clean (S1); all four locales valid JSON with the new namespaces (S6).
Level 2 (Proxy): i18n key-diff = 0 (P7); frontend no-new-failures gate (<= 7); targeted backend suites pass.
Level 3 (Deferred): locale visual/overflow review in a real browser (DEFER-20-03).
</verification>

<success_criteria>
SC-6 + phase gate: all four locales are key-identical (zero diff) with every phase-20 surface localized,
a reusable parity script is committed, and the full house gate is green with zero new regressions.
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/20-grd-frontend-wiring/20-06-SUMMARY.md`
recording the final key-diff count, build status, frontend failure count, and the disclosed backend
targeted-run substitution.
</output>
