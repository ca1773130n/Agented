---
phase: 23-stackable-policy-governance-engine
plan: 05
type: execute
wave: 5
depends_on: ["23-03", "23-04"]
files_modified:
  - frontend/src/services/api/policies.ts
  - frontend/src/services/api/index.ts
  - frontend/src/services/api/budgets.ts
  - frontend/src/components/policy/PolicyEditor.vue
  - frontend/src/components/policy/PolicyAskCard.vue
  - frontend/src/locales/en.json
  - frontend/src/locales/ko.json
  - frontend/src/locales/ja.json
  - frontend/src/locales/zh.json
  - frontend/src/components/policy/__tests__/PolicyEditor.spec.ts
  - frontend/src/components/policy/__tests__/PolicyAskCard.spec.ts
  - backend/tests/test_policy_e2e.py
autonomous: true
verification_level: deferred
hypothesis: "A policies.ts API module (apiFetch), a PolicyEditor CRUD component, a PolicyAskCard that renders the policy_ask SSE event and POSTs to /admin/policies/decision, budgets.ts surfacing cost-cap verdicts, and four key-identical locale namespaces complete the operator surface; an end-to-end test then proves a configured DENY blocks, an ASK pauses-then-resumes on approve, and an ALLOW passes through a real session."
predicted_outcome: "Vitest mounts PolicyEditor + PolicyAskCard (no new failures); locale parity assertion passes (en/ko/ja/zh key-identical for policy.*); test_policy_e2e.py drives a session where DENY blocks the action, ASK pauses for a card and resumes on approve, ALLOW passes; just build + frontend test:run show no NEW failures."

must_haves:
  truths:
    - "policies.ts exposes list/upsert/remove/decide via apiFetch and is re-exported through the api barrel (SC4)"
    - "PolicyEditor.vue performs policy CRUD; PolicyAskCard.vue renders a policy_ask event and POSTs approve/deny to /admin/policies/decision (SC4)"
    - "budgets.ts surfaces the cost-cap verdict (DENY/ASK from cost_budget) so the budgets UI reflects policy decisions (SC4)"
    - "en/ko/ja/zh each gain a key-identical policy.* namespace; no locale key drift (SC4 + i18n rule)"
    - "End-to-end: a configured DENY blocks the action; an ASK pauses for an approval card and resumes on approve; an ALLOW passes through a real session (SC5)"
    - "House gates pass: just build; backend pytest (watchdog + disclosed targeted substitution if the ~40-48% hang hits); frontend no NEW failures (SC6)"
  artifacts:
    - path: "frontend/src/services/api/policies.ts"
      provides: "policyApi { list, upsert, remove, decide }"
      exports: ["policyApi"]
    - path: "frontend/src/components/policy/PolicyEditor.vue"
      provides: "Policy CRUD editor"
    - path: "frontend/src/components/policy/PolicyAskCard.vue"
      provides: "ASK approval card bound to the policy_ask SSE event + /decision"
    - path: "backend/tests/test_policy_e2e.py"
      provides: "ALLOW/DENY/ASK end-to-end real-session drive (SC5)"
    - path: "frontend/src/locales/en.json"
      provides: "policy.* namespace (ko/ja/zh key-identical siblings)"
      contains: "policy"
  key_links:
    - from: "frontend/src/services/api/policies.ts"
      to: "frontend/src/services/api/client.ts (apiFetch)"
      via: "import { apiFetch }"
      pattern: "import \\{ apiFetch \\} from './client'"
    - from: "frontend/src/services/api/index.ts"
      to: "policyApi"
      via: "barrel re-export"
      pattern: "export \\{ policyApi \\}"
    - from: "frontend/src/components/policy/PolicyAskCard.vue"
      to: "/admin/policies/decision"
      via: "policyApi.decide(sessionId, decision)"
      pattern: "policyApi.decide|/admin/policies/decision"
    - from: "backend/tests/test_policy_e2e.py"
      to: "PolicyService.submit_policy_decision + ExecutionService/goal_loop enforcement"
      via: "drive DENY/ASK/ALLOW through a real session"
      pattern: "submit_policy_decision|PolicyService.evaluate"
---

<objective>
Ship the FRONTEND surface and the END-TO-END proof: a `policies.ts` API module, a `PolicyEditor`
CRUD component, a `PolicyAskCard` bound to the `policy_ask` SSE event + `/admin/policies/decision`,
`budgets.ts` surfacing cost-cap verdicts, four key-identical locale namespaces, and an e2e test that
drives a real session through ALLOW / DENY / ASK. Then run the house gates.

Purpose: Satisfy the frontend half of SC4, the locked i18n rule, SC5 (e2e ALLOW/DENY/ASK), and SC6
(house gates). This is the final plan in the phase.
Output: policies.ts + barrel re-export, PolicyEditor.vue + PolicyAskCard.vue + their Vitest specs,
budgets.ts verdict surfacing, en/ko/ja/zh policy.* namespaces, and test_policy_e2e.py.
Research basis: 23-RESEARCH.md Code Examples (policies.ts mirrors budgets.ts + client.ts apiFetch),
Architecture file layout (policy components + four locales), Pitfall 4 (add all four locales in the
same change), Experiment Design item 5 (SC5 e2e), Verification Strategy (house gates / watchdog).
</objective>

<context>
@.planning/milestones/anonymous/phases/23-stackable-policy-governance-engine/23-RESEARCH.md
@.planning/milestones/anonymous/phases/23-stackable-policy-governance-engine/23-03-SUMMARY.md
@.planning/milestones/anonymous/phases/23-stackable-policy-governance-engine/23-04-SUMMARY.md
@frontend/src/services/api/budgets.ts
@frontend/src/services/api/index.ts
@frontend/src/services/api/client.ts
@frontend/src/locales/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: policies.ts API module + budgets.ts cost-cap verdict + barrel</name>
  <files>frontend/src/services/api/policies.ts, frontend/src/services/api/index.ts, frontend/src/services/api/budgets.ts</files>
  <action>
    Create policies.ts mirroring budgets.ts (import { apiFetch } from './client'; export const policyApi):
      - list: () => apiFetch<{ policies: Policy[] }>('/admin/policies')
      - upsert: (p: PolicyInput) => apiFetch<Policy>('/admin/policies', { method: 'PUT', body: JSON.stringify(p) })
      - remove: (id) => apiFetch<void>(`/admin/policies/${id}`, { method: 'DELETE' })
      - decide: (sessionId, decision: 'approve'|'deny') => apiFetch<{ ok: boolean }>('/admin/policies/decision',
          { method: 'POST', body: JSON.stringify({ session_id: sessionId, decision }) })
      Define Policy / PolicyInput / PolicyVerdict TS types (scope, scope_id, kind, effect, params, enabled,
      priority) in policies.ts or under types/ following the budgets.ts type convention.
    budgets.ts: add a small surfacing of the cost-cap verdict — extend the existing BudgetCheck shape or
    add a helper that maps a cost_budget policy verdict (DENY at max_cost_usd / ASK at ask_thresholds_usd)
    so the budgets UI reflects the policy decision. Keep it additive (don't break existing budgetApi callers).
    index.ts: add `export { policyApi } from './policies';` to the barrel (next to the budgets/triggers exports).
  </action>
  <verify>
    `cd frontend && npx vue-tsc --noEmit` (or rely on `just build`) — policies.ts + barrel typecheck clean.
  </verify>
  <done>policyApi { list, upsert, remove, decide } exists, re-exported via the barrel; budgets.ts surfaces the cost-cap verdict additively.</done>
</task>

<task type="auto">
  <name>Task 2: PolicyEditor.vue + PolicyAskCard.vue + Vitest specs + four locales</name>
  <files>frontend/src/components/policy/PolicyEditor.vue, frontend/src/components/policy/PolicyAskCard.vue, frontend/src/components/policy/__tests__/PolicyEditor.spec.ts, frontend/src/components/policy/__tests__/PolicyAskCard.spec.ts, frontend/src/locales/en.json, frontend/src/locales/ko.json, frontend/src/locales/ja.json, frontend/src/locales/zh.json</files>
  <action>
    PolicyEditor.vue: list policies (policyApi.list), create/edit (scope server|team|session, kind from the
    builtin set + custom, effect allow|deny|ask, params per-kind: max_cost_usd + ask_thresholds_usd for
    cost_budget, max_tool_calls, etc.), delete. Use the project conventions: <script setup> + ref/reactive,
    props/emits, useI18n for all strings (no hardcoded copy), Geist/dark-theme CSS custom props. Reuse an
    existing GRD/settings component as the structural template.
    PolicyAskCard.vue: render an incoming policy_ask SSE event ({policy_id, kind, reason, scope}) as an
    approval card with Approve / Deny actions calling policyApi.decide(sessionId, 'approve'|'deny'). It must
    block visually until resolved and clear on policy_ask_resolved. Mirror the existing ask_user_question
    card rendering if one exists.
    Vitest specs (happy-dom + @vue/test-utils): mount PolicyEditor (renders list, emits upsert on save) and
    PolicyAskCard (renders reason, calls decide with the right args on Approve/Deny). Gate is NO NEW failures.
    Locales — add a key-identical `policy.*` namespace to ALL FOUR (en, ko, ja, zh) in the SAME change
    (standing i18n rule; 23-RESEARCH.md Pitfall 4): titles, scope/kind/effect labels, param labels, the ASK
    card copy (reason, approve, deny, awaiting). Add a parity assertion (reuse the existing locale-parity test
    if present, else assert Object.keys(en.policy) deep-equals the other three) so drift fails the suite.
  </action>
  <verify>
    `cd frontend && npm run test:run -- src/components/policy` — PolicyEditor + PolicyAskCard specs pass (Level 2: Proxy).
    Locale parity: the four policy.* key sets are identical (parity test green). NO NEW frontend failures vs the 7-known baseline.
  </verify>
  <done>PolicyEditor + PolicyAskCard render and act correctly; all four locales carry a key-identical policy.* namespace; specs pass with no new failures.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end ALLOW/DENY/ASK real-session test + house gates</name>
  <files>backend/tests/test_policy_e2e.py</files>
  <action>
    Write test_policy_e2e.py (isolated_db) that drives a REAL session through the enforcement boundaries
    (23-RESEARCH.md Experiment Design item 5, SC5):
      - DENY: seed a session-scope DENY policy (e.g. cost_budget over cap, or a deny effect row); start the
        action through ExecutionService (mock subprocess.Popen) and assert the action is BLOCKED (PolicyDenied,
        Popen not called).
      - ASK: seed a policy producing ASK (e.g. ask_on_os_tools for a shell action); start the action in a
        thread, assert it BLOCKS (awaiting), then call PolicyService.submit_policy_decision(session_id, 'approve')
        and assert the action RESUMES; repeat with 'deny' and assert it aborts. Assert a policy_ask broadcast
        was emitted (spy ProjectSessionManager._broadcast).
      - ALLOW: no matching deny/ask policy -> the action PASSES THROUGH (Popen reached / no PolicyDenied).
    Then run the house gates (SC6) and record results in the SUMMARY:
      1. `just build` (vue-tsc + vite) — must pass.
      2. `cd backend && uv run pytest` under a ~12-min watchdog. On the KNOWN ~40-48% hang: kill it and run a
         comprehensive targeted set — `uv run pytest tests/test_policy_evaluator.py tests/test_policy_builtins.py
         tests/test_policy_enforcement.py tests/test_policies_router.py tests/test_policy_middleware.py
         tests/test_policy_e2e.py tests/test_migration_176_policies.py` PLUS the execution/streaming/goal-loop
         regression suites — and DISCLOSE the substitution in the SUMMARY (never present targeted as full).
      3. `cd frontend && npm run test:run` — gate is NO NEW failures beyond the 7 known baseline failures.
  </action>
  <verify>
    `cd backend && uv run pytest tests/test_policy_e2e.py -v` — DENY blocks, ASK pauses+resumes on approve / aborts on deny, ALLOW passes (Level 3: Deferred — full pipeline).
    `just build` passes; targeted backend set green (with substitution disclosed if the hang hit); frontend no new failures.
  </verify>
  <done>SC5 e2e proven (DENY blocks / ASK pauses+resumes / ALLOW passes); all three house gates pass (or targeted-substitution disclosed) and are recorded in the SUMMARY.</done>
</task>

</tasks>

<verification>
Level 2 (Proxy): PolicyEditor/PolicyAskCard Vitest specs + locale parity.
Level 3 (Deferred): test_policy_e2e.py real-session ALLOW/DENY/ASK drive (SC5); house gates (SC6) —
just build, backend pytest under the watchdog (targeted substitution disclosed on the known hang),
frontend no-new-failures.
</verification>

<success_criteria>
- policyApi { list, upsert, remove, decide } shipped and barrel-exported; budgets.ts surfaces cost-cap verdicts.
- PolicyEditor + PolicyAskCard work; en/ko/ja/zh carry a key-identical policy.* namespace (parity asserted).
- E2E: configured DENY blocks the action; ASK pauses for an approval card and resumes on approve; ALLOW passes through.
- House gates: just build green; backend pytest green or targeted-substitution disclosed; frontend no new failures.
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/23-stackable-policy-governance-engine/23-05-SUMMARY.md`
recording the e2e outcomes, the exact house-gate results (and any disclosed pytest substitution), and the
final locale parity confirmation.
</output>
