---
phase: 23-stackable-policy-governance-engine
plan: 04
type: execute
wave: 4
depends_on: ["23-01", "23-03"]
files_modified:
  - backend/app_litestar/routes/policies.py
  - backend/app_litestar/main.py
  - backend/app_litestar/middleware.py
  - backend/tests/test_policies_router.py
  - backend/tests/test_policy_middleware.py
autonomous: true
verification_level: proxy
hypothesis: "Mirroring budgets_router for /admin/policies CRUD, adding a /admin/policies/decision route that calls submit_policy_decision (the way grd_routes.loop_gate_decision calls submit_gate_decision), and an ASGIMiddleware subclass that attaches a policy verdict to the request context, gives the frontend a complete CRUD + ASK-resolution + middleware surface with no new patterns."
predicted_outcome: "test_policies_router.py passes a CRUD round-trip (PUT creates, GET lists, DELETE removes) + a /decision POST that resolves a pending ASK; test_policy_middleware.py asserts PolicyMiddleware attaches the verdict/scope to request context without breaking unrelated routes."

must_haves:
  truths:
    - "/admin/policies supports list (GET), upsert (PUT), delete (DELETE /{id}) round-trips against the policies table (SC4)"
    - "/admin/policies/decision (POST {session_id, decision}) resolves a pending ASK via PolicyService.submit_policy_decision (mirrors grd_routes.loop_gate_decision:1441) (SC4)"
    - "policies_router is registered in main.py alongside budgets_router (SC4)"
    - "A PolicyMiddleware (ASGIMiddleware subclass in middleware.py) is added and registered, attaching the policy verdict/scope to the request context without altering unrelated responses (SC4)"
  artifacts:
    - path: "backend/app_litestar/routes/policies.py"
      provides: "policies_router: CRUD handlers + /decision handler"
      exports: ["policies_router"]
      contains: "Router("
    - path: "backend/app_litestar/middleware.py"
      provides: "PolicyMiddleware(ASGIMiddleware)"
      contains: "class PolicyMiddleware"
    - path: "backend/app_litestar/main.py"
      provides: "import + registration of policies_router (+ PolicyMiddleware if registered in middleware list)"
      contains: "policies_router"
    - path: "backend/tests/test_policies_router.py"
      provides: "CRUD + decision route tests (SC4)"
    - path: "backend/tests/test_policy_middleware.py"
      provides: "middleware attaches verdict / passes through (SC4)"
  key_links:
    - from: "backend/app_litestar/routes/policies.py (/decision handler)"
      to: "backend/app/services/policy_service.py submit_policy_decision"
      via: "decision resolution call"
      pattern: "submit_policy_decision"
    - from: "backend/app_litestar/main.py"
      to: "policies_router"
      via: "import + route_handlers registration"
      pattern: "from .routes.policies import policies_router|policies_router"
    - from: "backend/app_litestar/routes/policies.py (CRUD)"
      to: "backend/app/services/policy_service.py CRUD helpers"
      via: "create/list/get/update/delete_policy"
      pattern: "PolicyService\\.(create|list|get|update|delete)_policy"
---

<objective>
Ship the BACKEND SURFACE: a `/admin/policies` CRUD router (mirroring `budgets_router`), a
`/admin/policies/decision` route that resolves a pending ASK (mirroring
`grd_routes.loop_gate_decision`), and a `PolicyMiddleware` (`ASGIMiddleware` subclass) that attaches
the policy verdict to the request context.

Purpose: Satisfy the backend half of SC4 — `/admin/policies` CRUD + `middleware.py` policy middleware
(route + middleware tests). The frontend editor / budgets.ts / locales land in 23-05.
Output: `policies.py` router, `PolicyMiddleware` in `middleware.py`, registration in `main.py`, and
two test files.
Research basis: 23-RESEARCH.md Standard Stack (mirror `budgets.py:292` Router, `middleware.py`
ASGIMiddleware subclasses, `main.py:42/260` registration) + Code Examples (decision route mirrors
`loop_gate_decision`, grd_routes.py:1441).
</objective>

<context>
@.planning/milestones/anonymous/phases/23-stackable-policy-governance-engine/23-RESEARCH.md
@.planning/milestones/anonymous/phases/23-stackable-policy-governance-engine/23-03-SUMMARY.md
@backend/app_litestar/routes/budgets.py
@backend/app_litestar/middleware.py
@backend/app_litestar/main.py
@backend/app/services/policy_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: /admin/policies CRUD + /decision router</name>
  <files>backend/app_litestar/routes/policies.py, backend/app_litestar/main.py, backend/tests/test_policies_router.py</files>
  <action>
    Create policies.py mirroring budgets.py (Router at budgets.py:292; @get/@put/@delete/@post decorators
    with sync_to_thread=False):
      - @get("", ...) list_policies -> {"policies": PolicyService.list_policies(scope=optional query param)}
      - @put("", ...) upsert_policy(data) -> create_policy or update_policy (id present) via PolicyService;
        return the row. Validate scope in {server,team,session}, kind in the builtin set + "custom",
        effect in {allow,deny,ask}; 400 on bad input.
      - @delete("/{policy_id:str}", ...) -> PolicyService.delete_policy(id).
      - @post("/decision", ...) decide(data) -> body {session_id, decision in {approve,deny}, message?}
        -> {"ok": PolicyService.submit_policy_decision(session_id, decision, message)}. This is the HTTP
        entry the ASK card POSTs to — mirror grd_routes.loop_gate_decision (grd_routes.py:1441,
        registered :2101).
      - `policies_router = Router(path="/admin/policies", route_handlers=[...])`.
    Register in main.py: add `from .routes.policies import policies_router` near the budgets import
    (line 42) and include policies_router in the route_handlers list where budgets_router is registered
    (~line 260).
    test_policies_router.py (Litestar TestClient): CRUD round-trip (PUT creates a session-scope deny row,
    GET lists it, DELETE removes it); POST /decision resolves a pending ASK (seed _POLICY_DECISIONS via a
    started await in a thread OR assert submit_policy_decision returns the expected bool). Per the Litestar
    caplog note in CLAUDE.md, spy on module.logger.warning via monkeypatch if asserting log output. Use
    the isolated_db fixture + the standard X-API-Key/admin auth the other /admin tests use.
  </action>
  <verify>
    `cd backend && uv run pytest tests/test_policies_router.py -v` — CRUD + /decision round-trips pass (Level 2: Proxy).
    `cd backend && uv run ruff format --check app_litestar/routes/policies.py`.
  </verify>
  <done>/admin/policies GET/PUT/DELETE + /decision work end-to-end via TestClient; policies_router registered in main.py.</done>
</task>

<task type="auto">
  <name>Task 2: PolicyMiddleware (ASGIMiddleware subclass)</name>
  <files>backend/app_litestar/middleware.py, backend/tests/test_policy_middleware.py</files>
  <action>
    Add `class PolicyMiddleware(ASGIMiddleware)` following the existing subclass pattern
    (RequestContextMiddleware:107, ApiKeyMiddleware:148). Keep it LIGHT: it should NOT block requests —
    enforcement happens at the action boundaries (23-03). Its job is to make the active policy
    context/verdict observable on the request (e.g. attach a resolved scope summary or the current
    session's matching policy verdict to the request state/contextvar) so routes/logging can read it.
    - Scope it to the relevant paths (e.g. session/execution routes) or make it a cheap pass-through that
      only annotates when a session id is present in the request context (reuse the current-session
      contextvar set by RequestContextMiddleware). Do not perform a blocking await in the middleware.
    - Register it in the middleware stack in middleware.py / main.py in the correct order (after
      RequestContextMiddleware so the session contextvar is populated).
    test_policy_middleware.py: assert the middleware attaches the expected verdict/scope annotation for a
    request carrying a session id, and is a clean pass-through (status + body unchanged) for an unrelated
    route. Spy on module.logger.warning via monkeypatch if needed (Litestar caplog note).
  </action>
  <verify>
    `cd backend && uv run pytest tests/test_policy_middleware.py -v` — annotation present + pass-through intact (Level 2: Proxy).
    `cd backend && uv run ruff format --check app_litestar/middleware.py`.
  </verify>
  <done>PolicyMiddleware exists, subclasses ASGIMiddleware, is registered after RequestContextMiddleware, annotates the request context, and never blocks/alters unrelated responses.</done>
</task>

</tasks>

<verification>
Level 2 (Proxy): test_policies_router.py (CRUD + /decision) and test_policy_middleware.py (annotate +
pass-through) green via TestClient.
Level 3 (Deferred): house gates run in 23-05.
</verification>

<success_criteria>
- /admin/policies CRUD round-trips; /admin/policies/decision resolves a pending ASK via submit_policy_decision.
- policies_router registered in main.py next to budgets_router.
- PolicyMiddleware (ASGIMiddleware subclass) registered, annotates request context, non-blocking, pass-through for unrelated routes.
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/23-stackable-policy-governance-engine/23-04-SUMMARY.md`
documenting the exact route paths/methods, the /decision request/response shape, and the policy_ask
event payload the frontend ASK card consumes in 23-05.
</output>
