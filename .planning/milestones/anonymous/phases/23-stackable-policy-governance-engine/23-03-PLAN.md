---
phase: 23-stackable-policy-governance-engine
plan: 03
type: execute
wave: 3
depends_on: ["23-01", "23-02"]
files_modified:
  - backend/app/services/policy_service.py
  - backend/app/services/execution_service.py
  - backend/app/services/goal_loop_runner.py
  - backend/tests/test_policy_enforcement.py
autonomous: true
verification_level: proxy
hypothesis: "Evaluating PolicyService.evaluate() just before the ExecutionService Popen launch and at a goal_loop_runner human-gate, and resolving ASK by reusing the _await_gate/submit_gate_decision SSE round-trip (a sibling session-keyed await helper), will make a DENY block launch and an ASK block until an operator decision arrives — without hand-rolling a second SSE mechanism."
predicted_outcome: "test_policy_enforcement.py passes: a DENY verdict prevents subprocess.Popen (raises PolicyDenied, mocked Popen never called); an ASK verdict blocks until PolicyService.submit_policy_decision(session_id, 'approve') resolves it, then proceeds, and 'deny' aborts; broadcast emits a policy_ask event."

must_haves:
  truths:
    - "ExecutionService evaluates PolicyService.evaluate() AFTER cmd/proc_env are built but BEFORE subprocess.Popen (execution_service.py:767); a DENY raises PolicyDenied and Popen is never called (SC3)"
    - "An ASK verdict at the Popen boundary blocks the launching call until resolved, bounded by max_wall_seconds, defaulting to DENY on timeout (governance fail-safe) (SC3)"
    - "ASK reuses the existing SSE round-trip shape: PolicyService.await_decision broadcasts a policy_ask event via ProjectSessionManager._broadcast and polls a session-keyed decision registry resolved by submit_policy_decision (NOT a new transport) (SC3)"
    - "goal_loop_runner gains a policy human-gate hook: before the gated action it calls PolicyService.evaluate and, on ASK, routes through the existing _await_gate/gate_decision path (SC3)"
    - "The goal_loop exit-ladder cost budget (max_cost_usd, goal_loop_runner.py:776) and bot-security/bot-pr-review checks ROUTE THROUGH PolicyService rather than remaining parallel inline controls; policies anchor on session id, never on bot/trigger id (SC2/session-not-bot)"
  artifacts:
    - path: "backend/app/services/policy_service.py"
      provides: "await_decision(session_id, verdict) + submit_policy_decision(session_id, decision, message=None) + session-keyed decision registry"
      exports: ["PolicyService"]
    - path: "backend/app/services/execution_service.py"
      provides: "policy evaluation + ASK await + PolicyDenied at the Popen boundary"
      contains: "PolicyService.evaluate"
    - path: "backend/app/services/goal_loop_runner.py"
      provides: "policy human-gate hook reusing _await_gate"
      contains: "PolicyService.evaluate"
    - path: "backend/tests/test_policy_enforcement.py"
      provides: "DENY-blocks-Popen + ASK-blocks-until-resolved + timeout-default-deny tests (SC3)"
  key_links:
    - from: "backend/app/services/execution_service.py (~line 760)"
      to: "subprocess.Popen (line 767)"
      via: "policy verdict gate before launch"
      pattern: "PolicyService.evaluate.*subprocess.Popen|if verdict.*deny"
    - from: "backend/app/services/policy_service.py (await_decision)"
      to: "backend/app/services/project_session_manager.py:1853 (_broadcast)"
      via: "policy_ask SSE event"
      pattern: "_broadcast\\(.*policy_ask|ProjectSessionManager._broadcast"
    - from: "backend/app/services/goal_loop_runner.py (exit ladder, ~line 776)"
      to: "PolicyService.evaluate cost_budget builtin"
      via: "route exit-ladder budget through policy layer"
      pattern: "PolicyService.evaluate"
---

<objective>
Wire ENFORCEMENT at the two action boundaries: the `ExecutionService` subprocess launch and a
`goal_loop_runner` human-gate. A DENY blocks the action; an ASK surfaces an approval card over SSE
and blocks until resolved — REUSING the existing `_await_gate`/`submit_gate_decision`/`_broadcast`
machinery rather than inventing a new round-trip. Route today's scattered controls (goal_loop
exit-ladder cost budget + bot-security/bot-pr-review checks) THROUGH the policy layer.

Purpose: Satisfy SC3 (enforcement + ASK-blocks-until-resolved) and the consolidation half of SC2.
Output: a session-keyed ASK await helper + decision resolver on PolicyService, two enforcement
call-sites, the exit-ladder + bot-check rerouting, and enforcement tests.
Research basis: 23-RESEARCH.md Recommendation 1 (reuse `_await_gate`, goal_loop_runner.py:452/487) +
Pitfall 1 (evaluate BEFORE Popen, never block stream-reader threads) + Pitfall 3 (bounded wait,
default DENY on timeout) + Pitfall 5 (route bot checks through, don't double-govern).
</objective>

<context>
@.planning/milestones/anonymous/phases/23-stackable-policy-governance-engine/23-RESEARCH.md
@.planning/milestones/anonymous/phases/23-stackable-policy-governance-engine/23-01-SUMMARY.md
@.planning/milestones/anonymous/phases/23-stackable-policy-governance-engine/23-02-SUMMARY.md
@backend/app/services/goal_loop_runner.py
@backend/app/services/execution_service.py
@backend/app/services/policy_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: ASK await helper + decision resolver on PolicyService (reuse the SSE round-trip)</name>
  <files>backend/app/services/policy_service.py</files>
  <action>
    Add a session-keyed ASK round-trip that MIRRORS goal_loop_runner._await_gate (line 487) and
    submit_gate_decision (line 452) — do NOT invent a new transport (23-RESEARCH.md Recommendation 1,
    "Don't Hand-Roll" table).
    - A module-level registry `_POLICY_DECISIONS: dict[str, tuple[str, str|None] | None]` (in-process is
      coherent — gunicorn workers=1).
    - `await_decision(cls, session_id, verdict, *, max_wall_seconds=600) -> str`:
        * broadcast the ASK card: `ProjectSessionManager._broadcast(session_id, "policy_ask",
          {"policy_id": verdict["policy_id"], "kind": verdict["kind"], "reason": verdict["reason"],
           "scope": verdict["scope"]})` (Source: project_session_manager.py:1853; ask_user_question precedent).
        * set `_POLICY_DECISIONS[session_id] = None`; poll every `_PAUSE_POLL_SECONDS`-style interval
          (reuse the same small sleep constant value) until a decision is set or max_wall_seconds elapses.
        * on timeout return "deny" (GOVERNANCE FAIL-SAFE — distinct from the goal-gate's "abort";
          23-RESEARCH.md Pitfall 3 + Production Considerations).
        * broadcast a `policy_ask_resolved` event with the decision; clean up the registry entry; return
          the decision string ("approve"|"deny").
    - `submit_policy_decision(cls, session_id, decision, message=None) -> bool`: set
      `_POLICY_DECISIONS[session_id] = (decision, message)`; return whether a wait was pending.
      (The HTTP entry point for this is added in 23-04, mirroring grd_routes.loop_gate_decision:1441.)
    - Import ProjectSessionManager lazily inside the function (avoid import cycles, as goal_loop_runner does).
  </action>
  <verify>
    `cd backend && uv run pytest tests/test_policy_enforcement.py -k await_decision -v` — await blocks
    until submit_policy_decision resolves; timeout returns "deny" (Level 2: Proxy).
  </verify>
  <done>await_decision/submit_policy_decision exist, broadcast policy_ask/policy_ask_resolved, are bounded by max_wall_seconds, and default to DENY on timeout.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce at the ExecutionService Popen boundary + goal_loop human-gate</name>
  <files>backend/app/services/execution_service.py, backend/app/services/goal_loop_runner.py</files>
  <action>
    ExecutionService (insert AFTER proc_env build at line 742 and cwd at 625, BEFORE Popen at line 767 —
    23-RESEARCH.md Pitfall 1: evaluate before launch, never block the daemon stream-reader threads):
      - Resolve the session id (prefer AGENTED_SESSION_ID from proc_env / the execution's session;
        fall back to execution_id) and team_id if available; build
        action = {"kind": "process_launch", "cmd": cmd, "backend": <backend>, "sandboxed": False,
        "total_cost_usd": <known spend or 0.0>, "tool_calls": <0>}.
      - `verdict = PolicyService.evaluate(session_id=..., team_id=..., action=action)`.
      - decision=="deny" -> raise PolicyDenied(verdict["reason"]) (caller surfaces it; do not Popen).
      - decision=="ask" -> `if PolicyService.await_decision(session_id, verdict) != "approve": raise
        PolicyDenied("operator denied")`.
      - decision=="allow" -> proceed to the existing Popen unchanged.
    goal_loop_runner human-gate hook:
      - At the gated-action point (near the exit-ladder, ~line 749 where _await_gate is already called,
        and the cost check at line 776), call `PolicyService.evaluate(session_id=session_id,
        team_id=getattr(state, "team_id", None), action={"kind": "iteration", "total_cost_usd":
        state.total_cost_usd, "tool_calls": <iteration/tool count>})`.
      - On ASK, route through the EXISTING _await_gate path (set state.gate_decision flow) so the same
        SSE card + submit_gate_decision resolves it — DO NOT add a parallel poll loop here.
      - On DENY, trigger the loop's existing abort/exit path with the verdict reason.
    Consolidation (Pitfall 5 — do not double-govern):
      - Replace/route the inline exit-ladder cost gate (line 776 `max_cost_usd > 0 and total_cost_usd >=`)
        so the cost_budget builtin is the source of truth (pass spend in via the action ctx). Keep the
        loop's exit behavior identical for the DENY case.
      - For bot-security/bot-pr-review: make their inline gate DELEGATE to PolicyService.evaluate (action
        anchored on the SESSION running them), rather than keying any policy on the bot/trigger id. If
        the existing inline check is in audit_service / the trigger dispatch, add the delegation there and
        leave a comment that policy is session-scoped (session-not-bot rule).
  </action>
  <verify>
    `cd backend && uv run pytest tests/test_policy_enforcement.py -v` — DENY prevents Popen (mocked Popen
    asserted not called); ASK blocks then proceeds on approve / raises on deny (Level 2: Proxy).
    `cd backend && uv run pytest tests/test_goal_loop_runner.py -k "gate or budget" -v` (or the existing
    goal-loop suite) — no regression in the human-gate path.
  </verify>
  <done>Policy evaluated before Popen and at the goal-loop gate; DENY blocks; ASK blocks-until-resolved; exit-ladder cost + bot checks route through PolicyService; policies anchored on session id only.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): PolicyDenied raised on DENY; await_decision returns "deny" on timeout.
Level 2 (Proxy): test_policy_enforcement.py — DENY-blocks-Popen + ASK-blocks-until-resolved (mocked
Popen + submit_policy_decision); goal-loop gate regression suite green.
Level 3 (Deferred): real-session ALLOW/DENY/ASK e2e lands in 23-05; house gates there.
</verification>

<success_criteria>
- A DENY verdict prevents subprocess.Popen (asserted not called) and raises PolicyDenied.
- An ASK verdict blocks the launch until submit_policy_decision resolves it; approve proceeds, deny aborts; timeout defaults to DENY.
- goal_loop_runner exit-ladder cost budget + bot-security/bot-pr-review checks route through PolicyService; no policy keys on a bot/trigger id.
- ASK reuses the existing _broadcast + gate-decision shape; no second SSE transport introduced.
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/23-stackable-policy-governance-engine/23-03-SUMMARY.md`
documenting the await_decision/submit_policy_decision signatures, the policy_ask event payload, and the
exact lines where ExecutionService + goal_loop enforce (for 23-04's decision route + 23-05's e2e).
</output>
