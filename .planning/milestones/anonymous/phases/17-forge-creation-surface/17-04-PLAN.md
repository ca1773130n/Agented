---
phase: 17-forge-creation-surface
plan: 04
type: tdd
verification_level: proxy
wave: 3
depends_on: ["17-02"]
files_modified:
  - backend/app/services/forge_materialization_service.py
  - backend/app/services/context_compiler_service.py
  - backend/app/services/context_renderers/claude.py
  - backend/app/services/context_renderers/codex.py
  - backend/app/services/context_renderers/gemini.py
  - backend/app/services/context_renderers/opencode.py
  - backend/tests/test_forge_materialization.py
  - backend/tests/test_prompt_renderer.py
  - backend/tests/services/test_context_compiler_service.py
autonomous: true
verification_level: sanity
hypothesis: "Adding a single subagent WRITE branch to materialize_primitives (→ .claude/agents/<safe>.md) plus a subagent projection in all four renderers (claude native-discovery overlay; codex/gemini/opencode prompt-prefix block) projects a bound subagent deterministically across every backend, mirroring how those renderers already degrade for kinds the backend lacks natively."
predicted_outcome: "tests/test_forge_materialization.py, tests/test_prompt_renderer.py, and tests/services/test_context_compiler_service.py pass: a bound subagent materializes to a byte-stable .claude/agents/<name>.md with agented-kind/agented-asset-id frontmatter, appears in the compiled ContextBundle, and each of the 4 renderers emits its golden projection (claude does not inline the body; codex/gemini/opencode emit a named system-prompt block)."
eval_metrics:
  primary: "4-backend subagent renderer golden parity"
  secondary: ["byte-stable .claude/agents materialization", "subagent in ContextBundle"]
  baseline: "subagent not renderable (17-02 left a TODO write branch)"
  target: "all 4 backends project subagent; golden + determinism pass"
must_haves:
  truths:
    - "materialize_primitives(kinds=['subagent']) writes .claude/agents/<safe-name>.md with frontmatter agented-kind: subagent + agented-asset-id; manifest tracks it under paths_by_kind.subagent (success criterion #1, materialization half)"
    - "A second identical materialize run produces byte-identical output (determinism / no manifest churn)"
    - "All four renderers (claude/codex/gemini/opencode) project a bound subagent per their golden fixture; claude uses native agents/ discovery (does NOT duplicate body inline), codex/gemini/opencode emit a named prompt-prefix block (success criterion #1, renderer half — all-four-backends house rule)"
    - "ContextCompilerService includes bound subagents in the compiled ContextBundle"
  artifacts:
    - path: "backend/tests/test_forge_materialization.py"
      provides: "golden .claude/agents/ assertion + determinism"
      contains: ".claude/agents"
    - path: "backend/tests/test_prompt_renderer.py"
      provides: "4-backend subagent golden"
    - path: "backend/app/services/context_renderers/claude.py"
      provides: "subagent projection (native overlay)"
  key_links:
    - from: "backend/app/services/forge_materialization_service.py"
      to: ".claude/agents/<name>.md"
      via: "subagent write branch + WrittenFile append"
      pattern: "agents/|WrittenFile.*subagent"
    - from: "backend/app/services/context_compiler_service.py"
      to: "backend/app/db/subagents.py::get_subagent"
      via: "compiler resolves bound subagents"
      pattern: "get_subagent|subagent"
---

<objective>
Complete the subagent materialization + rendering half of success criterion #1:
(a) add the WRITE branch in `materialize_primitives` that emits
`.claude/agents/<safe>.md` (frontmatter + body, Agented markers), and (b) add
subagent projection to all four context renderers. claude uses native
sub-agent discovery (an `agents/` overlay entry, no inline body duplication);
codex/gemini/opencode have no native sub-agent concept and degrade to a named
prompt-prefix block, consistent with how those renderers already handle kinds
the backend lacks (RESEARCH.md §"Per-backend renderers").

Purpose: all-four-backends parity is a hard house rule. Depends on 17-02 (the
subagents table, `_get_asset` dispatch, and the WRITE-branch TODO marker).

Output: write branch, 4 renderer branches, compiler hookup, golden tests in the
existing golden harness (`test_forge_materialization.py`) + `test_prompt_renderer.py`
+ `test_context_compiler_service.py`.
Research basis: 17-RESEARCH.md §17-04.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/milestones/anonymous/phases/17-forge-creation-surface/17-RESEARCH.md
@backend/app/services/forge_materialization_service.py
@backend/app/services/context_compiler_service.py
@backend/app/services/context_renderers/base.py
@backend/app/services/context_renderers/claude.py
@backend/tests/test_forge_materialization.py
@.planning/milestones/anonymous/phases/17-forge-creation-surface/17-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED+GREEN — subagent write branch + golden materialization</name>
  <files>backend/app/services/forge_materialization_service.py, backend/tests/test_forge_materialization.py</files>
  <action>
    RED: extend `test_forge_materialization.py` (existing golden harness) with a
    test that seeds a subagent (create_subagent with a known name + content) and
    a project binding for kind 'subagent', calls `materialize_primitives(project,
    ['subagent'], workspace_path)`, and asserts:
      - file `.claude/agents/<safe-name>.md` exists,
      - frontmatter contains `agented-kind: subagent`, `agented-asset-id: <id>`,
        `agented-source: forge`, plus `name` and `description` (per
        HarnessLoaderService._import_agents frontmatter contract),
      - body content present,
      - `result.rel_paths()` (or equivalent) includes the path,
      - manifest tracks it under `paths_by_kind.subagent`,
      - a SECOND identical run yields byte-identical file + no manifest churn.
    Run → FAIL (no write branch; 17-02 left a TODO).

    GREEN: in `materialize_primitives`, add the `subagent` write branch mirroring
    the `command` branch: compute safe name, write
    `.claude/agents/<safe>.md` = `_frontmatter({...markers...}) + content`,
    `result.written.append(WrittenFile(rel, "subagent", asset_id))`. Ensure
    `_finalize_manifest` reconciles the `subagent` bucket like commands (per-asset
    files, not in `_NEVER_DELETE`). Resolve the 17-02 TODO in `_get_asset`.
  </action>
  <verify>
    cd backend &amp;&amp; uv run pytest tests/test_forge_materialization.py -v
    (Level 2: Proxy — golden + determinism)
  </verify>
  <done>Golden .claude/agents/ test passes incl. byte-stable second run; manifest tracks subagent bucket.</done>
  <commit>feat(17-04): subagent materialization to .claude/agents + golden test</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED+GREEN — 4-backend renderer subagent projection</name>
  <files>backend/app/services/context_renderers/claude.py, backend/app/services/context_renderers/codex.py, backend/app/services/context_renderers/gemini.py, backend/app/services/context_renderers/opencode.py, backend/tests/test_prompt_renderer.py</files>
  <action>
    RED: extend `test_prompt_renderer.py` with per-backend golden assertions for a
    bound subagent (one test per backend, or a parametrized matrix over
    claude/codex/gemini/opencode):
      - claude: subagent surfaces via native agents/ discovery; the renderer does
        NOT duplicate the subagent body inline in the system prompt.
      - codex/gemini/opencode: renderer emits a named prompt-prefix block
        containing the subagent name + body (the degrade path, consistent with
        existing prompt-prepend handling in context_compiler_service.py:7-9).
      - determinism: same input → same output bytes per backend.
    Run → FAIL.

    GREEN: add a `subagent` branch to each renderer. Wherever a renderer
    enumerates kinds, add `"subagent"`. Keep the claude vs non-claude asymmetry
    explicit and commented (RESEARCH.md documents this asymmetry).
  </action>
  <verify>
    cd backend &amp;&amp; uv run pytest tests/test_prompt_renderer.py -v
    (Level 2: Proxy — 4-backend golden parity)
  </verify>
  <done>All four renderers project a bound subagent per golden; claude does not inline body; codex/gemini/opencode emit named block; deterministic.</done>
  <commit>feat(17-04): 4-backend subagent renderer projection + goldens</commit>
</task>

<task type="auto">
  <name>Task 3: compiler includes bound subagents in ContextBundle</name>
  <files>backend/app/services/context_compiler_service.py, backend/tests/services/test_context_compiler_service.py</files>
  <action>
    Ensure `ContextCompilerService.compile` resolves bound subagents
    (import `get_subagent`) and includes their name/body in the compiled
    ContextBundle so the renderers above receive them. Add a test in
    `test_context_compiler_service.py`: seed a subagent + binding, compile, assert
    the subagent name/body appears in the bundle and that `get_subagent` is the
    resolution path. If the compiler already iterates a generic bound-asset list,
    confirm 'subagent' flows through and add the test rather than new branching.
  </action>
  <verify>
    cd backend &amp;&amp; uv run pytest tests/services/test_context_compiler_service.py -v
    (Level 2: Proxy)
  </verify>
  <done>Bound subagent appears in compiled ContextBundle; test passes.</done>
  <commit>feat(17-04): compiler includes bound subagents in ContextBundle</commit>
</task>

</tasks>

<verification>
Level 1 (Sanity): `ruff check` on renderers + materialization service clean.
Level 2 (Proxy):
- `cd backend && uv run pytest tests/test_forge_materialization.py tests/test_prompt_renderer.py tests/services/test_context_compiler_service.py -v` all pass.
</verification>

<success_criteria>
Bound subagent materializes to a byte-stable `.claude/agents/<name>.md` tracked
in the manifest, appears in the compiled ContextBundle, and is projected by all
four renderers (claude native; codex/gemini/opencode prompt-prefix), with golden
+ determinism assertions. (Materialization + renderer half of success criterion #1.)
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/17-forge-creation-surface/17-04-SUMMARY.md`.
</output>
