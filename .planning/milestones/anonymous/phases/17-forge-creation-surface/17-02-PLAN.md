---
phase: 17-forge-creation-surface
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - backend/app/db/migrations/v07_features.py
  - backend/app/db/ids.py
  - backend/app/db/subagents.py
  - backend/app/db/project_forge_bindings.py
  - backend/app/db/__init__.py
  - backend/app_litestar/routes/project_subagents.py
  - backend/app_litestar/main.py
  - backend/app/services/forge_materialization_service.py
  - backend/tests/test_subagents_db.py
autonomous: true
verification_level: sanity
hypothesis: "Mirroring the rule/command forge-primitive pattern for a brand-new subagents table (subag- prefix, CRUD module, route module, VALID_KINDS entry, _get_asset branch) registers 'subagent' as a first-class forge kind without colliding with the legacy agents table."
predicted_outcome: "tests/test_subagents_db.py passes: create returns a 'subag-' id, get/list/delete round-trip, the name UNIQUE constraint raises on duplicate, and 'subagent' is present in BOTH VALID_KINDS and VALID_FORGE_BINDING_KINDS; the legacy agents table is untouched."
eval_metrics:
  primary: "subagent CRUD + dual kind-registry membership"
  secondary: ["subag- prefix", "name UNIQUE enforced", "legacy agents table distinct"]
  baseline: "no subagent kind exists"
  target: "subagent is a valid forge kind end-to-end at DB+registry+_get_asset layer"
must_haves:
  truths:
    - "subagents table exists after init (migration 155); rows use a 'subag-' id prefix (success criterion #1, DB half)"
    - "create/get/list/delete round-trip works; duplicate name raises (UNIQUE)"
    - "'subagent' is in both VALID_KINDS and VALID_FORGE_BINDING_KINDS (registry consistency — Risk 2 / EVAL S3)"
    - "forge_materialization_service._get_asset resolves a 'subagent' asset by its str id (the materialize WRITE branch is 17-04)"
    - "The legacy agents table / create_agent is NOT modified or reused — subagents is a separate table"
  artifacts:
    - path: "backend/app/db/subagents.py"
      provides: "subagent CRUD"
      exports: ["create_subagent", "get_subagent", "list_subagents", "update_subagent", "delete_subagent"]
    - path: "backend/app_litestar/routes/project_subagents.py"
      provides: "subagent CRUD routes mirroring rules_plugins_hooks_commands"
    - path: "backend/tests/test_subagents_db.py"
      provides: "DB-layer + registry tests"
    - path: "backend/app/db/migrations/v07_features.py"
      provides: "migration 155 subagents table"
      contains: "subagents"
  key_links:
    - from: "backend/app/db/__init__.py"
      to: "backend/app/db/subagents.py"
      via: "re-export of subagent CRUD"
      pattern: "from .subagents import"
    - from: "backend/app/db/project_forge_bindings.py::VALID_KINDS"
      to: "subagent kind"
      via: "set membership"
      pattern: "\"subagent\""
    - from: "backend/app/services/forge_materialization_service.py::_get_asset"
      to: "backend/app/db/subagents.py::get_subagent"
      via: "subagent dispatch branch"
      pattern: "get_subagent|subagent"
---

<objective>
Register `subagent` as a new forge primitive kind by mirroring the
rule/hook/command pattern exactly: a new `subagents` table (migration 155,
`subag-` id prefix), a CRUD DB module, a Litestar CRUD route module, a
`VALID_KINDS` entry (re-exported as `VALID_FORGE_BINDING_KINDS`), a
`db/__init__` re-export, and a `_get_asset` dispatch branch in the
materialization service.

Purpose: this is the DB+registry half of success criterion #1. The
materialization WRITE branch and the 4-backend renderers are 17-04 (which
depends on this); the atomic create endpoint is 17-05.

CRITICAL PITFALL (HIGH, RESEARCH.md §17-02 + Pitfall 1): the legacy `agents`
table + `create_agent` (used by `HarnessLoaderService._import_agents`) is NOT
this primitive. `subagents` is a brand-new, separate table. Do not reuse,
rename, or cross-wire them — confusing them corrupts success criterion #1.

Output: migration 155, `subagents.py`, `project_subagents.py`, id helpers,
registry + re-export edits, `_get_asset` branch, DB tests.
Research basis: 17-RESEARCH.md §17-02.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/milestones/anonymous/phases/17-forge-creation-surface/17-RESEARCH.md
@backend/app/db/skills.py
@backend/app_litestar/routes/rules_plugins_hooks_commands.py
@backend/app/db/project_forge_bindings.py
@backend/app/db/ids.py
@backend/app/services/forge_materialization_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: migration 155 + subagents CRUD module + id helper</name>
  <files>backend/app/db/migrations/v07_features.py, backend/app/db/ids.py, backend/app/db/subagents.py, backend/app/db/__init__.py</files>
  <action>
    1. `ids.py`: add `SUBAGENT_ID_PREFIX = "subag-"` and `generate_subagent_id()`
       mirroring `generate_team_id()` (ids.py:202) → `generate_id(SUBAGENT_ID_PREFIX)`.
    2. Migration: add `_migrate_155_subagents(conn)` and append
       `(155, "subagents", _migrate_155_subagents)` to `V07_MIGRATIONS`
       (~L1094; numerically keyed; highest existing is 154; 156 is owned by
       17-03 — claim 155 here and record it in SUMMARY). Schema (idempotent
       CREATE IF NOT EXISTS), columns mirroring commands/rules shape:
       `subagents(id TEXT PRIMARY KEY, name TEXT UNIQUE NOT NULL,
        description TEXT, content TEXT NOT NULL, enabled INTEGER NOT NULL DEFAULT 1,
        project_id TEXT, source_path TEXT, created_at TEXT NOT NULL,
        updated_at TEXT NOT NULL)`. `content` holds the FULL `.claude/agents/*.md`
        body including frontmatter.
    3. `subagents.py`: CRUD mirroring `skills.py`, using `get_connection()`:
       `create_subagent(name, content, description=None, enabled=1,
        project_id=None, source_path=None) -> dict` (id via generate_subagent_id);
       `get_subagent(subagent_id)`, `list_subagents(project_id=None)`,
       `update_subagent(subagent_id, **fields)`, `delete_subagent(subagent_id)`.
    4. Re-export from `db/__init__.py` (forge block ~L556 or skills block ~L737).
    DO NOT touch the legacy `agents` table or `create_agent`.
  </action>
  <verify>
    cd backend &amp;&amp; uv run python -c "import tempfile,os; os.environ['DB_PATH']=tempfile.mktemp(suffix='.db'); import app.db.schema; from app.db.database import get_connection; c=get_connection().__enter__(); t={r[0] for r in c.execute(\"SELECT name FROM sqlite_master WHERE type='table'\")}; assert 'subagents' in t and 'agents' in t; print('OK')"
    (Level 1: Sanity — both subagents AND legacy agents exist, distinct)
  </verify>
  <done>subagents table created at init; CRUD module importable + re-exported; subag- id helper added; legacy agents table untouched.</done>
  <commit>feat(17-02): subagents table (migration 155) + CRUD + subag- id</commit>
</task>

<task type="auto">
  <name>Task 2: VALID_KINDS entry, route module, _get_asset branch</name>
  <files>backend/app/db/project_forge_bindings.py, backend/app_litestar/routes/project_subagents.py, backend/app_litestar/main.py, backend/app/services/forge_materialization_service.py</files>
  <action>
    1. `project_forge_bindings.py:24`: add `"subagent"` to `VALID_KINDS`
       (auto-flows to `VALID_FORGE_BINDING_KINDS` re-export).
    2. New `backend/app_litestar/routes/project_subagents.py` mirroring
       `rules_plugins_hooks_commands.py`: `@post("/")` reads `data: dict`, calls
       `create_subagent(...)`, returns `{"message": ..., "subagent": get_subagent(id)}`;
       plus get/list/delete handlers; build a `Router(path=..., route_handlers=[...])`.
       Register the router in the app factory (`main.py`) next to where the
       rules/commands routers are registered.
    3. `forge_materialization_service.py::_get_asset`: add a `subagent` branch.
       Per the existing comment ("rule/hook/command take INT ids; mcp_server takes
       STR"), subagent ids are STR (`subag-…`) — resolve via `get_subagent(asset_id)`.
       This is the READ/dispatch half ONLY; the WRITE branch (writing
       `.claude/agents/<name>.md`) is 17-04 — leave a clear TODO marker there so
       17-04 picks it up.
  </action>
  <verify>
    cd backend &amp;&amp; uv run python -c "from app.db import VALID_FORGE_BINDING_KINDS; from app.db.project_forge_bindings import VALID_KINDS; assert 'subagent' in VALID_KINDS and 'subagent' in VALID_FORGE_BINDING_KINDS; import os; os.environ['AGENTED_LITESTAR_SKIP_STARTUP']='1'; from app_litestar.main import create_app; app=create_app(); print('OK')"
    (Level 1: Sanity — registries consistent + app builds with new router)
  </verify>
  <done>'subagent' in both registries; subagent router registered and app builds; _get_asset resolves subagent by str id.</done>
  <commit>feat(17-02): subagent VALID_KINDS + routes + _get_asset dispatch</commit>
</task>

<task type="auto">
  <name>Task 3: subagent DB + registry test</name>
  <files>backend/tests/test_subagents_db.py</files>
  <action>
    New test module with `isolated_db`:
    - `test_create_returns_subag_prefix`: create_subagent → id startswith "subag-".
    - `test_crud_round_trip`: create → get → list (contains) → update → delete →
      get returns None.
    - `test_name_unique`: creating two subagents with the same name raises
      (IntegrityError / sqlite3.IntegrityError).
    - `test_subagent_in_both_kind_registries`: assert "subagent" in VALID_KINDS
      and in VALID_FORGE_BINDING_KINDS.
    - `test_legacy_agents_table_distinct`: assert get_subagent of a fresh id does
      not touch / is independent of the legacy agents table (sanity: both tables
      present, separate row sets).
  </action>
  <verify>
    cd backend &amp;&amp; uv run pytest tests/test_subagents_db.py -v
    (Level 2: Proxy)
  </verify>
  <done>All subagent DB/registry tests pass.</done>
  <commit>test(17-02): subagent CRUD + dual kind-registry membership</commit>
</task>

</tasks>

<verification>
Level 1 (Sanity): subagents table at init; both registries contain 'subagent'; app builds with new router; `ruff check` on new modules clean.
Level 2 (Proxy): `cd backend && uv run pytest tests/test_subagents_db.py -v` all pass.
</verification>

<success_criteria>
subagents table (subag- prefix) + CRUD + route module exist; 'subagent' is a
member of both kind registries; _get_asset resolves a subagent; the legacy
agents table is untouched. (DB+registry half of phase success criterion #1.)
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/17-forge-creation-surface/17-02-SUMMARY.md`.
Record migration number used (155) and the _get_asset TODO left for 17-04.
</output>
