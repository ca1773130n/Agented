---
phase: 17-forge-creation-surface
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/db/migrations/v07_features.py
  - backend/app/db/forge_bundles.py
  - backend/app/db/__init__.py
  - backend/app_litestar/routes/project_forge_bindings.py
  - backend/tests/test_forge_bundles_db.py
autonomous: true
verification_level: sanity
hypothesis: "A new cross-kind forge_bundles/forge_bundle_items pair plus a conn-accepting internal _add_binding lets a single bundle-bind endpoint bind every item of any kind in one connection atomically, while leaving the legacy skill_sets table byte-for-byte unchanged."
predicted_outcome: "tests/test_forge_bundles_db.py passes: a bundle holding items of >=2 distinct kinds round-trips ordered by position, delete cascades to items, and a sqlite_master assertion shows skill_sets DDL identical to its migration-87 definition."
eval_metrics:
  primary: "cross-kind bundle round-trip + ordered items"
  secondary: ["skill_sets DDL unchanged", "delete cascades items"]
  baseline: "no bundle tables exist"
  target: "bundle binds every item cross-kind in one call; skill_sets untouched"
must_haves:
  truths:
    - "forge_bundles and forge_bundle_items tables exist after schema init (migration 156) (success criterion #4)"
    - "A bundle holding items of at least two distinct kinds round-trips, items returned ordered by position"
    - "Deleting a bundle cascades to its forge_bundle_items rows"
    - "skill_sets DDL is byte-for-byte unchanged vs its migration-87 definition (assert via sqlite_master)"
    - "An internal conn-accepting _add_binding exists so a bundle-bind loop binds every item in one get_connection() block (foundation for the 17-05 bundle-bind route)"
  artifacts:
    - path: "backend/app/db/forge_bundles.py"
      provides: "forge_bundles/forge_bundle_items CRUD + _add_binding(conn,...) helper"
      exports: ["create_forge_bundle", "get_forge_bundle", "list_forge_bundle_items", "add_bundle_item", "delete_forge_bundle"]
    - path: "backend/tests/test_forge_bundles_db.py"
      provides: "cross-kind bundle CRUD + skill_sets-unchanged guard"
    - path: "backend/app/db/migrations/v07_features.py"
      provides: "migration 156 creating bundle tables"
      contains: "forge_bundle_items"
  key_links:
    - from: "backend/app/db/__init__.py"
      to: "backend/app/db/forge_bundles.py"
      via: "re-export of bundle CRUD"
      pattern: "from .forge_bundles import"
    - from: "backend/app/db/forge_bundles.py::_add_binding"
      to: "backend/app/db/project_forge_bindings.py"
      via: "shared upsert logic (conn-accepting variant)"
      pattern: "INSERT.*project_forge_bindings|ON CONFLICT"
---

<objective>
Add cross-kind bundles to the forge: a `forge_bundles` table (a named,
scope-tagged group) and a `forge_bundle_items(bundle_id, kind, asset_id,
position)` join table that can hold primitives of ANY kind in one bundle.
Provide the DB CRUD and a conn-accepting internal `_add_binding(conn, ...)`
so a future bundle-bind endpoint (built in 17-05) binds every item in a single
connection — true single-call atomicity.

Purpose: skill_sets (migration 87) is skills-only legacy; the forge needs a
cross-kind grouping primitive. This is success criterion #4. Independent of the
subagent work, so it runs in Wave 1 alongside 17-01.

Output: migration 156, `backend/app/db/forge_bundles.py`, `__init__` re-exports,
a stub bundle-bind route wiring, and a DB test that also pins skill_sets.
Research basis: 17-RESEARCH.md §17-03 + Open Question 3 (use the conn-accepting
internal variant for true atomicity).
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/milestones/anonymous/phases/17-forge-creation-surface/17-RESEARCH.md
@backend/app/db/skill_sets.py
@backend/app/db/project_forge_bindings.py
@backend/app/db/migrations/v07_features.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: migration 156 — forge_bundles + forge_bundle_items</name>
  <files>backend/app/db/migrations/v07_features.py</files>
  <action>
    Add `_migrate_156_forge_bundles(conn)` and append
    `(156, "forge_bundles", _migrate_156_forge_bundles)` to `V07_MIGRATIONS`
    (tuples are numerically keyed, not file-ordered; highest existing is 154 —
    confirm and use the next free number; 155 is owned by 17-02 subagents).
    NOTE: 17-02 and 17-03 both add migrations; coordinate by number — this plan
    owns 156, 17-02 owns 155. If executed out of order, pick the next free int
    and record it in SUMMARY.

    Schema (mirror existing migration DDL style — idempotent CREATE IF NOT EXISTS):
    - `forge_bundles(id TEXT PRIMARY KEY, name TEXT UNIQUE NOT NULL,
       description TEXT, scope TEXT NOT NULL DEFAULT 'project',
       created_at TEXT NOT NULL)`  -- id uses a 'bundle-' prefix.
    - `forge_bundle_items(bundle_id TEXT NOT NULL, kind TEXT NOT NULL,
       asset_id TEXT NOT NULL, position INTEGER NOT NULL DEFAULT 0,
       PRIMARY KEY (bundle_id, kind, asset_id),
       FOREIGN KEY (bundle_id) REFERENCES forge_bundles(id) ON DELETE CASCADE)`.
    Add an index on `forge_bundle_items(bundle_id, position)` for ordered reads.
    Do NOT touch skill_sets / skill_set_items.
  </action>
  <verify>
    cd backend &amp;&amp; uv run python -c "import tempfile,os; os.environ['DB_PATH']=tempfile.mktemp(suffix='.db'); import app.db.schema as s; from app.db.database import get_connection; c=get_connection().__enter__(); t={r[0] for r in c.execute(\"SELECT name FROM sqlite_master WHERE type='table'\")}; assert {'forge_bundles','forge_bundle_items'}&lt;=t; print('OK')"
    (Level 1: Sanity)
  </verify>
  <done>Both tables created at schema init; migration registered with a unique number recorded in SUMMARY.</done>
  <commit>feat(17-03): migration 156 forge_bundles + forge_bundle_items</commit>
</task>

<task type="auto">
  <name>Task 2: forge_bundles DB module + conn-accepting _add_binding</name>
  <files>backend/app/db/forge_bundles.py, backend/app/db/__init__.py</files>
  <action>
    Create `backend/app/db/forge_bundles.py` mirroring the CRUD shape of
    `skill_sets.py` / command CRUD, using `get_connection()`:
    - `create_forge_bundle(name, description=None, scope="project") -> dict`
      (id via `generate_id("bundle-")`; UNIQUE name).
    - `get_forge_bundle(bundle_id)`, `get_forge_bundle_by_name(name)`,
      `list_forge_bundles(scope=None)`.
    - `add_bundle_item(bundle_id, kind, asset_id, position=None)` — validate
      `kind in VALID_KINDS` (import from project_forge_bindings); auto-assign
      position = current max+1 when None.
    - `list_forge_bundle_items(bundle_id)` ordered by position.
    - `delete_forge_bundle(bundle_id)` (relies on ON DELETE CASCADE; if PRAGMA
      foreign_keys is not enabled in this connection, delete items explicitly
      too — check the codebase's get_connection PRAGMA and match it).

    Add a conn-accepting internal binding helper for atomic bundle-bind:
    `_add_binding(conn, project_id, kind, asset_id, role="user", enabled=1,
      position=0, source_scope="project", source_shared_binding_id=None,
      fingerprint=None, conflict_policy="local_wins")` — the SAME upsert SQL as
      `project_forge_bindings.add_binding` but taking an existing `conn` (no
      open/commit of its own). Refactor `add_binding` to delegate to this helper
      inside its own `get_connection()` block IF that is a clean refactor;
      otherwise duplicate the SQL and add a comment that the two must stay in
      sync (RESEARCH.md Open Question 3). Carry all four provenance columns
      (consistency with 17-01).

    Re-export the public bundle CRUD from `backend/app/db/__init__.py`
    (forge block, ~L556).
  </action>
  <verify>
    cd backend &amp;&amp; uv run python -c "import app.db.forge_bundles; from app.db import create_forge_bundle; print('OK')"
    (Level 1: Sanity)
  </verify>
  <done>forge_bundles module importable; CRUD + _add_binding(conn,...) present; re-exported through db/__init__.</done>
  <commit>feat(17-03): forge_bundles CRUD + conn-accepting _add_binding</commit>
</task>

<task type="auto">
  <name>Task 3: cross-kind bundle DB test + skill_sets-unchanged guard</name>
  <files>backend/tests/test_forge_bundles_db.py</files>
  <action>
    New test module with `isolated_db`:
    - `test_cross_kind_bundle_round_trip`: create a bundle, add items of at least
      two distinct kinds (e.g. "skill" + "command", or "rule" + "subagent" if
      17-02 has merged), assert `list_forge_bundle_items` returns them ordered by
      position.
    - `test_delete_bundle_cascades_items`: create bundle + items, delete bundle,
      assert no orphan rows remain in forge_bundle_items.
    - `test_skill_sets_schema_unchanged`: SELECT the `skill_sets` CREATE statement
      from `sqlite_master` and assert it matches the exact DDL produced by
      migration 87 (read the expected DDL from the migration source or pin the
      normalized string). This is the byte-for-byte guard for success criterion #4.
  </action>
  <verify>
    cd backend &amp;&amp; uv run pytest tests/test_forge_bundles_db.py -v
    (Level 2: Proxy)
  </verify>
  <done>All three tests pass; skill_sets DDL assertion green.</done>
  <commit>test(17-03): cross-kind bundle CRUD + skill_sets-unchanged guard</commit>
</task>

</tasks>

<verification>
Level 1 (Sanity): tables exist at init; module imports; `ruff check app/db/forge_bundles.py` clean.
Level 2 (Proxy): `cd backend && uv run pytest tests/test_forge_bundles_db.py -v` all pass.
</verification>

<success_criteria>
forge_bundles + forge_bundle_items exist; a bundle binds/holds items cross-kind
in one structure with ordered items and cascade delete; an internal
conn-accepting _add_binding exists for atomic bundle-bind; skill_sets DDL is
unchanged. (Maps to phase success criterion #4; bundle-bind ROUTE is 17-05.)
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/17-forge-creation-surface/17-03-SUMMARY.md`.
Record the migration number actually used (155/156 coordination with 17-02).
</output>
