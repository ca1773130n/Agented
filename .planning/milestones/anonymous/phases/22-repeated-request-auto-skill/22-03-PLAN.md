---
phase: 22-repeated-request-auto-skill
plan: 03
type: standard
wave: 2
depends_on: ["22-01", "22-02"]
files_modified:
  - backend/app/services/repeated_request_detector.py
  - backend/tests/test_repeated_request_detector.py
  - backend/tests/fixtures/repeated_request_transcripts.py
autonomous: true
verification_level: proxy
hypothesis: "A new register_session_handler callback that extracts user-turn text via _FETCHERS, embeds it, and cosine-matches (≥0.83) against existing signal embeddings will coalesce paraphrased recurrences into one growing signal across all five session kinds, while emit_session_complete's per-handler try/except keeps a detector crash from blocking the bus."
predicted_outcome: "Replaying 3 fixture transcripts of the same paraphrased request produces ONE signal with occurrence_count=3 (cosine-matched); two unrelated requests stay 2 separate signals; with embed_text monkeypatched to None the detector falls back to exact-hash match without crashing; a monkeypatched-to-raise detect_for_session does not propagate out of emit_session_complete."

eval_metrics:
  primary: "paraphrase grouping precision on labeled fixtures"
  secondary: ["embed-disabled fallback correctness", "non-blocking proof"]
  baseline: "no detector exists (0 signal coverage)"
  target: "paraphrase group → 1 signal/occ=3; unrelated → 2 signals; cosine ≥ 0.83"

must_haves:
  truths:
    - "detect_for_session(session_kind, session_id, project_id) extracts user-request turns via _FETCHERS for all 5 kinds"
    - "request text is embedded via embed_text and matched against existing signal embeddings with cosine_similarity_batch; a match (≥0.83) upserts onto the matched signal, else a new signal is created"
    - "when embed_text returns None, the detector falls back to exact normalize_request_hash match (no crash, paraphrases stay separate, verbatim repeats coalesce)"
    - "the detector is registered as a NEW register_session_handler callback (mirroring harness_failure_annotator.py:14-16) — on_session_complete is NOT edited"
    - "a detector exception does not escape emit_session_complete (verified at the bus level), and other registered handlers still run"
  artifacts:
    - path: "backend/app/services/repeated_request_detector.py"
      provides: "detect_for_session + module-import register_session_handler call"
      exports: ["detect_for_session"]
    - path: "backend/tests/fixtures/repeated_request_transcripts.py"
      provides: "≥3 paraphrase transcripts + 2 unrelated, for P1/A1"
    - path: "backend/tests/test_repeated_request_detector.py"
      provides: "cosine (P1), embed_disabled (A1), non_blocking (P4) tests"
  key_links:
    - from: "backend/app/services/repeated_request_detector.py"
      to: "backend/app/services/execution_events.py"
      via: "register_session_handler(detect callback) at module import"
      pattern: "register_session_handler"
    - from: "backend/app/services/repeated_request_detector.py"
      to: "backend/app/services/harness_failure_annotator.py"
      via: "import _FETCHERS for per-kind user-turn extraction"
      pattern: "_FETCHERS"
    - from: "backend/app/services/repeated_request_detector.py"
      to: "backend/app/db/repeated_request_signals.py"
      via: "upsert_signal / list_signals / normalize_request_hash"
      pattern: "upsert_signal|list_signals|normalize_request_hash"
---

<objective>
The detection handler (REQ-23): a NEW handler on the session-completion bus that,
for every completed session of all five kinds, extracts the user-request turns,
embeds them, cosine-matches against existing signals (≥0.83), and UPSERTs into the
22-01 store — accumulating recurrence evidence. Verification-record awareness and
the tesserae cross-check feed `verified_success_count` and dedup downstream.

CRITICAL (22-RESEARCH.md corrections, honored exactly):
- Register a THIRD `register_session_handler(...)` callback at module import,
  mirroring `harness_failure_annotator.py:14-16`. Do NOT edit `on_session_complete`.
- Non-blocking is ALREADY guaranteed by `emit_session_complete`'s per-handler
  try/except (execution_events.py:60). EVAL P4 is re-scoped: assert at the BUS level
  (monkeypatch the detector to raise, call `emit_session_complete`, confirm it returns
  and other handlers still run) — not inside the takeaway extractor.
- 0.83 is a NEW Phase-22 constant: `_COSINE_MATCH_THRESHOLD = 0.83`. Comment it as
  such; do not assume it exists elsewhere.
- Embed-disabled (A1): when `embed_text` returns None, fall back to exact
  `normalize_request_hash` match.

Purpose: Turn raw session completions into a growing, deduplicated signal stream.
Output: One detector module (with self-registration), fixtures, three test groups.
Research basis: 22-RESEARCH.md REQ-23; reuse _FETCHERS, embed_text,
cosine_similarity_batch (don't hand-roll). EVAL P1, P4, A1.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/milestones/anonymous/phases/22-repeated-request-auto-skill/22-RESEARCH.md
@.planning/milestones/anonymous/phases/22-repeated-request-auto-skill/22-EVAL.md
@backend/app/services/execution_events.py
@backend/app/services/harness_failure_annotator.py
@backend/app/services/harness_takeaway_extractor.py
@backend/app/services/embedding_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: detect_for_session + bus self-registration</name>
  <files>backend/app/services/repeated_request_detector.py</files>
  <action>
    Create `repeated_request_detector.py`.

    `detect_for_session(session_kind, session_id, project_id) -> None`:
      1. `fetcher = _FETCHERS.get(session_kind)`; if None, return (guard idiom from
         harness_takeaway_extractor.py:1064). Import `_FETCHERS` from
         `harness_failure_annotator`.
      2. `payload = fetcher(session_id)`; if None, return. Extract user-request turn
         text from the SessionPayload (reuse whatever user-turn accessor the takeaway
         extractor uses on the same payload type — read extract_for_session to match it;
         do not invent a new field). Concatenate/strip into a single request string.
      3. `emb = embed_text(text)`.
         - If emb is not None: load existing signals for this project via
           `list_signals(project_id=project_id)`, build the candidate embedding list
           (skip rows with NULL embedding), and call `cosine_similarity_batch(emb,
           candidates)`. If the best score ≥ `_COSINE_MATCH_THRESHOLD` (0.83), UPSERT
           onto that matched signal's request_hash (passing the new session_id);
           otherwise UPSERT a brand-new signal keyed by `normalize_request_hash(text)`.
         - If emb is None (A1 fallback): UPSERT keyed by `normalize_request_hash(text)`
           directly (exact match coalesces verbatim repeats only).
      4. Pass the embedding through to `upsert_signal` so new rows store it.

    Define the registration callback `on_session_complete_detect(session_kind, session_id,
    project_id, status, output)` matching the SessionCallback signature; it calls
    `detect_for_session(session_kind, session_id, project_id)`. At module import, run
    `register_session_handler(on_session_complete_detect)` — mirror
    harness_failure_annotator.py:14-16. (Name the callback distinctly so it does not
    collide with the annotator/extractor `on_session_complete`.)

    Add `_COSINE_MATCH_THRESHOLD = 0.83` as a module constant with a Phase-22 comment.
    Wrap the body of detect_for_session in defensive logging is NOT required for
    non-blocking (the bus already isolates handlers) — keep it simple.
  </action>
  <verify>
    cd /Users/neo/Developer/Projects/Agented/backend && uv run python -c "from app.services.repeated_request_detector import detect_for_session; import app.services.repeated_request_detector as d; print(d._COSINE_MATCH_THRESHOLD); print('OK')" && uv run ruff check app/services/repeated_request_detector.py
    (Level 1: Sanity — S1/S2)
  </verify>
  <done>Module imports; constant is 0.83; detect_for_session callable; a register_session_handler call runs at import.</done>
</task>

<task type="auto">
  <name>Task 2: Fixtures + cosine/embed-disabled/non-blocking tests</name>
  <files>backend/tests/fixtures/repeated_request_transcripts.py, backend/tests/test_repeated_request_detector.py</files>
  <action>
    `tests/fixtures/repeated_request_transcripts.py`: provide (a) three paraphrased
    variants of ONE request (e.g. "add a dark mode toggle" / "can you add dark mode" /
    "please implement a dark-mode switch") and (b) two clearly unrelated requests. Shape
    each as a minimal SessionPayload-compatible structure so a fetcher monkeypatch can
    return them, or as raw user-turn strings the test feeds directly — match whatever
    detect_for_session consumes.

    `tests/test_repeated_request_detector.py` (isolated_db fixture):
      - `cosine` test (P1): drive the three paraphrases through detect_for_session (real
        embedding backend) → assert exactly ONE signal with occurrence_count=3; then the
        two unrelated → assert 2 additional distinct signals. Zero mis-groupings.
        Skip-guard if embed_text returns None in the env (xfail/skip with a clear reason)
        so the suite stays green where sentence-transformers is absent.
      - `embed_disabled` test (A1): monkeypatch
        `repeated_request_detector.embed_text` to return None; feed two DIFFERENTLY-worded
        paraphrases → assert they become 2 separate signals (no cosine), and two VERBATIM
        copies → assert they coalesce to occurrence_count=2. No crash.
      - `non_blocking` test (P4, re-scoped to the bus): register a second sentinel handler
        via register_session_handler; monkeypatch detect_for_session (or the detect
        callback) to raise RuntimeError; call
        `emit_session_complete(...)` and assert (a) it returns without raising and (b) the
        sentinel handler still ran. Use clear_session_handlers() in setup/teardown.
  </action>
  <verify>
    cd /Users/neo/Developer/Projects/Agented/backend && uv run pytest tests/test_repeated_request_detector.py -v -k "cosine or embed_disabled or non_blocking"
    (Level 2: Proxy — P1, A1, P4)
  </verify>
  <done>Paraphrases → 1 signal/occ=3; unrelated → 2 signals; embed-disabled falls back to exact hash without crash; detector exception does not escape emit_session_complete and the sentinel handler still runs.</done>
</task>

</tasks>

<verification>
Level 2 (Proxy):
- P1 cosine grouping precision on fixtures (same embedding backend as prod).
- A1 embed-disabled exact-hash fallback.
- P4 non-blocking proven at the bus (emit_session_complete isolation), re-scoped per research.
Level 1 (Sanity): import + ruff clean on the detector module.
</verification>

<success_criteria>
- Detector runs on the session-completion bus for all 5 kinds, extracts user turns via
  _FETCHERS, matches by cosine ≥ 0.83, falls back to exact hash when embed disabled,
  registered as a new handler, non-blocking at the bus (Success Criterion 2).
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/22-repeated-request-auto-skill/22-03-SUMMARY.md`.
</output>
