---
phase: 22-repeated-request-auto-skill
plan: 02
type: standard
wave: 1
depends_on: []
files_modified:
  - backend/app/services/tesserae_integration.py
  - backend/app/services/harness_evolver.py
  - backend/tests/test_build_harness_session_kinds.py
autonomous: true
verification_level: sanity
hypothesis: "Adding project_session/workflow/team_session normalizers to _build_harness_session and updating the evolver prompt strings to declare skills writable will make all five session kinds resolvable and let the evolver LLM know it may create/update skills — closing the two REQ-26 consistency gaps that otherwise silently drop 3 of 5 kinds."
predicted_outcome: "test_build_harness_session_kinds.py passes for all five kinds (project_session, workflow, team_session, super_agent, trigger_execution), and grep of harness_evolver._DESIGN_GUIDE/_PROMPT_TEMPLATE no longer contains 'read-only' / 'deferred' language about skills."

must_haves:
  truths:
    - "_build_harness_session returns a normalized HarnessSession for all 5 kinds (no None for project_session/workflow/team_session)"
    - "each new normalizer SELECTs from its table and maps to HarnessSession fields, mirroring _normalize_super_agent_session / _normalize_trigger_execution"
    - "evolver _DESIGN_GUIDE and _PROMPT_TEMPLATE describe skills as writable (create/update allowed), not read-only/deferred"
    - "the evolver change is text-only — no logic/dispatch change (skills are already in WRITABLE_KINDS and _create_dispatch)"
  artifacts:
    - path: "backend/app/services/tesserae_integration.py"
      provides: "3 new normalizers + dispatch entries in _build_harness_session"
      contains: "_normalize_project_session"
    - path: "backend/tests/test_build_harness_session_kinds.py"
      provides: "5-kind normalization test (S5)"
  key_links:
    - from: "backend/app/services/tesserae_integration.py"
      to: "_build_harness_session dispatch"
      via: "session_kind → normalizer branch for project_session/workflow/team_session"
      pattern: "_normalize_project_session|_normalize_workflow|_normalize_team_session"
---

<objective>
Close the two REQ-26 consistency gaps. This plan is FULLY INDEPENDENT (touches no
new-module files) and can land at any time.

Gap 1 — `tesserae_integration._build_harness_session` (line 375-422) currently
returns `None` in its `else` branch for `project_session`, `workflow`, and
`team_session` ("not normalized yet… Skip"). The detector (22-03) drives off the
same five session kinds via `_FETCHERS`; if three kinds normalize to None in the
tesserae cross-check, signal coverage silently collapses to 2/5. Add the three
missing normalizers, mirroring `_normalize_super_agent_session` (:273) and
`_normalize_trigger_execution` (:313).

Gap 2 — The evolver's `_DESIGN_GUIDE` (:308) and `_PROMPT_TEMPLATE` (:383) still
describe skills as read-only/deferred ("Skills create/update is deferred",
"skills/<name>.json (read-only — do not edit)"). But `WRITABLE_KINDS` (:67) and
`_create_dispatch["skill"]` already make skills writable. Update the prompt text so
the LLM knows it MAY create/update skills. Text edit only — no logic change.

Purpose: Make the substrate the detector and AUTO path rely on actually cover all
five kinds and tell the evolver the truth about skill writability.
Output: 3 normalizers + dispatch wiring, 2 prompt-string edits, one 5-kind test.
Research basis: 22-RESEARCH.md REQ-26 section; EVAL S5.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/milestones/anonymous/phases/22-repeated-request-auto-skill/22-RESEARCH.md
@backend/app/services/tesserae_integration.py
@backend/app/services/harness_evolver.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Three _build_harness_session normalizers + 5-kind test</name>
  <files>backend/app/services/tesserae_integration.py, backend/tests/test_build_harness_session_kinds.py</files>
  <action>
    In `tesserae_integration.py`, study the existing `_normalize_super_agent_session`
    (:273) and `_normalize_trigger_execution` (:313) for the exact HarnessSession
    field mapping (title / agent_label / messages / timestamps — match what those two
    produce). Add three parallel normalizers:
      - `_normalize_project_session(row) -> HarnessSession`
      - `_normalize_workflow(row) -> HarnessSession`
      - `_normalize_team_session(row) -> HarnessSession`
    Each issues a `SELECT * FROM <table> WHERE id = ?` using the
    `app.db.connection.get_connection()` ctx manager already used in this module, then
    maps columns to HarnessSession fields. Confirm the real table + id column names by
    reading the existing fetchers in `harness_failure_annotator.py` (_fetch_project_session,
    _fetch_workflow, _fetch_team_session) — reuse those table/column names; do not guess.

    Replace the `else: return None` branch (:418) with a dispatch that routes
    project_session/workflow/team_session to the new normalizers, keeping the existing
    super_agent and trigger_execution branches intact. Only return None for a genuinely
    unknown kind or a missing row.

    Write `tests/test_build_harness_session_kinds.py` (isolated_db fixture). For each of
    the five kinds, seed a minimal row in the relevant table, call
    `_build_harness_session(kind, id)`, and assert a non-None HarnessSession with the
    expected title/kind. The three previously-skipped kinds must now return a record.
  </action>
  <verify>
    cd /Users/neo/Developer/Projects/Agented/backend && uv run pytest tests/test_build_harness_session_kinds.py -v
    (Level 1: Sanity — S5) — all 5 kind assertions green
  </verify>
  <done>All five session kinds return a normalized HarnessSession; no kind falls through to None except unknown/missing.</done>
</task>

<task type="auto">
  <name>Task 2: Evolver prompt strings reflect writable skills</name>
  <files>backend/app/services/harness_evolver.py</files>
  <action>
    Edit ONLY the prompt-text constants `_DESIGN_GUIDE` (:308) and `_PROMPT_TEMPLATE`
    (:383). Remove/rewrite every clause that calls skills read-only or deferred
    (research cites lines 16-18, 65-66, 328, 339, 391: "Skills create/update is deferred",
    "skills/<name>.json (read-only — do not edit)"). Replace with language stating the
    evolver MAY create and update skills (writing `.claude/skills/<name>/SKILL.md`),
    consistent with the other writable kinds (rule/hook/command/mcp_server). Do NOT touch
    `WRITABLE_KINDS`, `_create_dispatch`, `_update_dispatch`, or any logic — text only.
    Verify by grep that no residual "read-only"/"deferred" skill phrasing remains in the
    two constants.
  </action>
  <verify>
    cd /Users/neo/Developer/Projects/Agented/backend && uv run python -c "import app.services.harness_evolver as e; g=(e._DESIGN_GUIDE+e._PROMPT_TEMPLATE).lower(); assert 'read-only' not in g and 'deferred' not in g, 'stale skill phrasing remains'; print('OK')" && uv run ruff check app/services/harness_evolver.py
    (Level 1: Sanity)
  </verify>
  <done>_DESIGN_GUIDE and _PROMPT_TEMPLATE describe skills as writable; no read-only/deferred phrasing remains; module imports clean.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- S5: `uv run pytest tests/test_build_harness_session_kinds.py -v` — 5 kinds green.
- Evolver grep assertion passes; ruff clean on both touched files.
</verification>

<success_criteria>
- _build_harness_session normalizes project_session/workflow/team_session/super_agent/
  trigger_execution (Success Criterion 5, EVAL S5).
- Evolver _DESIGN_GUIDE/_PROMPT_TEMPLATE reflect writable skills (Success Criterion 5).
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/22-repeated-request-auto-skill/22-02-SUMMARY.md`.
</output>
