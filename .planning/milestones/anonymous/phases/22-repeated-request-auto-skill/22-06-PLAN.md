---
phase: 22-repeated-request-auto-skill
plan: 06
type: standard
wave: 4
depends_on: ["22-01", "22-02", "22-03", "22-04", "22-05"]
files_modified:
  - backend/tests/test_repeated_request_dogfood.py
  - .planning/milestones/anonymous/phases/22-repeated-request-auto-skill/22-DOGFOOD.md
autonomous: true
verification_level: deferred
hypothesis: "Replaying ≥3 real session transcripts that share a recurring request through the assembled detect_for_session → signal store → evaluate_signal → skill-create pipeline will produce either one auto-created, scan-clean, correctly-provenanced skill or one operator-queue entry — proving the loop works end-to-end on real text, not just hand-crafted fixtures."
predicted_outcome: "The dogfood replay drives one signal to occurrence_count ≥ 3; evaluate_signal returns AUTO (skill_created=1 on the signal row, skill file passes scan_skill_content, origin_hash present in forge_origin) or PROPOSE (one queue entry at confidence 0.65); no exception propagates to the emit_session_complete caller; results recorded in 22-DOGFOOD.md."

eval_metrics:
  primary: "end-to-end pipeline outcome on ≥3 real transcripts (AUTO skill or PROPOSE entry)"
  secondary: ["skill file passes scan_skill_content", "origin_hash recorded", "no exception to bus caller"]
  baseline: "pipeline only proven on hand-crafted fixtures (P1/P2/P3)"
  target: "≥3 real transcripts → 1 recurring signal occ≥3 → AUTO skill (clean+provenanced) OR PROPOSE entry; D2 operator judges skill useful/correctly-scoped"

must_haves:
  truths:
    - "≥3 real session transcripts sharing a recurring request are replayed through detect_for_session and drive one signal to occurrence_count ≥ 3"
    - "evaluate_signal/convert_signal on that signal yields either an AUTO skill (skill_created=1, file passes scan_skill_content, origin_hash recorded) or a PROPOSE entry at confidence 0.65"
    - "no exception propagates to the emit_session_complete caller during the replay"
    - "house gates pass: targeted pytest suite green, ruff clean, just build exit 0"
  artifacts:
    - path: "backend/tests/test_repeated_request_dogfood.py"
      provides: "replay harness over ≥3 transcripts (real or recorded-real)"
    - path: ".planning/milestones/anonymous/phases/22-repeated-request-auto-skill/22-DOGFOOD.md"
      provides: "D1/D2 results record + operator quality judgment"
  key_links:
    - from: "backend/tests/test_repeated_request_dogfood.py"
      to: "backend/app/services/repeated_request_detector.py"
      via: "detect_for_session end-to-end through gate"
      pattern: "detect_for_session"
---

<objective>
Live dogfood (REQ-22 success criterion 6; EVAL D1/D2). Replay ≥3 REAL session
transcripts that share a genuinely recurring request through the FULL assembled
pipeline (detector → signal store → gate → skill-create / operator queue) and confirm
it produces a usable outcome on real text — the unit fixtures (P1/P2/P3) cannot prove
this because they are hand-crafted.

Per the project's dogfood rule (MEMORY: "run ≥3 real rows through any new parser before
declaring done"). Prefer real session_ids from the live DB; if no live instance is
available at execution time, capture ≥3 real transcripts into the test as recorded
fixtures and replay those, and note the substitution in 22-DOGFOOD.md.

Purpose: Catch runtime bugs (session-kind dispatch miss, embedding error path) that
fixtures miss by construction; close DEFER-22-01 and DEFER-22-02.
Output: A replay harness test + a DOGFOOD.md results record with operator judgment.
Research basis: 22-EVAL.md D1/D2; 22-RESEARCH.md dogfood pitfalls.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/milestones/anonymous/phases/22-repeated-request-auto-skill/22-EVAL.md
@.planning/milestones/anonymous/phases/22-repeated-request-auto-skill/22-RESEARCH.md
@.planning/milestones/anonymous/phases/22-repeated-request-auto-skill/22-03-SUMMARY.md
@.planning/milestones/anonymous/phases/22-repeated-request-auto-skill/22-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replay harness over ≥3 real transcripts (D1)</name>
  <files>backend/tests/test_repeated_request_dogfood.py</files>
  <action>
    Build `tests/test_repeated_request_dogfood.py` (isolated_db → init_db so forge_origin
    exists). Source ≥3 REAL session transcripts that share a recurring request: either
    SELECT real session_ids from a live agented.db if reachable, or paste ≥3 genuine
    recorded transcripts (real wording, not synthetic) as module constants — disclose which
    in 22-DOGFOOD.md.

    Replay: for each transcript, invoke `detect_for_session(kind, session_id, project_id)`
    (monkeypatching the relevant `_FETCHERS` entry to return the recorded payload when not
    pulling from a live DB). Then:
      1. Assert the shared-request signal reaches occurrence_count ≥ 3.
      2. Run the gate path (evaluate_signal / convert_signal) over that signal with a
         project policy enabling AUTO; mock the evolver skill-create dispatch so the test is
         hermetic. Assert EITHER AUTO (skill_created=1, the synthesized content passes
         scan_skill_content, origin_hash recorded in forge_origin) OR PROPOSE (one
         queue-entry at confidence 0.65) — whichever the verification evidence supports.
      3. Assert no exception propagates to the bus caller: drive one transcript through
         `emit_session_complete(...)` and confirm it returns cleanly.
    Keep the test deterministic and green where the embedding backend is absent (fall back
    to A1 exact-hash path and assert verbatim coalescing instead of cosine).
  </action>
  <verify>
    cd /Users/neo/Developer/Projects/Agented/backend && uv run pytest tests/test_repeated_request_dogfood.py -v
    (Level 3: Deferred — D1)
  </verify>
  <done>≥3 real transcripts replay to one signal occ≥3; gate yields AUTO (clean+provenanced skill) or PROPOSE (0.65 entry); no exception reaches the bus caller.</done>
</task>

<task type="auto">
  <name>Task 2: DOGFOOD.md record + operator quality review (D2) + house gates</name>
  <files>.planning/milestones/anonymous/phases/22-repeated-request-auto-skill/22-DOGFOOD.md</files>
  <action>
    Write `22-DOGFOOD.md` recording: the ≥3 transcript sources (live session_ids or
    recorded-real, disclosed), the resulting signal occurrence_count, the gate decision
    (AUTO/PROPOSE) and confidence, the skill content (if AUTO) or queue entry (if PROPOSE),
    and the D2 operator judgment — whether the auto-created skill is "useful and correctly
    scoped" (or, for PROPOSE, whether the queue entry carries enough info to approve/reject).
    Note any embedding-backend substitution (A1 fallback) used.

    Then run the full house gates and record their results in the same file:
      - targeted pytest suite (the recommended set from EVAL "Evaluation Scripts")
      - ruff format --check + ruff check
      - just build (exit 0)
    Disclose any full-suite-hang substitution per CLAUDE.md verification policy.
  </action>
  <verify>
    cd /Users/neo/Developer/Projects/Agented/backend && uv run pytest tests/test_repeated_request_signals_db.py tests/test_build_harness_session_kinds.py tests/test_repeated_request_detector.py tests/test_skill_safety_scanner.py tests/test_skill_dedup_provenance.py tests/test_repeated_request_gate.py tests/test_repeated_request_dogfood.py -v --tb=short && uv run ruff format --check app/db/schema/_repeated_request_signals.py app/db/repeated_request_signals.py app/models/repeated_request_signal.py app/services/repeated_request_detector.py app/services/skill_safety_scanner.py app/services/repeated_request_gate.py && uv run ruff check app/db/schema/_repeated_request_signals.py app/db/repeated_request_signals.py app/models/repeated_request_signal.py app/services/repeated_request_detector.py app/services/skill_safety_scanner.py app/services/repeated_request_gate.py
    Then: cd /Users/neo/Developer/Projects/Agented && just build
    (Level 3: Deferred — D2 + house gates S1/S7)
  </verify>
  <done>22-DOGFOOD.md records D1 outcome + D2 operator judgment; targeted suite green; ruff clean; just build exit 0.</done>
</task>

</tasks>

<verification>
Level 3 (Deferred):
- D1: `uv run pytest tests/test_repeated_request_dogfood.py -v` — real-transcript replay.
- D2: operator quality judgment recorded in 22-DOGFOOD.md.
Level 1 (Sanity, phase-wide closeout): targeted suite + ruff + just build all pass.
</verification>

<success_criteria>
- ≥3 real session transcripts replayed through the detector before sign-off; the loop
  produces an AUTO skill (clean + provenanced) or a PROPOSE queue entry; house gates pass
  (Success Criterion 6).
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/22-repeated-request-auto-skill/22-06-SUMMARY.md`.
</output>
