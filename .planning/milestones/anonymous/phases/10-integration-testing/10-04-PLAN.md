---
phase: 10-integration-testing
plan: 04
type: execute
wave: 3
depends_on: ["10-01", "10-02", "10-03"]
files_modified:
  - frontend/vitest.config.ts
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "`just build` passes with zero vue-tsc errors — TypeScript strict compilation succeeds for all tour files"
    - "No `any` types (`: any` or `as any`) exist in tourMachine.ts, useTourMachine.ts, TourOverlay.vue, or any tour-related test files"
    - "All Vitest unit tests pass: `cd frontend && npm run test:run` exits with code 0"
    - "All backend tests pass: `cd backend && uv run pytest` exits with code 0"
    - "Coverage thresholds are enforced: tourMachine.ts and useTourMachine.ts >= 90% branch coverage in vitest config"
  artifacts:
    - path: "frontend/vitest.config.ts"
      provides: "Updated vitest config with per-file coverage thresholds for tour files"
      contains: "thresholds"
  key_links:
    - from: "frontend/vitest.config.ts"
      to: "frontend/src/machines/tourMachine.ts"
      via: "coverage threshold configuration"
      pattern: "tourMachine"
---

<objective>
Build verification and type safety audit — confirm the entire codebase passes `just build` (vue-tsc + vite), enforce coverage thresholds in vitest config, and verify zero `any` types in tour code.

Purpose: This is the final gate. It confirms that all tour code is type-safe, all tests pass, and coverage thresholds are enforced for future regressions.
Output: Updated vitest.config.ts with coverage thresholds, verified clean build
</objective>

<execution_context>
@.planning/milestones/anonymous/phases/10-integration-testing/10-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add coverage thresholds and run full verification</name>
  <files>frontend/vitest.config.ts</files>
  <action>
    **Step 1: Add per-file coverage thresholds to vitest.config.ts**

    Update `frontend/vitest.config.ts` to add coverage threshold configuration. The existing config has `coverage.provider: 'v8'` but no thresholds. Add a `thresholds` section:

    ```typescript
    coverage: {
      provider: 'v8',
      reporter: ['text', 'json', 'html'],
      include: ['src/**/*.{ts,vue}'],
      exclude: ['src/main.ts', 'src/**/*.d.ts', 'src/test/**'],
      thresholds: {
        // Tour state machine: >= 90% branch coverage
        'src/machines/tourMachine.ts': {
          branches: 90,
          functions: 90,
          lines: 90,
        },
        // Tour composable: >= 90% branch coverage
        'src/composables/useTourMachine.ts': {
          branches: 90,
          functions: 80,
          lines: 80,
        },
      },
    }
    ```

    Note: Check the exact Vitest coverage.thresholds syntax. If Vitest v4 doesn't support path-pattern thresholds in this format, use the global threshold override or a verification script approach. The research (10-RESEARCH.md) notes that exact syntax for per-file thresholds needs runtime validation. If per-file thresholds aren't supported, add a global floor and rely on the verification commands below.

    **Step 2: Audit tour files for `any` types**

    Search all tour-related files for `: any` and `as any` patterns:
    - `frontend/src/machines/tourMachine.ts`
    - `frontend/src/composables/useTourMachine.ts`
    - `frontend/src/components/tour/TourOverlay.vue`
    - `frontend/src/machines/__tests__/tourMachine.test.ts`
    - `frontend/src/composables/__tests__/useTourMachine.test.ts`

    If any `any` types are found, replace them with proper types. Common replacements:
    - `as any` in test mocks → use `as unknown as Type` pattern
    - `: any` in function params → use the correct type from XState or Vue
    - `unknown` is acceptable; `any` is not

    **Step 3: Run full verification suite**

    Execute all three verification commands from CLAUDE.md:

    1. `just build` — must pass with zero vue-tsc errors
    2. `cd backend && uv run pytest` — all backend tests pass
    3. `cd frontend && npm run test:run` — all frontend tests pass (including new tour tests from plans 01 and 02)

    If any step fails:
    - vue-tsc errors: fix type errors in tour files (do NOT use `// @ts-ignore` or `any`)
    - Backend test failures: investigate whether they are pre-existing (01-01 SUMMARY notes pre-existing failures in App.test.ts and WelcomePage.test.ts) vs new failures from this phase
    - Frontend test failures: fix failing tests

    **Step 4: Run coverage verification**

    `cd frontend && npx vitest run --coverage` — verify the thresholds are enforced and coverage meets targets.
  </action>
  <verify>
    just build (Level 1: Sanity — zero vue-tsc errors)
    cd backend && uv run pytest (Level 1: Sanity — all backend tests pass)
    cd frontend && npm run test:run (Level 1: Sanity — all frontend tests pass)
    cd frontend && npx vitest run --coverage (Level 2: Proxy — coverage thresholds met)
    grep -rn ': any\|as any' frontend/src/machines/tourMachine.ts frontend/src/composables/useTourMachine.ts frontend/src/components/tour/TourOverlay.vue (Level 1: Sanity — zero matches)
  </verify>
  <done>
    `just build` passes with zero vue-tsc errors. All backend and frontend tests pass. No `any` types in tour files. Coverage thresholds enforced in vitest.config.ts: tourMachine.ts >= 90% branch, useTourMachine.ts >= 90% branch. The complete tour system is validated end-to-end.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): `just build` passes, `cd backend && uv run pytest` passes, `cd frontend && npm run test:run` passes, zero `any` types in tour code
Level 2 (Proxy): `cd frontend && npx vitest run --coverage` — thresholds enforced, >= 90% branch on tour machine files
</verification>

<success_criteria>
- `just build` exits with code 0 (vue-tsc + vite clean)
- `cd backend && uv run pytest` exits with code 0
- `cd frontend && npm run test:run` exits with code 0
- Zero `any` types in tour source and test files
- vitest.config.ts has coverage.thresholds for tourMachine.ts and useTourMachine.ts
- Coverage report shows >= 90% branch coverage for both tour files
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/10-integration-testing/10-04-SUMMARY.md`
</output>
