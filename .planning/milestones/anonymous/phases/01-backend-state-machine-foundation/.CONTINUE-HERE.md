# Continue Here — Phase 1 Complete, Phase 2 Next

**Paused:** 2026-03-21T11:17:02Z
**Milestone:** v0.5.0 — Production-Level Onboarding Experience
**Branch:** feat/unified-auth-redesign

## Current State

- **Phase 1** (Backend + State Machine Foundation): **COMPLETE** — 2/2 plans executed
- **Phase 2** (Visual Layer): NOT STARTED — needs planning first
- **Remaining phases:** 2-10 (9 phases left)

## Completed Work (This Session)

### Before GRD Milestone
1. **Rewrote useTour.ts** — replaced driver.js with step/substep state machine (localStorage persistence, route navigation)
2. **Rewrote TourOverlay.vue** — full overlay with CSS spotlight (box-shadow), bottom bar, spinner, skip/next
3. **Updated App.vue** — added TourOverlay rendering with prop bindings
4. **Playwright testing found and fixed 3 bugs:**
   - Tour overlay showing on welcome page (fixed: `!isWelcomePage` guard)
   - `.tour-dim` blocks pointer events on highlighted elements (fixed: removed dim, use spotlight box-shadow only)
   - Substep message shows parent's generic message (fixed: prefer `effectiveTarget.message`)
5. **Code simplification** — replaced RAF loop with scroll/resize listeners, consolidated cleanup

### GRD Phase 1 Execution
6. **Plan 01-01** (3 commits):
   - Backend `app_meta` table in schema + migration 96
   - `/health/instance-id` endpoint (no auth required)
   - XState v5 `tourMachine.ts` with all states/events/guards/actions
7. **Plan 01-02** (3 commits):
   - `useTourMachine.ts` composable with persistence + instance_id validation
   - Z-index CSS custom properties in App.vue (--z-tour-overlay through --z-tour-progress)

## Remaining Work

### Phase 2: Visual Layer (8 requirements: OB-09 through OB-16)
- Overlay dimming, spotlight tracking, adaptive padding
- Floating UI tooltip positioning
- Dark theme integration
- Step transitions, pulsing glow, progress bar

### Phases 3-10
- Phase 3: Welcome Flow + Tour Entry
- Phase 4: Core Step Content (workspace, backends, monitoring, harness)
- Phase 5: Form Field Guidance (auto-discovery of .form-group)
- Phase 6: Navigation Controls (bottom bar, substep labels, skip, keyboard)
- Phase 7: Loading + Error Resilience
- Phase 8: Accessibility (focus trap, ARIA, reduced motion)
- Phase 9: Post-Tour Experience (celebration, checklist, restart)
- Phase 10: Integration Testing (unit + E2E)

## Decisions Made

1. **Drop driver.js** — build custom overlay system (addresses all user-reported issues)
2. **XState v5** for state machine — hierarchical states, guards, snapshot persistence
3. **Custom CSS overlay** (box-shadow spotlight) instead of driver.js or separate dim layer
4. **No worktree isolation** — executing directly on current branch per user request
5. **Toast z-index conflict** noted (hardcoded 10000 vs tour 10000) — deferred to Phase 7

## Blockers

None — Phase 1 complete, ready for Phase 2 planning.

## Context

- **Existing tour code** (`useTour.ts`, `TourOverlay.vue`) still exists and is used by App.vue
- **New tour code** (`tourMachine.ts`, `useTourMachine.ts`) is parallel — replacement happens in Phase 3
- **Uncommitted files:** TourOverlay.vue and useTour.ts have uncommitted changes from pre-GRD fixes
- **Test screenshots** in repo root (tour-test-*.png) — can be deleted

## Next Action

```
/clear
/grd:plan-phase 2
```

Then `/grd:execute-phase 2` to build the visual layer.

## Research Available

All research docs at `.planning/milestones/v0.5.0/research/`:
- FEATURES.md — onboarding UX patterns from Linear, Figma, Vercel, etc.
- ARCHITECTURE.md — component architecture, animation patterns, Floating UI
- STACK.md — driver.js hybrid, focus-trap, CSS animations
- PITFALLS.md — 28 pitfalls with prevention strategies
- SUMMARY.md — synthesis of all findings

## ROADMAP Location

`.planning/milestones/v0.5.0/ROADMAP.md` (also copied to `.planning/ROADMAP.md` for GRD tools)
