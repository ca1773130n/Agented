---
phase: 24-os-level-harness-sandboxing-egress-control
plan: 05
type: tdd
wave: 3
depends_on: [03, 04]
files_modified:
  - backend/tests/test_sandbox_escape.py
  - docs/sandboxing.md
  - docs/sandboxing.ko.md
provides:
  - "backend/tests/test_sandbox_escape.py:test_escape_write_outside_workspace_blocked"
  - "backend/tests/test_sandbox_escape.py:test_escape_connect_non_allowlisted_blocked"
  - "docs/sandboxing.md:operator-runbook"
requires:
  - "backend/app/services/sandbox_wrap.py:build_sandbox_prefix"
  - "backend/app/services/egress_proxy.py:start_egress_proxy"
  - "backend/app/services/execution_service.py:_apply_sandbox_and_enforce"
  - "backend/app/services/policy_enforcement.py:enforce_action"
integration_points:
  - "backend/app/services/sandbox_wrap.py:sandbox_available"
  - "backend/app/services/cloud_sandbox_runner.py:select_runner"
autonomous: true
verification_level: deferred
hypothesis: "An end-to-end escape test that, on a host WITH a usable bwrap/seatbelt, runs a real wrapped harness command attempting to write outside the workspace AND connect to a non-allowlisted host — and asserts BOTH are blocked and logged (skip-if-sandbox-unavailable) — proves the FS + egress boundaries actually hold (crit 5), while an EN+KO operator runbook documents the v1 best-effort ceiling and the house-gate procedure."
predicted_outcome: "On a sandbox-capable host: test_sandbox_escape.py runs a wrapped `sh -c 'echo x > /etc/escape_probe'` and asserts the file does NOT appear at /etc/escape_probe (write blocked); runs a wrapped client CONNECT to a non-allowlisted host through the egress proxy and asserts a 403 + a deny log; both tests @skipUnless(sandbox_available()). docs/sandboxing.md + docs/sandboxing.ko.md exist (1:1 structure) covering the bwrap/SBPL model, deny-by-default egress, the enforce_sandbox gate, cloud runners, the best-effort ceiling, and the house-gate runbook."
must_haves:
  truths:
    - "An escape integration test, on a host where sandbox_available() is True, runs a REAL wrapped harness command that attempts a write OUTSIDE the workspace (e.g. /etc/escape_probe) and asserts the write did NOT land + was contained (crit 5)"
    - "The same test (or a sibling) runs a REAL wrapped command attempting a CONNECT to a NON-allowlisted host through the egress proxy and asserts it is blocked (403) and logged as a deny (crit 5)"
    - "Both escape tests are @skipUnless(sandbox_available()) so CI without bwrap/seatbelt skips cleanly rather than failing (CI reality from RESEARCH §Test Design)"
    - "docs/sandboxing.md (EN) documents the sandbox model (bwrap/SBPL), deny-by-default egress + allowlist, the Phase-23 enforce_sandbox gate, optional cloud runners, the v1 env+proxy best-effort ceiling + airtight netns/nftables upgrade path, and the house-gate runbook"
    - "docs/sandboxing.ko.md is a 1:1 Korean sibling per the repo i18n rule (locale-suffix, code/paths untranslated, identical structure)"
  artifacts:
    - path: "backend/tests/test_sandbox_escape.py"
      provides: "crit-5 escape verification: write-outside-workspace blocked + connect-non-allowlisted blocked, skip-if-unavailable"
      exports: ["test_escape_write_outside_workspace_blocked", "test_escape_connect_non_allowlisted_blocked"]
    - path: "docs/sandboxing.md"
      provides: "operator runbook: sandbox model, egress allowlist, enforce_sandbox gate, cloud runners, ceiling/upgrade path, house-gate procedure"
      contains: "enforce_sandbox"
    - path: "docs/sandboxing.ko.md"
      provides: "Korean 1:1 sibling of sandboxing.md"
      contains: "enforce_sandbox"
  key_links:
    - from: "backend/tests/test_sandbox_escape.py"
      to: "backend/app/services/sandbox_wrap.py:build_sandbox_prefix"
      via: "the test launches a command through the real prefix builder on a sandbox-capable host"
      pattern: "build_sandbox_prefix"
    - from: "backend/tests/test_sandbox_escape.py"
      to: "backend/app/services/egress_proxy.py:start_egress_proxy"
      via: "the connect-attempt routes through a real egress proxy and asserts the deny"
      pattern: "start_egress_proxy"
---

<objective>
Prove the boundaries actually hold and document them (crit 5 + crit 6). An end-to-end escape test, on a host WITH a usable bwrap/seatbelt, runs a real wrapped harness command that (a) tries to write outside the workspace and (b) tries to connect to a non-allowlisted host — asserting BOTH are blocked and logged, skipping cleanly when the OS sandbox is unavailable. Plus an operator runbook (EN + a Korean 1:1 sibling per the repo i18n rule) covering the sandbox model, deny-by-default egress, the `enforce_sandbox` gate, optional cloud runners, the v1 best-effort ceiling + airtight upgrade path, and the house-gate procedure.

Purpose: the security claim is only real once a live escape attempt is demonstrably contained; operators need the runbook to run + upgrade it.
Output: `test_sandbox_escape.py` + `docs/sandboxing.md` + `docs/sandboxing.ko.md`; run the house gates.
Research basis: 24-RESEARCH.md §Test Design (escape row, skip-if-unavailable), Pitfall 3 (document best-effort, don't overclaim), §Verification Strategy (house gates).
SECURITY PHASE: do NOT simplify away the escape test.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/anonymous/phases/24-os-level-harness-sandboxing-egress-control/24-RESEARCH.md
@.planning/milestones/anonymous/phases/24-os-level-harness-sandboxing-egress-control/24-03-SUMMARY.md
@.planning/milestones/anonymous/phases/24-os-level-harness-sandboxing-egress-control/24-04-SUMMARY.md
@docs/i18n.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: real escape-attempt integration test (crit 5, skip-if-unavailable) (RED→GREEN)</name>
  <files>backend/tests/test_sandbox_escape.py</files>
  <action>
    RED→GREEN — test_sandbox_escape.py, both decorated `@pytest.mark.skipif(not sandbox_available(), reason="no usable bwrap/seatbelt")`:
    - test_escape_write_outside_workspace_blocked: create a tmp workspace; build prefix = build_sandbox_prefix(["sh","-c","echo escaped > /etc/agented_escape_probe || echo escaped > /escape_probe"], workspace, net=False); run it via subprocess; assert NEITHER /etc/agented_escape_probe NOR /escape_probe exists afterward (write outside workspace contained) AND a write INSIDE the workspace DID succeed (proves it's a boundary, not a total block).
    - test_escape_connect_non_allowlisted_blocked: start a real egress_proxy with allowlist={a benign allowed host}; build prefix with net=True, proxy_url=proxy.url; run a wrapped command that attempts to reach a NON-allowlisted host via the proxy (e.g. python -c urllib through HTTPS_PROXY); assert the connection is refused (non-zero / 403 surfaced) AND a deny was logged by the proxy.
    Use the real build_sandbox_prefix + start_egress_proxy (no mocks) — this is the L3 boundary proof.
    AVOID: asserting airtight no-bypass (v1 is env+proxy best-effort — the test proves the configured boundary holds for a cooperating client, not a hostile netns escape; note this in the test docstring).
  </action>
  <verify>
    On a sandbox-capable host: `cd /Users/neo/Developer/Projects/Agented/backend && uv run pytest tests/test_sandbox_escape.py -v` green; on a host without bwrap/seatbelt the two tests SKIP cleanly (Level 3: Deferred — skip-if-unavailable).
  </verify>
  <done>The escape test proves write-outside-workspace and connect-non-allowlisted are both blocked + logged on a sandbox-capable host, and skips cleanly otherwise.</done>
</task>

<task type="auto">
  <name>Task 2: operator runbook (EN + Korean sibling) + house-gate run</name>
  <files>docs/sandboxing.md, docs/sandboxing.ko.md</files>
  <action>
    1. docs/sandboxing.md (EN): sections — (a) Sandbox model: bwrap (Linux) / sandbox-exec SBPL (macOS), workspace-only rw, what degrade-to-unsandboxed means + the logged warning; (b) Egress: deny-by-default allowlist proxy, how to set the per-run allowlist, the required-host set; (c) enforce_sandbox gate: how a Phase-23 DENY refuses an unsandboxed launch; (d) Cloud runners: E2B/Modal optional extras, the credential envs (E2B_API_KEY / MODAL_TOKEN_*), graceful skip; (e) Ceiling + upgrade path: v1 env+proxy is BEST-EFFORT, airtight = netns+nftables (deferred), mitmproxy for path/body filtering; (f) House-gate runbook: just build; backend pytest under ~12-min watchdog (on hang → targeted execution/streaming/harness + the new sandbox suites, disclose substitution); frontend no-new-failures.
    2. docs/sandboxing.ko.md: 1:1 Korean translation per docs/i18n.md — same structure/headings, code blocks + file paths + env-var names + flags untranslated, lang-switcher header.
    3. Run the house gates and record the outcome (incl. any watchdog substitution) into the SUMMARY.
    AVOID: claiming airtight enforcement in the docs (Pitfall 3); skipping the Korean sibling (repo i18n rule).
  </action>
  <verify>
    `just build` passes; `cd /Users/neo/Developer/Projects/Agented/backend && uv run pytest` under the ~12-min watchdog (on hang: targeted execution/streaming/harness + tests/test_sandbox_*.py + tests/test_egress_proxy.py + tests/test_enforce_sandbox_gate.py + tests/test_cloud_sandbox_runner.py, substitution disclosed); `cd frontend && npm run test:run` no NEW failures; both docs files exist with identical heading structure (Level 1/2 + house gates).
  </verify>
  <done>EN + KO runbooks exist (1:1, best-effort ceiling documented); house gates pass (build + backend watchdog-disclosed + frontend no-new-failures).</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): both docs files exist with identical heading structure; ruff clean on the test.
Level 2 (Proxy): the connect-non-allowlisted leg uses a real egress proxy + asserts the deny log.
Level 3 (Deferred): the full escape attempt (write-outside + connect-non-allowlisted) on a real bwrap/seatbelt host — @skipUnless(sandbox_available); this is the crit-5 boundary proof.
House gates (crit 6): just build; backend pytest under ~12-min watchdog (on hang → comprehensive targeted set incl. all new sandbox suites + execution/streaming/harness regressions, substitution disclosed in SUMMARY); frontend no-new-failures (baseline 7 known pre-existing).
</verification>

<success_criteria>
- Escape test: write-outside-workspace blocked + connect-non-allowlisted blocked + logged on a sandbox-capable host; skips cleanly where unavailable (crit 5).
- docs/sandboxing.md + docs/sandboxing.ko.md: 1:1 operator runbook covering model, egress, enforce_sandbox gate, cloud runners, the best-effort ceiling/upgrade path, and the house-gate procedure (i18n rule honored).
- House gates pass: just build; backend pytest (watchdog procedure, substitution disclosed if the known hang hits); frontend no-new-failures (crit 6).
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/24-os-level-harness-sandboxing-egress-control/24-05-SUMMARY.md`
</output>
