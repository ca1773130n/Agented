---
phase: 24-os-level-harness-sandboxing-egress-control
plan: 05
type: auto
wave: 3
depends_on: ["01", "02", "03"]
files_modified:
  - backend/tests/test_sandbox_escape.py
  - docs/sandbox-egress.md
  - docs/sandbox-egress.ko.md
autonomous: true
verification_level: deferred
hypothesis: "A skip-if-unavailable escape test that, on a host WITH a real bwrap/sandbox-exec, runs a wrapped command attempting (a) a write outside the workspace and (b) a CONNECT to a non-allowlisted host, will observe both blocked + logged — verifying criterion 5 where the OS primitive exists, and skipping cleanly in CI where it doesn't."
predicted_outcome: "test_sandbox_escape.py: @skipUnless(sandbox_available) — inside a real wrapped harness, `echo x > /etc/escape_probe` fails (write blocked) and a CONNECT to evil.test via the egress proxy returns 403 + a deny log; on a host without bwrap/seatbelt the test SKIPS (no false pass)."

must_haves:
  truths:
    - "An escape test attempts a write outside the workspace AND a connect to a non-allowlisted host from inside a wrapped harness; both are blocked and logged"
    - "The test is gated @skipUnless(sandbox_wrap.sandbox_available()) so CI without bwrap/seatbelt skips rather than fails (no false pass)"
    - "Operator docs explain: what is sandboxed (FS workspace-only, egress deny-by-default), the enforce_sandbox policy gate, the v1 best-effort egress ceiling (env+proxy, not netns), and the cloud-runner opt-in"
    - "A Korean sibling docs/sandbox-egress.ko.md exists with 1:1 structure (per repo i18n rule; code/paths not translated)"
  artifacts:
    - path: "backend/tests/test_sandbox_escape.py"
      provides: "crit-5 L3 escape test (skip-if-unavailable)"
    - path: "docs/sandbox-egress.md"
      provides: "operator docs (EN)"
    - path: "docs/sandbox-egress.ko.md"
      provides: "operator docs (KO, 1:1 structure)"
  key_links:
    - from: "backend/tests/test_sandbox_escape.py"
      to: "backend/app/services/sandbox_wrap.py"
      via: "skipUnless(sandbox_available); build_sandbox_prefix to wrap the probe command"
      pattern: "sandbox_available|build_sandbox_prefix"
---

<objective>
The verification + documentation capstone. Add the criterion-5 escape test (real
sandbox-escape attempt blocked + logged, skip-if-unavailable so CI is honest) and the
operator docs (EN + Korean sibling per the repo i18n rule). Then run the house gates.

Purpose: prove the wrapping actually contains a hostile harness where the OS primitive
exists, and document the v1 best-effort egress ceiling honestly.
Output: one L3 test + two docs + house-gate evidence in the SUMMARY.
Research basis: 24-RESEARCH.md "escape attempt (crit 5)", Verification Strategy, Pitfall 3.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/milestones/anonymous/phases/24-os-level-harness-sandboxing-egress-control/24-RESEARCH.md
@.planning/milestones/anonymous/phases/24-os-level-harness-sandboxing-egress-control/24-01-PLAN.md
@.planning/milestones/anonymous/phases/24-os-level-harness-sandboxing-egress-control/24-02-PLAN.md
@.planning/milestones/anonymous/phases/24-os-level-harness-sandboxing-egress-control/24-03-PLAN.md
@backend/app/services/sandbox_wrap.py
@backend/app/services/egress_proxy.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: criterion-5 escape test (skip-if-unavailable)</name>
  <files>backend/tests/test_sandbox_escape.py</files>
  <action>
    Write `tests/test_sandbox_escape.py`, gated with
    `pytest.mark.skipif(not sandbox_wrap.sandbox_available(), reason="no OS sandbox")`:
      - FS escape: build a prefix for a tiny command that tries to write outside the workspace
        (e.g. `sh -c 'echo x > /etc/agented_escape_probe || echo BLOCKED'`); run it with the
        real wrapper via subprocess; assert the file does NOT exist outside the workspace and/or
        the command reported BLOCKED.
      - Egress escape: start the 24-02 proxy with an empty allowlist; run a wrapped command that
        attempts a CONNECT to a non-allowlisted host through the proxy; assert it is refused (403)
        and a deny was logged. (If exercising the proxy through a real wrapped child is heavy,
        it is acceptable to drive the proxy directly here and rely on 24-02's tunnel test for the
        wrapped-child path — note the choice in a comment.)
      Keep the test deterministic and self-cleaning (remove any probe file in a finally).
  </action>
  <verify>
    cd /Users/neo/Developer/Projects/Agented/backend && uv run pytest tests/test_sandbox_escape.py -v
    (Level 3: Deferred — runs where bwrap/seatbelt exists, SKIPS otherwise; either is a pass for the gate)
  </verify>
  <done>Escape test passes where a real sandbox exists (writes/egress blocked + logged) and skips cleanly where it doesn't.</done>
</task>

<task type="auto">
  <name>Task 2: operator docs EN + KO</name>
  <files>docs/sandbox-egress.md, docs/sandbox-egress.ko.md</files>
  <action>
    Write `docs/sandbox-egress.md` covering: what runs sandboxed (every harness Popen via
    build_sandbox_prefix; bwrap on Linux, sandbox-exec on macOS); the FS boundary (workspace-only
    writes); egress (deny-by-default CONNECT allowlist proxy via HTTPS_PROXY/HTTP_PROXY); the
    enforce_sandbox policy gate (Phase-23 seam — DENY refuses an unsandboxed launch); the v1
    best-effort egress ceiling honestly (env+proxy, NOT airtight; netns+nftables is the upgrade
    path) and how to add allowlist hosts; the optional cloud runner (E2B/Modal, credential-gated,
    highest-risk only); and the graceful-degrade behavior where the OS primitive is absent.
    Write `docs/sandbox-egress.ko.md` as a 1:1 Korean translation (per repo docs/i18n.md:
    locale-suffix sibling, same structure/headings, code & paths NOT translated). Add the
    lang-switcher header line both files use in this repo (check an existing `*.ko.md` for the
    exact header convention and match it).
  </action>
  <verify>
    cd /Users/neo/Developer/Projects/Agented && test -f docs/sandbox-egress.md && test -f docs/sandbox-egress.ko.md && echo "docs present"
  </verify>
  <done>EN + KO operator docs exist with matching structure; egress ceiling documented honestly.</done>
</task>

<task type="auto">
  <name>Task 3: house gates</name>
  <files></files>
  <action>
    Run the three house gates and record results verbatim in the SUMMARY:
      1. `cd /Users/neo/Developer/Projects/Agented && just build` (vue-tsc + vite).
      2. Backend pytest under the watchdog procedure (CLAUDE.md): attempt the full suite under a
         ~12-min watchdog; on the known ~40-48% hang, kill it and run a comprehensive TARGETED set
         instead — all Phase-24 suites (test_sandbox_wrap, test_egress_proxy, test_sandbox_enforcement,
         test_harness_popen_sweep, test_cloud_sandbox_runner, test_sandbox_escape) PLUS execution/
         streaming/harness regressions (test_execution*, test_conversation_streaming*, sandbox_eval).
         Disclose the substitution in the SUMMARY; never present a targeted run as the full suite.
      3. `cd /Users/neo/Developer/Projects/Agented/frontend && npm run test:run` — gate is
         no-NEW failures vs the 7 known pre-existing failures.
  </action>
  <verify>
    Record the three gate outcomes in 24-05-SUMMARY.md (build pass/fail; backend targeted set green + disclosure; frontend no-new-failures).
  </verify>
  <done>just build green; backend targeted set green (substitution disclosed if the hang hit); frontend no new failures.</done>
</task>

</tasks>

<verification>
Level 3 (Deferred) + house gates:
- Escape test passes where a real sandbox exists, skips otherwise (criterion 5).
- EN + KO operator docs present, egress ceiling documented honestly.
- just build green; backend targeted regression green (watchdog disclosure); frontend no-new-failures.
</verification>

<success_criteria>
- A sandbox-escape attempt (write outside workspace + connect to non-allowlisted host) is blocked
  and logged where the OS primitive exists (Criterion 5).
- House gates pass (Criterion 6).
- Operator docs (EN + KO) ship per the repo i18n rule.
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/24-os-level-harness-sandboxing-egress-control/24-05-SUMMARY.md`.
</output>
