---
phase: 24-os-level-harness-sandboxing-egress-control
plan: 03
type: auto
wave: 2
depends_on: ["01", "02"]
files_modified:
  - backend/app/services/policy_enforcement.py
  - backend/app/services/policy_builtins.py
  - backend/app/services/execution_service.py
  - backend/app/services/conversation_streaming.py
  - backend/app/services/cli_agent_runner_service.py
  - backend/app/services/setup_execution_service.py
  - backend/app/services/base_generation_service.py
  - backend/app/services/replay_service.py
  - backend/tests/test_sandbox_enforcement.py
  - backend/tests/test_harness_popen_sweep.py
autonomous: true
verification_level: proxy
hypothesis: "Prepending the 24-01 prefix at the single stdbuf site, setting PolicyContext.sandbox_enabled from the real wrap result, and calling enforce_action before Popen lets an enforce_sandbox DENY refuse an unsandboxed launch (process never starts) — and routing every harness Popen site through build_sandbox_prefix closes the defense-in-depth gap, asserted by a grep sweep test."
predicted_outcome: "test_sandbox_enforcement.py: with an enforce_sandbox DENY seeded and sandboxed=False, enforce_action raises PolicyDeniedError and a Popen sentinel is NOT called. test_harness_popen_sweep.py: every harness-launching service module that calls subprocess.Popen also imports/references build_sandbox_prefix."

must_haves:
  truths:
    - "A minimal real policy seam exists matching the locked Phase-23 contract: PolicyContext dataclass (action, tool_name, sandbox_enabled, approved_ask_ids), PolicyDeniedError, enforce_action(ctx); eval_enforce_sandbox DENYs when action requires sandbox and sandbox_enabled is False, ALLOWs otherwise"
    - "execution_service prepends build_sandbox_prefix at the existing stdbuf site (L617-621), sets ctx.sandbox_enabled = sandboxed, and calls enforce_action(ctx) BEFORE subprocess.Popen"
    - "A DENY raises PolicyDeniedError and the process is never started"
    - "The egress proxy is started per harness session and its proxy_env merged into the subprocess env on the main execution path"
    - "Every harness Popen site (conversation_streaming, cli_agent_runner_service, setup_execution_service, base_generation_service, replay_service) routes its command through build_sandbox_prefix (FS isolation everywhere; egress on the main path)"
    - "The seam degrades: when sandbox_available() is False and no enforce_sandbox DENY applies, launch proceeds unsandboxed with a logged warning (backwards compatible)"
  artifacts:
    - path: "backend/app/services/policy_enforcement.py"
      provides: "Phase-24 enforce_sandbox seam of the locked Phase-23 contract (PolicyContext, PolicyDeniedError, enforce_action). Phase 23 EXTENDS this module."
      exports: ["PolicyContext", "PolicyDeniedError", "enforce_action"]
    - path: "backend/app/services/policy_builtins.py"
      provides: "eval_enforce_sandbox(ctx) -> verdict"
      exports: ["eval_enforce_sandbox"]
    - path: "backend/tests/test_sandbox_enforcement.py"
      provides: "crit-4 test: enforce_sandbox DENY + sandboxed=False ⇒ raise, Popen not called"
    - path: "backend/tests/test_harness_popen_sweep.py"
      provides: "defense-in-depth sweep: each harness Popen site uses build_sandbox_prefix"
  key_links:
    - from: "backend/app/services/execution_service.py"
      to: "backend/app/services/sandbox_wrap.py"
      via: "prepend build_sandbox_prefix at the stdbuf site"
      pattern: "build_sandbox_prefix"
    - from: "backend/app/services/execution_service.py"
      to: "backend/app/services/policy_enforcement.py"
      via: "enforce_action(ctx) pre-Popen with ctx.sandbox_enabled set from the wrap result"
      pattern: "enforce_action|sandbox_enabled"
    - from: "backend/app/services/execution_service.py"
      to: "backend/app/services/egress_proxy.py"
      via: "start_proxy + proxy_env merged into subprocess env"
      pattern: "start_proxy|proxy_env"
---

<objective>
Wire the sandbox at the single chokepoint and close the defense-in-depth gap. This
plan: (a) creates the MINIMAL real `enforce_sandbox` policy seam matching the locked
Phase-23 contract (Phase 23, when built, EXTENDS these modules — they are not throwaway
stubs); (b) prepends `build_sandbox_prefix` at the `execution_service` `stdbuf` site,
sets `PolicyContext.sandbox_enabled` from the real wrap result, starts the egress proxy
and merges its `proxy_env`, then calls `enforce_action(ctx)` before `subprocess.Popen`;
(c) routes every other harness Popen site through the prefix builder.

Purpose: satisfy criteria 1 (wrap), 2 (egress on the live path), 4 (enforce_sandbox
DENY refuses an unsandboxed launch), and Pitfall 1 (sweep — no harness runs fully
unsandboxed).
Output: 2 small policy modules + edits to 6 service files + 2 tests.
Research basis: 24-RESEARCH.md "Phase-23 Policy Contract", Pitfall 1, Architecture Patterns.

NOTE on the Phase-23 dependency: Phase 23 is NOT on disk. Build the enforce_sandbox slice
of the contract here (locked names/signatures from RESEARCH §"Phase-23 Policy Contract"),
so Phase 23 builds ON it rather than conflicting. Add a header comment to each policy
module: "Phase-24 seam — Phase 23 extends this with the full ALLOW/DENY/ASK evaluator."
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
@${CLAUDE_PLUGIN_ROOT}/references/tdd.md
</execution_context>

<context>
@.planning/milestones/anonymous/phases/24-os-level-harness-sandboxing-egress-control/24-RESEARCH.md
@.planning/milestones/anonymous/phases/24-os-level-harness-sandboxing-egress-control/24-01-PLAN.md
@.planning/milestones/anonymous/phases/24-os-level-harness-sandboxing-egress-control/24-02-PLAN.md
@backend/app/services/execution_service.py
@backend/app/services/execution_runner.py
@backend/app/services/sandbox_eval.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Minimal enforce_sandbox policy seam (locked Phase-23 contract)</name>
  <files>backend/app/services/policy_enforcement.py, backend/app/services/policy_builtins.py, backend/tests/test_sandbox_enforcement.py</files>
  <action>
    Create `app/services/policy_enforcement.py`:
      - `class PolicyDeniedError(Exception)`: `__init__(self, reason, policy_id=None)`.
      - `@dataclass class PolicyContext`: fields `action: str`, `tool_name: str | None = None`,
        `sandbox_enabled: bool = False`, `approved_ask_ids: set[str] = field(default_factory=set)`.
      - `def enforce_action(ctx, *, execution_id=None, broadcast=None) -> None`: evaluate via
        `eval_enforce_sandbox(ctx)`; on DENY raise `PolicyDeniedError(reason, policy_id="enforce_sandbox")`
        WITHOUT running anything; on ALLOW return None. (ASK is a Phase-23 extension — for now
        treat non-DENY as pass-through.)
    Create `app/services/policy_builtins.py`:
      - `_SANDBOX_REQUIRED_ACTIONS = {"sandbox", "shell", "process-launch"}` (configurable later).
      - `def eval_enforce_sandbox(ctx) -> tuple[str, str]`: returns `("DENY", reason)` when
        `ctx.action in _SANDBOX_REQUIRED_ACTIONS and ctx.sandbox_enabled is False` AND a module
        flag `enforce_sandbox_enabled()` is True (read from a simple env/config toggle, default
        OFF so existing launches are unaffected until a policy turns it on); else `("ALLOW", "")`.
      Header comment on BOTH files: "Phase-24 seam — Phase 23 extends this with the full
      ALLOW/DENY/ASK evaluator (see 24-RESEARCH §Phase-23 Policy Contract)."
    Write `tests/test_sandbox_enforcement.py`:
      - With enforce_sandbox turned ON (monkeypatch the toggle) + a PolicyContext(action="shell",
        sandbox_enabled=False): `enforce_action` raises PolicyDeniedError.
      - sandbox_enabled=True ⇒ enforce_action returns None.
      - toggle OFF ⇒ ALLOW even when sandbox_enabled=False (backwards compatible default).
  </action>
  <verify>
    cd /Users/neo/Developer/Projects/Agented/backend && uv run pytest tests/test_sandbox_enforcement.py -v
    (Level 2)
  </verify>
  <done>Policy seam works: DENY raises when required+unsandboxed and toggle on; ALLOW otherwise; default-off is backwards compatible.</done>
</task>

<task type="auto">
  <name>Task 2: Wire prefix + proxy + enforce_action into execution_service pre-Popen</name>
  <files>backend/app/services/execution_service.py</files>
  <action>
    At the `stdbuf` prepend site (execution_service.py ~L617-621), AFTER the stdbuf wrap and
    BEFORE `subprocess.Popen` (~L767):
      1. Start a per-session egress proxy: `proxy, proxy_addr = await start_proxy(allowlist)`
         where allowlist is deny-by-default for autonomous/auto-implement runs (empty + a small
         configurable required set; for interactive runs a permissive default is acceptable in v1
         — read from config, name the default in a comment). If the call site is sync, start the
         proxy on the service event loop or a dedicated loop/thread — match how the module already
         runs async helpers (inspect the file; do NOT introduce a new async framework). If wiring
         the proxy into this sync path is non-trivial, gate it behind the autonomous flag and leave
         interactive runs proxy-less in v1 with a `# ponytail:` comment naming the follow-up.
      2. Merge `proxy_env(proxy_addr)` into the env overrides that feed `_build_subprocess_env`.
      3. `prefix, sandboxed = build_sandbox_prefix(cmd, workspace=effective_cwd, proxy_addr=proxy_addr)`
         and set `cmd = prefix` (prefix already includes the original cmd tail).
      4. `ctx = PolicyContext(action="process-launch", sandbox_enabled=sandboxed)`;
         `enforce_action(ctx, execution_id=...)`. A `PolicyDeniedError` must propagate so the
         launch is refused (let the existing error path surface it; ensure the proxy is stopped
         on refusal — try/finally or stop in the except).
    Keep everything reversible and minimal; do not add a second Popen path. Ensure the proxy is
    stopped when the process exits/terminates (hook into the existing cleanup/termination path).
  </action>
  <verify>
    cd /Users/neo/Developer/Projects/Agented/backend && uv run python -c "import ast,sys; src=open('app/services/execution_service.py').read(); assert 'build_sandbox_prefix' in src and 'enforce_action' in src and ('start_proxy' in src or 'proxy_env' in src); ast.parse(src); print('wired+parses')"
    (Level 1: import/parse smoke)
  </verify>
  <done>execution_service prepends the sandbox prefix, sets sandbox_enabled, merges proxy_env, and calls enforce_action before Popen; module parses.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep — route remaining harness Popen sites through the prefix builder</name>
  <files>backend/app/services/conversation_streaming.py, backend/app/services/cli_agent_runner_service.py, backend/app/services/setup_execution_service.py, backend/app/services/base_generation_service.py, backend/app/services/replay_service.py, backend/tests/test_harness_popen_sweep.py</files>
  <action>
    For each of the 5 service modules, find each `subprocess.Popen(...)` that launches a harness
    CLI and, immediately before it, `prefix, sandboxed = build_sandbox_prefix(cmd, workspace=<cwd
    used for that Popen>, proxy_addr=None)` then pass `prefix` as the command. proxy_addr=None ⇒
    FS-isolation only on these secondary paths (egress enforcement stays on the main
    execution_service path in v1). Do NOT change behavior beyond wrapping; if a site's command isn't
    a list, adapt minimally. If a Popen in one of these files is NOT a harness launch (e.g. a helper
    subprocess), leave it and note why in the SUMMARY.
    Write `tests/test_harness_popen_sweep.py`: read each of the 6 harness service source files
    (the 5 here + execution_service.py); for any file whose source contains `subprocess.Popen`,
    assert it also contains `build_sandbox_prefix`. This is the bug-class sweep guard.
  </action>
  <verify>
    cd /Users/neo/Developer/Projects/Agented/backend && uv run pytest tests/test_harness_popen_sweep.py -v && uv run ruff check app/services/policy_enforcement.py app/services/policy_builtins.py
    (Level 2: sweep guard)
  </verify>
  <done>Every harness Popen site references build_sandbox_prefix; sweep test green; ruff clean on new modules.</done>
</task>

</tasks>

<verification>
Level 2 (Proxy):
- crit-4: enforce_sandbox DENY + sandboxed=False ⇒ PolicyDeniedError raised, Popen not reached.
- sweep: every harness Popen site routes through build_sandbox_prefix.
- import/parse smoke on execution_service after the edit; ruff clean on new modules.
Level 1: `uv run python -c "import app.services.execution_service"` imports without error.
</verification>

<success_criteria>
- Harness Popen wrapped at the chokepoint; egress proxy on the live path (Criteria 1, 2).
- enforce_sandbox DENY refuses an unsandboxed launch (Criterion 4).
- No harness Popen runs fully unsandboxed (Pitfall 1 sweep).
- Default-off toggle keeps existing flows working until a policy enables enforcement.
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/24-os-level-harness-sandboxing-egress-control/24-03-SUMMARY.md`.
</output>
