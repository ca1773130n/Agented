---
phase: 24-os-level-harness-sandboxing-egress-control
plan: 04
type: tdd
wave: 2
depends_on: [01]
files_modified:
  - backend/app/services/cloud_sandbox_runner.py
  - backend/app/services/competitor_strategy_service.py
  - backend/app/services/harness_autonomy.py
  - backend/pyproject.toml
  - backend/tests/test_cloud_sandbox_runner.py
provides:
  - "backend/app/services/cloud_sandbox_runner.py:select_runner"
  - "backend/app/services/cloud_sandbox_runner.py:E2BRunner"
  - "backend/app/services/cloud_sandbox_runner.py:ModalRunner"
  - "backend/app/services/cloud_sandbox_runner.py:LocalRunner"
requires:
  - "backend/app/services/sandbox_wrap.py:build_sandbox_prefix"
integration_points:
  - "backend/app/services/competitor_strategy_service.py:start_autoimplement"
  - "backend/app/services/harness_autonomy.py:process_project_autonomy"
  - "backend/app/services/harness_autonomy.py:autonomous_apply_eligible"
  - "backend/app/services/goal_loop_runner.py:start_runner"
autonomous: true
verification_level: sanity
hypothesis: "A thin select_runner(risk, config) that returns LocalRunner (OS-sandbox from Plan 01) by default and an E2BRunner/ModalRunner only when the matching credential is present AND the consumer is highest-risk (competitive-intel auto-implement, life-harness autonomy) — with absent-credential graceful fallback to local — gives the riskiest autonomous runs an optional offboard sandbox without a hard cloud dependency or a crash when keys are missing (crit 3)."
predicted_outcome: "test_cloud_sandbox_runner.py is green: with no E2B_API_KEY / MODAL_TOKEN_* in env, select_runner(risk='high', config) returns a LocalRunner and logs a graceful-skip info (no exception, no import error even if e2b/modal not installed); with E2B_API_KEY set + risk high it returns an E2BRunner; with Modal tokens set it returns a ModalRunner; for non-high risk it always returns LocalRunner regardless of creds."
must_haves:
  truths:
    - "select_runner(*, risk, config) -> Runner returns LocalRunner by default; returns E2BRunner when E2B_API_KEY present AND risk is highest-risk; ModalRunner when MODAL_TOKEN_ID+MODAL_TOKEN_SECRET present AND highest-risk (crit 3)"
    - "Absent credentials ⇒ graceful fallback to LocalRunner + a logged skip; the e2b/modal SDKs are imported lazily inside the adapter so a missing optional dep NEVER raises ImportError at module import (crit 3 graceful skip)"
    - "E2BRunner wraps Sandbox.create(timeout=300)/commands.run/kill; ModalRunner wraps App.lookup(create_if_missing=True)/Sandbox.create/.exec(timeout=)/terminate per the pinned SDK surfaces; both sync the goal-loop worktree in/out (contract via goal_loop_runner.start_runner)"
    - "competitor_strategy_service.start_autoimplement and harness_autonomy.process_project_autonomy consult select_runner(risk='high', config) so the two highest-risk autonomous consumers use cloud when configured, local otherwise (crit 3 wiring)"
    - "pyproject.toml pins e2b and modal as OPTIONAL extras (e.g. [project.optional-dependencies] cloud-sandbox = [\"e2b==<pin>\", \"modal==<pin>\"]) — not runtime-required deps"
  artifacts:
    - path: "backend/app/services/cloud_sandbox_runner.py"
      provides: "select_runner + LocalRunner/E2BRunner/ModalRunner adapters, credential-gated, lazy SDK import, graceful skip"
      exports: ["select_runner", "E2BRunner", "ModalRunner", "LocalRunner"]
      min_lines: 90
    - path: "backend/app/services/competitor_strategy_service.py"
      provides: "start_autoimplement consults select_runner for the auto-implement goal-loop"
      contains: "select_runner"
    - path: "backend/app/services/harness_autonomy.py"
      provides: "process_project_autonomy consults select_runner for the autonomy loop"
      contains: "select_runner"
    - path: "backend/pyproject.toml"
      provides: "e2b + modal pinned as optional cloud-sandbox extras"
      contains: "e2b"
    - path: "backend/tests/test_cloud_sandbox_runner.py"
      provides: "runner-selection + absent-credential graceful-skip tests"
      exports: ["test_select_runner_local_without_creds", "test_select_runner_e2b_with_key", "test_absent_creds_no_importerror"]
  key_links:
    - from: "backend/app/services/competitor_strategy_service.py:start_autoimplement"
      to: "backend/app/services/cloud_sandbox_runner.py:select_runner"
      via: "select_runner(risk='high', config) chooses cloud vs local for the auto-implement goal-loop"
      pattern: "select_runner"
    - from: "backend/app/services/harness_autonomy.py:process_project_autonomy"
      to: "backend/app/services/cloud_sandbox_runner.py:select_runner"
      via: "autonomy loop chooses runner via select_runner"
      pattern: "select_runner"
---

<objective>
Add the optional cloud-sandbox runner (REQ-33): a `cloud_sandbox_runner.py` with `select_runner(risk, config)` that defaults to the local OS-sandbox (Plan 01) and selects an E2B or Modal cloud sandbox only when the matching credential is present AND the consumer is highest-risk — the competitive-intel auto-implement loop and the life-harness autonomy loop. Absent credentials degrade gracefully to local (logged skip, lazy SDK import so a missing optional dep never crashes import). Wire it as the execution-target selector for both consumers.

Purpose: give the riskiest fully-autonomous runs an offboard sandbox option without a hard cloud dependency.
Output: `cloud_sandbox_runner.py` + two consumer edits + pinned optional extras + tests.
Research basis: 24-RESEARCH.md Rec 4 (E2B/Modal SDK snippets, auth envs, graceful skip), §Cloud consumers (exact integration points), §Open Q3 (worktree sync contract via goal_loop_runner.start_runner).
Cloud runner MUST degrade gracefully without credentials.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/anonymous/phases/24-os-level-harness-sandboxing-egress-control/24-RESEARCH.md
@.planning/milestones/anonymous/phases/24-os-level-harness-sandboxing-egress-control/24-01-SUMMARY.md
@backend/app/services/competitor_strategy_service.py
@backend/app/services/harness_autonomy.py
@backend/pyproject.toml
</context>

<tasks>

<task type="tdd">
  <name>Task 1: cloud_sandbox_runner.py — select_runner + E2B/Modal adapters, credential-gated, lazy import (RED→GREEN)</name>
  <files>backend/app/services/cloud_sandbox_runner.py, backend/pyproject.toml, backend/tests/test_cloud_sandbox_runner.py</files>
  <action>
    Pin E2B/Modal SDK versions: use Context7/PyPI to get current e2b + modal versions at exec time; add to pyproject.toml as `[project.optional-dependencies] cloud-sandbox = ["e2b==<pin>", "modal==<pin>"]`.
    RED — test_cloud_sandbox_runner.py:
    - test_select_runner_local_without_creds: monkeypatch env to remove E2B_API_KEY/MODAL_TOKEN_*; assert `select_runner(risk="high", config=...)` returns a LocalRunner and a skip was logged.
    - test_select_runner_e2b_with_key: set E2B_API_KEY; assert E2BRunner returned for risk="high".
    - test_select_runner_modal_with_tokens: set MODAL_TOKEN_ID+MODAL_TOKEN_SECRET (no E2B key); assert ModalRunner.
    - test_absent_creds_no_importerror: with e2b/modal NOT installed (simulate via sys.modules sentinel) and no creds, importing cloud_sandbox_runner and calling select_runner must NOT raise ImportError.
    - test_low_risk_always_local: risk="normal" + creds present ⇒ LocalRunner.
    GREEN — cloud_sandbox_runner.py:
    1. `LocalRunner` — runs via build_sandbox_prefix (Plan 01) at the local chokepoint (thin; the actual launch stays in execution_service/goal_loop).
    2. `E2BRunner` — lazy `from e2b import Sandbox` INSIDE methods; `Sandbox.create(timeout=300)` / `commands.run` / `kill`; api_key from E2B_API_KEY.
    3. `ModalRunner` — lazy `import modal`; `App.lookup("agented-sandbox", create_if_missing=True)` / `Sandbox.create` / `.exec(..., timeout=300)` / `terminate`; auth via MODAL_TOKEN_*.
    4. `select_runner(*, risk, config) -> Runner` — highest-risk + E2B_API_KEY → E2BRunner; elif highest-risk + Modal tokens → ModalRunner; else LocalRunner + (when high-risk but no creds) logger.info graceful-skip.
    AVOID: top-level `import e2b`/`import modal` (would crash without the optional dep); selecting cloud for non-high-risk runs.
  </action>
  <verify>
    `cd /Users/neo/Developer/Projects/Agented/backend && uv run pytest tests/test_cloud_sandbox_runner.py -v` green; `uv run python -c "import app.services.cloud_sandbox_runner"` succeeds with e2b/modal absent (Level 1: Sanity).
  </verify>
  <done>select_runner returns local by default and cloud only with creds+high-risk; absent creds degrade to local + skip log; no ImportError when SDKs missing; e2b/modal pinned as optional extras.</done>
</task>

<task type="tdd">
  <name>Task 2: wire select_runner into the two highest-risk autonomous consumers (RED→GREEN)</name>
  <files>backend/app/services/competitor_strategy_service.py, backend/app/services/harness_autonomy.py, backend/tests/test_cloud_sandbox_runner.py</files>
  <action>
    First read start_autoimplement (~L336, → goal_loop_runner.start_runner) and process_project_autonomy (~L147) / autonomous_apply_eligible (~L41).
    RED — add test_autoimplement_uses_select_runner: monkeypatch select_runner to a spy; trigger start_autoimplement's runner-selection branch; assert select_runner(risk="high", ...) was consulted. Same for process_project_autonomy.
    GREEN — in start_autoimplement, before launching the worktree goal-loop, call `runner = select_runner(risk="high", config=...)`; pass the chosen runner through to the launch (LocalRunner keeps today's goal_loop_runner.start_runner path; cloud runners execute in E2B/Modal with the worktree synced in/out per the start_runner contract). Mirror in harness_autonomy.process_project_autonomy for the autonomy loop.
    AVOID: changing the default behavior when no cloud creds — LocalRunner must reproduce today's path exactly (no regression to the worktree goal-loop).
  </action>
  <verify>
    `cd /Users/neo/Developer/Projects/Agented/backend && uv run pytest tests/test_cloud_sandbox_runner.py -v` green; targeted regression on the two consumers' existing tests green (Level 1: Sanity).
  </verify>
  <done>start_autoimplement and process_project_autonomy consult select_runner; default (no creds) reproduces today's local goal-loop path with no regression.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- select_runner returns local w/o creds, E2B with key, Modal with tokens, local for low risk.
- module imports clean with e2b/modal absent (no ImportError).
- both consumers consult select_runner; local path unchanged.
- ruff clean on the new + edited modules.
Level 2 (Proxy): n/a (cloud SDK round-trips need credentials — covered by graceful-skip, not a live call in CI).
Level 3 (Deferred): live E2B/Modal round-trip when credentials are provisioned (out of CI scope).
</verification>

<success_criteria>
- select_runner: local default; cloud only with matching credential AND highest-risk.
- Absent credentials degrade gracefully to local with a skip log; lazy SDK import — no ImportError when e2b/modal absent.
- e2b + modal pinned as optional extras (not runtime-required).
- Both highest-risk consumers (competitor_strategy.start_autoimplement, harness_autonomy.process_project_autonomy) wired; local path unchanged.
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/24-os-level-harness-sandboxing-egress-control/24-04-SUMMARY.md`
</output>
