---
phase: 24-os-level-harness-sandboxing-egress-control
plan: 04
type: auto
wave: 2
depends_on: ["01"]
files_modified:
  - backend/app/services/cloud_sandbox_runner.py
  - backend/app/services/competitor_strategy_service.py
  - backend/app/services/harness_autonomy.py
  - backend/tests/test_cloud_sandbox_runner.py
autonomous: true
verification_level: sanity
hypothesis: "A thin select_runner(risk, config) that returns 'local' unless cloud credentials are present AND the consumer is highest-risk, with E2B/Modal SDKs imported lazily inside try/except, gives an optional cloud-sandbox target that never crashes when credentials or packages are absent."
predicted_outcome: "test_cloud_sandbox_runner.py passes: no E2B_API_KEY/MODAL_* env ⇒ select_runner(risk='high') returns 'local' (graceful skip, no import error); with creds present + risk='high' ⇒ returns the configured cloud runner; low-risk always ⇒ 'local'."

must_haves:
  truths:
    - "select_runner(risk, config) returns 'local' by default; 'cloud' only when a cloud runner is configured AND credentials are present AND risk is highest"
    - "E2B (e2b) and Modal (modal) SDKs are imported lazily inside the adapter try/except — absent package ⇒ graceful local fallback, never an ImportError at module load"
    - "Absent credentials (no E2B_API_KEY / no MODAL_TOKEN_ID+MODAL_TOKEN_SECRET) ⇒ log + fall back to local, no exception (graceful skip)"
    - "competitor_strategy.start_autoimplement and harness_autonomy consult select_runner to choose the execution target for highest-risk autonomous runs"
    - "Nothing is added to hard dependencies; e2b/modal stay optional extras"
  artifacts:
    - path: "backend/app/services/cloud_sandbox_runner.py"
      provides: "select_runner + E2BRunner/ModalRunner adapters (lazy import, credential-gated, graceful skip)"
      exports: ["select_runner", "cloud_runner_available"]
    - path: "backend/tests/test_cloud_sandbox_runner.py"
      provides: "selection + absent-credential graceful-skip tests (crit 3)"
  key_links:
    - from: "backend/app/services/competitor_strategy_service.py"
      to: "backend/app/services/cloud_sandbox_runner.py"
      via: "start_autoimplement consults select_runner for highest-risk runs"
      pattern: "select_runner"
    - from: "backend/app/services/harness_autonomy.py"
      to: "backend/app/services/cloud_sandbox_runner.py"
      via: "autonomy loop consults select_runner"
      pattern: "select_runner"
---

<objective>
An optional cloud-sandbox runner (E2B / Modal) for the highest-risk autonomous
consumers, behind absent-credential graceful skip. Local OS-sandbox (24-01/03) is the
default; cloud is chosen only when configured AND credentials present AND the consumer
is highest-risk (competitive-intel auto-implement, life-harness autonomy).

Purpose: REQ-33 — give untrusted autonomous runs a stronger isolation target without
making E2B/Modal a hard dependency or breaking credential-less environments.
Output: one runner module + selection wiring at two consumers + one test.
Research basis: 24-RESEARCH.md Rec 4; "any cloud/LLM feature degrades gracefully without creds".
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/milestones/anonymous/phases/24-os-level-harness-sandboxing-egress-control/24-RESEARCH.md
@backend/app/services/competitor_strategy_service.py
@backend/app/services/harness_autonomy.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: cloud_sandbox_runner.py — select_runner + lazy E2B/Modal adapters</name>
  <files>backend/app/services/cloud_sandbox_runner.py, backend/tests/test_cloud_sandbox_runner.py</files>
  <action>
    Create `app/services/cloud_sandbox_runner.py`:
      - `def cloud_runner_available(config=None) -> str | None`: returns "e2b" if `E2B_API_KEY`
        env set, "modal" if `MODAL_TOKEN_ID` and `MODAL_TOKEN_SECRET` set, else None. (config may
        override which provider is preferred.)
      - `def select_runner(*, risk: str, config=None) -> str`: return the available cloud provider
        name when `risk == "high"` AND `cloud_runner_available(config)` is not None AND config does
        not disable cloud; otherwise "local". Default config ⇒ "local".
      - `class E2BRunner` / `class ModalRunner`: each with `run(cmd, *, workspace, timeout=300)`
        that imports its SDK lazily inside the method body within try/except ImportError; on
        ImportError or missing creds, log a warning and raise a typed `CloudRunnerUnavailable`
        (callers catch → fall back to local). Mirror the SDK calls in 24-RESEARCH Rec 4
        (E2B: Sandbox.create/commands.run/kill; Modal: App.lookup/Sandbox.create/exec/terminate).
        Pin nothing at import time — the SDKs are optional extras.
      # ponytail: cloud is an opt-in target for the riskiest runs only; absent creds/packages ⇒ local. No workspace sync built yet (RESEARCH Open Q3) — name it in the SUMMARY.
    Write `tests/test_cloud_sandbox_runner.py`:
      - monkeypatch env to clear E2B/MODAL ⇒ select_runner(risk="high") == "local"; no exception.
      - monkeypatch E2B_API_KEY set ⇒ cloud_runner_available()=="e2b" and select_runner(risk="high")=="e2b".
      - risk="low" with creds ⇒ "local".
  </action>
  <verify>
    cd /Users/neo/Developer/Projects/Agented/backend && uv run pytest tests/test_cloud_sandbox_runner.py -v && uv run ruff check app/services/cloud_sandbox_runner.py
    (Level 1: Sanity)
  </verify>
  <done>select_runner picks local by default, cloud only with creds+high risk; absent creds/packages never raise; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire select_runner into the two highest-risk autonomous consumers</name>
  <files>backend/app/services/competitor_strategy_service.py, backend/app/services/harness_autonomy.py</files>
  <action>
    In `competitor_strategy_service.start_autoimplement` (~L336, before it calls
    goal_loop_runner.start_runner): call `select_runner(risk="high", config=...)` and record the
    chosen runner (pass it through / log it). Keep the existing local path as the default; the
    cloud branch is taken only when select_runner returns a cloud provider — and if the cloud
    runner is unavailable at call time, catch `CloudRunnerUnavailable` and fall back to local.
    Do the same minimal consult in `harness_autonomy` where an autonomous apply is launched
    (`autonomous_apply_eligible`/`process_project_autonomy`). v1 may simply select + log the target
    (full cloud workspace-sync execution is deferred per RESEARCH Open Q3); the success criterion
    is that the selection happens and degrades gracefully — assert via the unit test in Task 1 and
    a grep that both consumers reference `select_runner`.
  </action>
  <verify>
    cd /Users/neo/Developer/Projects/Agented/backend && uv run python -c "import ast; [ast.parse(open('app/services/'+f).read()) for f in ('competitor_strategy_service.py','harness_autonomy.py')]; print('parses')" && grep -l "select_runner" app/services/competitor_strategy_service.py app/services/harness_autonomy.py
    (Level 1)
  </verify>
  <done>Both consumers consult select_runner and fall back to local on unavailability; modules parse.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- select_runner: local by default; cloud only with creds + high risk; absent-cred graceful skip (no exception).
- Both autonomous consumers reference select_runner and degrade to local.
- `uv run ruff check app/services/cloud_sandbox_runner.py` clean.
</verification>

<success_criteria>
- Optional E2B/Modal cloud-sandbox runner exists, wired as a selectable target for the two
  highest-risk autonomous consumers, with absent-credential graceful skip (Criterion 3).
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/24-os-level-harness-sandboxing-egress-control/24-04-SUMMARY.md`.
</output>
