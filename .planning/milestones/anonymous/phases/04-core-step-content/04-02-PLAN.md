---
phase: 04-core-step-content
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/src/machines/__tests__/tourMachine.test.ts
  - frontend/src/composables/__tests__/useTourMachine.test.ts
  - frontend/src/views/__tests__/App.test.ts
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "tourMachine.test.ts navigation sequences include the monitoring state between backends and verification"
    - "toVerification sequence includes a NEXT from monitoring, toComplete includes NEXT from verification"
    - "completedSteps assertions include 'monitoring' in the full forward path"
    - "BACK from verification goes to monitoring (not backends)"
    - "BACK from monitoring goes to backends"
    - "SKIP from monitoring goes to verification"
    - "All skip-all path tests account for the extra monitoring SKIP step"
    - "All existing tests pass plus new monitoring-specific tests exist"
    - "App.test.ts mocks are compatible with updated TOUR_STEP_META"
  artifacts:
    - path: "frontend/src/machines/__tests__/tourMachine.test.ts"
      provides: "Updated machine tests with monitoring state coverage"
      contains: "monitoring"
    - path: "frontend/src/composables/__tests__/useTourMachine.test.ts"
      provides: "Updated composable tests compatible with new machine states"
      contains: "monitoring"
  key_links:
    - from: "frontend/src/machines/__tests__/tourMachine.test.ts"
      to: "frontend/src/machines/tourMachine.ts"
      via: "import and createActor testing"
      pattern: "from.*tourMachine"
---

<objective>
Update all tour-related test files to account for the new monitoring state inserted between backends and verification in Plan 01.

Purpose: Plan 01 changes the machine topology by adding a monitoring state. This breaks ~15 existing tests that assert on navigation sequences, completedSteps accumulation, BACK from verification, and skip-all paths. These tests must be updated and new monitoring-specific tests added to maintain the >= 90% branch coverage target for Phase 10.

Output: All tour tests pass with the new monitoring state in the machine flow.
</objective>

<execution_context>
@/Users/neo/.claude-personal2/plugins/cache/claude-plugin-marketplace/grd/0.3.14/references/execute-plan.md
@/Users/neo/.claude-personal2/plugins/cache/claude-plugin-marketplace/grd/0.3.14/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/anonymous/phases/04-core-step-content/04-01-PLAN.md
@frontend/src/machines/tourMachine.ts
@frontend/src/machines/__tests__/tourMachine.test.ts
@frontend/src/composables/__tests__/useTourMachine.test.ts
@frontend/src/views/__tests__/App.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update tourMachine.test.ts navigation sequences and assertions for monitoring state</name>
  <files>frontend/src/machines/__tests__/tourMachine.test.ts</files>
  <action>
    The monitoring state was inserted between backends and verification. Every test referencing the backends->verification or verification->complete path needs updating.

    **1. Update common event sequences (top of file):**
    ```
    const toBackendsOpencode = [...toBackendsGemini, { type: 'NEXT' }] as const
    const toMonitoring = [...toBackendsOpencode, { type: 'NEXT' }] as const  // NEW
    const toVerification = [...toMonitoring, { type: 'NEXT' }] as const      // CHANGED: was toBackendsOpencode + NEXT
    const toComplete = [...toVerification, { type: 'NEXT' }] as const
    ```

    **2. Update forward navigation tests:**
    - The test "backends.opencode -> NEXT -> verification (parent handler fires)" must now expect `monitoring` not `verification`. Update assertion to:
      ```
      expect(snap.value).toBe('monitoring')
      ```
      Also rename the test: "backends.opencode -> NEXT -> monitoring (parent handler fires)"

    - Add NEW test: "monitoring -> NEXT -> verification (marks monitoring completed)":
      ```
      it('monitoring -> NEXT -> verification (marks monitoring completed)', () => {
        const actor = startActor()
        navigateTo(actor, [...toMonitoring])
        actor.send({ type: 'NEXT' })

        const snap = actor.getSnapshot()
        expect(snap.value).toBe('verification')
        expect(snap.context.completedSteps).toContain('monitoring')
      })
      ```

    - The "complete forward path accumulates all completed steps" test must include 'monitoring' in the expected array:
      ```
      expect(snap.context.completedSteps).toEqual([
        'welcome',
        'workspace',
        JSON.stringify({ backends: 'claude' }),
        JSON.stringify({ backends: 'codex' }),
        JSON.stringify({ backends: 'gemini' }),
        JSON.stringify({ backends: 'opencode' }),
        'monitoring',
        'verification',
      ])
      ```

    **3. Update backward navigation tests:**
    - "verification -> BACK -> backends (re-enters at initial child claude)" must now expect monitoring:
      ```
      it('verification -> BACK -> monitoring', () => {
        const actor = startActor()
        navigateTo(actor, [...toVerification])
        actor.send({ type: 'BACK' })
        expect(actor.getSnapshot().value).toBe('monitoring')
      })
      ```

    - Add NEW test: "monitoring -> BACK -> backends (re-enters at initial child claude)":
      ```
      it('monitoring -> BACK -> backends (re-enters at initial child claude)', () => {
        const actor = startActor()
        navigateTo(actor, [...toMonitoring])
        actor.send({ type: 'BACK' })
        expect(actor.getSnapshot().value).toEqual({ backends: 'claude' })
      })
      ```

    **4. Update skip navigation tests:**
    - "backends.opencode -> SKIP -> verification (parent SKIP handler)" must now expect monitoring:
      ```
      it('backends.opencode -> SKIP -> monitoring (parent SKIP handler)', () => {
        ...
        expect(actor.getSnapshot().value).toBe('monitoring')
      })
      ```

    - Add NEW test: "monitoring -> SKIP -> verification":
      ```
      it('monitoring -> SKIP -> verification', () => {
        const actor = startActor()
        navigateTo(actor, [...toMonitoring])
        actor.send({ type: 'SKIP' })
        expect(actor.getSnapshot().value).toBe('verification')
      })
      ```

    - The "skip-all path results in empty completedSteps" test needs an extra SKIP for monitoring:
      ```
      actor.send({ type: 'SKIP' }) // opencode -> monitoring (parent)
      actor.send({ type: 'SKIP' }) // monitoring -> verification
      actor.send({ type: 'SKIP' }) // verification -> complete
      ```

    **5. Update hierarchical backends substates tests:**
    - "BACK from verification goes to backends at initial child claude" must now test monitoring -> BACK -> backends. Update or replace with monitoring-aware test:
      ```
      it('BACK from monitoring goes to backends at initial child claude', () => {
        const actor = startActor()
        navigateTo(actor, [...toMonitoring])
        actor.send({ type: 'BACK' })
        expect(actor.getSnapshot().value).toEqual({ backends: 'claude' })
      })
      ```

    - "opencode has no local NEXT handler; parent NEXT fires" should assert transition to monitoring:
      ```
      actor.send({ type: 'NEXT' })
      expect(actor.getSnapshot().value).toBe('monitoring')
      ```

    - "opencode has no local SKIP handler; parent SKIP fires" should assert transition to monitoring:
      ```
      actor.send({ type: 'SKIP' })
      expect(actor.getSnapshot().value).toBe('monitoring')
      ```

    Do NOT change any tests that don't involve the backends->verification path (idle tests, welcome tests, workspace tests, RESTART tests, markStepCompleted tests for non-monitoring steps, etc.).
  </action>
  <verify>
    cd frontend && npx vitest run src/machines/__tests__/tourMachine.test.ts 2>&1 | tail -20
    Verify: all tests pass, including new monitoring tests (Level 2: Proxy)
  </verify>
  <done>All tourMachine.test.ts tests pass with monitoring state in the flow, new monitoring forward/backward/skip tests added, completedSteps assertions updated</done>
</task>

<task type="auto">
  <name>Task 2: Update useTourMachine.test.ts and App.test.ts for monitoring state compatibility</name>
  <files>frontend/src/composables/__tests__/useTourMachine.test.ts, frontend/src/views/__tests__/App.test.ts</files>
  <action>
    **useTourMachine.test.ts changes:**

    Check tests that navigate through the full machine flow. Any test that reaches verification or complete state will need adjusted event sequences (the extra NEXT for monitoring). Key areas to update:

    1. Any `getSnapshotAtState` calls that build a sequence to reach verification must include the extra NEXT through monitoring. For example, if a test creates a snapshot at the verification state:
       ```
       // Old: [START, NEXT, NEXT, NEXT, NEXT, NEXT, NEXT, NEXT]
       //       idle->welcome, welcome->workspace, workspace->backends.claude, claude->codex, codex->gemini, gemini->opencode, opencode->verification
       // New: add one more NEXT for monitoring->verification
       ```

    2. Any test checking `checkAndAutoAdvance` behavior -- add a test for the monitoring auto-advance case:
       If a test mocks fetch responses, add a mock for the monitoring config endpoint (`/api/monitoring/config`). If the test doesn't need monitoring to be configured, mock it as `{ enabled: false }`. If testing auto-advance past monitoring, mock it as `{ enabled: true }`.

    3. The fetch mock setup in `beforeEach` -- the existing mock returns `{ instance_id: 'test-uuid-1' }` for all fetch calls. Since Plan 01 adds a new fetch call to `/api/monitoring/config` in `runGuardChecks`, the default mock should handle this. Check if the existing `vi.fn().mockResolvedValue(...)` pattern will handle the new endpoint. If it always returns `{ instance_id: 'test-uuid-1' }`, the monitoring check will see `enabled: undefined` which is falsy, so `monitoringConfigured` will be false. This is correct default behavior -- no changes needed to the default mock.

    4. If there are snapshot-based persistence tests that create snapshots at verification state via event sequences, update those sequences to include the monitoring step.

    **App.test.ts changes:**

    Read the current App.test.ts to understand what mocks exist for useTourMachine. The mock should still work since it mocks the composable return value, not the machine internals. However, check for:

    1. Any assertions on `totalSteps` prop passed to TourOverlay (if it asserts 3, change to 4)
    2. Any assertions on step number mapping
    3. Any references to `harness-status` (should be `harness-plugins` after Plan 01)

    If App.test.ts has no assertions on these specific values, no changes needed there. Only modify what's actually broken.

    Run full test suite after changes to confirm no regressions.
  </action>
  <verify>
    cd frontend && npm run test:run 2>&1 | tail -15
    Verify: all frontend tests pass (no new failures beyond pre-existing ones) (Level 2: Proxy)

    Then run build:
    cd frontend && npm run build 2>&1 | tail -5
    Verify: build succeeds (Level 1: Sanity)
  </verify>
  <done>
    All tour-related test files updated for monitoring state.
    useTourMachine.test.ts snapshot sequences include monitoring step.
    App.test.ts compatible with new step count and metadata.
    Full frontend test suite passes, build succeeds.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- `cd frontend && npx vitest run src/machines/__tests__/tourMachine.test.ts` all pass
- `cd frontend && npx vitest run src/composables/__tests__/useTourMachine.test.ts` all pass
- `just build` passes

Level 2 (Proxy):
- `cd frontend && npm run test:run` shows no new failures (pre-existing failures may remain)
- tourMachine.test.ts includes tests for: monitoring -> NEXT -> verification, monitoring -> BACK -> backends, monitoring -> SKIP -> verification, backends -> NEXT -> monitoring

Level 3 (Deferred to Phase 10):
- Full branch coverage measurement (>= 90% target)
</verification>

<success_criteria>
- All tourMachine.test.ts tests pass with monitoring in the navigation flow
- New tests exist for monitoring forward, backward, and skip transitions
- completedSteps accumulation tests include monitoring in the expected array
- useTourMachine.test.ts snapshot sequences updated for monitoring step
- No new test failures introduced
- `just build` passes
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/04-core-step-content/04-02-SUMMARY.md`
</output>
