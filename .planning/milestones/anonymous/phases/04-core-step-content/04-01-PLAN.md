---
phase: 04-core-step-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/machines/tourMachine.ts
  - frontend/src/composables/useTourMachine.ts
  - frontend/src/App.vue
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "tourMachine.ts has a 'monitoring' state between 'backends' and 'verification'"
    - "TOUR_STEP_META in App.vue has entries for all 7 machine states: workspace, backends.claude, backends.codex, backends.gemini, backends.opencode, monitoring, verification"
    - "totalTourSteps is 4 (workspace, backends, monitoring, verification)"
    - "STEP_NUMBER_MAP assigns workspace=1, backends.*=2, monitoring=3, verification=4"
    - "Monitoring step routes to /settings#general and targets [data-tour='token-monitoring']"
    - "Verification step routes to /settings#harness and targets [data-tour='harness-plugins'] (not harness-status)"
    - "OpenCode substep targets [data-tour='opencode-info'] instead of add-account-btn (which is hidden for OpenCode)"
    - "Route watcher navigates correctly for all steps including hash-based navigation"
    - "Guard checks in useTourMachine include monitoring skip logic"
  artifacts:
    - path: "frontend/src/machines/tourMachine.ts"
      provides: "XState machine with monitoring state"
      contains: "monitoring:"
    - path: "frontend/src/App.vue"
      provides: "TOUR_STEP_META with all step entries"
      contains: "monitoring"
    - path: "frontend/src/composables/useTourMachine.ts"
      provides: "Guard checks for monitoring step"
      contains: "monitoring"
  key_links:
    - from: "frontend/src/App.vue"
      to: "frontend/src/machines/tourMachine.ts"
      via: "TOUR_STEP_META keys match machine state names"
      pattern: "monitoring"
    - from: "frontend/src/App.vue"
      to: "frontend/src/composables/useTourMachine.ts"
      via: "route navigation watch on currentStep"
      pattern: "watch.*currentStep"
---

<objective>
Add the missing monitoring state to the tour machine, fix the verification step target selector and route, handle the OpenCode special case, update all step metadata and numbering, and add monitoring guard logic.

Purpose: The machine and metadata from Phase 3 only had 3 steps (workspace, backends, verification). Phase 4 requires 4 core steps (OB-17 through OB-21) with correct routing, selectors, and skip guards for each.

Output: tourMachine.ts with monitoring state, App.vue with complete TOUR_STEP_META for all 7 state entries, useTourMachine.ts with monitoring guard check, corrected target selectors and routes.
</objective>

<execution_context>
@/Users/neo/.claude-personal2/plugins/cache/claude-plugin-marketplace/grd/0.3.14/references/execute-plan.md
@/Users/neo/.claude-personal2/plugins/cache/claude-plugin-marketplace/grd/0.3.14/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/anonymous/phases/03-welcome-flow-tour-entry/03-01-SUMMARY.md
@frontend/src/machines/tourMachine.ts
@frontend/src/composables/useTourMachine.ts
@frontend/src/App.vue
@frontend/src/components/settings/GeneralSettings.vue
@frontend/src/components/settings/HarnessSettings.vue
@frontend/src/views/BackendDetailPage.vue
@frontend/src/views/SettingsPage.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add monitoring state to tourMachine and fix verification transitions</name>
  <files>frontend/src/machines/tourMachine.ts</files>
  <action>
    Add a `monitoring` state to the tour machine between `backends` and `verification`.

    Current flow: idle -> welcome -> workspace -> backends -> verification -> complete
    New flow: idle -> welcome -> workspace -> backends -> monitoring -> verification -> complete

    Specific changes to tourMachine.ts:

    1. Add `isMonitoringConfigured` guard alongside existing guards:
       ```
       isMonitoringConfigured: () => false,
       ```

    2. Add `monitoring` state after `backends` and before `verification`:
       ```
       monitoring: {
         on: {
           NEXT: {
             target: 'verification',
             actions: ['markStepCompleted'],
           },
           BACK: { target: 'backends' },
           SKIP: { target: 'verification' },
         },
       },
       ```

    3. Update `backends` parent-level NEXT transition to target `monitoring` instead of `verification`:
       ```
       // In backends.on:
       NEXT: {
         target: 'monitoring',
         actions: ['markStepCompleted'],
       },
       ```

    4. Update `backends` parent-level SKIP to also target `monitoring`:
       ```
       SKIP: { target: 'monitoring' },
       ```

    5. Update `verification` BACK to target `monitoring`:
       ```
       BACK: { target: 'monitoring' },
       ```

    6. Fix the `opencode` substep -- currently it has no NEXT or SKIP handler on the opencode substep itself (only BACK). The parent backends NEXT is used to leave backends entirely. This is correct per the existing architecture -- the parent-level NEXT on backends handles advancing from the last substep. Leave this as-is.

    Do NOT change the context type, event type, or initial context. Only add the monitoring state and update transition targets.
  </action>
  <verify>
    cd frontend && npx tsc --noEmit --pretty 2>&1 | head -20
    Verify: zero type errors related to tourMachine.ts (Level 1: Sanity)
  </verify>
  <done>tourMachine.ts has monitoring state, backends transitions point to monitoring, verification BACK points to monitoring, new guard placeholder exists</done>
</task>

<task type="auto">
  <name>Task 2: Update TOUR_STEP_META, step numbering, routes, selectors, and guard checks</name>
  <files>frontend/src/App.vue, frontend/src/composables/useTourMachine.ts, frontend/src/views/BackendDetailPage.vue</files>
  <action>
    **App.vue changes:**

    1. Add `monitoring` entry to TOUR_STEP_META:
       ```
       'monitoring': {
         target: '[data-tour="token-monitoring"]',
         title: 'Token Monitoring',
         message: 'Configure rate limit monitoring to track token usage across your AI backend accounts.',
         skippable: true,
         route: '/settings',
         routeHash: '#general',
       },
       ```

    2. Fix `verification` entry -- change target from `[data-tour="harness-status"]` to `[data-tour="harness-plugins"]` and route from `/plugins` to `/settings` with routeHash `#harness`:
       ```
       'verification': {
         target: '[data-tour="harness-plugins"]',
         title: 'Harness Verification',
         message: 'Verify the harness integration plugin is configured for deploying skills to your marketplace.',
         skippable: true,
         route: '/settings',
         routeHash: '#harness',
       },
       ```

    3. Update `backends.opencode` entry -- change target from `[data-tour="add-account-btn"]` to `[data-tour="opencode-info"]` because BackendDetailPage.vue hides the "Add Account" button for OpenCode (it uses `v-if="!isOpenCode"`). Update the message to explain OpenCode routes through other backends:
       ```
       'backends.opencode': {
         target: '[data-tour="opencode-info"]',
         title: 'AI Backend Accounts',
         message: 'OpenCode routes through other AI backends. No separate account needed if you registered one above.',
         skippable: true,
         route: '/backends/backend-opencode',
       },
       ```

    4. Enrich backend substep messages to be more actionable per OB-19:
       - backends.claude: "Register a Claude Code account. Click 'Add Account' to enter your Anthropic credentials."
       - backends.codex: "Register a Codex CLI account for OpenAI integration (optional). Click 'Add Account' to configure."
       - backends.gemini: "Register a Gemini CLI account for Google AI (optional). Click 'Add Account' to configure."

    5. Update STEP_NUMBER_MAP to include monitoring:
       ```
       const STEP_NUMBER_MAP: Record<string, number> = {
         'workspace': 1,
         'backends.claude': 2,
         'backends.codex': 2,
         'backends.gemini': 2,
         'backends.opencode': 2,
         'monitoring': 3,
         'verification': 4,
       };
       ```

    6. Update totalTourSteps from 3 to 4.

    7. Update the tourSubstepLabel computed to also show substep label for monitoring and verification (they are not substeps, so ensure they return null -- verify the existing computed already handles this correctly since they are simple strings that won't match the labels Record).

    **useTourMachine.ts changes:**

    8. Add monitoring check to `runGuardChecks`:
       Add a `monitoringConfigured` field to GuardCheckResults:
       ```
       interface GuardCheckResults {
         workspaceConfigured: boolean
         hasClaudeAccount: boolean
         hasAnyBackend: boolean
         monitoringConfigured: boolean
       }
       ```
       Check monitoring by fetching the monitoring config API. If monitoring is enabled, mark as configured:
       ```
       const monitoringResp = await fetchWithAuth<{ enabled?: boolean }>('/api/monitoring/config')
       if (monitoringResp?.enabled) {
         results.monitoringConfigured = true
       }
       ```

    9. Add monitoring auto-advance logic in `checkAndAutoAdvance`:
       After the workspace check, add:
       ```
       if (val === 'monitoring' && checks.monitoringConfigured) {
         send({ type: 'NEXT' })
         return
       }
       ```

    **BackendDetailPage.vue changes:**

    10. Add a `data-tour="opencode-info"` attribute to the OpenCode info note section. Find the `.opencode-note` div (around line 87 in template: `<div v-if="isOpenCode" class="opencode-note">`) and add `data-tour="opencode-info"` to it:
        ```
        <div v-if="isOpenCode" class="opencode-note" data-tour="opencode-info">
        ```

    IMPORTANT: When editing App.vue, preserve ALL existing code outside of TOUR_STEP_META, STEP_NUMBER_MAP, and totalTourSteps. Do not alter toast system, sidebar, health polling, error handler, or any other functionality.
  </action>
  <verify>
    cd frontend && npm run build 2>&1 | tail -5
    Verify: build succeeds with zero errors (Level 2: Proxy)

    Then run tests:
    cd frontend && npm run test:run 2>&1 | tail -10
    Verify: no new test failures introduced (Level 2: Proxy)

    Then verify selectors exist on their target pages:
    grep -n 'data-tour="token-monitoring"' frontend/src/components/settings/GeneralSettings.vue
    grep -n 'data-tour="harness-plugins"' frontend/src/components/settings/HarnessSettings.vue
    grep -n 'data-tour="opencode-info"' frontend/src/views/BackendDetailPage.vue
    grep -n 'data-tour="workspace-root"' frontend/src/components/settings/GeneralSettings.vue
    grep -n 'data-tour="add-account-btn"' frontend/src/views/BackendDetailPage.vue
    Verify: all 5 selectors found in their respective files (Level 1: Sanity)
  </verify>
  <done>
    TOUR_STEP_META has 7 entries matching all 7 machine states.
    totalTourSteps is 4, STEP_NUMBER_MAP has monitoring=3 and verification=4.
    Verification step targets harness-plugins on /settings#harness (not harness-status on /plugins).
    OpenCode substep targets opencode-info (not add-account-btn which is hidden).
    Monitoring step targets token-monitoring on /settings#general.
    Guard checks include monitoring configuration detection.
    All data-tour attributes exist on their target DOM elements.
    Build passes, tests pass.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- tourMachine.ts has states: idle, welcome, workspace, backends (claude/codex/gemini/opencode), monitoring, verification, complete
- TOUR_STEP_META has entries for workspace, backends.claude, backends.codex, backends.gemini, backends.opencode, monitoring, verification
- Every target selector in TOUR_STEP_META has a matching data-tour attribute in its target component
- totalTourSteps equals 4

Level 2 (Proxy):
- `just build` passes (vue-tsc + vite)
- `cd frontend && npm run test:run` shows no new failures
- Tour state transitions follow: workspace -> backends.claude -> ... -> backends.opencode -> (parent NEXT) -> monitoring -> verification -> complete

Level 3 (Deferred to Phase 10):
- End-to-end tour flow with real backend accounts
- Visual confirmation that each step highlights the correct element
</verification>

<success_criteria>
- Machine has 7 navigable states (excluding idle/complete) with correct transitions
- Every TOUR_STEP_META entry has a valid route, routeHash (where needed), and target selector
- All target selectors correspond to real data-tour attributes in the codebase
- Guard auto-advance works for monitoring step (if monitoring already configured)
- OpenCode step uses a visible target element (opencode-info note, not hidden add-account-btn)
- `just build` passes, `npm run test:run` shows no regressions
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/04-core-step-content/04-01-SUMMARY.md`
</output>
