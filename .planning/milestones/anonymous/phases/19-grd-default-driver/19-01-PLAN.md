---
phase: 19-grd-default-driver
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
verification_level: sanity
hypothesis: "A pure resolve_execution_driver() reading turn→SA→instance→project→global sources with an injectable degrade check returns the correct driver for every precedence case and silently falls back to cli_agent when GRD is unavailable, without crashing the turn on any read failure."
predicted_outcome: "test_cli_agent_runner.py precedence-matrix + degrade tests pass (each level wins in order; grd→cli_agent when injected available()=False or workspace ValueError); migration 158 adds projects.default_driver + project_sa_instances.driver columns (idempotent under re-run)."
files_modified:
  - backend/app/db/migrations/v07_features.py
  - backend/app/db/project_sa_instances.py
  - backend/app/db/projects.py
  - backend/app/services/cli_agent_runner_service.py
  - backend/tests/test_cli_agent_runner.py
provides:
  - "backend/app/services/cli_agent_runner_service.py:resolve_execution_driver"
  - "backend/app/db/migrations/v07_features.py:_migrate_158_driver_columns"
  - "projects.default_driver"
  - "project_sa_instances.driver"
  - "backend/app/db/projects.py:get_project_default_driver"
  - "backend/app/db/project_sa_instances.py:get_instance_driver"
requires: []
integration_points:
  - "backend/app/services/cli_agent_runner_service.py:should_route_via_cli_agent"
  - "backend/app/services/grd_cli_service.py:GrdCliService.available"
  - "backend/app/services/project_workspace_service.py:ProjectWorkspaceService.resolve_working_directory"
  - "backend/app/db/super_agents.py:get_super_agent (config_json.driver)"
  - "backend/app/db/migrations/__init__.py:VERSIONED_MIGRATIONS"

must_haves:
  truths:
    - "resolve_execution_driver() returns one of 'cliproxy'|'cli_agent'|'grd' for every input"
    - "Precedence honored: turn override (turn_driver / use_cli_agent) → SuperAgent.config_json.driver → project_sa_instances.driver → projects.default_driver → global default 'grd'"
    - "Degrade path: when driver resolves to 'grd' but injected GrdCliService.available() reports unavailable OR ProjectWorkspaceService.resolve_working_directory raises ValueError, the function returns 'cli_agent' (never crashes)"
    - "Any DB read failure during precedence resolution degrades toward the legacy choice (cli_agent/cliproxy) rather than raising"
    - "Migration 158 adds projects.default_driver and project_sa_instances.driver as nullable TEXT, idempotently (guarded by PRAGMA table_info check)"
  artifacts:
    - path: "backend/app/services/cli_agent_runner_service.py"
      provides: "resolve_execution_driver() pure resolver beside should_route_via_cli_agent"
      contains: "def resolve_execution_driver"
    - path: "backend/app/db/migrations/v07_features.py"
      provides: "_migrate_158_driver_columns + (158,...) registration tuple"
      contains: "_migrate_158_driver_columns"
    - path: "backend/tests/test_cli_agent_runner.py"
      provides: "precedence-matrix + degrade-path tests"
  key_links:
    - from: "backend/app/db/migrations/__init__.py"
      to: "v07_features.py:V07_MIGRATIONS"
      via: "(158, 'driver_columns', _migrate_158_driver_columns) appended to V07_MIGRATIONS list"
      pattern: "158.*driver_columns"
    - from: "resolve_execution_driver"
      to: "GrdCliService.available / ProjectWorkspaceService.resolve_working_directory"
      via: "injected degrade-check callables (default to real services)"
      pattern: "available\\(|resolve_working_directory"
---

<objective>
Build the driver spine foundation: the 3-way `resolve_execution_driver()` resolver
and migration 158 with its DB accessors. This is the pure, unit-testable heart of
Phase 19 — everything else wires into it.

Purpose: Replace the 2-way boolean routing with a precedence-driven, default-GRD,
degrade-safe driver resolution that the streaming funnel honors (REQ-10, success
criterion 1).
Output: `resolve_execution_driver()`, migration 158 (`projects.default_driver`,
`project_sa_instances.driver`), DB accessors, precedence + degrade tests.
Research basis: 19-RESEARCH.md §1 (signature, precedence table), §6 (migration 158,
guarded-ALTER convention).
</objective>

<context>
@.planning/ROADMAP.md
@.planning/milestones/anonymous/phases/19-grd-default-driver/19-RESEARCH.md
@backend/app/services/cli_agent_runner_service.py
@backend/app/db/migrations/v07_features.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 158 — driver columns + DB accessors</name>
  <files>backend/app/db/migrations/v07_features.py, backend/app/db/projects.py, backend/app/db/project_sa_instances.py</files>
  <action>
    In `v07_features.py`, add `_migrate_158_driver_columns(conn)` mirroring the
    guarded-ALTER convention used by `_migrate_*` siblings (e.g. line ~117 and
    line ~620 `tesserae_project_root`): read existing columns via
    `cols = {row[1] for row in conn.execute("PRAGMA table_info(<table>)")}` then
    conditionally ALTER:
      - `projects`: if "default_driver" not in cols →
        `ALTER TABLE projects ADD COLUMN default_driver TEXT`  (NULL = inherit global "grd")
      - `project_sa_instances`: if "driver" not in cols →
        `ALTER TABLE project_sa_instances ADD COLUMN driver TEXT` (NULL = inherit project/global)
    Register the migration by appending `(158, "driver_columns", _migrate_158_driver_columns)`
    to the `V07_MIGRATIONS` list (after the `(157, "forge_origin", ...)` tuple at line ~1277).
    Do NOT touch `migrations/__init__.py` — it already assembles V07_MIGRATIONS into
    VERSIONED_MIGRATIONS (line 49–50).

    Add accessors (raw SQLite via `get_connection()`, no ORM):
      - `backend/app/db/projects.py`: `get_project_default_driver(project_id) -> str | None`
        — SELECT default_driver FROM projects WHERE id=?; return None on missing row.
      - `backend/app/db/project_sa_instances.py`: `get_instance_driver(instance_id) -> str | None`
        — SELECT driver FROM project_sa_instances WHERE id=?; and thread `driver` into the
        existing insert/update helpers in that file (add the column to the column list,
        defaulting to NULL) so the value round-trips.
    Ruff line-length=100, py310.
  </action>
  <verify>
    cd backend && uv run python -c "from app.db.migrations.v07_features import _migrate_158_driver_columns, V07_MIGRATIONS; assert any(t[0]==158 for t in V07_MIGRATIONS)"
    cd backend && uv run pytest tests/ -k "migration and (158 or driver)" -q  (Level 1: Sanity)
  </verify>
  <done>
    Migration 158 present + registered as tuple (158,"driver_columns",...); re-running the
    migration on a DB that already has the columns is a no-op (PRAGMA guard); accessors
    return the stored driver or None.
  </done>
</task>

<task type="auto">
  <name>Task 2: resolve_execution_driver() pure resolver + degrade injection</name>
  <files>backend/app/services/cli_agent_runner_service.py</files>
  <action>
    Add `resolve_execution_driver(*, backend, use_cli_agent=None, turn_driver=None,
    super_agent_id=None, project_id=None, instance_id=None, _grd_available=None,
    _resolve_workspace=None) -> str` BESIDE `should_route_via_cli_agent` (line ~530).
    Keep `should_route_via_cli_agent` intact (callers migrate in 19-04); the new fn is additive.

    Precedence (19-RESEARCH.md §1 table), first non-None wins, normalized to the
    literal set {"cliproxy","cli_agent","grd"}:
      1. turn override: `turn_driver` if set; else legacy `use_cli_agent` (True→"cli_agent",
         False→"cliproxy"); else fall through.
      2. SuperAgent: `json.loads(config_json or "{}").get("driver")` for super_agent_id
         (read via existing super_agents DB helper; wrap in try/except → fall through on error).
      3. instance: `get_instance_driver(instance_id)` (try/except → fall through).
      4. project default: `get_project_default_driver(project_id)` (try/except → fall through).
      5. global default: literal "grd".
    Guard: backend ∉ {claude,codex,gemini} CLI-runnable set must still be respected the same
    way `should_route_via_cli_agent` does (a non-CLI backend cannot run grd/cli_agent → resolve
    to "cliproxy"); reuse the existing `_CLI_RUNNABLE_BACKENDS` constant.

    Degrade (grd→cli_agent): when the resolved driver is "grd", check availability:
      - `_grd_available` callable (default lambda: `GrdCliService().available()` →
        truthy `grd_tools_available`/`gd_available` per grd_cli_service.py:144).
      - `_resolve_workspace` callable (default `ProjectWorkspaceService().resolve_working_directory`)
        called with project_id; if it raises ValueError (no clone path,
        project_workspace_service.py:74) → unavailable.
      If either reports unavailable → log at INFO ("GRD driver unavailable, degrading to
      cli_agent for project=...") and return "cli_agent". The two callables are injectable so
      the degrade test runs with no real binary/workspace.
    Defensive: wrap the whole body so ANY unexpected exception degrades toward the legacy
    choice (return "cli_agent" if backend is CLI-runnable else "cliproxy"); never propagate.
    Do NOT hardcode claude anywhere.
  </action>
  <verify>
    cd backend && uv run python -c "from app.services.cli_agent_runner_service import resolve_execution_driver; assert resolve_execution_driver(backend='claude')=='grd' or True"
    (Level 1: Sanity — import + signature)
  </verify>
  <done>
    resolve_execution_driver returns the precedence-correct driver; degrade callables are
    injectable; no claude hardcode; defensive fallthrough on read errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Precedence-matrix + degrade-path tests</name>
  <files>backend/tests/test_cli_agent_runner.py</files>
  <action>
    Extend `test_cli_agent_runner.py` (19-RESEARCH.md §9 row 1) with the `isolated_db`
    fixture. Cover the precedence matrix — one test per level proving it wins in order:
      - turn_driver overrides everything;
      - use_cli_agent=True/False maps to cli_agent/cliproxy when turn_driver is None;
      - SuperAgent config_json.driver wins over instance/project;
      - instance driver wins over project default;
      - project default_driver wins over global;
      - no source set → "grd" (global default).
    Degrade tests (inject `_grd_available=lambda: {"grd_tools_available": False}` and
    `_resolve_workspace` raising ValueError): each independently degrades "grd"→"cli_agent".
    Read-failure test: monkeypatch an accessor to raise → resolver degrades, does NOT raise.
    Non-CLI backend (e.g. backend="openai") → "cliproxy".
    Use the live DB accessors/migration so the columns exist under isolated_db.
  </action>
  <verify>
    cd backend && uv run pytest tests/test_cli_agent_runner.py -q  (Level 1: Sanity)
    All precedence + degrade + read-failure cases green.
  </verify>
  <done>
    Every REQ-10 precedence level + both degrade triggers + read-failure safety asserted and
    passing.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): resolver imports; migration registers + is idempotent;
test_cli_agent_runner.py precedence/degrade suite passes.
Level 2 (Proxy): n/a (pure-fn plan; behavioral coverage lives in 19-04 funnel regression).
Level 3 (Deferred): real-binary GRD availability is exercised in integration, not here.
</verification>

<success_criteria>
- resolve_execution_driver() honors the full REQ-10 precedence + degrade (success criterion 1).
- Migration 158 + DB accessors provide projects.default_driver / project_sa_instances.driver.
- Degrade and read-failure paths are default-safe (TOP RISK mitigation: never crash the turn).
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/19-grd-default-driver/19-01-SUMMARY.md`
</output>
