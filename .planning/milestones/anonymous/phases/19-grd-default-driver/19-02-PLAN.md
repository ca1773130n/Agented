---
phase: 19-grd-default-driver
plan: 02
type: execute
wave: 1
depends_on: []
autonomous: true
verification_level: sanity
hypothesis: "A self-contained classify_turn() reusing the SketchRoutingService keyword→cache→LLM→fallback shape (with turn-specific keywords) splits chat turns into task vs conversational and maps task turns to the correct /grd: command, with the LLM fallback honoring {backend_kind, model_override} and never hardcoding claude."
predicted_outcome: "test_turn_classifier.py passes: keyword-clear task/conversational turns classified without an LLM call; ambiguous turns trigger the LLM fallback which is invoked with the passed backend_kind/model_override (asserted via monkeypatched completion); research/plan keywords map to /grd:research and /grd:plan-phase, default task → /grd:quick."
files_modified:
  - backend/app/services/turn_classifier_service.py
  - backend/tests/test_turn_classifier.py
provides:
  - "backend/app/services/turn_classifier_service.py:classify_turn"
  - "backend/app/services/turn_classifier_service.py:GRD_COMMAND_MAP"
requires: []
integration_points:
  - "backend/app/services/sketch_routing_service.py:SketchRoutingService (pattern reuse: keyword→cache→LLM→fallback, KEYWORD_CONFIDENCE_THRESHOLD=0.6)"
  - "backend/app/services/settings (get_setting model override)"

must_haves:
  truths:
    - "classify_turn(text, *, backend_kind, model_override) returns {'shape': 'task'|'conversational', 'grd_command': str|None}"
    - "Keyword-clear turns are classified without any LLM call (threshold-gated at 0.6, mirroring SketchRoutingService)"
    - "Ambiguous turns trigger the LLM fallback, which is invoked with the passed backend_kind and model_override and selects a per-kind default model when model_override is None — NEVER a hardcoded claude default"
    - "Command mapping: research-flavored→'/grd:research', planning-flavored→'/grd:plan-phase', default task→'/grd:quick', conversational→None"
  artifacts:
    - path: "backend/app/services/turn_classifier_service.py"
      provides: "classify_turn + GRD_COMMAND_MAP + turn-specific keyword seeds"
      contains: "def classify_turn"
    - path: "backend/tests/test_turn_classifier.py"
      provides: "keyword + LLM-fallback + backend-kind tests"
  key_links:
    - from: "turn_classifier_service.py:classify_turn"
      to: "litellm.completion (LLM fallback)"
      via: "model resolved from {backend_kind, model_override}, never literal 'claude'"
      pattern: "backend_kind|model_override"
---

<objective>
Build the standalone turn classifier that splits chat turns into task-shaped vs
conversational and maps task turns to a `/grd:` command. Self-contained and
unit-testable; consumed by the funnel's GRD branch in 19-04.

Purpose: REQ-11's "lightweight turn classifier" — conversational turns stay on
cliproxy, task turns spawn a GRD PSM session (success criterion 2).
Output: `classify_turn()`, `GRD_COMMAND_MAP`, classifier tests.
Research basis: 19-RESEARCH.md §4 (reuse SketchRoutingService keyword→cache→LLM→fallback
SHAPE with turn keywords; honor {backend_kind, model_override}); §9 row 5 (test target).
House rule: feedback_llm_features_support_all_backends — LLM fallback accepts
{backend_kind, model_override}, per-kind default model, never claude-only.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/milestones/anonymous/phases/19-grd-default-driver/19-RESEARCH.md
@backend/app/services/sketch_routing_service.py
@backend/tests/test_sketch_routing_scoped.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: classify_turn() + command mapping</name>
  <files>backend/app/services/turn_classifier_service.py</files>
  <action>
    Create `turn_classifier_service.py`. Mirror the PIPELINE SHAPE of
    `SketchRoutingService.classify` (sketch_routing_service.py:98–129): keyword score →
    (optional cache) → LLM fallback → deterministic fallback. Do NOT import sketch domain
    keyword dicts — define turn-specific seeds:
      - TASK_RESEARCH = {"research","investigate","survey","compare papers","literature"}
      - TASK_PLAN = {"plan","roadmap","phase","break down","milestone"}
      - TASK_GENERIC = {"build","implement","fix","add","refactor","run","create","test"}
      - CONVERSATIONAL = short/question turns ("what","why","how","explain","?") and
        turns under a small token threshold.
    Reuse the 0.6 threshold idea (KEYWORD_CONFIDENCE_THRESHOLD): if the top keyword bucket
    clears threshold, classify deterministically (no LLM). Otherwise call the LLM fallback.

    `GRD_COMMAND_MAP = {"research": "/grd:research", "plan": "/grd:plan-phase",
    "generic": "/grd:quick"}`. Conversational → grd_command=None.

    `classify_turn(text, *, backend_kind, model_override=None) -> dict` returns
    `{"shape": "task"|"conversational", "grd_command": <str|None>, "intent": <bucket>}`.
    LLM fallback (`_llm_classify`): resolve the model from `model_override` if set, else a
    PER-KIND default keyed on `backend_kind` (e.g. a small DEFAULT_MODELS map for
    claude/codex/gemini/...). NEVER hardcode "claude". Call via `litellm.completion` exactly
    as SketchRoutingService._llm_classify does (sketch_routing_service.py:217), returning a
    safe conversational/task fallback if the LLM errors. Ruff line-length=100, py310.
  </action>
  <verify>
    cd backend && uv run python -c "from app.services.turn_classifier_service import classify_turn, GRD_COMMAND_MAP; print(classify_turn('plan the next phase', backend_kind='claude'))"
    (Level 1: Sanity) — returns shape=task, grd_command=/grd:plan-phase.
  </verify>
  <done>
    classify_turn returns correct shape+command for keyword-clear inputs; LLM fallback path
    is reachable and parameterized by {backend_kind, model_override}; no claude hardcode.
  </done>
</task>

<task type="auto">
  <name>Task 2: Classifier tests (keyword + LLM fallback + backend-kind)</name>
  <files>backend/tests/test_turn_classifier.py</files>
  <action>
    Create `test_turn_classifier.py` mirroring `test_sketch_routing_scoped.py`. Cases:
      - keyword task: "implement the resolver" → shape=task, grd_command=/grd:quick, no LLM call.
      - keyword research: "research RoPE variants" → /grd:research.
      - keyword plan: "plan phase 20" → /grd:plan-phase.
      - conversational: "what does this do?" / "why?" → shape=conversational, grd_command=None.
      - LLM fallback invoked for an ambiguous turn: monkeypatch `litellm.completion` (or the
        module's completion symbol) to a spy; assert it is called and that the model arg
        derives from backend_kind when model_override is None, and equals model_override when set.
      - backend-kind matrix: classify the same ambiguous turn with backend_kind in
        {claude, codex, gemini}; assert the spy received the per-kind default model (NOT a
        constant "claude") for each.
      - LLM error → safe deterministic fallback (no raise).
  </action>
  <verify>
    cd backend && uv run pytest tests/test_turn_classifier.py -q  (Level 1: Sanity)
  </verify>
  <done>
    Keyword, conversational, LLM-fallback, and per-backend-kind model-selection cases pass;
    no claude-only default; LLM errors degrade safely.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): classify_turn imports + keyword cases deterministic; tests green.
Level 2 (Proxy): LLM-fallback model-selection asserted via spy (behavioral).
Level 3 (Deferred): live multi-backend LLM tiebreak quality measured in integration.
</verification>

<success_criteria>
- Turn classifier provides the task/conversational split + /grd: command mapping (REQ-11 /
  success criterion 2).
- LLM fallback honors {backend_kind, model_override} with per-kind defaults (house rule).
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/19-grd-default-driver/19-02-SUMMARY.md`
</output>
