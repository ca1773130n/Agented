---
phase: 19-grd-default-driver
plan: 05
type: execute
wave: 3
depends_on: ["19-01", "19-02", "19-04"]
autonomous: true
verification_level: proxy
hypothesis: "Threading resolve_execution_driver() into the single funnel run_streaming_response (plus base_conversation_service and grd_routes.project_chat) and branching cliproxy|cli_agent|grd — with the cliproxy block kept byte-identical and a grd branch that classifies the turn (conversational falls through to cliproxy, task dispatches GrdChatSessionHandler+bridge) — makes GRD the default driver without altering the conversational cliproxy path."
predicted_outcome: "cliproxy regression test passes (a conversational turn with driver=grd produces a byte-for-byte identical delta sequence to the pre-change baseline); a task turn with driver=grd dispatches grd_chat and bridges deltas; resolver is the single source of routing at all three call sites; full house gates (just build, targeted backend pytest, npm test:run) green."
files_modified:
  - backend/app/services/streaming_helper.py
  - backend/app/services/base_conversation_service.py
  - backend/app_litestar/routes/grd_routes.py
  - backend/tests/test_streaming_helper_driver.py
provides:
  - "backend/app/services/streaming_helper.py:run_streaming_response (3-way driver branch)"
requires:
  - "backend/app/services/cli_agent_runner_service.py:resolve_execution_driver"
  - "backend/app/services/turn_classifier_service.py:classify_turn"
  - "backend/app/services/execution_type_handler.py:HANDLER_REGISTRY['grd_chat']"
  - "backend/app/services/grd_chat_bridge.py:bridge_psm_to_chat"
integration_points:
  - "backend/app/services/streaming_helper.py:run_streaming_response (funnel, line 61/263)"
  - "backend/app/services/base_conversation_service.py:552/562 (design conversations branch)"
  - "backend/app_litestar/routes/grd_routes.py:project_chat (597)"
  - "backend/app/services/chat_state_service.py:ChatStateService.push_delta"

must_haves:
  truths:
    - "run_streaming_response computes driver = resolve_execution_driver(...) and branches: driver=='cliproxy' → unchanged cliproxy block; driver=='grd' → GRD branch; else → existing cli_agent block"
    - "The cliproxy block is byte-identical to its pre-change form (gated behind driver=='cliproxy')"
    - "GRD branch: classify_turn() routes conversational turns BACK into the cliproxy block; task turns dispatch HANDLER_REGISTRY['grd_chat'] and bridge PSM output via bridge_psm_to_chat"
    - "base_conversation_service and grd_routes.project_chat call resolve_execution_driver instead of should_route_via_cli_agent (all three legacy call sites migrated)"
    - "A conversational turn with driver=grd produces a byte-for-byte identical delta sequence vs the cliproxy baseline (regression test)"
    - "Resolver read failures degrade toward the legacy choice; the turn never crashes"
  artifacts:
    - path: "backend/app/services/streaming_helper.py"
      provides: "3-way driver branch at the funnel"
      contains: "resolve_execution_driver"
    - path: "backend/tests/test_streaming_helper_driver.py"
      provides: "cliproxy regression + grd-task dispatch tests"
  key_links:
    - from: "streaming_helper.py:run_streaming_response"
      to: "cli_agent_runner_service.py:resolve_execution_driver"
      via: "driver = resolve_execution_driver(backend, use_cli_agent, turn_driver, super_agent_id, project_id, instance_id)"
      pattern: "resolve_execution_driver"
    - from: "streaming_helper.py (grd branch)"
      to: "execution_type_handler.py:HANDLER_REGISTRY['grd_chat'] + grd_chat_bridge"
      via: "task turn → get_handler('grd_chat').start + bridge_psm_to_chat"
      pattern: "grd_chat|bridge_psm_to_chat"
---

<objective>
Integrate the driver spine at the single streaming funnel: branch cliproxy | cli_agent |
grd, keep the cliproxy path byte-identical, and route task-shaped grd turns through the
handler+bridge. Migrate the two other legacy call sites. This is sequenced LAST among the
backend plans so the cliproxy block is touched only here.

Purpose: REQ-10/REQ-11 wiring + the TOP-RISK cliproxy regression isolation (success
criteria 1, 2, 3). GRD becomes the default driver at the funnel.
Output: 3-way branch in run_streaming_response; resolver in base_conversation_service +
project_chat; cliproxy-regression + grd-dispatch tests.
Research basis: 19-RESEARCH.md §2 (funnel branch at streaming_helper.py:263; recommended
refactor), §1 (three call sites), §10 risk 1 (cliproxy regression isolation).
TOP RISK: the cliproxy conversational path must stay byte-identical — gate behind
driver=='cliproxy' and prove it with the regression test.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/milestones/anonymous/phases/19-grd-default-driver/19-RESEARCH.md
@backend/app/services/streaming_helper.py
@backend/app/services/base_conversation_service.py
@backend/app_litestar/routes/grd_routes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 3-way branch in run_streaming_response (cliproxy byte-identical)</name>
  <files>backend/app/services/streaming_helper.py</files>
  <action>
    At the branch point (streaming_helper.py:263), replace
    `if not should_route_via_cli_agent(backend, use_cli_agent):` with:
      `driver = resolve_execution_driver(backend=backend, use_cli_agent=use_cli_agent,
        turn_driver=<turn override if plumbed>, super_agent_id=super_agent_id,
        project_id=<project from session/cwd ctx>, instance_id=instance_id)`
    then:
      - `if driver == "cliproxy":` → the EXISTING cliproxy block (lines 263–290) MOVED
        VERBATIM under this guard. Do not edit a single line of its body
        (stream_llm_response, ToolUseEvent → push_delta("tool_use"),
        chunk → push_delta("content_delta"), _finalize, return). This byte-identity is the
        success-criterion-3 requirement.
      - `elif driver == "grd":` → NEW branch: call `classify_turn(text, backend_kind=backend,
        model_override=<from config>)`. If shape=="conversational", fall through into the
        SAME cliproxy block (call the extracted cliproxy function / share the code path — do
        NOT duplicate-and-diverge it). If shape=="task", dispatch
        `get_handler("grd_chat").start(session_config={..., intent, forge_bundle,
        super_agent_id})`, then run `bridge_psm_to_chat(session_id, psm_events,
        chat_state_service)`.
      - `else:` (cli_agent) → the EXISTING cli_agent block with rate-limit rotation
        (lines 291+), unchanged.
    Import resolve_execution_driver (replace the should_route_via_cli_agent import at :127).
    Defensive: resolver already degrades on error; if the grd dispatch fails at runtime, log
    and fall through to the cliproxy/cli_agent path rather than dropping the turn.
    Ruff line-length=100.
  </action>
  <verify>
    cd backend && uv run python -c "import inspect; import app.services.streaming_helper as m; s=inspect.getsource(m.run_streaming_response); assert 'resolve_execution_driver' in s and 'driver == \"cliproxy\"' in s"
    (Level 1: Sanity)
  </verify>
  <done>
    Funnel branches cliproxy|grd|cli_agent off the resolver; cliproxy block byte-identical and
    shared (not duplicated) by the grd-conversational fallthrough; grd-task dispatches handler+
    bridge.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate base_conversation_service + project_chat to the resolver</name>
  <files>backend/app/services/base_conversation_service.py, backend/app_litestar/routes/grd_routes.py</files>
  <action>
    base_conversation_service.py (import :552, branch :562): swap
    `should_route_via_cli_agent(...)` for `resolve_execution_driver(...)` and branch on the
    returned driver string (design conversations: treat "grd" the same as "cli_agent" if the
    design-conversation path has no PSM/chat-SSE surface — i.e. only cliproxy vs non-cliproxy
    matters here; document the choice). Preserve existing behavior for cliproxy.
    grd_routes.py project_chat (call at :597): swap to the resolver too, passing the
    super_agent_id / project_id / instance_id available in that handler. (cwd + backend hardcode
    already fixed in 19-03; this plan only swaps the routing call.) Keep the GRD task path
    consistent with the funnel — prefer routing project_chat task turns through the same
    handler+bridge if it shares run_streaming_response; otherwise route conversational to
    cliproxy as before.
  </action>
  <verify>
    cd backend && uv run python -c "import inspect; from app.services import base_conversation_service as b; from app_litestar.routes import grd_routes as g; assert 'resolve_execution_driver' in inspect.getsource(b) and 'resolve_execution_driver' in inspect.getsource(g)"
    (Level 1: Sanity — all three legacy sites migrated)
  </verify>
  <done>
    Both remaining call sites use resolve_execution_driver; cliproxy behavior preserved.
  </done>
</task>

<task type="auto">
  <name>Task 3: cliproxy regression + grd-dispatch tests</name>
  <files>backend/tests/test_streaming_helper_driver.py</files>
  <action>
    New `test_streaming_helper_driver.py` (19-RESEARCH.md §9 row 4 — the criterion-3
    regression). Capture the baseline cliproxy delta sequence for a conversational turn
    (driver resolved to cliproxy). Then with driver=grd + a conversational classification,
    assert the delta sequence is byte-for-byte identical to the cliproxy baseline (the turn
    fell through to the unchanged block). Add a task-turn case (driver=grd, shape=task):
    monkeypatch get_handler('grd_chat').start + bridge to spies; assert the handler is
    dispatched and the bridge runs (no cliproxy stream_llm_response call). Add a degrade case:
    resolver returns cli_agent (grd unavailable) → cli_agent block taken, turn not dropped.
  </action>
  <verify>
    cd backend && uv run pytest tests/test_streaming_helper_driver.py -q  (Level 2: Proxy)
    Then the full house gate set: cd backend && uv run pytest (watchdog; on hang run targeted
    execution/streaming/harness regressions per CLAUDE.md) ; cd .. && just build ; cd frontend
    && npm run test:run (no NEW failures vs baseline).
  </verify>
  <done>
    Conversational driver=grd output byte-identical to cliproxy baseline; task driver=grd
    dispatches handler+bridge; degrade path takes cli_agent; house gates green.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): all three call sites import/use resolve_execution_driver; cliproxy guard present.
Level 2 (Proxy): cliproxy regression (byte-identical conversational deltas) + grd-task dispatch +
degrade path asserted.
Level 3 (Deferred): real GRD task turn round-trips through PSM to the frontend (integration).
</verification>

<success_criteria>
- The funnel honors the 3-way driver; cliproxy conversational path provably unchanged
  (success criterion 3); task grd turns spawn the handler and bridge to chat SSE (criterion 2);
  all three legacy call sites migrated (criterion 1 wiring). House gates pass (criterion 6).
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/19-grd-default-driver/19-05-SUMMARY.md`
</output>
