---
phase: 19-grd-default-driver
plan: 03
type: execute
wave: 1
depends_on: []
autonomous: true
verification_level: proxy
hypothesis: "Resolving the project workspace via ProjectWorkspaceService.resolve_working_directory at the three cwd=None sites (execute_delegate, _scan_mentions_and_notify, grd_routes.project_chat) and deriving backend from the SuperAgent's backend_type instead of the literal 'claude' makes delegated/mention/project-chat turns run in the correct project clone with the correct backend."
predicted_outcome: "Delegation cwd tests pass: execute_delegate / _scan_mentions_and_notify / project_chat pass a resolved (non-None) cwd, and project_chat no longer references the literal 'claude' (grep shows backend derived from SA backend_type); existing sketch-execution tests stay green."
files_modified:
  - backend/app/services/sketch_execution_service.py
  - backend/app_litestar/routes/grd_routes.py
  - backend/tests/test_sketch_execution.py
provides:
  - "backend/app/services/sketch_execution_service.py:execute_delegate (cwd-resolved)"
  - "backend/app/services/sketch_execution_service.py:_scan_mentions_and_notify (cwd-resolved)"
  - "backend/app_litestar/routes/grd_routes.py:project_chat (cwd-resolved, backend-derived)"
requires: []
integration_points:
  - "backend/app/services/project_workspace_service.py:ProjectWorkspaceService.resolve_working_directory"
  - "backend/app/services/streaming_helper.py:run_streaming_response (cwd consumer)"
  - "backend/app/db/super_agents.py:get_super_agent (backend_type)"

must_haves:
  truths:
    - "execute_delegate resolves ProjectWorkspaceService.resolve_working_directory(project_id) and passes it as the session/stream cwd instead of None"
    - "_scan_mentions_and_notify resolves and passes the project workspace cwd instead of None"
    - "grd_routes.project_chat passes the resolved workspace cwd instead of cwd=None"
    - "grd_routes.project_chat derives backend from the SuperAgent backend_type (or a non-claude-hardcoded default), removing the literal backend='claude' at both call sites"
    - "When the workspace is unresolvable (ValueError), the sites degrade gracefully (documented fallback) rather than crashing the turn"
  artifacts:
    - path: "backend/app/services/sketch_execution_service.py"
      provides: "cwd-resolved execute_delegate + _scan_mentions_and_notify"
      contains: "resolve_working_directory"
    - path: "backend/app_litestar/routes/grd_routes.py"
      provides: "cwd-resolved, backend-derived project_chat"
      contains: "resolve_working_directory"
    - path: "backend/tests/test_sketch_execution.py"
      provides: "delegation cwd + backend-derivation tests"
  key_links:
    - from: "sketch_execution_service.py:execute_delegate"
      to: "project_workspace_service.py:resolve_working_directory"
      via: "cwd resolution passed into streaming/session launch"
      pattern: "resolve_working_directory"
    - from: "grd_routes.py:project_chat"
      to: "super_agents backend_type"
      via: "backend derived from SA, not literal 'claude'"
      pattern: "backend_type"
---

<objective>
Fix the three cwd=None / backend='claude' bugs so delegated, mention-triggered, and
project-chat turns run in the project clone with the correct backend. Independent of
the driver spine — runs in parallel in Wave 1.

Purpose: REQ-12 (cwd/backend fixes; success criterion 4). Also removes the
backend='claude' hardcode the house rules forbid reintroducing.
Output: cwd-resolved execute_delegate / _scan_mentions_and_notify / project_chat;
backend-derived project_chat; delegation cwd tests.
Research basis: 19-RESEARCH.md §5 (bug table with anchors), §7/§1 (ProjectWorkspaceService
helper). House rule: feedback_llm_features_support_all_backends — do not reintroduce a
claude-only default.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/milestones/anonymous/phases/19-grd-default-driver/19-RESEARCH.md
@backend/app/services/sketch_execution_service.py
@backend/app_litestar/routes/grd_routes.py
@backend/app/services/project_workspace_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve cwd in execute_delegate + _scan_mentions_and_notify</name>
  <files>backend/app/services/sketch_execution_service.py</files>
  <action>
    In `execute_delegate` (sketch_execution_service.py:495) and
    `_scan_mentions_and_notify` (:264), resolve the project workspace before launching the
    stream/session:
      `cwd = ProjectWorkspaceService().resolve_working_directory(project_id)`
    and pass it through wherever the streaming/session is currently launched with cwd=None.
    The backend fallback in execute_delegate (:613,
    `backend = (sa.get("backend_type") if sa else None) or "claude"`) — leave the SA-derived
    backend logic but ensure a non-claude SA backend_type is respected (it already is; do not
    regress it). Wrap `resolve_working_directory` in try/except ValueError: on failure, log a
    warning and fall back to the prior behavior (cwd=None) rather than crashing — document the
    fallback in a comment. Ruff line-length=100.
  </action>
  <verify>
    cd backend && uv run python -c "import ast,inspect; import app.services.sketch_execution_service as m; src=inspect.getsource(m); assert src.count('resolve_working_directory')>=2"
    (Level 1: Sanity — both sites call the resolver)
  </verify>
  <done>
    Both execute_delegate and _scan_mentions_and_notify resolve and pass the project workspace
    cwd; ValueError degrades gracefully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix project_chat cwd + backend hardcode</name>
  <files>backend/app_litestar/routes/grd_routes.py</files>
  <action>
    In `project_chat` (grd_routes.py:520): at the launch sites (~:597 and :603) replace
    `cwd=None` with `cwd = ProjectWorkspaceService().resolve_working_directory(project_id)`
    (try/except ValueError → warn + prior fallback). Remove the literal `backend="claude"`
    at both spots: derive backend from the SuperAgent's `backend_type` (look up the SA for the
    turn; default to a sensible non-claude-hardcoded value, e.g. None passed through to the
    resolver/stream which already has its own default). This is REQ-12's explicit
    anti-regression — do NOT reintroduce a claude literal anywhere in project_chat.
    Keep the existing `should_route_via_cli_agent(...)` call shape for now (19-04 swaps it to
    the 3-way resolver); this plan only fixes cwd+backend.
  </action>
  <verify>
    cd backend && uv run python -c "import inspect; from app_litestar.routes import grd_routes as g; src=inspect.getsource(g.project_chat); assert 'resolve_working_directory' in src and '\"claude\"' not in src and \"'claude'\" not in src"
    (Level 1: Sanity — workspace resolved, no claude literal)
  </verify>
  <done>
    project_chat passes a resolved cwd and derives backend from the SA; no 'claude' literal
    remains in the function.
  </done>
</task>

<task type="auto">
  <name>Task 3: Delegation cwd + backend-derivation tests</name>
  <files>backend/tests/test_sketch_execution.py</files>
  <action>
    Extend the sketch-execution tests (19-RESEARCH.md §9 row 6). Monkeypatch
    `ProjectWorkspaceService.resolve_working_directory` to return a known path and the
    streaming/session launcher to a spy. Assert:
      - execute_delegate passes the resolved cwd (non-None) to the launcher.
      - _scan_mentions_and_notify passes the resolved cwd.
      - project_chat passes the resolved cwd and a backend that is NOT the literal "claude"
        when the SA's backend_type differs (e.g. set SA backend_type="codex" → assert the
        launch backend is "codex", not "claude").
      - ValueError from resolve_working_directory → graceful fallback (no exception bubbles).
  </action>
  <verify>
    cd backend && uv run pytest tests/test_sketch_execution.py -q  (Level 2: Proxy — behavioral)
    Add the grd_routes project_chat case to whichever route test file already exercises it.
  </verify>
  <done>
    All three sites verified to pass resolved cwd; project_chat backend follows SA backend_type;
    fallback path covered.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): both services + route reference resolve_working_directory; no claude literal
in project_chat.
Level 2 (Proxy): tests assert resolved cwd + non-claude backend forwarded through the launchers.
Level 3 (Deferred): real clone-path delegation exercised in integration.
</verification>

<success_criteria>
- execute_delegate / _scan_mentions_and_notify / project_chat resolve the project workspace
  instead of cwd=None; project_chat no longer hardcodes backend='claude' (REQ-12 / success
  criterion 4).
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/19-grd-default-driver/19-03-SUMMARY.md`
</output>
