---
phase: 19-grd-default-driver
plan: 06
type: execute
wave: 3
depends_on: ["19-01"]
autonomous: true
verification_level: sanity
hypothesis: "A driver selector (default GRD) on project + superagent settings, persisted through the api modules to projects.default_driver / SuperAgent.config_json.driver / project_sa_instances.driver, plus a chat-transcript GRD-session link, gives the operator selectable driver control and surfaces the GRD session linkage."
predicted_outcome: "Component tests pass: the selector defaults to GRD and offers cliproxy/cli_agent/grd; persisting a choice calls the project/super-agent api with the chosen driver; the transcript renders a GRD session link when a turn ran on grd; vue-tsc + npm test:run show no NEW failures; all four locales carry key-identical driver.* keys."
files_modified:
  - frontend/src/views/ProjectSettingsPage.vue
  - frontend/src/components/super-agents/SuperAgentDriverSelector.vue
  - frontend/src/components/projects/ProjectTeamLeaderChat.vue
  - frontend/src/services/api/grd.ts
  - frontend/src/services/api/super-agents.ts
  - frontend/src/services/api/types/projects.ts
  - frontend/src/services/api/types/agents.ts
  - frontend/src/locales/en.json
  - frontend/src/locales/ko.json
  - frontend/src/locales/ja.json
  - frontend/src/locales/zh.json
  - frontend/src/components/projects/__tests__/DriverSelector.test.ts
provides:
  - "frontend/src/components/super-agents/SuperAgentDriverSelector.vue:SuperAgentDriverSelector"
requires:
  - "projects.default_driver"
  - "project_sa_instances.driver"
integration_points:
  - "backend resolve_execution_driver precedence (project/instance/SA driver sources)"
  - "frontend/src/views/ProjectSettingsPage.vue (settings host)"
  - "frontend/src/components/projects/ProjectTeamLeaderChat.vue (transcript)"
  - "frontend/src/composables/useConversation.ts (SSE consumer / session linkage)"
  - "frontend/src/services/api/index.ts (barrel re-export)"

must_haves:
  truths:
    - "A driver selector offering 'cliproxy'|'cli_agent'|'grd' with default 'grd' appears on project settings and superagent settings"
    - "Selecting a driver persists it via the project api (projects.default_driver) and the super-agent api (config_json.driver), default GRD"
    - "The chat transcript shows GRD session linkage (a link/badge to the spawned PSM session) when a turn ran on the grd driver"
    - "Driver i18n keys exist key-identical in all four locales (en, ko, ja, zh)"
    - "vue-tsc passes and npm run test:run shows no NEW failures vs the 7-known-failure baseline"
  artifacts:
    - path: "frontend/src/components/super-agents/SuperAgentDriverSelector.vue"
      provides: "reusable driver <select> defaulting to grd"
      contains: "grd"
    - path: "frontend/src/services/api/types/projects.ts"
      provides: "default_driver?: 'cliproxy'|'cli_agent'|'grd' on the project type"
      contains: "default_driver"
    - path: "frontend/src/locales/en.json"
      provides: "driver.* namespace keys"
      contains: "driver"
    - path: "frontend/src/components/projects/__tests__/DriverSelector.test.ts"
      provides: "default-GRD + transcript-linkage component test"
  key_links:
    - from: "ProjectSettingsPage.vue / SuperAgentDriverSelector.vue"
      to: "services/api/grd.ts + super-agents.ts"
      via: "persist selected driver to projects.default_driver / config_json.driver"
      pattern: "default_driver|driver"
    - from: "ProjectTeamLeaderChat.vue"
      to: "useConversation.ts session info"
      via: "render GRD session link for grd-driver turns"
      pattern: "session"
---

<objective>
Ship the operator-facing slice: driver selectors (default GRD) on project + superagent
settings, the api/types/i18n plumbing to persist them, and a chat-transcript GRD-session
link. Last, independent wave — only needs the migration columns from 19-01.

Purpose: REQ-13 — driver is operator-selectable and chat shows GRD session linkage
(success criterion 5). Bulk of the phase is backend; this is the thin frontend cap.
Output: SuperAgentDriverSelector.vue, project-settings selector, transcript linkage, api +
types + 4-locale i18n, component test.
Research basis: 19-RESEARCH.md §7 (frontend file map; driver enum
'cliproxy'|'cli_agent'|'grd' default 'grd'; verify SSE delta strings in useConversation.ts).
House rule: i18n keys added to ALL FOUR locales, key-identical.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/milestones/anonymous/phases/19-grd-default-driver/19-RESEARCH.md
@frontend/src/views/ProjectSettingsPage.vue
@frontend/src/components/projects/ProjectTeamLeaderChat.vue
@frontend/src/services/api/grd.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Driver selector component + settings wiring + api/types</name>
  <files>frontend/src/components/super-agents/SuperAgentDriverSelector.vue, frontend/src/views/ProjectSettingsPage.vue, frontend/src/services/api/grd.ts, frontend/src/services/api/super-agents.ts, frontend/src/services/api/types/projects.ts, frontend/src/services/api/types/agents.ts</files>
  <action>
    Create `SuperAgentDriverSelector.vue`: a reusable `<select>` over
    `'cliproxy' | 'cli_agent' | 'grd'` with default `'grd'`, props `modelValue` + emit
    `update:modelValue`, labels from i18n `driver.*`. Mount it on `ProjectSettingsPage.vue`
    (project default_driver) and on the superagent settings surface
    (SuperAgentInspectorPage / super-agents config editor → config_json.driver).
    Types: add `default_driver?: 'cliproxy'|'cli_agent'|'grd'` to the project type in
    `types/projects.ts`; add a `driver?` field to the agent/super-agent type in `types/agents.ts`.
    api: extend the project update call in `grd.ts` (or the project api module) to send
    `default_driver`, and the super-agent update in `super-agents.ts` to persist
    `config_json.driver`. Re-export any new symbol through `services/api/index.ts` per the
    barrel convention. Do NOT hand-roll fetch — use the shared `apiFetch`/client.
  </action>
  <verify>
    cd frontend && npx vue-tsc --noEmit  (Level 1: Sanity — types compile)
  </verify>
  <done>
    Selector defaults to GRD, mounted on both settings surfaces, persists via api to
    default_driver / config_json.driver; types + barrel updated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Transcript GRD-session linkage + 4-locale i18n</name>
  <files>frontend/src/components/projects/ProjectTeamLeaderChat.vue, frontend/src/locales/en.json, frontend/src/locales/ko.json, frontend/src/locales/ja.json, frontend/src/locales/zh.json</files>
  <action>
    In `ProjectTeamLeaderChat.vue`, when a turn ran on the grd driver (the spawned PSM session
    id surfaced via the SSE finish/session metadata — verify the field name against
    useConversation.ts, 19-RESEARCH.md §7/risk 2: confirm the SSE wire strings
    content_delta/tool_use/finish/error and the session-id field before binding), render a link
    or badge to the GRD session (target: ProjectSessionPanel route for that psess- id).
    Add a `driver.*` namespace to all four locales `src/locales/{en,ko,ja,zh}.json`,
    key-identical: labels for cliproxy/cli_agent/grd, the selector title/help, and the
    transcript "View GRD session" link text. Code/paths/enum literals stay untranslated.
  </action>
  <verify>
    cd frontend && node -e "const e=require('./src/locales/en.json'),k=require('./src/locales/ko.json'),j=require('./src/locales/ja.json'),z=require('./src/locales/zh.json'); const kk=o=>JSON.stringify(Object.keys(o.driver||{}).sort()); if(!(kk(e)===kk(k)&&kk(e)===kk(j)&&kk(e)===kk(z))) process.exit(1)"
    (Level 1: Sanity — driver.* keys identical across locales)
  </verify>
  <done>
    Transcript links to the GRD session for grd-driver turns; driver.* keys present and
    key-identical in en/ko/ja/zh.
  </done>
</task>

<task type="auto">
  <name>Task 3: Component test (default GRD + transcript linkage)</name>
  <files>frontend/src/components/projects/__tests__/DriverSelector.test.ts</files>
  <action>
    Add a Vitest + @vue/test-utils test (19-RESEARCH.md §9 row 7) beside the existing
    ProjectTeamLeaderChat test sibling: mount SuperAgentDriverSelector and assert the default
    selected value is 'grd' and all three options render; assert changing the value emits
    update:modelValue with the chosen driver. Add a transcript case: render the transcript with
    a grd-driver turn carrying a session id and assert the GRD-session link/badge renders with
    the i18n label. Keep within the no-NEW-failures gate.
  </action>
  <verify>
    cd frontend && npm run test:run  (Level 1: Sanity — no NEW failures vs the 7-known baseline)
    cd frontend && npm run build  (vue-tsc + vite)
  </verify>
  <done>
    Selector defaults to GRD and emits on change; transcript renders the GRD-session link;
    no NEW frontend failures; build green.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): vue-tsc compiles; locale-parity check passes; component test green; build green.
Level 2 (Proxy): n/a (UI slice).
Level 3 (Deferred): live operator selects driver → backend precedence honors it (integration).
</verification>

<success_criteria>
- Driver selector (default GRD) on project + superagent settings, persisted to
  default_driver / config_json.driver; chat transcript shows GRD session linkage; 4-locale
  i18n parity (REQ-13 / success criterion 5). House frontend gate: no NEW failures (criterion 6).
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/19-grd-default-driver/19-06-SUMMARY.md`
</output>
