---
phase: 18-sketch-primitive-routing
plan: "01"
type: tdd
wave: 1
depends_on: []
autonomous: true
verification_level: sanity
files_modified:
  - backend/app/models/sketch.py
  - backend/app/services/sketch_routing_service.py
  - backend/tests/test_sketch_action_classification.py
provides:
  - "backend/app/models/sketch.py:SketchStatus.COLLABORATING"
  - "backend/app/services/sketch_routing_service.py:ACTION_KEYWORDS"
  - "backend/app/services/sketch_routing_service.py:SketchRoutingService.classify[action]"
  - "backend/app/services/sketch_routing_service.py:SketchRoutingService._keyword_classify[action]"
  - "backend/app/services/sketch_routing_service.py:SketchRoutingService._llm_classify[action,provider_kind,model_override]"
requires: []
integration_points:
  - "backend/app/models/sketch.py:UpdateSketchRequest.status"
  - "backend/app/services/sketch_routing_service.py:SketchRoutingService._llm_classify (litellm.completion call site)"
hypothesis: "Adding an ACTION_KEYWORDS dimension to both the keyword and LLM classifier stages (the LLM stage threaded with provider_kind/model_override resolving a litellm model STRING via a per-kind default-model map) plus a backend SketchStatus.COLLABORATING member will make every classify() return path emit a valid action without breaking existing phase/domain/complexity outputs."
predicted_outcome: "test_sketch_action_classification.py passes: 7 keyword-action cases + 7 LLM-mocked cases each assert classification['action'] equals the expected member; SketchStatus.COLLABORATING == 'collaborating'; existing test_sketch_routing_scoped.py still green."
eval_metrics:
  primary: "action-classification accuracy on the 7 seeded keyword cases + 7 LLM-mocked cases (target: 14/14)"
  secondary: ["no regression in phase/domain/complexity assertions in existing sketch tests"]
  baseline: "0% — no action dimension exists today"
  target: "14/14 action cases pass; existing sketch suite green"
must_haves:
  truths:
    - "SketchStatus enum includes COLLABORATING = 'collaborating' (backend parity with the FE type)"
    - "classify() emits an action in {create_skill, create_rule, create_hook, create_command, create_subagent, improve_primitive, none} on EVERY return path (keyword-early-return at conf>=0.6, cache, LLM, keyword-fallback)"
    - "_keyword_classify scores ACTION_KEYWORDS and returns action + action_confidence"
    - "_llm_classify includes 'action' in the prompt schema and in the required-key set, and accepts provider_kind/model_override that resolve a litellm model STRING via a per-kind default-model map passed to litellm.completion(model=...) (no hard-coded claude-only DEFAULT_LLM_MODEL)"
  artifacts:
    - path: "backend/app/models/sketch.py"
      provides: "SketchStatus.COLLABORATING member"
      contains: "COLLABORATING ="
    - path: "backend/app/services/sketch_routing_service.py"
      provides: "ACTION_KEYWORDS dict + action dimension on classify/_keyword_classify/_llm_classify"
      contains: "ACTION_KEYWORDS"
    - path: "backend/tests/test_sketch_action_classification.py"
      provides: "Per-action keyword + LLM-mocked unit tests"
      contains: "improve_primitive"
  key_links:
    - from: "backend/app/services/sketch_routing_service.py::_llm_classify"
      to: "litellm.completion(model=<string>)"
      via: "provider_kind/model_override resolve a litellm model STRING via a per-kind default-model map (model_override wins, else per-kind default)"
      pattern: "litellm.completion"
    - from: "backend/app/services/sketch_routing_service.py::classify"
      to: "classification['action']"
      via: "action present on all return paths"
      pattern: "\"action\""
---

<objective>
Lay the backend foundation for sketch→primitive routing: add the missing
`SketchStatus.COLLABORATING` enum member and an `action` classification
dimension to `SketchRoutingService` (keyword stage + LLM stage), threaded
with `provider_kind`/`model_override` per the 4-backend house rule.

Purpose: Downstream plans (18-02 routing precedence, 18-03 forge service)
require a reliable `action` field on the classification dict and the
`COLLABORATING` status; without them the primitive_generator branch and the
ambiguity→collaborating gate cannot exist.
Output: Updated `sketch.py` enum, `sketch_routing_service.py` classifier,
new `test_sketch_action_classification.py`.
Research basis: 18-RESEARCH.md §"The classification pipeline" + house rule
`feedback_llm_features_support_all_backends`.
</objective>

<context>
@.planning/milestones/anonymous/phases/18-sketch-primitive-routing/18-RESEARCH.md
@backend/app/models/sketch.py
@backend/app/services/sketch_routing_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1 (RED): Add SketchStatus.COLLABORATING + write failing action-classification tests</name>
  <files>backend/app/models/sketch.py, backend/tests/test_sketch_action_classification.py</files>
  <action>
    1. In `backend/app/models/sketch.py`, add `COLLABORATING = "collaborating"`
       to the `SketchStatus` enum, between `IN_PROGRESS` and `COMPLETED` to
       mirror the frontend type ordering. This is the #1 trap from RESEARCH —
       `UpdateSketchRequest.status: Optional[SketchStatus]` rejects
       "collaborating" until this exists. Bug-class sweep: grep every
       `SketchStatus` consumer and confirm no exhaustive match/validation
       breaks (RESEARCH notes status is written as plain strings in routes, so
       low risk — confirm, don't refactor).
    2. Create `backend/tests/test_sketch_action_classification.py` using the
       `isolated_db` fixture. Write:
       - One assertion `SketchStatus.COLLABORATING.value == "collaborating"`.
       - 7 keyword-stage tests (one per action member: create_skill, create_rule,
         create_hook, create_command, create_subagent, improve_primitive, none),
         each feeding a representative sketch text to `_keyword_classify` (or
         `classify`) and asserting `result["action"]` equals the expected member
         and an `action_confidence` (or `confidence` contribution) is present.
       - 7 LLM-mocked tests: monkeypatch the LLM call inside `_llm_classify`
         (the `litellm.completion` path) to return
         a canned JSON string containing `"action": "<member>"`, and assert
         `classify(...)["action"]` equals it. Mirror the takeaway-extractor test
         mocking approach (monkeypatch at module namespace so the service's own
         import is patched).
    These tests MUST fail now (no ACTION_KEYWORDS, action not in classify output).
  </action>
  <verify>
    cd backend && uv run pytest tests/test_sketch_action_classification.py -q
    Expect: the COLLABORATING assertion passes; all action tests FAIL (RED).
    (Level 1: Sanity)
  </verify>
  <done>SketchStatus.COLLABORATING exists; test file written with 14 action cases that currently fail because the action dimension is not implemented.</done>
</task>

<task type="auto">
  <name>Task 2 (GREEN): Implement ACTION_KEYWORDS + action dimension in keyword and LLM stages</name>
  <files>backend/app/services/sketch_routing_service.py</files>
  <action>
    1. Add a module-level `ACTION_KEYWORDS: dict[str, list[str]]` alongside the
       existing PHASE_KEYWORDS/DOMAIN_KEYWORDS/COMPLEXITY_SIGNALS dicts, with
       phrase lists per action, e.g.:
       - improve_primitive: ["improve", "update the skill", "fix the rule",
         "enhance the hook", "tweak the command", "refine the"]
       - create_skill: ["create a skill", "new skill", "add a skill"]
       - create_rule: ["create a rule", "new rule", "add a rule"]
       - create_hook: ["create a hook", "new hook", "add a hook"]
       - create_command: ["create a command", "new command", "slash command"]
       - create_subagent: ["create a subagent", "new subagent", "new agent"]
       (none = default when no action phrase matches)
    2. In `_keyword_classify`, score ACTION_KEYWORDS the same way as the other
       dicts; emit `action` (best-scoring member or "none") and an
       `action_confidence` in the returned dict.
    3. In `classify`, surface `action` (+ its confidence) on ALL return paths —
       the keyword early-return at conf>=KEYWORD_CONFIDENCE_THRESHOLD, the cache
       path, the LLM path, and the keyword fallback. Default to "none" if absent.
    4. In `_llm_classify`, add `action` to the system prompt schema (enumerate the
       7 allowed values) and add "action" to the `required` set; on validation,
       coerce an out-of-enum action to "none". Add `provider_kind: str = "anthropic"`
       and `model_override: str | None = None` params (default-preserving). Replace
       the hard-coded module-level `DEFAULT_LLM_MODEL = "openai/claude-sonnet-4..."`
       with a per-kind litellm-model-string map, e.g.
       `_LLM_MODELS: dict[str, str] = {"anthropic": "...", "claude": "...",
       "openai": "...", "gemini": "...", "google": "...", ...}`, and resolve the
       model STRING as `model_override or _LLM_MODELS.get(provider_kind, <safe
       default>)` (the `get_setting("sketch_llm_model")` override still wins if
       set). Pass THAT string into `litellm.completion(model=...)` — do NOT use
       `resolve_llm_cmd` (it returns a subprocess argv template, incompatible with
       `litellm.completion(model=...)`). Per house rule
       `feedback_llm_features_support_all_backends`, never default claude-only —
       the model string flows from provider_kind through the per-kind map. Thread
       `provider_kind`/`model_override` up through `classify(...)` as optional
       kwargs so callers can override.
    Keep the litellm fallback behavior intact for the non-LLM path; tests
    monkeypatch the `litellm.completion` call, so the existing in-method
    `import litellm` (or a module-namespace import) is what tests patch.
  </action>
  <verify>
    cd backend && uv run pytest tests/test_sketch_action_classification.py -q
    AND cd backend && uv run pytest tests/test_sketch_routing_scoped.py -q
    Expect: all 14 action cases GREEN; existing routing tests still pass.
    (Level 1: Sanity)
  </verify>
  <done>classify() emits a valid action on every return path; _llm_classify carries action in prompt+required and resolves a litellm model STRING from provider_kind/model_override via a per-kind model map passed to litellm.completion(model=...); all action tests pass; no regression in existing sketch routing tests.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): `uv run pytest tests/test_sketch_action_classification.py tests/test_sketch_routing_scoped.py -q` — 14 action cases pass, COLLABORATING assertion passes, no regressions.
Level 3 (Deferred): full backend suite under watchdog (run at phase close, 18-04).
</verification>

<success_criteria>
- SketchStatus.COLLABORATING == "collaborating".
- classify() returns action ∈ the 7-member set on all paths (keyword early-return, cache, LLM, fallback).
- _llm_classify resolves a litellm model STRING from provider_kind/model_override via a per-kind model map (passed to litellm.completion(model=...)) — no hard-coded claude model, no resolve_llm_cmd.
- 14/14 action tests pass; existing sketch routing suite green.
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/18-sketch-primitive-routing/18-01-SUMMARY.md`.
</output>
