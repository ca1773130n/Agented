---
phase: 18-sketch-primitive-routing
plan: "02"
type: tdd
wave: 2
depends_on: ["18-01"]
autonomous: true
verification_level: sanity
files_modified:
  - backend/app/services/sketch_routing_service.py
  - backend/tests/test_sketch_primitive_routing.py
provides:
  - "backend/app/services/sketch_routing_service.py:SketchRoutingService.route[primitive_generator]"
requires:
  - "backend/app/services/sketch_routing_service.py:SketchRoutingService.classify[action]"
  - "backend/app/services/sketch_routing_service.py:ACTION_KEYWORDS"
integration_points:
  - "backend/app_litestar/routes/leaf_crud_g.py:route_sketch"
hypothesis: "Inserting a primitive_generator branch at the very top of route() — before any DB load — gated at confidence >= 0.6 on a primitive action will make primitive routing win precedence over SA/team without altering existing SA/team/none outcomes."
predicted_outcome: "test_sketch_primitive_routing.py passes: a create_<kind> action at conf>=0.6 returns {target_type:'primitive_generator', target_id:<kind>}; improve_primitive returns target_id:'improve'; the same action at conf<0.6 falls through to SA/team/none; an SA-eligible classification with no primitive action still returns super_agent (precedence preserved)."
eval_metrics:
  primary: "routing-precedence test pass rate (target: all cases)"
  secondary: ["no regression in existing route() SA/team/none cases"]
  baseline: "0% — primitive_generator target does not exist; primitive actions unreachable"
  target: "primitive_generator returned for primitive actions at conf>=0.6, ahead of SA/team; conf<0.6 falls through"
must_haves:
  truths:
    - "route() returns target_type 'primitive_generator' with target_id=kind for create_<kind> actions when confidence >= 0.6, evaluated BEFORE SA/team/none resolution"
    - "improve_primitive at conf>=0.6 returns target_type 'primitive_generator' with target_id 'improve' (kind resolved later by the forge service)"
    - "a primitive action below 0.6 confidence falls through to the existing SA/team/none logic unchanged"
    - "non-primitive classifications (action 'none') route exactly as before (SA/team/none precedence preserved)"
  artifacts:
    - path: "backend/app/services/sketch_routing_service.py"
      provides: "primitive_generator precedence branch at top of route()"
      contains: "primitive_generator"
    - path: "backend/tests/test_sketch_primitive_routing.py"
      provides: "Routing-precedence tests (create kinds, improve, sub-threshold fallthrough, SA-precedence)"
      contains: "primitive_generator"
  key_links:
    - from: "backend/app/services/sketch_routing_service.py::route"
      to: "classification['action'] / classification['confidence']"
      via: "top-of-route gate on primitive action + confidence>=0.6"
      pattern: "primitive_generator"
---

<objective>
Give `SketchRoutingService.route()` a `primitive_generator` target that wins
precedence over super-agent/team resolution when the classification carries a
primitive `action` at confidence >= 0.6.

Purpose: Success criterion #2 — primitive actions must route to the forge
pipeline ahead of SA/team. This is the dispatch fork 18-03's
`PrimitiveForgeService` hangs off of.
Output: Updated `route()` in `sketch_routing_service.py` + new
`test_sketch_primitive_routing.py`.
Research basis: 18-RESEARCH.md §"The routing precedence (where
primitive_generator slots in)".
</objective>

<context>
@.planning/milestones/anonymous/phases/18-sketch-primitive-routing/18-RESEARCH.md
@backend/app/services/sketch_routing_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1 (RED): Write routing-precedence tests for primitive_generator</name>
  <files>backend/tests/test_sketch_primitive_routing.py</files>
  <action>
    Create `backend/tests/test_sketch_primitive_routing.py` (isolated_db fixture).
    Build classification dicts directly (no DB classify needed) and call
    `SketchRoutingService.route(classification, project_id=...)`. Cases:
    1. action="create_rule", confidence=0.7 → assert target_type ==
       "primitive_generator", target_id == "rule", and "action" key == "create_rule".
    2. One case per remaining create kind (create_hook→"hook",
       create_command→"command", create_subagent→"subagent", create_skill→"skill")
       at conf 0.7 → target_id == kind.
    3. action="improve_primitive", confidence=0.8 → target_type ==
       "primitive_generator", target_id == "improve".
    4. action="create_rule", confidence=0.5 (below 0.6) → target_type is NOT
       "primitive_generator" (falls through to SA/team/none).
    5. Precedence: a classification that WOULD route to super_agent (e.g.
       phase="research"/"planning") but with action="none" → still returns
       target_type "super_agent" (primitive branch does not hijack non-primitive
       classifications).
    Map create_<kind> → <kind>: create_rule→rule, create_hook→hook,
    create_command→command, create_subagent→subagent, create_skill→skill.
    These MUST fail now (no primitive branch in route()).
  </action>
  <verify>
    cd backend && uv run pytest tests/test_sketch_primitive_routing.py -q
    Expect: FAIL (RED) — route() has no primitive_generator branch yet.
    (Level 1: Sanity)
  </verify>
  <done>Test file written; precedence + threshold + fallthrough cases fail because the branch is unimplemented.</done>
</task>

<task type="auto">
  <name>Task 2 (GREEN): Insert primitive_generator precedence branch at top of route()</name>
  <files>backend/app/services/sketch_routing_service.py</files>
  <action>
    In `route(cls, classification, project_id=None)`, immediately after the
    `phase`/`domains` extraction (before any DB load / SA/team logic), insert:
    - Read `action = classification.get("action", "none")` and
      `confidence = classification.get("confidence", 0.0)`.
    - Define the primitive action set {create_skill, create_rule, create_hook,
      create_command, create_subagent, improve_primitive}.
    - If `action` is in that set AND `confidence >= 0.6`:
        - For create_<kind>: target_id = kind (strip the "create_" prefix).
        - For improve_primitive: target_id = "improve" (kind resolved later in
          PrimitiveForgeService per RESEARCH Open Question 3).
        - return {"target_type": "primitive_generator", "target_id": target_id,
          "reason": <human-readable>, "action": action}.
    - Otherwise fall through to the existing SA/team/review/psa/none logic
      UNCHANGED.
    Use a module-level constant for the 0.6 gate (reuse/align with
    KEYWORD_CONFIDENCE_THRESHOLD or a new PRIMITIVE_CONFIDENCE_THRESHOLD = 0.6).
    Do not touch the existing branch bodies.
  </action>
  <verify>
    cd backend && uv run pytest tests/test_sketch_primitive_routing.py tests/test_sketch_routing_scoped.py tests/test_sketch_instance_routing.py -q
    Expect: all primitive-routing cases GREEN; existing routing tests still pass.
    (Level 1: Sanity)
  </verify>
  <done>route() returns primitive_generator with target_id=kind (or "improve") for primitive actions at conf>=0.6 ahead of SA/team; sub-threshold and non-primitive classifications route exactly as before.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): `uv run pytest tests/test_sketch_primitive_routing.py tests/test_sketch_routing_scoped.py tests/test_sketch_instance_routing.py -q` — precedence/threshold/fallthrough pass, no regressions.
</verification>

<success_criteria>
- route() returns {target_type:"primitive_generator", target_id:kind} for create_<kind> at conf>=0.6.
- improve_primitive → target_id "improve".
- conf<0.6 and action "none" fall through to unchanged SA/team/none behavior.
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/18-sketch-primitive-routing/18-02-SUMMARY.md`.
</output>
