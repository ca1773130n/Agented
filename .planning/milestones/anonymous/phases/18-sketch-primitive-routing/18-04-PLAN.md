---
phase: 18-sketch-primitive-routing
plan: "04"
type: implement
wave: 4
depends_on: ["18-03"]
autonomous: true
verification_level: proxy
files_modified:
  - frontend/src/services/api/types/sketches.ts
  - frontend/src/services/api/sketches.ts
  - frontend/src/components/sketches/SketchRouting.vue
  - frontend/src/composables/useSketchChat.ts
  - frontend/src/locales/en.json
  - frontend/src/locales/ko.json
  - frontend/src/locales/ja.json
  - frontend/src/locales/zh.json
  - frontend/src/components/sketches/__tests__/SketchRouting.spec.ts
provides:
  - "frontend/src/services/api/types/sketches.ts:SketchPrimitiveOutcome"
  - "frontend/src/services/api/sketches.ts:sketchApi.undoPrimitive"
  - "frontend/src/components/sketches/SketchRouting.vue:primitive_generator-card"
  - "frontend/src/locales/en.json:sketchPrimitiveOutcome"
requires:
  - "backend/app/services/primitive_forge_service.py:PrimitiveForgeService"
  - "backend/app_litestar/routes/leaf_crud_g.py:route_sketch[primitive_generator]"
  - "backend/app_litestar/routes/leaf_crud_g.py:undo_sketch_primitive"
integration_points:
  - "backend/app/models/sketch.py:SketchStatus.COLLABORATING"
  - "frontend/src/composables/useSketchChat.ts:collaborating-branch"
hypothesis: "Adding a SketchPrimitiveOutcome type, a primitive_generator branch in SketchRouting.vue (kind/name/diff/bound-projects + undo button), a useSketchChat handler, an undoPrimitive API method, and a key-identical sketchPrimitiveOutcome.* namespace across all four locales will render the outcome card and wire one-click undo with no new FE test failures."
predicted_outcome: "SketchRouting.spec.ts passes: the component renders a primitive outcome card (kind, name, diff, bound-projects) when routing.target_type=='primitive_generator', shows an undo button that calls sketchApi.undoPrimitive; the four locale catalogs are key-identical (sketchPrimitiveOutcome.* present in all); npm run test:run shows no NEW failures beyond the 7 known baseline."
eval_metrics:
  primary: "SketchRouting primitive-outcome component test passes; locale parity holds (4 catalogs key-identical)"
  secondary: ["undo button invokes sketchApi.undoPrimitive", "collaborating status renders the clarification question"]
  baseline: "no primitive outcome card; 7 known pre-existing FE failures"
  target: "outcome card + undo render and test green; no NEW FE failures; vue-tsc clean"
must_haves:
  truths:
    - "SketchRouting.vue renders a primitive-created/updated card (kind, name, diff, bound-project list) when routing.target_type == 'primitive_generator'"
    - "the card exposes a one-click undo button that calls sketchApi.undoPrimitive(sketchId) → the backend undo endpoint"
    - "a collaborating outcome renders the clarification question + candidates (handled in useSketchChat)"
    - "sketchPrimitiveOutcome.* i18n keys exist and are key-identical across en/ko/ja/zh"
    - "getTargetIcon/handleTargetClick switch statements handle the primitive_generator case"
  artifacts:
    - path: "frontend/src/services/api/types/sketches.ts"
      provides: "SketchPrimitiveOutcome interface + routing shape extension"
      contains: "SketchPrimitiveOutcome"
    - path: "frontend/src/components/sketches/SketchRouting.vue"
      provides: "primitive_generator outcome card branch + undo button"
      contains: "primitive_generator"
    - path: "frontend/src/services/api/sketches.ts"
      provides: "sketchApi.undoPrimitive method"
      contains: "undoPrimitive"
    - path: "frontend/src/locales/en.json"
      provides: "sketchPrimitiveOutcome namespace"
      contains: "sketchPrimitiveOutcome"
    - path: "frontend/src/locales/ko.json"
      provides: "sketchPrimitiveOutcome namespace (key-identical)"
      contains: "sketchPrimitiveOutcome"
    - path: "frontend/src/locales/ja.json"
      provides: "sketchPrimitiveOutcome namespace (key-identical)"
      contains: "sketchPrimitiveOutcome"
    - path: "frontend/src/locales/zh.json"
      provides: "sketchPrimitiveOutcome namespace (key-identical)"
      contains: "sketchPrimitiveOutcome"
    - path: "frontend/src/components/sketches/__tests__/SketchRouting.spec.ts"
      provides: "primitive outcome card render + undo test"
      contains: "primitive_generator"
  key_links:
    - from: "frontend/src/components/sketches/SketchRouting.vue"
      to: "frontend/src/services/api/types/sketches.ts:SketchPrimitiveOutcome"
      via: "typed routing.outcome render"
      pattern: "SketchPrimitiveOutcome|primitive_generator"
    - from: "frontend/src/components/sketches/SketchRouting.vue"
      to: "frontend/src/services/api/sketches.ts:sketchApi.undoPrimitive"
      via: "undo button click handler"
      pattern: "undoPrimitive"
    - from: "frontend/src/services/api/sketches.ts:sketchApi.undoPrimitive"
      to: "backend/app_litestar/routes/leaf_crud_g.py:undo_sketch_primitive"
      via: "POST /admin/sketches/{id}/primitive/undo"
      pattern: "primitive/undo"
---

<objective>
Render the "primitive created/updated" outcome card in the sketch panel with
one-click undo, and add the `sketchPrimitiveOutcome.*` i18n namespace across all
four locales.

Purpose: Success criteria #5, #9 — the panel surfaces the primitive outcome
(kind, name, diff, bound-project list) with a working undo, and the
`collaborating` clarification renders. Phase 18 is the first phase to touch the
frontend (Phase 17 deferred FE work), so this plan delivers it.
Output: `SketchPrimitiveOutcome` type + routing extension, the
`primitive_generator` card in `SketchRouting.vue`, the `useSketchChat.ts`
handler, `sketchApi.undoPrimitive`, four updated locale catalogs, and a
component test.
Research basis: 18-RESEARCH.md §"The frontend panel" + §"Frontend types + API
client"; house rule i18n 4-locale parity.
</objective>

<context>
@.planning/milestones/anonymous/phases/18-sketch-primitive-routing/18-RESEARCH.md
@frontend/src/components/sketches/SketchRouting.vue
@frontend/src/composables/useSketchChat.ts
@frontend/src/services/api/sketches.ts
@frontend/src/services/api/types/sketches.ts
@frontend/src/locales/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types + API method + i18n namespace (4 locales)</name>
  <files>frontend/src/services/api/types/sketches.ts, frontend/src/services/api/sketches.ts, frontend/src/locales/en.json, frontend/src/locales/ko.json, frontend/src/locales/ja.json, frontend/src/locales/zh.json</files>
  <action>
    1. types/sketches.ts — add a `SketchPrimitiveOutcome` interface:
       `{ kind: string; name: string; asset_id: string|number; binding_id?: string|number;
          diff?: string; bound_projects: string[]; undo: { op: 'create'|'improve';
          kind: string; asset_id: string|number; binding_id?: string|number;
          old_content?: string } }`. Extend the routing shape so
       `target_type: 'primitive_generator'` carries `outcome?: SketchPrimitiveOutcome`
       and the collaborating fields `question?`/`candidates?`. (`SketchStatus` already
       includes 'collaborating' on the FE — no change needed there.)
    2. sketches.ts — add `undoPrimitive(sketchId: string)` to `sketchApi`, POSTing
       to `/admin/sketches/${sketchId}/primitive/undo` via the shared apiFetch.
    3. Locales — add a `sketchPrimitiveOutcome` namespace to en.json with keys:
       `title.created`, `title.updated`, `kind`, `name`, `diff`, `boundProjects`,
       `undo`, `undone`, `clarify` (the collaborating prompt). Replicate the SAME
       key set into ko.json / ja.json / zh.json with translated values — keys MUST
       be identical across all four (house rule; code/paths never translated).
  </action>
  <verify>
    cd frontend && npx vue-tsc --noEmit  (types compile)
    AND a key-parity check: the sketchPrimitiveOutcome key set is identical across
    en/ko/ja/zh. (Level 2: Proxy)
  </verify>
  <done>SketchPrimitiveOutcome type + routing extension compile; sketchApi.undoPrimitive exists; sketchPrimitiveOutcome.* present and key-identical in all four locales.</done>
</task>

<task type="auto">
  <name>Task 2: SketchRouting.vue outcome card + undo + useSketchChat handler + component test</name>
  <files>frontend/src/components/sketches/SketchRouting.vue, frontend/src/composables/useSketchChat.ts, frontend/src/components/sketches/__tests__/SketchRouting.spec.ts</files>
  <action>
    1. SketchRouting.vue — add a `v-else-if="routing.target_type === 'primitive_generator'"`
       block rendering the outcome card: kind, name, a diff display (from
       `routing.outcome.diff`, monospace), the bound-projects list, and an "Undo"
       button bound to a handler that calls `sketchApi.undoPrimitive(sketchId)` and
       emits the result. Use the `sketchPrimitiveOutcome.*` i18n keys via useI18n
       (title.created vs title.updated keyed on undo.op). Add a `primitive_generator`
       case to the `getTargetIcon` and `handleTargetClick` switch statements so the
       existing icon/click logic does not fall through.
    2. useSketchChat.ts — add a `primitive_generator` branch alongside the existing
       `sketch.status === 'collaborating'` (:278) and `target_type === 'none'` (:299)
       handling: push an outcome message carrying routing.outcome (or the
       collaborating question/candidates) and expose an undo action wired to
       sketchApi.undoPrimitive.
    3. Test — create
       `frontend/src/components/sketches/__tests__/SketchRouting.spec.ts` (Vitest +
       happy-dom + @vue/test-utils): mount SketchRouting with a
       `routing.target_type='primitive_generator'` + outcome fixture; assert kind,
       name, diff, bound-projects render; assert clicking Undo calls a stubbed
       sketchApi.undoPrimitive. Add a collaborating fixture case asserting the
       clarification question renders.
  </action>
  <verify>
    cd frontend && npm run test:run -- SketchRouting
    AND cd frontend && npm run build  (vue-tsc + vite)
    Expect: new component test green; no NEW failures beyond the 7 known baseline;
    build clean. (Level 2: Proxy)
  </verify>
  <done>SketchRouting.vue renders the primitive outcome card (kind/name/diff/bound-projects) with a working undo button; useSketchChat handles the primitive_generator + collaborating branches; component test passes; build clean; no new FE failures.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full sketch→primitive pipeline (18-01 → 18-04): action classification, primitive_generator routing, PrimitiveForgeService create/improve/undo, and the panel outcome card with undo.</what-built>
  <how-to-verify>
    DOGFOOD (success criterion #6, Tier-3 deferred eval — run against the LIVE
    pipeline, not unit fixtures; per CLAUDE.md "dogfood new pipelines against live
    data"):
    1. `just dev-backend` + `just dev-frontend`; open the sketch panel.
    2. Submit >= 3 real sketches through the live pipeline, including at least one
       create (e.g. "create a rule that ...") and at least one improve (e.g.
       "improve the <bound primitive> to ...").
    3. Confirm: the outcome card shows kind/name/diff/bound-projects; the created
       primitive's row + binding + .claude/ file exist; one-click undo reverts
       (binding+asset removed for create / content reverted for improve); an
       ambiguous improve reference yields the collaborating clarification.
    4. House gates: `just build`; `cd backend && uv run pytest` under the ~12-min
       watchdog (on hang, run the targeted set: test_sketch_action_classification,
       test_sketch_primitive_routing, test_primitive_forge_service,
       tests/routes/test_sketch_primitive_routes + execution/streaming/harness
       regressions — disclose the substitution); `cd frontend && npm run test:run`
       (no NEW failures beyond the 7 baseline).
  </how-to-verify>
  <resume-signal>Type "approved" once >=3 live sketches routed (>=1 create, >=1 improve) with a visible outcome card + working undo, and all three house gates pass; or describe failures.</resume-signal>
</task>

</tasks>

<verification>
Level 2 (Proxy): `npm run test:run -- SketchRouting` + `npm run build` — component test green, vue-tsc clean, no new FE failures; locale key-parity holds.
Level 3 (Deferred): >=3 live-pipeline sketches (>=1 create, >=1 improve) with outcome card + undo; full house gates (just build / pytest watchdog / FE no-new-failures).
</verification>

<success_criteria>
- SketchRouting.vue renders the primitive outcome card (kind/name/diff/bound-projects) for target_type 'primitive_generator' with a working undo button → sketchApi.undoPrimitive.
- collaborating clarification renders via useSketchChat.
- sketchPrimitiveOutcome.* key-identical across en/ko/ja/zh.
- Build clean; no NEW FE failures; >=3 live sketches dogfooded with create + improve + undo.
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/18-sketch-primitive-routing/18-04-SUMMARY.md`.
Log the dogfood run (sketches routed, created/improved primitives, undo results) to `.planning/experiments/`.
</output>
