---
phase: 06-navigation-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/tour/TourProgressBar.vue
  - frontend/src/components/tour/TourOverlay.vue
  - frontend/src/components/tour/__tests__/TourProgressBar.test.ts
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "Progress bar left section shows step title (from step.title prop) above the step counter"
    - "Skipping a step with skipNeedsConfirm=true shows an inline confirmation ('Skip X? You can complete this later.') with Confirm Skip / Cancel buttons"
    - "Skipping a step with skipNeedsConfirm=false (or skip confirmation cancelled) emits skip/cancel without confirmation dialog"
    - "Step title is passed from TourOverlay down to TourProgressBar so App.vue does not need changes"
    - "All existing TourProgressBar tests still pass; new tests cover title rendering, confirmation flow, and cancel"
  artifacts:
    - path: "frontend/src/components/tour/TourProgressBar.vue"
      provides: "Updated progress bar with stepTitle prop and skip confirmation"
      contains: "skipNeedsConfirm"
    - path: "frontend/src/components/tour/__tests__/TourProgressBar.test.ts"
      provides: "Tests for new props and confirmation behavior"
      contains: "skipNeedsConfirm"
  key_links:
    - from: "frontend/src/components/tour/TourOverlay.vue"
      to: "frontend/src/components/tour/TourProgressBar.vue"
      via: ":step-title and :skip-needs-confirm props"
      pattern: "step-title|stepTitle"
    - from: "frontend/src/App.vue"
      to: "frontend/src/components/tour/TourOverlay.vue"
      via: "existing :step prop (step.title already present in StepLike)"
      pattern: "tourStep"
---

<objective>
Add step title display and skip confirmation to the bottom navigation bar.

Purpose: OB-29 requires the step label (title) visible in the progress bar left section. OB-30 requires substep context labels. OB-31 requires that skipping meaningful steps (backends, etc.) shows a confirmation dialog before the skip fires.

Output: Updated TourProgressBar.vue with stepTitle prop and skip confirmation inline state; updated TourOverlay.vue to pass stepTitle down; new test cases covering the expanded surface.

The progress bar already has: step counter, substep label, Skip/Next buttons. Missing: step title text in the left section, and skip confirmation modal logic.
</objective>

<execution_context>
@/Users/neo/.claude-personal2/plugins/cache/claude-plugin-marketplace/grd/0.3.15/references/execute-plan.md
@/Users/neo/.claude-personal2/plugins/cache/claude-plugin-marketplace/grd/0.3.15/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/components/tour/TourProgressBar.vue
@frontend/src/components/tour/TourOverlay.vue
@frontend/src/components/tour/__tests__/TourProgressBar.test.ts
@frontend/src/App.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add stepTitle prop and title display to TourProgressBar</name>
  <files>frontend/src/components/tour/TourProgressBar.vue, frontend/src/components/tour/TourOverlay.vue</files>
  <action>
    In TourProgressBar.vue:
    - Add `stepTitle: string` to the props definition (required, alongside existing props)
    - In the template's `.tour-bar-left` section, add a `<span class="tour-step-title">{{ stepTitle }}</span>` above the existing `.tour-step-counter` span
    - Style `.tour-step-title`: font-size 13px, font-weight 600, color var(--text-primary), white-space nowrap, display block, margin-bottom 2px
    - The layout of `.tour-bar-left` should now show: title on top, then counter + substep label on the row below — change `.tour-bar-left` to `flex-direction: column; align-items: flex-start; gap: 2px` and move the counter+substep into their own inner `<div class="tour-bar-meta">` with `display: flex; align-items: center; gap: 8px`

    In TourOverlay.vue:
    - Add `stepTitle` to TourProgressBar's binding: `:step-title="step.title"` (step.title already exists on the StepLike interface and is always provided)
    - The StepLike interface already has `title: string` — no interface change needed

    Ensure no hardcoded color values (hex, rgba()) appear in the new style additions — use only CSS custom properties.
  </action>
  <verify>
    cd /Users/neo/Developer/Workspaces/projects/Agented && just build 2>&1 | tail -5
    Check: zero TypeScript errors, build succeeds (Level 1: Sanity)
  </verify>
  <done>
    TourProgressBar renders stepTitle above the step counter in the left section. TourOverlay passes step.title down as step-title. Build passes with zero errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add skip confirmation to TourProgressBar and wire in TourOverlay</name>
  <files>frontend/src/components/tour/TourProgressBar.vue, frontend/src/components/tour/TourOverlay.vue, frontend/src/components/tour/__tests__/TourProgressBar.test.ts</files>
  <action>
    In TourProgressBar.vue:
    - Add `skipNeedsConfirm: boolean` prop (default false)
    - Add local `ref<boolean> confirmingSkip = ref(false)` state
    - Change skip button behavior: if skipNeedsConfirm is true, clicking Skip sets `confirmingSkip.value = true` (does NOT emit skip yet)
    - When confirmingSkip is true, show an inline confirmation row replacing the action buttons:
      ```html
      <div v-if="confirmingSkip" class="tour-skip-confirm">
        <span class="tour-skip-confirm-text">Skip this step? You can complete it later from the setup checklist.</span>
        <button class="tour-confirm-skip-btn" @click="onConfirmSkip">Skip anyway</button>
        <button class="tour-cancel-skip-btn" @click="confirmingSkip = false">Keep going</button>
      </div>
      <div v-else class="tour-actions">
        <!-- existing Skip + Next buttons -->
      </div>
      ```
    - `onConfirmSkip()` sets confirmingSkip.value = false and emits 'skip'
    - If skipNeedsConfirm is false, skip button still emits 'skip' directly (unchanged behavior)
    - Reset `confirmingSkip` to false whenever the `skippable` or `stepTitle` props change (watch) — prevents stale confirmation state when step changes
    - Style `.tour-skip-confirm`: display flex, align-items center, gap 8px, flex-shrink 0
    - Style `.tour-skip-confirm-text`: font-size 12px, color var(--text-secondary)
    - Style `.tour-confirm-skip-btn`: same style as existing `.tour-skip-btn` but with accent-crimson border
    - Style `.tour-cancel-skip-btn`: same style as existing `.tour-next-btn`
    - No hardcoded colors — use var(--accent-crimson), var(--border-default), etc.

    In TourOverlay.vue:
    - Pass `:skip-needs-confirm="step.skippable && isSignificantStep(step)"` to TourProgressBar
    - Add helper function `isSignificantStep(step: StepLike): boolean` that returns true when `step.title` is 'AI Backend Accounts' (backends step) — this matches the TOUR_STEP_META for backends.* states. For Phase 9 product/project steps, this function can be extended. Keep it simple: check step.title against a hardcoded set ['AI Backend Accounts'].
    - The `isSignificantStep` function should live as a plain function inside TourOverlay's script, not exported.

    In TourProgressBar.test.ts:
    - Add test: 'shows step title in left section' — mount with stepTitle='Workspace Directory', expect .tour-step-title text
    - Add test: 'skip without confirmation emits skip directly' — skipNeedsConfirm=false, skippable=true, click skip, expect emitted skip
    - Add test: 'skip with confirmation shows confirm row first' — skipNeedsConfirm=true, skippable=true, click skip, expect .tour-skip-confirm visible and no skip emitted
    - Add test: 'confirm skip emits skip after confirmation' — confirmingSkip state, click 'Skip anyway', expect skip emitted
    - Add test: 'cancel skip hides confirm row' — click 'Keep going', expect .tour-skip-confirm gone, no skip emitted
    - Update baseProps to include stepTitle: 'Configure Workspace'

    Note: The existing tests use baseProps without stepTitle — add stepTitle to baseProps to fix them all in one shot.
  </action>
  <verify>
    cd /Users/neo/Developer/Workspaces/projects/Agented && cd frontend && npm run test:run -- TourProgressBar 2>&1 | tail -20
    Check: all TourProgressBar tests pass, including new ones (Level 2: Proxy)
  </verify>
  <done>
    Skip confirmation renders for significant steps. Direct skip works for trivial steps. confirmingSkip resets on step change. All TourProgressBar tests pass. Build passes.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): `just build` passes — zero TypeScript errors, TourProgressBar and TourOverlay compile cleanly with new props.

Level 2 (Proxy): `cd frontend && npm run test:run -- TourProgressBar` — all tests pass, including new stepTitle and skip confirmation tests.

Manual check (deferred to Phase 10 E2E): Skip confirmation appears when skipping "AI Backend Accounts" step. No confirmation for workspace step (non-skippable). Progress bar left shows step title correctly.
</verification>

<success_criteria>
- TourProgressBar shows stepTitle above step counter in left section
- Skip confirmation appears for AI Backend Accounts steps (skipNeedsConfirm=true)
- Direct skip works for all other skippable steps
- confirmingSkip state resets when step changes — no stale dialogs
- All tests pass, build is clean
</success_criteria>

<output>
After completion, create `.planning/milestones/anonymous/phases/06-navigation-controls/06-01-SUMMARY.md`
</output>
