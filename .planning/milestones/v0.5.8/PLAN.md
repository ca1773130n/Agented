# v0.5.8 — path-Y second cluster

5 more chat-bearing pages migrated from Agented's local
`AiChatPanel.vue` to consuming `AiChatPanelManaged` from
`@ai-accounts/vue-styled`. Same single-line import swap as v0.5.7.

After v0.5.8: 9 of 11 path-Y migrations complete. v0.5.9 finishes
the last 2 (smart-chat fallback callers) and runs the cleanup tail.
