---
phase: 07-workflow-automation-and-pipeline-intelligence
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - frontend/src/components/workflow/nodes/ApprovalGateNode.vue
  - frontend/src/components/workflow/nodes/ConditionalNode.vue
  - frontend/src/components/workflow/WorkflowCanvas.vue
  - frontend/src/components/workflow/NodeConfigPanel.vue
  - frontend/src/components/workflow/WorkflowSidebar.vue
  - frontend/src/components/workflow/ApprovalModal.vue
  - frontend/src/composables/useWorkflowCanvas.ts
  - frontend/src/composables/useWorkflowExecution.ts
  - frontend/src/composables/useWorkflowValidation.ts
  - frontend/src/services/api/workflows.ts
  - frontend/src/types/workflow.ts
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "ApprovalGateNode.vue renders as a distinct visual node with pause icon, timeout display, and approve/reject status indicator"
    - "Approval gate node type is draggable from WorkflowSidebar, droppable onto canvas, and saveable as part of the DAG"
    - "NodeConfigPanel shows approval-gate-specific fields: timeout (seconds), notification message, required approver"
    - "ConditionalNode config panel supports expression-based conditions with a text input for the expression string"
    - "WorkflowCanvas registers approval_gate in nodeTypes with markRaw()"
    - "useWorkflowExecution handles pending_approval SSE events and surfaces approval status in the execution monitor"
    - "ApprovalModal allows users to approve or reject a pending approval gate with optional comment"
    - "Workflow API client has approveNode() and rejectNode() methods"
    - "Frontend build (vue-tsc + vite) completes with zero type errors"
  artifacts:
    - path: "frontend/src/components/workflow/nodes/ApprovalGateNode.vue"
      provides: "Visual node component for approval gate with status indicators and handles"
    - path: "frontend/src/components/workflow/ApprovalModal.vue"
      provides: "Modal dialog for approving/rejecting workflow approval gates"
    - path: "frontend/src/components/workflow/WorkflowCanvas.vue"
      provides: "Updated canvas with approval_gate registered in nodeTypes"
    - path: "frontend/src/components/workflow/NodeConfigPanel.vue"
      provides: "Config forms for approval_gate and enhanced conditional expression"
    - path: "frontend/src/services/api/workflows.ts"
      provides: "API methods for approve/reject/pending-approvals endpoints"
    - path: "frontend/src/composables/useWorkflowExecution.ts"
      provides: "SSE handling for pending_approval status events"
  key_links:
    - from: "frontend/src/components/workflow/WorkflowCanvas.vue"
      to: "frontend/src/components/workflow/nodes/ApprovalGateNode.vue"
      via: "nodeTypes registration with markRaw()"
      pattern: "approval_gate.*markRaw|ApprovalGateNode"
    - from: "frontend/src/services/api/workflows.ts"
      to: "backend/app/routes/workflows.py"
      via: "HTTP calls to approve/reject endpoints"
      pattern: "approveNode|rejectNode|pending-approvals"
    - from: "frontend/src/composables/useWorkflowExecution.ts"
      to: "frontend/src/components/workflow/ApprovalModal.vue"
      via: "Execution state triggers approval modal display"
      pattern: "pending_approval|showApprovalModal"
---

<objective>
Build the frontend components for the new approval gate node type, enhance the conditional node config UI, and wire up the approval workflow UX (modal, SSE events, API calls).

Purpose: Completes the user-facing side of WF-01 (visual DAG builder with new node types), WF-02 (conditional expression config UI), and WF-03 (approval gate UX). Users need to drag approval gates onto the canvas, configure conditions with expressions, and approve/reject gates during execution.

Output: New ApprovalGateNode.vue component, enhanced ConditionalNode config, ApprovalModal, updated canvas registration, and API client methods for approve/reject.

Research basis: Research confirms VueFlow provides everything needed for custom nodes, handles, and DnD -- no new npm dependencies required (07-RESEARCH.md). New node types must be registered with `markRaw()` following the existing pattern (research Pitfall 5).
</objective>

<execution_context>
@.planning/milestones/v0.2.0/phases/07-workflow-automation-and-pipeline-intelligence/07-RESEARCH.md
@.planning/milestones/v0.2.0/phases/07-workflow-automation-and-pipeline-intelligence/07-01-SUMMARY.md
@frontend/src/components/workflow/WorkflowCanvas.vue
@frontend/src/components/workflow/nodes/ConditionalNode.vue
@frontend/src/components/workflow/nodes/TriggerNode.vue
@frontend/src/components/workflow/NodeConfigPanel.vue
@frontend/src/components/workflow/WorkflowSidebar.vue
@frontend/src/composables/useWorkflowExecution.ts
@frontend/src/services/api/workflows.ts
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: ApprovalGateNode component, enhanced ConditionalNode config, and NodeConfigPanel forms</name>
  <files>frontend/src/components/workflow/nodes/ApprovalGateNode.vue, frontend/src/components/workflow/nodes/ConditionalNode.vue, frontend/src/components/workflow/NodeConfigPanel.vue, frontend/src/components/workflow/WorkflowSidebar.vue, frontend/src/types/workflow.ts</files>
  <action>
    **Part A: ApprovalGateNode.vue**

    Create `frontend/src/components/workflow/nodes/ApprovalGateNode.vue` following the exact pattern of existing node components (e.g., `TriggerNode.vue`, `ConditionalNode.vue`).

    Structure:
    - `<script setup lang="ts">` with props: `id: string`, `data: ApprovalGateNodeData`
    - Interface `ApprovalGateNodeData`: `label: string`, `config: Record<string, unknown>`, `executionStatus?: string`
    - Computed: `statusClass` (same pattern as other nodes), `timeoutDisplay` (format timeout_seconds into human-readable "30m", "1h")
    - Template:
      - Outer div: `class="['workflow-node', 'node-approval-gate', statusClass]"`
      - Target Handle (top) -- input from upstream
      - Node header: pause icon (&#x23F8; or similar), label
      - Node body: shows timeout display, and during execution shows status (pending/approved/rejected)
      - Source Handle (bottom) -- output to downstream (only one handle, not branching)
    - Use `<style src="./workflow-node.css"></style>` for shared styles
    - Scoped styles: approval-specific colors (amber/yellow for pending, green for approved, red for rejected)

    Visual design cues:
    - Icon: &#x1F6D1; (stop sign) or &#x23F8; (pause) -- use a text entity, not an emoji
    - Color scheme: amber (#f59e0b) as primary accent
    - Status indicator: colored dot or badge showing pending_approval / approved / rejected

    **Part B: Enhanced ConditionalNode Config**

    The `ConditionalNode.vue` already shows `conditionType` from config. No changes needed to the node component itself -- the expression input goes in NodeConfigPanel.

    **Part C: NodeConfigPanel Additions**

    In `frontend/src/components/workflow/NodeConfigPanel.vue`, add config form sections for:

    1. **approval_gate** type:
       - Timeout field: number input for seconds (default: 1800), with helper text "Time before auto-timeout"
       - Notification message: textarea for custom message shown during approval
       - Required approver: text input (optional, for future use)

    2. **conditional** type enhancement:
       - Add a new condition_type option: "expression" alongside existing "has_text", "exit_code_zero", "contains"
       - When "expression" is selected, show a text input for the expression string
       - Add placeholder text: `pr.lines_changed > 500 and pr.draft == False`
       - Add helper text explaining available operators: ==, !=, >, <, >=, <=, in, not in, and, or

    Follow the existing pattern in NodeConfigPanel for adding type-specific fields. Look at how "command" and "script" types render their config fields and follow that exact pattern.

    **Part D: WorkflowSidebar Update**

    In `frontend/src/components/workflow/WorkflowSidebar.vue`, add the approval_gate node type to the draggable node palette. Follow the existing pattern for how trigger, skill, command, agent, script, conditional, and transform nodes are listed. Use an appropriate icon and label "Approval Gate".

    **Part E: Type Updates**

    In `frontend/src/types/workflow.ts` (or `frontend/src/services/api/types.ts` if that's where workflow types live), ensure the `WorkflowNodeType` type union includes `'approval_gate'`. Check if `WorkflowExecutionStatus` includes `'pending_approval'` and `NodeExecutionStatus` includes `'pending_approval'`. Add if missing.

    AVOID: Do NOT add new npm dependencies. VueFlow and the existing component library provide everything needed. Always use `markRaw()` when registering node types (research Pitfall 5).
  </action>
  <verify>
    cd frontend && npx vue-tsc --noEmit 2>&1 | tail -20 (Level 1: Sanity -- type check passes)

    Verify file exists and has correct structure:
    ls -la frontend/src/components/workflow/nodes/ApprovalGateNode.vue (Level 1: Sanity)

    grep -c "approval_gate" frontend/src/components/workflow/NodeConfigPanel.vue (Level 1: Sanity -- config panel has approval_gate section)
    grep -c "expression" frontend/src/components/workflow/NodeConfigPanel.vue (Level 1: Sanity -- config panel has expression input)
  </verify>
  <done>
    ApprovalGateNode.vue exists with correct VueFlow Handle setup, status indicators, and styling. NodeConfigPanel has approval_gate config form (timeout, message) and enhanced conditional config (expression input). WorkflowSidebar includes approval_gate in the draggable palette. TypeScript types include approval_gate and pending_approval. vue-tsc type check passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Canvas registration, execution SSE handling, approval modal, and API client</name>
  <files>frontend/src/components/workflow/WorkflowCanvas.vue, frontend/src/components/workflow/ApprovalModal.vue, frontend/src/composables/useWorkflowExecution.ts, frontend/src/composables/useWorkflowCanvas.ts, frontend/src/composables/useWorkflowValidation.ts, frontend/src/services/api/workflows.ts</files>
  <action>
    **Part A: Canvas Registration**

    In `frontend/src/components/workflow/WorkflowCanvas.vue`:
    1. Import `ApprovalGateNode` from `'./nodes/ApprovalGateNode.vue'`
    2. Add to `nodeTypes` object: `approval_gate: markRaw(ApprovalGateNode) as NodeTypesObject[string]`
    3. Follow the exact pattern of existing registrations (lines 29-36)

    **Part B: API Client Methods**

    In `frontend/src/services/api/workflows.ts`, add methods to the `workflowExecutionApi` object (or `workflowApi` -- check which object handles execution operations):

    ```typescript
    approveNode: (executionId: string, nodeId: string) =>
      apiFetch<{ message: string }>(`/admin/workflows/executions/${executionId}/nodes/${nodeId}/approve`, { method: 'POST' }),

    rejectNode: (executionId: string, nodeId: string) =>
      apiFetch<{ message: string }>(`/admin/workflows/executions/${executionId}/nodes/${nodeId}/reject`, { method: 'POST' }),

    listPendingApprovals: () =>
      apiFetch<{ approvals: Array<{ execution_id: string; node_id: string; status: string; requested_at: string; timeout_seconds: number }> }>('/admin/workflows/pending-approvals'),
    ```

    **Part C: ApprovalModal.vue**

    Create `frontend/src/components/workflow/ApprovalModal.vue`:
    - Props: `visible: boolean`, `executionId: string`, `nodeId: string`, `nodeLabel: string`
    - Emits: `approve`, `reject`, `close`
    - Template: Modal overlay (follow BaseModal pattern if one exists, otherwise build a simple modal)
      - Header: "Approval Required" with node label
      - Body: Shows node info and optional comment textarea
      - Footer: "Approve" button (green), "Reject" button (red), "Cancel" button
    - On approve click: emit `approve` event, call `workflowExecutionApi.approveNode(executionId, nodeId)`
    - On reject click: emit `reject` event, call `workflowExecutionApi.rejectNode(executionId, nodeId)`
    - Style: consistent with existing modals in the codebase (dark theme, Geist font)

    **Part D: useWorkflowExecution Enhancement**

    In `frontend/src/composables/useWorkflowExecution.ts`, handle the `pending_approval` status:
    1. When SSE sends a node status event with `status: "pending_approval"`, update the reactive node state
    2. Expose a computed `pendingApprovals` that returns nodes currently in pending_approval state
    3. Expose a `showApprovalModal` ref that toggles the ApprovalModal visibility
    4. When a node enters pending_approval, set `showApprovalModal = true` with the relevant node info

    Look at how the existing SSE handler processes `"running"`, `"completed"`, `"failed"` statuses and follow that exact pattern for `"pending_approval"`.

    **Part E: useWorkflowCanvas Enhancement**

    In `frontend/src/composables/useWorkflowCanvas.ts`, ensure the `approval_gate` node type is included in any node type lists, palette definitions, or default config templates. Check how existing node types are enumerated and add approval_gate to those lists.

    **Part F: useWorkflowValidation Enhancement**

    In `frontend/src/composables/useWorkflowValidation.ts`, add validation rules for approval_gate nodes:
    - Approval gate must have at least one input edge (cannot be a root node)
    - Approval gate timeout must be a positive number if specified

    Follow the existing validation pattern for other node types.

    **Part G: Build Verification**

    After all changes, run `cd frontend && npm run build` to verify:
    1. vue-tsc type checking passes (catches type mismatches)
    2. Vite build succeeds (catches import errors, missing files)

    IMPORTANT: Always use `markRaw()` for node type registration (per research Pitfall 5 -- VueFlow requires raw component references, not reactive proxies).
  </action>
  <verify>
    cd frontend && npm run build 2>&1 | tail -10 (Level 1: Sanity -- build succeeds with zero errors)

    grep "approval_gate" frontend/src/components/workflow/WorkflowCanvas.vue (Level 1: Sanity -- registered in nodeTypes)
    grep "approveNode\|rejectNode" frontend/src/services/api/workflows.ts (Level 1: Sanity -- API methods exist)
    grep "pending_approval" frontend/src/composables/useWorkflowExecution.ts (Level 1: Sanity -- SSE handling exists)
    grep -E "showApprovalModal|pendingApprovals" frontend/src/composables/useWorkflowExecution.ts (Level 2: Proxy -- verify that the composable exports showApprovalModal ref and pendingApprovals computed, not just handles the status string)

    cd frontend && npm run test:run 2>&1 | tail -10 (Level 1: Sanity -- existing frontend tests pass)
  </verify>
  <done>
    WorkflowCanvas registers approval_gate node type with markRaw(). ApprovalModal provides approve/reject UX with API integration. useWorkflowExecution handles pending_approval SSE events. API client has approveNode(), rejectNode(), listPendingApprovals() methods. useWorkflowValidation validates approval_gate nodes. Frontend build and tests pass with zero errors.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- ApprovalGateNode.vue file exists with Handle components
- WorkflowCanvas.vue has approval_gate in nodeTypes
- NodeConfigPanel has approval_gate and expression config sections
- Frontend build (vue-tsc + vite) succeeds with zero errors
- Existing frontend tests pass

Level 2 (Proxy):
- Dragging approval_gate from sidebar onto canvas creates a properly rendered node
- Config panel shows correct fields when approval_gate node is selected
- Conditional node config shows expression input when "expression" condition type is selected
- API client methods are callable (verified via grep/import check)
- useWorkflowExecution exposes pendingApprovals computed

Level 3 (Deferred):
- Full E2E: drag approval gate onto canvas, connect edges, save, execute, approve/reject in UI
- Visual regression testing of new node styling
- Multi-user approval flow (one user waits, another approves)
</verification>

<success_criteria>
1. `npm run build` succeeds with zero type errors and zero build errors
2. ApprovalGateNode.vue renders with correct handles, icon, and status styling
3. NodeConfigPanel shows timeout/message fields for approval_gate and expression input for conditional
4. WorkflowCanvas has approval_gate registered with markRaw()
5. API client has approveNode(), rejectNode(), and listPendingApprovals() methods
6. useWorkflowExecution handles pending_approval status from SSE
7. All existing frontend tests pass
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.2.0/phases/07-workflow-automation-and-pipeline-intelligence/07-03-SUMMARY.md`
</output>
