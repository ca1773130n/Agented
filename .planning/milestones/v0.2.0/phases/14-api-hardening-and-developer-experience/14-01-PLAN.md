---
phase: 14-api-hardening-and-developer-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/common.py
  - backend/app/__init__.py
  - backend/app/middleware.py
  - frontend/src/services/api/client.ts
  - backend/tests/test_error_response.py
  - backend/tests/test_request_id.py
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "All Flask error handlers (404, 405, 413, 500, sqlite3.OperationalError) return JSON with code, message, and details fields"
    - "Frontend ApiError reads data.message first, falling back to data.error for backward compatibility"
    - "Rate limiting returns HTTP 429 with Retry-After header when threshold is exceeded"
    - "X-Request-ID header is present on every HTTP response and matches the request_id in structured log output"
    - "Request ID is propagated to execution log records via ExecutionLogService"
  artifacts:
    - path: "backend/app/models/common.py"
      provides: "Extended ErrorResponse model with code/message/details + error_response() helper"
      contains: "error_response"
    - path: "backend/tests/test_error_response.py"
      provides: "Tests for unified error response format"
    - path: "backend/tests/test_request_id.py"
      provides: "Tests for request ID propagation and rate limit 429 response"
  key_links:
    - from: "backend/app/__init__.py"
      to: "backend/app/models/common.py"
      via: "error handlers use error_response() helper"
      pattern: "error_response"
    - from: "frontend/src/services/api/client.ts"
      to: "backend/app/models/common.py"
      via: "ApiError reads message field from ErrorResponse"
      pattern: "data\\.message.*data\\.error"
---

<objective>
Unify all error responses to a consistent ErrorResponse shape (code/message/details), verify rate limiting returns 429 with Retry-After, and extend request ID propagation to execution logs.

Purpose: Establish the foundational error response contract (API-02) that all other endpoints will follow, confirm rate limiting compliance (API-06), and complete request ID tracing (API-07).
Output: Extended ErrorResponse model, error_response() helper, updated Flask error handlers, updated frontend ApiError extraction, rate limit verification tests, request ID propagation tests.
</objective>

<execution_context>
@.planning/milestones/v0.2.0/phases/14-api-hardening-and-developer-experience/14-RESEARCH.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@backend/app/models/common.py
@backend/app/__init__.py
@backend/app/middleware.py
@frontend/src/services/api/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ErrorResponse model and update all Flask error handlers</name>
  <files>backend/app/models/common.py, backend/app/__init__.py, frontend/src/services/api/client.ts, backend/tests/test_error_response.py</files>
  <action>
    1. In `backend/app/models/common.py`:
       - Extend `ErrorResponse` to include `code: str`, `message: str`, `details: Optional[dict]`, `request_id: Optional[str]` fields
       - Keep the legacy `error: str` field for backward compatibility but make it Optional with a default of None
       - Add an `error_response(code: str, message: str, status: HTTPStatus, details: dict = None)` helper function that returns a tuple of `(dict, HTTPStatus)` -- the dict includes `code`, `message`, `error` (set to same value as message for backward compat), `details`, and `request_id` (from `flask.g`)
       - Import `HTTPStatus` from `http`, `Optional` from `typing`, and `g` from `flask`

    2. In `backend/app/__init__.py`:
       - Update ALL registered error handlers (404, 405, 413, 500, sqlite3.OperationalError) to use `error_response()` instead of ad-hoc `{"error": "..."}` dicts
       - Error codes should be machine-readable strings: `NOT_FOUND`, `METHOD_NOT_ALLOWED`, `PAYLOAD_TOO_LARGE`, `INTERNAL_SERVER_ERROR`, `SERVICE_UNAVAILABLE`
       - Import `error_response` from `app.models.common`

    3. In `frontend/src/services/api/client.ts`:
       - Update the `ApiError` class or the error extraction logic in `apiFetch` to read `data.message` first, falling back to `data.error` if `message` is not present
       - This ensures both old-format (`{"error": "..."}`) and new-format (`{"code": "...", "message": "..."}`) responses are handled correctly
       - Locate the line where `data.error` is extracted (approximately line 98) and change to: `data.message || data.error || 'Unknown error'`

    4. Write `backend/tests/test_error_response.py`:
       - Test that `error_response("NOT_FOUND", "Bot not found", HTTPStatus.NOT_FOUND)` returns correct shape with all fields
       - Test that the `error` field matches `message` for backward compat
       - Test the Flask test client against a non-existent route returns 404 with `code`, `message`, and `details` fields
       - Test that 405 (wrong method) returns the new shape
       - Use the `isolated_db` fixture from conftest.py

    AVOID: Do NOT change error returns in individual route handlers yet -- this task only changes the Flask-level error handlers and the model. Individual route handlers will be updated incrementally in future phases.
    AVOID: Do NOT remove the legacy `error` field -- include both `error` and `message` indefinitely for backward compatibility (per 14-RESEARCH.md recommendation).
  </action>
  <verify>
    cd backend && uv run pytest tests/test_error_response.py -v (Level 2: Proxy)
    cd frontend && npm run test:run (Level 1: Sanity -- ensures frontend still compiles and tests pass)
  </verify>
  <done>
    ErrorResponse model has code/message/details/request_id fields, error_response() helper exists, all 5 Flask error handlers return new shape, frontend ApiError reads message||error, all tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify rate limit 429 response and extend request ID to execution logs</name>
  <files>backend/app/middleware.py, backend/tests/test_request_id.py</files>
  <action>
    1. In `backend/app/middleware.py`:
       - In the `after_request` function, ensure `request_id` is set on `flask.g` (it likely already is via the `before_request` hook) so that `error_response()` can access it
       - Add a line to set `g.request_id = request_id_var.get(None)` in `before_request` if not already done (the ContextVar is set but g may not be populated)

    2. Write `backend/tests/test_request_id.py`:
       - Test that every response includes `X-Request-ID` header with a UUID-format value
       - Test that the same request ID appears in the response header
       - Test rate limiting: send requests to an admin endpoint at a rate exceeding 120/minute and verify:
         a. Response status is 429
         b. Response body contains `Retry-After` header (Flask-Limiter sets this automatically)
         c. The 429 response body has the structured error format (this requires the custom 429 handler from Phase 04 to use error_response)
       - Note: Flask-Limiter already has a custom 429 handler registered in create_app(). Verify it uses error_response() format. If it returns bare `{"error": "..."}`, update it to use error_response()
       - Test that request_id appears in structured log output (capture log output and parse JSON)
       - Use the `isolated_db` fixture

    3. If the custom 429 handler in `backend/app/__init__.py` does not use `error_response()`, update it:
       - It should return `error_response("RATE_LIMITED", "Rate limit exceeded", HTTPStatus.TOO_MANY_REQUESTS)`
       - Flask-Limiter automatically adds the `Retry-After` header to 429 responses

    AVOID: Do NOT tighten rate limits -- keep existing 120/minute for admin routes (per 04-02-SUMMARY.md decision).
    AVOID: Do NOT rate-limit SSE stream endpoints more aggressively -- SSE reconnects count against the rate limit (per 14-RESEARCH.md Pitfall 2).
  </action>
  <verify>
    cd backend && uv run pytest tests/test_request_id.py -v (Level 2: Proxy)
    cd backend && uv run pytest (Level 1: Sanity -- full test suite passes)
  </verify>
  <done>
    X-Request-ID header present on all responses, 429 response has Retry-After header and structured error format, request_id available via g.request_id for error_response(), all tests pass including full suite
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- ErrorResponse model imports and instantiates with code/message fields
- error_response() returns a tuple of (dict, HTTPStatus)
- Frontend builds without errors (npm run build)

Level 2 (Proxy):
- Flask error handlers (404, 405, 413, 500) all return code/message/details shape
- 429 response includes Retry-After header
- X-Request-ID header present on every response
- Frontend ApiError correctly extracts message from both old and new error shapes
</verification>

<success_criteria>
- Zero Flask error handlers return bare {"error": "..."} dicts -- all use error_response()
- The 429 rate limit response includes both structured error body and Retry-After header
- X-Request-ID is present on every HTTP response
- Frontend error display continues to work with both old and new error formats
- All backend tests pass, frontend tests pass, frontend builds
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.2.0/phases/14-api-hardening-and-developer-experience/14-01-SUMMARY.md`
</output>
