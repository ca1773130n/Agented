---
phase: 14-api-hardening-and-developer-experience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/routes/executions.py
  - backend/app/routes/triggers.py
  - backend/app/routes/workflows.py
  - backend/app/routes/backends.py
  - backend/app/routes/monitoring.py
  - backend/app/routes/audit.py
  - backend/app/routes/sketches.py
  - backend/app/routes/grd.py
  - backend/app/routes/super_agents.py
  - backend/app/routes/rotation.py
  - backend/app/routes/plugin_exports.py
  - backend/app/routes/budgets.py
  - backend/app/routes/product_owner.py
  - backend/app/db/triggers.py
  - backend/app/db/executions.py
  - backend/app/db/backends.py
  - backend/app/db/workflows.py
  - backend/app/db/sketches.py
  - backend/app/db/audit.py
  - backend/app/db/budgets.py
  - backend/app/models/common.py
  - backend/tests/test_pagination.py
  - backend/tests/test_execution_filter.py
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "All list endpoints accept limit and offset query parameters and return total_count alongside items"
    - "Pagination is pushed to SQL (LIMIT/OFFSET clauses) not Python list slicing"
    - "Requesting page 2 of a 25-item collection (limit=10, offset=10) returns items 11-20 with correct total_count=25"
    - "Execution list supports filtering by status, trigger_id, date_from, date_to, and text search (q parameter)"
    - "Filters compose with AND logic -- combining status=completed and trigger_id=trig-abc returns only completed executions for that trigger"
  artifacts:
    - path: "backend/app/models/common.py"
      provides: "ExecutionFilterQuery model extending PaginationQuery"
      contains: "ExecutionFilterQuery"
    - path: "backend/tests/test_pagination.py"
      provides: "Tests verifying pagination on previously unpaginated endpoints"
    - path: "backend/tests/test_execution_filter.py"
      provides: "Tests verifying execution filtering with composed filters"
  key_links:
    - from: "backend/app/routes/executions.py"
      to: "backend/app/db/executions.py"
      via: "SQL LIMIT/OFFSET for pagination and WHERE clauses for filtering"
      pattern: "LIMIT.*OFFSET"
    - from: "backend/app/routes/triggers.py"
      to: "backend/app/db/triggers.py"
      via: "SQL LIMIT/OFFSET for trigger list pagination"
      pattern: "LIMIT.*OFFSET"
---

<objective>
Add offset pagination to all list endpoints that currently lack it and implement composite execution filtering with status, trigger_id, date range, and text search.

Purpose: Eliminate unbounded memory usage on list endpoints (API-03) and enable users to find specific executions efficiently (API-04).
Output: All ~29 list endpoints support pagination, execution list supports multi-field filtering, SQL queries use LIMIT/OFFSET.
</objective>

<execution_context>
@.planning/milestones/v0.2.0/phases/14-api-hardening-and-developer-experience/14-RESEARCH.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@backend/app/models/common.py
@backend/app/routes/agents.py
@backend/app/routes/executions.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add offset pagination to all unpaginated list endpoints</name>
  <files>backend/app/routes/executions.py, backend/app/routes/triggers.py, backend/app/routes/workflows.py, backend/app/routes/backends.py, backend/app/routes/monitoring.py, backend/app/routes/audit.py, backend/app/routes/sketches.py, backend/app/routes/grd.py, backend/app/routes/super_agents.py, backend/app/routes/rotation.py, backend/app/routes/plugin_exports.py, backend/app/routes/budgets.py, backend/app/routes/product_owner.py, backend/app/db/triggers.py, backend/app/db/executions.py, backend/app/db/backends.py, backend/app/db/workflows.py, backend/app/db/sketches.py, backend/app/db/audit.py, backend/app/db/budgets.py, backend/tests/test_pagination.py</files>
  <action>
    The following list endpoints currently do NOT use PaginationQuery and need pagination added. For each endpoint:
    a. Add `query: PaginationQuery` parameter to the route function signature
    b. Pass `limit=query.limit, offset=query.offset or 0` to the underlying DB/service function
    c. Add `LIMIT ? OFFSET ?` to the SQL query in the corresponding DB function (if not already present)
    d. Add a count query or `count_*()` function call to get `total_count`
    e. Return `total_count` in the response alongside the items array

    **Endpoints to update (grouped by route file):**

    `routes/triggers.py`:
    - `list_triggers()` -- GET /admin/triggers
    - `list_trigger_paths()` -- GET /admin/triggers/<trigger_id>/paths (sub-resource, may not need pagination if always small)

    `routes/executions.py`:
    - `list_all_executions()` -- GET /admin/executions
    - `list_trigger_executions()` -- GET /admin/triggers/<trigger_id>/executions (may already have partial pagination)

    `routes/workflows.py`:
    - List workflows endpoint (check if exists)

    `routes/backends.py`:
    - `list_backends()` -- GET /admin/backends
    - `list_proxy_accounts()` -- GET /admin/proxy-accounts

    `routes/monitoring.py`:
    - Any list endpoints (monitoring snapshots, rate limit history)

    `routes/audit.py`:
    - Audit log list endpoint (currently returns all records with len(events) as total)

    `routes/sketches.py`:
    - `list_sketches()` -- GET /admin/sketches

    `routes/grd.py`:
    - `list_milestones()`, `list_phases()`, `list_plans()`, `list_sessions()` (some may already have limits)

    `routes/super_agents.py`:
    - List super agents endpoint

    `routes/rotation.py`:
    - List rotation schedules endpoint

    `routes/plugin_exports.py`:
    - `list_plugin_exports()` -- GET /admin/plugins/<plugin_id>/exports

    `routes/budgets.py`:
    - List budget entries endpoint

    `routes/product_owner.py`:
    - `list_product_decisions()`, `list_product_milestones()`, `list_milestone_projects()`

    **DB layer pattern (apply to each corresponding DB function):**
    ```python
    def get_all_<entity>(limit=None, offset=0):
        query = "SELECT * FROM <table> ORDER BY created_at DESC"
        params = []
        if limit is not None:
            query += " LIMIT ? OFFSET ?"
            params.extend([limit, offset])
        with get_connection() as conn:
            rows = conn.execute(query, params).fetchall()
            return [dict(row) for row in rows]
    ```

    If a DB function already has `limit`/`offset` params but the route doesn't pass them, just wire the route params through.

    **For sub-resource lists** (e.g., trigger paths, team members) that typically return small collections: still add pagination support but with a generous default (no limit by default). This ensures API consistency.

    AVOID: Do NOT use Python list slicing (`items[offset:offset+limit]`) -- always push pagination to SQL LIMIT/OFFSET.
    AVOID: Do NOT change the structure of existing response fields -- only add `total_count` alongside existing arrays.
    AVOID: Do NOT add pagination to SSE streaming endpoints or conversation message endpoints (these are real-time streams, not paginated lists).

    Write `backend/tests/test_pagination.py`:
    - Seed 25 test entities (use agents or triggers via isolated_db fixture)
    - Test default request returns all items with total_count
    - Test `limit=10&offset=0` returns 10 items, total_count=25
    - Test `limit=10&offset=10` returns 10 items (items 11-20), total_count=25
    - Test `limit=10&offset=20` returns 5 items (items 21-25), total_count=25
    - Test `limit=10&offset=30` returns 0 items, total_count=25
    - Test that at least 3 previously unpaginated endpoints now accept pagination params
  </action>
  <verify>
    cd backend && uv run pytest tests/test_pagination.py -v (Level 2: Proxy)
    cd backend && uv run pytest (Level 1: Sanity -- full suite passes)
  </verify>
  <done>
    All list endpoints accept limit/offset parameters, pagination is implemented at SQL level with LIMIT/OFFSET, total_count is returned, test verifies correct item ranges for page 2 of 25-item collection
  </done>
</task>

<task type="auto">
  <name>Task 2: Add composite execution filtering by status, trigger_id, date range, and text search</name>
  <files>backend/app/models/common.py, backend/app/routes/executions.py, backend/app/db/executions.py, backend/tests/test_execution_filter.py</files>
  <action>
    1. In `backend/app/models/common.py`, add an `ExecutionFilterQuery` model extending `PaginationQuery`:
       ```python
       class ExecutionFilterQuery(PaginationQuery):
           """Query parameters for filtering execution history (API-04)."""
           status: Optional[str] = Field(None, description="Filter by status (running, completed, failed, cancelled)")
           trigger_id: Optional[str] = Field(None, description="Filter by trigger ID")
           date_from: Optional[str] = Field(None, description="Start date (ISO 8601, e.g. 2026-03-01)")
           date_to: Optional[str] = Field(None, description="End date (ISO 8601, e.g. 2026-03-31)")
           q: Optional[str] = Field(None, description="Text search over execution output/logs")
       ```

    2. In `backend/app/routes/executions.py`:
       - Update `list_all_executions()` to accept `query: ExecutionFilterQuery`
       - Pass all filter params to a new or updated DB/service function
       - Update `list_trigger_executions()` to also accept ExecutionFilterQuery (trigger_id is already from path, but other filters should compose)

    3. In `backend/app/db/executions.py`:
       - Update or create a function `get_filtered_executions(limit, offset, status, trigger_id, date_from, date_to, q)`:
         - Build WHERE clause dynamically: each non-None filter adds an AND condition
         - `status` filter: `WHERE status = ?`
         - `trigger_id` filter: `WHERE trigger_id = ?`
         - `date_from` filter: `WHERE created_at >= ?`
         - `date_to` filter: `WHERE created_at <= ?`
         - `q` text search: `WHERE (output LIKE ? OR stderr LIKE ?)` using `%q%` pattern
         - All filters compose with AND logic
         - Add a corresponding count function `count_filtered_executions(...)` with the same WHERE clause
       - Use parameterized queries (?) for all filter values -- never string interpolation

    4. Write `backend/tests/test_execution_filter.py`:
       - Seed 20 execution records with varying statuses (completed, failed, running), trigger_ids, dates, and output text
       - Test filter by status alone: `?status=completed` returns only completed executions
       - Test filter by trigger_id alone: returns only executions for that trigger
       - Test filter by date range: `?date_from=2026-03-01&date_to=2026-03-02` returns only executions in range
       - Test filter by text search: `?q=security` returns executions containing "security" in output
       - Test composed filters: `?status=completed&trigger_id=trig-xxx` returns intersection (AND logic)
       - Test that pagination composes with filters: `?status=completed&limit=5&offset=0` returns first 5 completed
       - Use the `isolated_db` fixture

    AVOID: Do NOT use LIKE without escaping user input for SQL injection -- use parameterized queries.
    AVOID: Do NOT load all executions into memory and filter in Python -- push all filtering to SQL.
  </action>
  <verify>
    cd backend && uv run pytest tests/test_execution_filter.py -v (Level 2: Proxy)
    cd backend && uv run pytest (Level 1: Sanity)
  </verify>
  <done>
    ExecutionFilterQuery model exists, list_all_executions accepts all filter params, filters compose with AND logic at SQL level, pagination works with filters, all tests pass
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- PaginationQuery and ExecutionFilterQuery models import without error
- All list endpoints accept limit/offset params without crashing

Level 2 (Proxy):
- Page 2 of 25-item collection (limit=10, offset=10) returns items 11-20 with total_count=25
- Execution filter by status returns only matching executions
- Composed filters (status + trigger_id) narrow results with AND logic
- Text search over output returns matching executions
</verification>

<success_criteria>
- 100% of list endpoints support limit/offset pagination (all ~29 endpoints)
- Pagination uses SQL LIMIT/OFFSET, not Python list slicing
- Execution filtering supports 5 filter dimensions (status, trigger_id, date_from, date_to, q)
- Filters compose with AND logic
- All backend tests pass
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.2.0/phases/14-api-hardening-and-developer-experience/14-02-SUMMARY.md`
</output>
