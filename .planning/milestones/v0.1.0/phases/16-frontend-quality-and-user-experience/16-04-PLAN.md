---
phase: 16-frontend-quality-and-user-experience
plan: 04
type: execute
wave: 2
depends_on: ["16-01", "16-02"]
files_modified:
  - frontend/src/components/base/__tests__/ErrorBoundary.test.ts
  - frontend/src/services/__tests__/error-handler.test.ts
  - frontend/src/composables/__tests__/useEventSource.test.ts
  - frontend/src/views/__tests__/AppErrorHandling.test.ts
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "Unit tests verify ErrorBoundary catches rendering errors and displays fallback with recovery button"
    - "Unit tests verify ErrorBoundary recovery re-creates child subtree (key increment)"
    - "Unit tests verify formatApiError maps all STATUS_MAP entries to strings containing error codes"
    - "Unit tests verify handleApiError calls showToast for ApiError instances"
    - "Unit tests verify useEventSource returns reactive status ref and calls source.close() on unmount"
    - "Unit tests verify retrySidebarSection calls only the targeted section loader and clears only that section's error state"
    - "Unit tests verify sidebar loading coordination via Promise.allSettled (sidebarLoading lifecycle, per-section error independence)"
    - "All frontend tests pass (existing + new) and frontend build succeeds"
  artifacts:
    - path: "frontend/src/components/base/__tests__/ErrorBoundary.test.ts"
      provides: "ErrorBoundary component tests"
    - path: "frontend/src/services/__tests__/error-handler.test.ts"
      provides: "Centralized error handler tests"
    - path: "frontend/src/composables/__tests__/useEventSource.test.ts"
      provides: "useEventSource composable tests"
    - path: "frontend/src/views/__tests__/AppErrorHandling.test.ts"
      provides: "Sidebar loading coordination and retry integration tests"
  key_links:
    - from: "frontend/src/components/base/__tests__/ErrorBoundary.test.ts"
      to: "frontend/src/components/base/ErrorBoundary.vue"
      via: "import and mount for testing"
      pattern: "ErrorBoundary"
    - from: "frontend/src/services/__tests__/error-handler.test.ts"
      to: "frontend/src/services/api/error-handler.ts"
      via: "import formatApiError and handleApiError"
      pattern: "formatApiError|handleApiError"
---

<objective>
Write comprehensive unit tests for all new components created in Plans 01 and 02: ErrorBoundary, error-handler, and useEventSource composable.

Purpose: Verify that the error boundary catches rendering errors (UX-02), the centralized error handler produces correct messages with codes (UX-04, UX-06), and the shared SSE composable manages lifecycle correctly (UX-03). These tests serve as regression guards for the foundation components.

Output: Test files for ErrorBoundary, error-handler, useEventSource, and app-level error handling/retry with full coverage of the key behaviors.

Note: Context references to 16-01-SUMMARY.md and 16-02-SUMMARY.md are wave-ordering dependencies -- these files will be created by Plans 01 and 02 (wave 1) before this plan (wave 2) executes.
</objective>

<execution_context>
@.planning/milestones/v0.1.0/phases/16-frontend-quality-and-user-experience/16-RESEARCH.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/milestones/v0.1.0/phases/16-frontend-quality-and-user-experience/16-01-SUMMARY.md
@.planning/milestones/v0.1.0/phases/16-frontend-quality-and-user-experience/16-02-SUMMARY.md
@frontend/src/components/base/__tests__/ErrorState.test.ts
@frontend/src/test/setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ErrorBoundary and error-handler tests</name>
  <files>frontend/src/components/base/__tests__/ErrorBoundary.test.ts, frontend/src/services/__tests__/error-handler.test.ts</files>
  <action>
    **ErrorBoundary.test.ts** — Follow the existing test patterns from `ErrorState.test.ts` and the testing patterns in TESTING.md:

    1. Import `{ describe, it, expect, vi }` from `vitest`
    2. Import `{ mount }` from `@vue/test-utils`
    3. Import `{ defineComponent, h }` from `vue`
    4. Import `ErrorBoundary` from `../ErrorBoundary.vue`

    5. Create a helper component that throws during render:
       ```typescript
       const ThrowingComponent = defineComponent({
         props: { shouldThrow: { type: Boolean, default: true } },
         setup(props) {
           return () => {
             if (props.shouldThrow) throw new Error('Test render error');
             return h('div', 'Normal content');
           };
         },
       });
       ```

    6. Test cases:
       a. **"renders default slot content when no error"** — Mount ErrorBoundary with a non-throwing child, assert the child content is visible
       b. **"displays fallback UI when child throws during render"** — Mount ErrorBoundary with ThrowingComponent, assert:
          - Fallback UI is visible (role="alert")
          - Error message contains "Test render error"
          - Default title "Something went wrong" is displayed
          - "Try Again" button is visible
       c. **"uses custom fallbackTitle prop"** — Mount with `fallbackTitle="Custom Error"`, trigger error, assert custom title is displayed
       d. **"recovery button resets error state and re-renders child"** — Mount with ThrowingComponent, then:
          - Verify fallback is shown
          - Update ThrowingComponent's shouldThrow prop to false (or use a ref-based approach)
          - Click "Try Again" button
          - Assert fallback disappears and child content is re-rendered
          - This tests the `recoveryKey` increment mechanism
       e. **"does not propagate error to parent"** — Mount ErrorBoundary inside another component, verify the parent does NOT receive the error (SPA does not crash)

    **error-handler.test.ts** — Test the centralized error handler:

    1. Import `formatApiError` and `handleApiError` from the error handler module
    2. Import or mock `ApiError` from the API client

    3. Test cases for `formatApiError`:
       a. **"returns message with error code for known status codes"** — Test all STATUS_MAP entries:
          ```typescript
          it.each([
            [401, 'ERR-401'],
            [403, 'ERR-403'],
            [404, 'ERR-404'],
            [409, 'ERR-409'],
            [422, 'ERR-422'],
            [429, 'ERR-429'],
            [500, 'ERR-500'],
            [503, 'ERR-503'],
            [0, 'ERR-TIMEOUT'],
          ])('formats status %i with code %s', (status, expectedCode) => {
            const result = formatApiError(status);
            expect(result).toContain(expectedCode);
          });
          ```
       b. **"includes server message when it differs from default"** — `formatApiError(404, 'Agent not found')` should contain "Agent not found"
       c. **"excludes redundant server message"** — `formatApiError(404, 'HTTP 404')` should NOT contain "HTTP 404" in the detail
       d. **"handles unknown status codes"** — `formatApiError(418)` should contain "ERR-418"
       e. **"includes actionable suggestion for known codes"** — `formatApiError(401)` should contain "Settings" or "API key"

    4. Test cases for `handleApiError`:
       a. **"calls showToast with error type for ApiError"** — Create mock showToast, call handleApiError with new ApiError(404, 'Not found'), assert showToast called with ('error') type
       b. **"returns formatted message string"** — Assert return value contains the error code
       c. **"handles generic Error objects"** — Call with `new Error('Network failed')`, assert showToast called and message contains "ERR-UNKNOWN"
       d. **"uses fallback message for non-Error objects"** — Call with `'string error'`, assert fallback message is used

    **Follow existing test conventions:**
    - Use `describe` blocks grouped by function/component
    - Use `vi.fn()` for mock functions
    - Use `it.each` for parameterized tests
    - Keep tests focused on behavior, not implementation details
  </action>
  <verify>
    cd frontend && npm run test:run -- --reporter=verbose 2>&1 | tail -30
    Verify all new tests pass.
  </verify>
  <done>
    ErrorBoundary.test.ts covers: normal rendering, error fallback, custom title, recovery mechanism, error isolation.
    error-handler.test.ts covers: all STATUS_MAP entries, server message formatting, unknown codes, handleApiError with ApiError/Error/fallback.
    All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write useEventSource composable tests</name>
  <files>frontend/src/composables/__tests__/useEventSource.test.ts</files>
  <action>
    **useEventSource.test.ts** — Follow the composable testing pattern from `useDataPage.test.ts`:

    1. Import `{ describe, it, expect, vi, beforeEach, afterEach }` from `vitest`
    2. Import `{ mount, flushPromises }` from `@vue/test-utils`
    3. Import `{ defineComponent, nextTick }` from `vue`
    4. Import `useEventSource` from `../useEventSource`

    5. Mock `createAuthenticatedEventSource` from the API client:
       ```typescript
       vi.mock('../../services/api/client', () => ({
         createAuthenticatedEventSource: vi.fn(),
       }));
       ```

    6. Create a mock EventSource factory:
       ```typescript
       function createMockSource() {
         const listeners: Record<string, Function[]> = {};
         return {
           onopen: null as Function | null,
           onerror: null as Function | null,
           onmessage: null as Function | null,
           addEventListener: vi.fn((event: string, handler: Function) => {
             if (!listeners[event]) listeners[event] = [];
             listeners[event].push(handler);
           }),
           close: vi.fn(),
           _listeners: listeners,
           _triggerOpen: () => { if ((mockSource as any).onopen) (mockSource as any).onopen(); },
           _triggerError: (event: Event) => { if ((mockSource as any).onerror) (mockSource as any).onerror(event); },
         };
       }
       ```

    7. Create a test wrapper component (following useDataPage.test.ts pattern):
       ```typescript
       function createTestComponent(options: UseEventSourceOptions) {
         return defineComponent({
           setup() { return useEventSource(options); },
           template: '<div />',
         });
       }
       ```

    8. Test cases:
       a. **"starts with idle status"** — Mount component with autoConnect false, assert `wrapper.vm.status === 'idle'`
       b. **"transitions to connecting on connect()"** — Call `wrapper.vm.connect()`, assert status is 'connecting'
       c. **"transitions to open when source fires onopen"** — Connect, trigger onopen on mock source, assert status is 'open'
       d. **"transitions to error when source fires onerror"** — Connect, trigger onerror, assert status is 'error'
       e. **"transitions to closed on close()"** — Connect, call close(), assert status is 'closed' and mock source.close() was called
       f. **"registers event listeners from events map"** — Provide events: { message: handler, error: handler }, connect, assert addEventListener called with each event name
       g. **"calls close on existing source before reconnecting"** — Connect, then connect again, assert first source.close() was called before second source is created
       h. **"calls close() on unmount"** — Connect, unmount the component, assert source.close() was called
       i. **"supports function-form URL"** — Pass `url: () => '/dynamic/url'`, connect, assert createAuthenticatedEventSource called with '/dynamic/url'
       j. **"calls onOpen callback"** — Provide onOpen callback, connect, trigger onopen, assert callback was called
       k. **"calls onError callback"** — Provide onError callback, connect, trigger onerror, assert callback was called
       l. **"auto-connects when autoConnect is true"** — Mount with autoConnect: true, assert createAuthenticatedEventSource was called immediately
       m. **"does not auto-connect by default"** — Mount without autoConnect, assert createAuthenticatedEventSource was NOT called

    **Follow existing test conventions:**
    - Use `beforeEach` to reset mocks
    - Use `vi.mocked()` for accessing mock implementations
    - Mount the wrapper component and access `wrapper.vm` for composable return values
    - Use `await nextTick()` or `await flushPromises()` where reactivity needs to settle
  </action>
  <verify>
    cd frontend && npm run test:run -- --reporter=verbose 2>&1 | tail -30
    cd frontend && npm run build 2>&1 | tail -5
    Verify all tests pass and the build succeeds.
  </verify>
  <done>
    useEventSource.test.ts covers: initial status, status transitions (idle->connecting->open/error/closed),
    event listener registration, reconnection cleanup, unmount cleanup, function URL support, callbacks,
    autoConnect behavior. All tests pass. Frontend builds successfully.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write App-level error handling and sidebar retry integration tests</name>
  <files>frontend/src/views/__tests__/AppErrorHandling.test.ts</files>
  <action>
    **AppErrorHandling.test.ts** — Integration-level tests for sidebar loading coordination and retry functionality (UX-05, UX-08):

    1. Import `{ describe, it, expect, vi, beforeEach }` from `vitest`
    2. Import `{ mount, flushPromises }` from `@vue/test-utils`
    3. Import `{ defineComponent, ref, nextTick, provide }` from `vue`

    4. **Test group: "Sidebar loading coordination"** — Tests for the Promise.allSettled pattern and sidebarLoading state:
       a. **"sidebarLoading starts true and becomes false after all fetches settle"** — Create a minimal component that simulates the App.vue sidebar loading pattern with Promise.allSettled over mock loaders. Assert sidebarLoading is true initially, then false after all settle.
       b. **"sidebarErrors tracks per-section failures independently"** — Provide 7 mock loaders where 2 reject. Assert sidebarErrors has non-null values only for the 2 failed keys, and null for the other 5.
       c. **"successful sections are not affected by failing sections"** — Assert that when 1 section fails, the other 6 sections' data is still available (Promise.allSettled does not short-circuit).

    5. **Test group: "retrySidebarSection"** — Tests for per-section retry functionality (UX-05):
       a. **"retrySidebarSection calls only the targeted section loader"** — Set up the retry function with 7 loaders. Call `retrySidebarSection('teams')`. Assert only the 'teams' loader was called, not any of the other 6.
       b. **"retrySidebarSection clears only that section's error state on retry"** — Set sidebarErrors for 'teams' and 'products'. Call `retrySidebarSection('teams')` with a succeeding loader. Assert sidebarErrors['teams'] is null but sidebarErrors['products'] still has its error.
       c. **"retrySidebarSection sets error on second failure"** — Set up a loader that rejects. Call retrySidebarSection. Assert sidebarErrors[key] is set to the new error message.
       d. **"retrySidebarSection clears error before calling loader"** — Assert that sidebarErrors[key] is cleared (set to null) before the async loader runs, so the UI shows a loading state during retry.

    6. **Test group: "handleApiError integration"** — Verify that sidebar failures surface as toasts:
       a. **"sidebar fetch failure calls handleApiError which invokes showToast"** — Mock handleApiError and showToast. Simulate a sidebar fetch failure. Assert handleApiError was called with the error and showToast.
       b. **"handleApiError returns formatted message stored in sidebarErrors"** — Assert the return value from handleApiError is stored in the corresponding sidebarErrors entry.

    **Testing pattern:**
    - Create a minimal test component that reproduces the App.vue sidebar coordination logic (Promise.allSettled, sidebarLoading, sidebarErrors, retrySidebarSection) without mounting the full App.vue
    - Use vi.fn() for mock loaders that resolve/reject as needed
    - Use flushPromises() to wait for Promise.allSettled to settle
    - Keep tests focused on the coordination and retry logic, not the AppSidebar template rendering

    **Follow existing test conventions:**
    - Use `describe` blocks grouped by feature
    - Use `vi.fn()` for mock functions
    - Keep tests focused on behavior, not implementation details
  </action>
  <verify>
    cd frontend && npm run test:run -- --reporter=verbose 2>&1 | tail -30
    Verify all new tests pass including retry and sidebar coordination tests.
  </verify>
  <done>
    AppErrorHandling.test.ts covers: sidebar loading coordination (sidebarLoading lifecycle, per-section error tracking, Promise.allSettled independence),
    retrySidebarSection (targeted section reload, error state isolation, retry failure, pre-retry error clearing),
    and handleApiError integration (toast invocation, error message storage).
    All tests pass.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- `cd frontend && npm run test:run` passes with all new and existing tests
- `cd frontend && npm run build` succeeds
- ErrorBoundary.test.ts exists with at least 5 test cases
- error-handler.test.ts exists with at least 8 test cases (covering all STATUS_MAP entries)
- useEventSource.test.ts exists with at least 10 test cases
- AppErrorHandling.test.ts exists with at least 8 test cases covering sidebar coordination and retry
- Zero test failures in the full test suite
</verification>

<success_criteria>
1. All unit tests for ErrorBoundary pass, including error catching, recovery, and error isolation
2. All unit tests for formatApiError pass, covering every STATUS_MAP entry with correct error code inclusion
3. All unit tests for handleApiError pass, covering ApiError, generic Error, and non-Error inputs
4. All unit tests for useEventSource pass, covering lifecycle, cleanup, event registration, and autoConnect
5. All unit tests for sidebar loading coordination pass, covering sidebarLoading lifecycle and per-section error tracking
6. All unit tests for retrySidebarSection pass, covering targeted reload, error isolation, and retry failure
7. Full frontend test suite passes with zero failures
8. Frontend production build succeeds
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.1.0/phases/16-frontend-quality-and-user-experience/16-04-SUMMARY.md`
</output>
