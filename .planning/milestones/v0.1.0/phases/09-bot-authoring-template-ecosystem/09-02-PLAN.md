---
phase: 09-bot-authoring-template-ecosystem
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/db/prompt_snippets.py
  - backend/app/db/schema.py
  - backend/app/db/migrations.py
  - backend/app/db/__init__.py
  - backend/app/db/triggers.py
  - backend/app/models/prompt_snippet.py
  - backend/app/routes/prompt_snippets.py
  - backend/app/routes/__init__.py
  - backend/app/services/prompt_snippet_service.py
  - backend/app/services/prompt_renderer.py
  - backend/app/services/trigger_service.py
  - backend/app/routes/triggers.py
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "prompt_snippets table stores named snippets with name, content, description, and is_global fields"
    - "{{snippet_name}} references in prompt templates are resolved to snippet content at render time, before {placeholder} substitution"
    - "Circular snippet references (A->B->A) terminate within 5 iterations and leave the unresolved reference intact"
    - "Missing snippet references ({{nonexistent}}) are left unresolved in the rendered prompt"
    - "trigger_template_history table has author and diff_text columns tracking who changed what"
    - "log_prompt_template_change() computes unified diff and stores it alongside old/new templates"
    - "get_prompt_template_history() returns records with changed_at, author, diff_text fields"
    - "Rollback endpoint restores a previous prompt template version and logs the rollback as a new history entry"
    - "POST /admin/triggers/{id}/preview-prompt-full accepts custom JSON payload, renders prompt with snippets and placeholders, returns rendered_prompt + cli_command + unresolved lists"
    - "Preview endpoint does NOT spawn any subprocess"
  artifacts:
    - path: "backend/app/db/prompt_snippets.py"
      provides: "Prompt snippet CRUD functions"
    - path: "backend/app/models/prompt_snippet.py"
      provides: "Pydantic models for snippet API"
    - path: "backend/app/routes/prompt_snippets.py"
      provides: "Snippet library API endpoints"
    - path: "backend/app/services/prompt_snippet_service.py"
      provides: "SnippetService with resolve_snippets() for template variable expansion"
  key_links:
    - from: "backend/app/services/prompt_renderer.py"
      to: "backend/app/services/prompt_snippet_service.py"
      via: "snippet resolution call before placeholder substitution"
      pattern: "SnippetService.resolve_snippets"
    - from: "backend/app/services/trigger_service.py"
      to: "backend/app/services/prompt_renderer.py"
      via: "preview_prompt_full uses PromptRenderer.render()"
      pattern: "PromptRenderer|preview_prompt"
    - from: "backend/app/db/triggers.py"
      to: "difflib"
      via: "unified_diff for version history"
      pattern: "difflib.unified_diff"
    - from: "backend/app/routes/__init__.py"
      to: "backend/app/routes/prompt_snippets.py"
      via: "blueprint registration"
      pattern: "prompt_snippets_bp"
---

<objective>
Implement the backend for prompt snippet library (TPL-03), prompt template version control enhancement (TPL-04), and webhook payload test console (TPL-05).

Purpose: Enable reusable prompt fragments with {{snippet}} variable resolution, immutable version tracking with diffs and rollback, and a full dry-run preview that shows the rendered prompt and CLI command without spawning a process.

Output: Prompt snippet CRUD endpoints, enhanced version history with author/diff, rollback endpoint, and full preview endpoint.

Research basis: {{double_braces}} snippet syntax (09-RESEARCH.md Recommendation 2), immutable versioning with difflib (09-RESEARCH.md Recommendation 1), expanded preview-prompt (09-RESEARCH.md Recommendation 5).
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/codebase/ARCHITECTURE.md
@.planning/milestones/v0.1.0/phases/09-bot-authoring-template-ecosystem/09-RESEARCH.md
</execution_context>

<context>
@backend/app/db/triggers.py
@backend/app/db/schema.py
@backend/app/db/__init__.py
@backend/app/services/prompt_renderer.py
@backend/app/services/trigger_service.py
@backend/app/services/execution_service.py
@backend/app/routes/triggers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prompt snippet library and resolution pipeline — table, CRUD, SnippetService, PromptRenderer integration</name>
  <files>backend/app/db/prompt_snippets.py, backend/app/db/schema.py, backend/app/db/migrations.py, backend/app/db/__init__.py, backend/app/models/prompt_snippet.py, backend/app/routes/prompt_snippets.py, backend/app/routes/__init__.py, backend/app/services/prompt_snippet_service.py, backend/app/services/prompt_renderer.py</files>
  <action>
    Following the {{snippet_name}} convention from 09-RESEARCH.md Recommendation 2:

    1. Create `backend/app/db/prompt_snippets.py`:
       - CRUD functions: `add_snippet(name, content, description="", is_global=True) -> str`, `get_snippet(snippet_id) -> Optional[dict]`, `get_snippet_by_name(name) -> Optional[dict]`, `get_all_snippets() -> List[dict]`, `update_snippet(snippet_id, name=None, content=None, description=None) -> bool`, `delete_snippet(snippet_id) -> bool`
       - Use `generate_id("snip-")` prefix for IDs (add to ids.py if needed)
       - Schema: id TEXT PRIMARY KEY, name TEXT UNIQUE NOT NULL, content TEXT NOT NULL, description TEXT DEFAULT '', is_global INTEGER DEFAULT 1, created_at TEXT DEFAULT (datetime('now')), updated_at TEXT DEFAULT (datetime('now'))

    2. Add `prompt_snippets` CREATE TABLE to `backend/app/db/schema.py`:
       - Columns as described above
       - Add unique index on name

    3. Add migration `_migrate_v58_prompt_snippets_table` in `backend/app/db/migrations.py`:
       - Create prompt_snippets table
       - Register as (58, "prompt_snippets_table", _migrate_v58_prompt_snippets_table)

    4. Create `backend/app/services/prompt_snippet_service.py`:
       - `class SnippetService:`
       - `MAX_DEPTH = 5` class constant
       - `resolve_snippets(cls, text: str, depth: int = 0, visited: set = None) -> str` classmethod:
         - If depth >= MAX_DEPTH, return text unchanged
         - If visited is None, create empty set
         - Use `re.sub(r"\{\{(\w[\w\-]*)\}\}", replacer, text)` pattern
         - `replacer` function: extract snippet name from match group, check if name in visited (circular — return original match), look up snippet by name via `get_snippet_by_name()`, if found: add name to visited, recursively resolve nested snippets in the snippet content, return resolved content; if not found: return original match unchanged
       - IMPORTANT: Do NOT change PromptRenderer.render() signature — add snippet resolution as internal logic

    5. Integrate into `backend/app/services/prompt_renderer.py`:
       - Import SnippetService at top of file
       - In the `render()` method, add snippet resolution as the FIRST step before any {placeholder} substitution:
         `prompt = SnippetService.resolve_snippets(prompt)`
       - This ensures {{snippets}} are expanded before {placeholders} are substituted
       - Do NOT modify the render() method signature — it must remain compatible with all callers (ExecutionService.run_trigger(), TriggerService.preview_prompt(), etc.)

    6. Create `backend/app/models/prompt_snippet.py`:
       - `CreateSnippetRequest(BaseModel)`: name (str, min 1 char, regex `^[\w][\w-]*$`), content (str, min 1 char), description (str, default "")
       - `UpdateSnippetRequest(BaseModel)`: name (Optional[str]), content (Optional[str]), description (Optional[str])
       - `SnippetResponse(BaseModel)`: id, name, content, description, is_global, created_at, updated_at

    7. Create `backend/app/routes/prompt_snippets.py`:
       - `APIBlueprint("prompt_snippets", __name__, url_prefix="/admin/prompt-snippets", abp_tags=[tag])`
       - `GET /` — list all snippets
       - `POST /` — create snippet (validate name uniqueness)
       - `GET /<snippet_id>` — get single snippet
       - `PUT /<snippet_id>` — update snippet
       - `DELETE /<snippet_id>` — delete snippet
       - `POST /resolve` — test endpoint: accepts `{"text": "..."}` body, returns resolved text (for debugging)

    8. Register `prompt_snippets_bp` in `backend/app/routes/__init__.py`

    9. Re-export new functions in `backend/app/db/__init__.py`

    AVOID:
    - Do NOT use Jinja2 for snippet resolution (09-RESEARCH.md anti-pattern: injection risk and unnecessary complexity)
    - Do NOT use greedy regex — `\w[\w\-]*` correctly matches snippet names with hyphens
    - Do NOT change PromptRenderer.render() signature (09-RESEARCH.md Common Implementation Trap)
  </action>
  <verify>
    cd backend && uv run python -c "
from app.services.prompt_snippet_service import SnippetService
# Test basic resolution (no DB needed for missing snippets)
result = SnippetService.resolve_snippets('Hello {{missing}}')
assert result == 'Hello {{missing}}', f'Expected unresolved, got: {result}'
print('Missing snippet leaves reference intact: PASS')
" (Level 1: Sanity)
    cd backend && uv run pytest tests/ -x -q (Level 1: Sanity — existing tests pass)
  </verify>
  <done>
    prompt_snippets table created with CRUD, SnippetService.resolve_snippets() handles nested resolution with cycle detection, PromptRenderer.render() calls snippet resolution before placeholder substitution, snippet CRUD API endpoints functional
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhanced version history with diff/author, rollback, and full CLI preview endpoint</name>
  <files>backend/app/db/triggers.py, backend/app/db/migrations.py, backend/app/db/schema.py, backend/app/services/trigger_service.py, backend/app/routes/triggers.py</files>
  <action>
    Following 09-RESEARCH.md Recommendation 1 (immutable versioning) and Recommendation 5 (expanded preview):

    1. Enhance `trigger_template_history` table — add `author` and `diff_text` columns:
       - Add migration `_migrate_v59_template_history_author_diff` in `backend/app/db/migrations.py`:
         - `ALTER TABLE trigger_template_history ADD COLUMN author TEXT DEFAULT 'system'`
         - `ALTER TABLE trigger_template_history ADD COLUMN diff_text TEXT DEFAULT ''`
         - Register as (59, "template_history_author_diff", _migrate_v59_template_history_author_diff)
       - Update `trigger_template_history` CREATE TABLE in `backend/app/db/schema.py` to include author and diff_text columns

    2. Enhance `log_prompt_template_change()` in `backend/app/db/triggers.py`:
       - Add `author: str = "system"` parameter
       - Compute unified diff using `difflib.unified_diff()`:
         ```python
         import difflib
         diff_lines = list(difflib.unified_diff(
             old_template.splitlines(keepends=True),
             new_template.splitlines(keepends=True),
             fromfile="previous", tofile="current",
             lineterm=""
         ))
         diff_text = "\n".join(diff_lines)
         ```
       - Store author and diff_text in the INSERT statement
       - Update the INSERT SQL to include the new columns

    3. Enhance `get_prompt_template_history()` in `backend/app/db/triggers.py`:
       - Update SELECT to include `author, diff_text` columns in the result

    4. Add rollback endpoint to `backend/app/services/trigger_service.py`:
       - `rollback_prompt_template(trigger_id: str, version_id: int) -> Tuple[dict, HTTPStatus]` static method:
         a. Get the trigger (return 404 if not found)
         b. Get the history entry by version_id from trigger_template_history (return 404 if not found)
         c. Verify the history entry belongs to this trigger_id (return 400 if mismatch)
         d. Get current prompt_template from trigger
         e. Update trigger's prompt_template to the history entry's `old_template` (the version being rolled back TO)
         f. Log this change via `log_prompt_template_change()` with author="rollback"
         g. Return success response with old and new template values

    5. Add rollback route to `backend/app/routes/triggers.py`:
       - `POST /<trigger_id>/rollback-prompt` — accepts `{"version_id": int}` body
       - Calls `TriggerService.rollback_prompt_template(trigger_id, version_id)`

    6. Add full preview endpoint to `backend/app/services/trigger_service.py`:
       - `preview_prompt_full(trigger_id: str, payload: dict) -> Tuple[dict, HTTPStatus]` static method:
         a. Get the trigger (return 404 if not found)
         b. Import and use PromptRenderer.render() for consistent rendering (includes snippet resolution):
            - Pass payload values for message_text, paths_str, and event fields
            - Use defaults for missing fields: message="" paths="/path/to/project"
         c. Import ExecutionService.build_command() to construct the CLI command array WITHOUT executing:
            - Pass backend type, rendered prompt, model, allowed_tools
         d. Return dict with: rendered_prompt, cli_command (joined string), cli_command_parts (array), backend_type, model, trigger_name, unresolved_placeholders (re.findall of {name}), unresolved_snippets (re.findall of {{name}})
         e. CRITICAL: This method must NOT call subprocess.Popen or ExecutionService.run_trigger() — it is read-only

    7. Add full preview route to `backend/app/routes/triggers.py`:
       - `POST /<trigger_id>/preview-prompt-full` — accepts JSON payload body
       - Calls `TriggerService.preview_prompt_full(trigger_id, payload)`
       - This is a NEW endpoint separate from the existing `preview-prompt` to avoid breaking changes

    8. Add version history list route to `backend/app/routes/triggers.py`:
       - `GET /<trigger_id>/prompt-history` — returns prompt template change history for a trigger
       - Calls `get_prompt_template_history(trigger_id, limit=50)`
       - Returns list of history entries with id, old_template, new_template, author, diff_text, changed_at

    AVOID:
    - Do NOT store diffs as binary patches (09-RESEARCH.md anti-pattern: use human-readable unified text diffs)
    - Do NOT call subprocess.Popen in preview endpoint (09-RESEARCH.md Pitfall 4)
    - Do NOT modify existing preview-prompt endpoint behavior — add new endpoint alongside it
  </action>
  <verify>
    cd backend && uv run python -c "
import difflib
old = 'Review this PR: {pr_url}'
new = 'Review this PR carefully: {pr_url}\nCheck for security issues.'
diff = list(difflib.unified_diff(old.splitlines(keepends=True), new.splitlines(keepends=True), fromfile='previous', tofile='current'))
print('Diff computed:', len(diff), 'lines')
assert len(diff) > 0, 'Diff should have content'
print('difflib integration: PASS')
" (Level 1: Sanity)
    cd backend && uv run pytest tests/ -x -q (Level 1: Sanity — existing tests still pass)
    Verify preview-prompt-full does NOT import or call subprocess.Popen (grep check) (Level 1: Sanity)
  </verify>
  <done>
    trigger_template_history enhanced with author and diff_text columns, log_prompt_template_change() computes unified diff, rollback endpoint restores previous version and logs change, preview-prompt-full endpoint returns rendered prompt + CLI command without spawning subprocess, prompt-history endpoint returns version list with diffs
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- prompt_snippets table exists after migration
- trigger_template_history has author and diff_text columns after migration
- SnippetService.resolve_snippets("Hello {{missing}}") returns "Hello {{missing}}"
- Circular reference A->B->A terminates and returns unresolved text
- difflib.unified_diff produces non-empty diff for different templates
- preview-prompt-full code path contains no subprocess.Popen calls
- All existing tests pass

Level 2 (Proxy):
- Snippet CRUD: create, read, update, delete all return correct HTTP status codes
- Creating snippet with duplicate name returns 409
- PromptRenderer.render() resolves {{snippet}} before {placeholder}
- Rollback changes trigger's prompt_template and creates history entry with author="rollback"
- preview-prompt-full returns rendered_prompt, cli_command, and unresolved lists
- Frontend build passes (no import breakage)

Level 3 (Deferred):
- Snippet update propagates to all bots on next execution (requires running bot)
- Version history shows correct diffs for real prompt editing sequences
- Preview with actual webhook payloads renders correct prompt
</verification>

<success_criteria>
- Prompt snippets resolve {{name}} references in all prompt templates at render time
- Circular snippet references detected and handled safely within 5 iterations
- Every prompt template edit tracked with author, timestamp, and unified diff
- One-click rollback restores previous version and logs the rollback
- Full preview shows rendered prompt + CLI command without spawning any process
- All existing tests pass, no regressions
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.1.0/phases/09-bot-authoring-template-ecosystem/09-02-SUMMARY.md`
</output>
