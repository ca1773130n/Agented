# v0.6.0 — Performance audit + targeted fixes

Spec: `docs/superpowers/specs/2026-05-05-v0.6.0-perf-audit-design.md`

First milestone of v0.6.x. Builds the performance measurement
toolchain (`PerformanceMiddleware`, `profile.py`, `db_audit.py`),
ships the v0.5.12 deferred sessions-lookup-via-index fix, splits a
few frontend vendor chunks, and documents a baseline.

After v0.6.0 the platform has the tooling to make subsequent
performance work targeted and measurable.
